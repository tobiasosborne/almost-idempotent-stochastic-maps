%% GENERATED by scripts/gen-report-dag.py --- do not hand-edit.
%% Source of truth: argument/lemmas/*.md (parsed by scripts/argument.py).
%% Regenerate: python3 scripts/gen-report-dag.py   |   Gate: --check (scripts/check-all.sh)

% SHARD-ID: AISM-39-ARGUMENT-DAG
% SHARD-TITLE: Argument-DAG atlas: the Route-F proof chain for op-classical
% SHARD-SUMMARY: Generated atlas of the sub-DAG that carries the argument landing op-classical: the transitive deps/routes closure of the root together with the Route-F families, drawn layered and landscape, one detail page per phase.
% SHARD-SUMMARY: Nodes are colour- and glyph-coded by rigour rung (af-validated / cited / proved / seeded / proved-mod-audit-or-conjecture / stated / open) using the same proof_class function that colours the Mermaid twin argument/DAG.md, so the two views cannot disagree.
% SHARD-SUMMARY: Every node carries the anchor dag:<id> and links back to its report statement; the design-pending GAP compositions are drawn dashed and are explicitly NOT registry edges.
% SHARD-KEYWORDS: argument DAG, atlas, Route F, op-classical, dependency graph, rigour ladder, generated figure, crosslink anchors, GAP composition

\section{Argument-DAG atlas: the Route-F proof chain for \texorpdfstring{\texttt{op-classical}}{op-classical}}
\phantomsection\label{sec:argument-dag}

This section is \textbf{generated} by \texttt{scripts/gen-report-dag.py} directly from the registry shards \texttt{argument/lemmas/*.md} (parsed with \texttt{scripts/argument.py}'s own parser) and is re-checked by \texttt{scripts/check-all.sh}: if a shard's \texttt{status}, \texttt{af} state or \texttt{deps} change and this section is not regenerated, the gate fails. It is therefore a live view of the argument, not a snapshot. Its browsable twin is \texttt{argument/DAG.md} (Mermaid, also generated from the same shards, whole registry); this atlas is the print/PDF view, restricted to the landing chain.

\paragraph{What is drawn.} Not the whole registry (293 results) --- only the sub-DAG that carries the argument landing the north star: the transitive closure of \dagref{op-classical} along \texttt{deps:} and \texttt{routes:} edges, \emph{together with} the Route-F families named by the live strategy \texttt{docs/plans/CURRENT.md}. That is \textbf{140 results} and 278 edges. The Route-F chain is deliberately included even though it does not yet reach the root through registry edges: that missing composition is the point, and it is drawn as 2 dashed \textbf{design-pending (GAP)} links.

\paragraph{Honest reading.} Of the 140 results drawn, \textbf{67 are rigorous} in this repo's sense (\texttt{af: validated}, or \texttt{cited} and byte-matched to \texttt{refs/}). Everything else --- including every node coloured blue (\texttt{status: proved}) --- is a prose proof that has \emph{not} cleared an independent \texttt{af} validation, and the north star itself is \texttt{open}. A path of coloured boxes from left to right is \emph{not} a proof; it is a map of what would have to be discharged.

\paragraph{Crosslinking (the anchor scheme).} Every drawn result carries the hypertarget \texttt{dag:\textless registry-id\textgreater} on its node, and every imported definition carries \texttt{dag:\textless def-id\textgreater} on its index row. From anywhere in this lab-book, write \verb|\dagref{lem-prh}| or \verb|\dagdef{def-height}| to jump to the atlas, and \verb|\dagphase{comp}{...}| to jump to a phase page. In the other direction, a node links back (\S) to its typeset statement whenever the registry shard names one via its \texttt{provenance:} \texttt{report \textless label\textgreater} token --- the same join key \texttt{scripts/check-provenance.py} uses.

\input{generated/dag/legend}
\input{generated/dag/overview}
\input{generated/dag/phases}
\input{generated/dag/index}
\input{generated/dag/definitions}

