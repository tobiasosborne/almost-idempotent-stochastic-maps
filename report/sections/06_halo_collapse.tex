% SHARD-ID: AISM-06-HALO-COLLAPSE
% SHARD-TITLE: Halo-robust height collapse
% SHARD-SUMMARY: Reproduces the registry contract for conj-halo-collapse, the af-validated halo-robust bridge.
% SHARD-SUMMARY: Records the run-1 balloon, the factoring into three sub-lemmas, and the clean 20-node run-2 tree.
% SHARD-KEYWORDS: conj-halo-collapse, af validated, height collapse, halo-robust, invisible mass, bridge
\section{Halo-robust height collapse}\label{sec:halo-collapse}

\begin{lemma}[Registry contract for \texttt{conj-halo-collapse}]
\label{conj:halo-collapse}
\contractquote{Halo-robust height collapse: for an exact signed idempotent P with 0 < delta(P) <= 1/4, nonempty visible set W(P), and hidden top vertex v of height H, let sigma be the invisible mass of v, sigma_g the halo-robust invisible mass (the positive coefficient mass v places on rows at ell-1 distance > tau/4 from conv W, tau = sqrt(delta)), and nu_v the row negative mass; then H * (1 - sigma_g) <= (sigma - sigma_g) * tau/4 + nu_v * (2 + 4*delta).}
\end{lemma}

\noindent\textbf{Status.} \texttt{proved}; \texttt{af: validated}.
This is one of the rigorous in-repo results.

\noindent\textbf{Proof tree.} The registry shard records a 20-node
adversarial tree, root \texttt{validated}, taint \texttt{20/20 clean}
(run 2 on the factored workspace).  The export is
\texttt{proofs/conj-halo-collapse/export.md}.  The three
\texttt{af}-validated dependencies \ref{lem:mass-split},
\ref{lem:residual-lower}, and \ref{lem:residual-upper} are imported as
externals, so the root rests only on validated or cited inputs.

\noindent\textbf{Elevation history.} Run 1 (pre-factoring) ballooned
past the node cap (49 live nodes over the cap of 40) and was aborted as
a structure signal, not a mathematical refutation: the mass-split
bookkeeping and the residual-distance estimates were being re-derived
inline across siblings.  Factoring those into the three registry
sub-lemmas above cured it; run 2 closed at 20 nodes.

\noindent\textbf{Role in the chain.} This bridge refines the validated
collapse bound \ref{obs:height-collapse} by pricing recipients inside
the $\tau/4$-halo of the visible hull at their actual distance rather
than the worst case $H$, so the bound stays non-vacuous even when the
raw invisible mass exceeds one through self or halo mass
(\ref{obs:sigma-halo-nonrobust}).  Together with a halo-robust cap
$\tilde\sigma_g \le 1-c$ for hidden top vertices it would give
$H = O(\tau)$, the height cap of the Kernel Conjecture
\ref{conj:kernel}.  That cap is OPEN: nothing in this section promotes
the kernel chain.
