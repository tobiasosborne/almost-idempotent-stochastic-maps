<!--
ROLE: current state + START HERE + ranked next steps. The always-"now" file.
UPDATE POLICY: REWRITTEN (not appended) at each session close; keep ≤500 lines. Narrative history goes in
  docs/worklog.md (append-only); this file is always "now".
TRIGGER: session close, or a material change in the current frontier / next task.
-->

# HANDOFF — almost-idempotent-stochastic-maps

## START HERE

1. Read `PRD.md`, then `CLAUDE.md` (== `AGENTS.md`).
2. The proof sketch is `docs/plans/CURRENT.md` → **v50** (current).
3. **`op-classical` IS DISCHARGED AT T0 (2026-08-08).** Rigorous
   (af-validated, T0): **196**. Registry: **371**.
4. **SESSION-45 RECORD (2026-08-08, W138):** in one session, the W78 §5
   step-6 endgame ran end-to-end:
   - Fresh design → hostile audit **REJECT** (1 FATAL cap-budget + 3
     HIGH) → fresh v2 design (three first-class helper rows; option-(a)
     pre-forall scalar positivity; 30-item textbook-fact census; complete
     landing manifest) → fresh hostile re-audit **LAND, zero
     corrections** → **user ratification** → landing (registry 367 → 371;
     guard released; 11 stale report loci repaired).
   - Elevation queue banked 5/5 same-day: scalar-header-positivity
     (T0 191), factor-map-packet (192, first pass), factor-estimate-packet
     (193, first pass), **strengthened `lem-routef-k-ledger` (194, 7/7
     FIRST PASS)**, `lem-routef-f0-assembly` (195).
   - **User-ratified ROOT REWIRE + DISCHARGE**: audited OR-routes block;
     kind open-problem → theorem; root af tree 5/5 validated/clean;
     oracle + `fr verify` PASS; **`op-classical` = proved / af: validated
     (T0 196)**. Explicit witnesses: eta_0 = eta_K, C = K+4*sqrt(2K),
     dimension-free.
   - Report truthfulness sweep: 28 stock open-claims + 8 bespoke
     paragraphs + 5 shard headers + catalog rows updated; PRD current
     state rewritten; `check-all` OK incl. report build.
   - **USER P0 delivered** (bead `aism-aywn`): `paper/main.tex`, 4 pages
     (fresh-codex draft → separate fresh faithfulness audit
     CORRECTIONS-REQUIRED → all 4 corrections applied → post-discharge
     footnote update). PDF delivered to the user.

5. **THE HONEST BOUNDARY (do not overclaim past it):**
   - af-validated = L0 rung (b). **No Lean/mathlib proof exists.**
   - The discharged contract is the UPPER BOUND only (ratified D1 split):
     **sharpness (exponent 1/2) rests on `ex-hume`, still
     proved-mod-audit / af: seeded.**
   - The legacy signed route (thm-classical-factorization +
     prop-approx-simplex) is an unused, non-rigorous alternative.

6. **NEXT SESSION, ranked:**
   1. **`ex-hume` af elevation** (sharpness at T0). Workspace already
      seeded; small explicit 3x3 computation; provision
      `lem-classical-equiv` (T0) if the signed→stochastic transfer is
      needed. This closes the last mathematical claim of the PRD headline.
   2. **Report sync `aism-9kmt`**: anchor/reproduce the ~120–196 banks +
      the discharge narrative (op-classical and the K-ledger family are
      currently whitelisted in `report/UNWIRED.md`).
   3. Paper: user's call on polish/submission (constants table? refs?).
   4. Lean/mathlib: top rung; only on user elevation.
   5. Cross-device beads decision (carried).

7. **Worked-pattern notes for the next session:** elevation cadence and
   the balloon/stuck taxonomy in FINDINGS 2026-08-08 all held; two run-1
   max-rounds non-validations were same-tree resumes (legal — no
   ratification crossed), NOT re-seeds. The oracle claim string must be
   the registry id or the export path (`fr help bank-gate`).

8. **Open beads:** `aism-e30g` CLOSED (W138 complete). `aism-aywn` (paper,
   delivered — keep open for polish feedback). `aism-9kmt` (report sync,
   now the biggest debt). `aism-xuvw` (Route-F T0 epic — CLOSED this
   session). `aism-wazy`, `aism-xjnc`, carried P1s unchanged.

9. Gate: `sh scripts/check-all.sh` → `[check-all] OK` (verified at close).
   All work committed AND pushed. Nothing in flight (no af runs, no codex,
   no worktrees).

## Next steps (ranked)

1. `ex-hume` af elevation (sharpness at T0).
2. `aism-9kmt` report sync (incl. the discharge narrative).
3. Paper polish per user feedback.
4. Lean (only if user elevates).

## What is intentionally NOT here

- Any claim beyond the af-validated rung: **no Lean proof**; sharpness is
  NOT yet T0 (`ex-hume` proved-mod-audit).
- Any claim about the general positive-maps / JB problem (`op-npps`) —
  out of scope here; only the commutative shadow was discharged.
