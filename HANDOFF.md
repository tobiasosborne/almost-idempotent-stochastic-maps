<!--
ROLE: current state + START HERE + ranked next steps. The always-"now" file.
UPDATE POLICY: REWRITTEN (not appended) at each session close; keep ≤500 lines. Narrative history goes in
  docs/worklog.md (append-only); this file is always "now".
TRIGGER: session close, or a material change in the current frontier / next task.
-->

# HANDOFF — almost-idempotent-stochastic-maps

## START HERE

1. Read `PRD.md` (Kernel is THE theorem-facing input; (EX) is a separate attack route), then
   `CLAUDE.md` (== `AGENTS.md`).
2. Read **`docs/plans/2026-07-06-top-down-proof-sketch-v3.md`** — THE canonical strategic map
   (v3, session-9 redraw; v2 superseded in place, kept for line citations). Every wave names
   its node there. The OPEN LEDGER + UNSCOPED-SURFACE list at its end IS the progress metric.
   **STEWARDSHIP (user mandate, 2026-07-06, binding): the sketch is the project's most dynamic
   artifact — keeping it reconciled with newly banked evidence is a FIRST-CLASS DELIVERABLE of
   every session; a wave that changes the map without updating the sketch is incomplete work.**
3. **THE PHASE DISCIPLINE (user, 2026-07-05, binding): no progress theatre.** Progress =
   unscoped/unpriced surface shrinking — never commit/seeding/elevation counts. (bd memory
   `bfs-phase-discipline`; agent memory `no-progress-theatre`.)
4. Run `fr board` and `bd ready`. Newest wave artifacts: `docs/waves/2026-07-05-W18-*`,
   `2026-07-05-E1-*`, `2026-07-06-E2-*`, `2026-07-06-W19-*`; newest bundles:
   `runs/2026-07-05-e1-uniformity-pilot/`, `runs/2026-07-06-w19-sigma-frontier/`. `FINDINGS.md`
   has four new dated entries (07-05 W18/E1; 07-06 E2 death certificate / W19).
5. Gate: `sh scripts/check-all.sh` → `[check-all] OK`.

## Current state (2026-07-06, session 9 CLOSE) — four decisive waves; registry UNCHANGED

**Rigorous (af-validated, T0): 16** (`argument/INDEX.md`) — unchanged this session (~6
substantive; honesty note stands). **No registry shard was edited this session** — all output
is waves, bundles, FINDINGS, and the sketch redraw; three codification follow-ups are filed
(below) but deliberately NOT executed inline.

**Session 9 in one paragraph.** Four waves, each bounded, each two fresh codex workers, all
banked verbatim with orchestrator recomputation of the load-bearing arithmetic. **W18 (Route-A
wall re-read):** the B3/B4 walls do NOT bind the constant cap CAP-1/2 (`σ_g ≤ 1/2` at hidden
tops) as stated — verdict WALL-NARROWED; W17's D1 confirmed twice independently with exact
constants (`29/16`, `29/8`) and STRENGTHENED (under the cap ALL rows land within `29τ/8` of
`conv W`; Kernel's raw antecedent unused); the cap is strictly STRONGER than the height bound
(orchestrator's equivalence hypothesis refuted by both workers — refuters must target
`σ_g > 1/2` at ANY height); minimal residual = the constant-mass shallow-genuine exclusion;
the class-count dead route binds only the O(1) composition (C/τ opening). **E1 (arm E
activated, first pull ever):** staged Luo–Pang 1994 does NOT support the lit-review's
black-box-½ attribution (Assumption 4.1 requires quadratics nonnegative on the polyhedron;
`(E²−E)_ij` is sign-indefinite — T1 witness; fixed-n √ not even free, staged Ex. 4.2 exponent
≤ ¼; citation drift MS-1986-vs-1987 caught and corrected in the lit-review); verdict
GO-CONDITIONAL with two intermediates; the (EB) formulation is exactly clone-lift/block-sum
invariant (standing smell test); no-blowup pilot banked (true n=3 minima; `r ≈ 1.375` at the
stochasticized ex-hume anchor). **E2 (E-int-2 prove-or-refute):** REFUTED, KILL-1 — two
mutually blind adversarial workers proved the SAME n=2 no-go (every quadratic nonnegative on
`P_2` vanishing on `S_2` is identically zero; clone lift propagates the blindness to every n);
survivors: the degree-3 hatch `F₂ = (a−b)²((1−a)+b) ≤ η` with exact zero set, and
`aff(S_n)` = the full stochastic affine space (SOS-of-linear permanently dead). **W19 (Route-A
deciders):** the per-class hostable-mass folklore is UNPROVABLE-as-written (only trivial
`M_X ≤ 1+δ`; needs a sound production rule + a previously-unnamed coefficient-poke charge;
T0: the self-inclusive reading is exactly contradicted by banked instance B, `229/3200 > 1/20`
— self-mass exclusion mandatory); the `σ_g > 1/2` wall is EXPOSEDNESS ABSORPTION, not mass
capacity (exact LP places 5/4 designated mass; exact geometry absorbs recipients into W,
H = 0); certified record `σ_g = 5991/80000 ≈ 0.075` (rank-5 genuine SELF recipient);
duplicate-splitting exactly inert (one quotient class).

**Registry deltas: NONE.** (Sketch v2, FINDINGS, lit-review §1.3 correction, INDEX.md rows,
and two L3 bundles are the deltas.)

## Next steps (ranked by this session's evidence) — RESUME HERE

1. **`aism-pld` (P1) — THE Route-A decider pair.** (a) Codify `conj-external-poke-charge(A)`
   (W19-B's named target; MANDATORY self-mass exclusion — the self-inclusive form is
   T0-refuted); (b) the distinct-multi-class `σ_g` optimization: designs with m geometrically
   DISTINCT genuine-outside quotient classes each pushed toward its ceiling — does absorption
   bind classwise (supports CAP-1/2 via the C/τ count) or can total `σ_g` accumulate (stress)?
   Extends `runs/2026-07-06-w19-sigma-frontier/`.
2. **Cheap codifications, then their reviews:** `aism-yxa` (D1 as a registry lemma — cap ⇒
   `H ≤ 29τ/8`; twice worker-derived, needs one independent codex review; do NOT state it as an
   equivalence) and `aism-j3j` (E2's `obs-quadratic-residual-blindness` +
   `lem-aff-hull-idempotents`). Bookkeeping-grade; report as such.
3. **`aism-tq3` — obs-deep-leakage re-establishment/elevation.** Now load-bearing: every
   Route-A depth-ledger step consumes it and its status is HEURISTIC (W18 flag).
4. **Arm E wave 3 (`aism-5an`, re-scoped, released to open):** E-int-1 (fixed-n stratum-data √
   constants) and/or the degree-≥3 residual generalization (the n=2 cubic hatch is the seed;
   needs a non-Cor-4.1 engine or stratified application; clone-lift invariance is a standing
   constraint). Lower priority than Route A on current evidence.
5. Standing queue: α→1 continuation (K3 direct-FE kill/rescue, from session 8); K2 unified
   financing horn; trunk ⟨2⟩5–⟨2⟩7 transcription/audit (`aism-pu0`, released to open); refs
   ingest (`aism-5de` Kitaev+SBD; `aism-1nh` Mangasarian–Shiau 1986+1987 disambiguation,
   Facchinei–Pang, Kollár, D'Acunto–Kurdyka); `aism-z98` USER DECISION (C-financing term);
   report sections 14–15 (`aism-av0`).

## Standing rules (see CLAUDE.md + bd memories; session-9 additions in bold)

Codex workers only (no Fable subagents); ONE af orchestration at a time; single-minimal af
contracts; no argument//definitions edits while an orchestration runs; numerics = exact-ℚ with
orchestrator recomputation; waves = verbatim docs/waves/ artifacts, honest tiers, fr log per
pull, workers told no fr/bd; wave prompts/answers in the session scratchpad; independent codex
review before codifying worker tools; literature enters as `stated` until byte-matched.
**Session-9 additions: (i) adversarial MUTUALLY-BLIND worker pairs on prove-or-refute targets
work — E2's constructor and obstructor converged on the same lemma from opposite mandates
(strong independent confirmation at one wave's cost); (ii) the orchestrator recomputes the
load-bearing ARITHMETIC of every headline from the printed matrices alone — algebraic side
(idempotence, row sums, δ, mass placement); geometric certifications stay worker-asserted and
are said so; (iii) never bank a folklore claim without naming its missing charges — W19-B's
demotion of the poke-depth sentence is the template; (iv) the LP-relaxation-vs-exact-geometry
comparison is a reusable binding-constraint identifier (W19-A); (v) any cluster/mass lemma must
state its self-mass convention explicitly (T0 contradiction otherwise); (vi) orchestrator
hypotheses handed to workers must be marked as hypotheses-to-refute — both W18 workers
independently corrected the wave brief's equivalence claim, exactly as designed.**

## Recipes

```bash
sh scripts/check-all.sh
python3 scripts/seed-af-workspaces.py <id>       # then COMMIT before orchestrating
python3 scripts/af-orchestrate.py <id> --workers 8 --max-rounds 14 --node-cap 40   # background
fr verify <claim> --oracle <name>
codex exec --skip-git-repo-check -C <repo> -s workspace-write -o <answer> - < <prompt>
python3 runs/2026-07-05-e1-uniformity-pilot/scripts/e1_worker_b_pilot.py           # E1 pilot rerun
python3 runs/2026-07-06-w19-sigma-frontier/scripts/w19_worker_a.py                 # W19 frontier rerun
# session-9 wave pattern (works): name a falsifiable target -> TWO mutually-blind adversarial
#   codex workers (construct vs obstruct, or derive vs wall-audit) -> orchestrator recomputes
#   printed-matrix arithmetic + reruns scripts -> bank verbatim wave doc (+ L3 bundle if
#   numeric) -> FINDINGS/sketch in lockstep -> fr log one pull -> codification as bd follow-up.
```

## What is intentionally NOT here

- Any claim more than SIXTEEN results are rigorous (~6 substantive). Registry untouched this
  session; D1, the poke-charge conjecture, and the E2 no-go/aff-hull lemmas are WORKER
  T1/T0-witnessed and UNCODIFIED (bd aism-yxa / aism-pld / aism-j3j).
- Any claim CAP-1/2 is true: the certified record is `σ_g = 5991/80000 ≈ 0.075` — evidence
  with ~6.7× margin, and THREE separate not-an-emptiness stamps this session (W19-A, W17b
  precedent, E1 pilot scope).
- Any claim the degree-3 hatch generalizes beyond n = 2 (single exact witness).
- Any claim arm E can deliver dimension-free constants (E1 verdict is GO-CONDITIONAL; the
  black-box and quadratic-residual routes are both dead).
- A git remote (local-only by decision) — session close = commits + bd close-out, no push.
