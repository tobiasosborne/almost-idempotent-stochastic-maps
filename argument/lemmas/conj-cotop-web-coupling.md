---
id: conj-cotop-web-coupling
kind: lemma
contract: (CONJECTURE) Co-top web coupling: there exist universal c_* in (0,1) and delta_0 in (0, 1/4] such that for every exact signed idempotent P with 0 < delta(P) <= delta_0 and nonempty visible set, every hidden top vertex v with H > 16*tau, t*(v) > 0, disjoint always-tight hulls at v, and heaviness sum over {j : ||p_j - p_v||_1 < 4*tau, dist_1(p_j, conv W) > 16*tau} of max(P_vj, 0) >= 1 - 1/8: sum over {j : ||p_j - p_v||_1 >= 4*tau, dist_1(p_j, conv W) > H - 8*tau} of max(P_vj, 0) >= c_*.
defs: def-signed-idempotent; def-negative-mass; def-visible-set; def-exposed; def-height
deps: 
status: conjecture
af: none
provenance: W54 wave (docs/waves/2026-07-09-W54-huddle-charge-decomposition.md): Fable author L6's sub-leaf L6.5, V-L6-audited as fully quantified and sufficient (V-L6 finding 6: L6.5 => the tree's L6 leaf at u = v with c_m re-pinned to c_*/2, legal under G8-v3 monotonicity; NOT equivalent — L6.5 drops NOT-Q4, i.e. it is a STRONGER open sub-leaf)
owner: A
---

**Role (THE isolated wall, minimal form).** The lambda-vs-P^+ coupling — the named open
wall behind conj-min-a-w4 and the whole anti-splitting history — localized to its most
hypothesis-rich instance: the dual-REQUIRED co-top far web ([[lem-cotop-witness-pinning]]:
> 13/16 of the witness sits there; [[lem-downhill-cotop-conic-mass]]: disjointness plants
> g/(4*tau) nonclone co-top zero-face mass) must receive >= c_* of the top's OWN positive
coefficient mass. A prover may consume [[lem-psi-corner-trap]]'s t*-free toolkit. Closing
this closes the W54 tree's L6 leaf (Branch I, maximal rigidity) at u = v. Named successor
question (Fable L6 §4): MUTUAL-EXPOSURE RIGIDITY — can a straddling family of mutually
rho-far co-top vertices all be hidden? (Candidate unifier with the L2 plateau leaf.)

**Refuter target.** An internally-reproducing co-top plateau: the far web sustains its own
row reproduction without v-coefficient inflow while all L6.1-L6.4 constraints hold. Never
realized (W52, L3 evidence); the corner trap (a)/(b) constrains but does not yet exclude it.

**Status discipline.** A conjecture — promotes nothing; consumers carry it as a dep.
