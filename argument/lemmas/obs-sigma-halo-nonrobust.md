---
id: obs-sigma-halo-nonrobust
kind: obstruction
contract: The sigma-cap is FALSE as literally written at epsilon = 0: the exact 5x5 signed idempotent generated by build_from_LambdaC(C = [[28/25, 1/200, 0, -1/8]], R2 = [[-49/800], [-1/6], [-1/8], [-33/800]]) has delta = 252559/1280000 <= 1/4 and a hidden top vertex whose invisible mass is sigma = 5343/5000 > 1 (entirely self-mass P_vv at distance 0.02*tau from conv W), so 1 - sigma < 0; the halo-restricted invisible mass (recipients at dist_1 >= tau/4) is exactly 0 — any sigma-cap finisher must be stated halo-robustly.
defs: def-signed-idempotent; def-height; def-visible-set; def-invisible-mass; def-negative-mass
deps: 
status: numerical
af: none
provenance: runs/2026-07-02-sigma-cap-refuter/ (exact certificate, certify.py; independently recomputed by the orchestrator with fresh code: P^2=P over Q, delta, P_vv = 5343/5000 — confirmed); realizes the halo-non-robustness caveat already recorded in def-invisible-mass
owner: A
workspace: proofs/obs-sigma-halo-nonrobust
---

**Arm F wave-2 certified observation (L3 — numerical, NEVER rigorous).** The ε=0 invisible mass counts
v's own self-coefficient when v sits (however slightly) outside `C_W`; an exact idempotent can push
`P_vv > 1` while staying a hidden vertex, driving `σ̃ > 1` and making `1−σ̃ < 0` — with ZERO mass on
genuinely-outside recipients. Pure halo artifact, low height (`H/τ = 0.02`), kernel-irrelevant, but it
exactly refutes the ε=0 phrasing of the cap. Statements must use the halo-robust `σ̃_g` (recipients at
`dist ≥ τ/4`) — see [[conj-halo-collapse]] for the matching self-mass-immune collapse bound.
