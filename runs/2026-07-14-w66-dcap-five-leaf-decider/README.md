# Run bundle: W66 decider — five-leaf D-cap pre-creative batch: ALL BLOCKED/PARTIAL (one exact local C0 cell entrant); tallness binds for the SIXTH consecutive exact batch (2026-07-14, session 19)

## Hypothesis

bd `aism-nrag` / DCAP-ATTACK-W65.md §4.2: can any of the five creative leaves of
the W65 D-cap decomposition (N near disjoint hulls, G<4 low zero-face gauge, C0
collapsed large-gauge cloud, A-esc actorization escape, T-esc rotating scalar
tail) be refuted — or their hypothesis classes even ENTERED — by an exact
family, before creative spend? A genuine candidate must be an exact rational
factorization P = LB with BL = I (certifying P² = P), τ → 0, δ = τ², every row
negativity ≤ δ, the full I-base/all-center hypotheses, tallness H > 16τ, the
pinned ultra bounds (drift < bτ, width < bτ, θ < τ/D₀), an exhibited
D-certificate with M_X ≤ 1/8, M_I < 1/16, M_D > 1/16, true ray value
Z_v(q_A)/τ → 0, its exact 1/80 R1 priority package, and leaf deficit
𝓓_leaf < 0 (4.3). (Rigour tag: `numerical` — exact rational L3 evidence,
never proof.)

## Finding

**All five leaves BLOCKED in the tested families (C0: PARTIAL definition-level
cell entrant); ZERO genuine I-base data; ZERO genuine refuters; tallness
(H > 16τ) binds for the SIXTH consecutive independent exact batch.** Fresh
codex xhigh worker, exact rationals, orchestrator-reproduced 2026-07-14:

- **The tested spine** is the W63 transient diagonal plateau at τ = 1/k
  (k up to 2²⁰), now supplied with its explicit rank-three factorization
  P = LB, BL = I and its UNIQUE singleton reduced display (always-tight
  singletons T={f}, O={o}, Z={z}), so the reported actor-hull gap is the true
  singleton-hull distance, not an arbitrary witness. Exact display statistics:
  m_D* = 1−2τ, g/τ = 4(1+τ²), A = 2(1+τ²)/τ, ℓ/τ = 2τ.
- **N / G<4:** exact cell masses are 0 (every retained carrier is on the gap
  side, g > τ exactly; A − 4 > 0 exactly). Both short of 1/80 by exactly 1/80,
  and tallness fails (16τ − H > 0 exact margins recorded).
- **C0 (the by-catch):** first exact DEFINITION-LEVEL C0 cell entrant —
  η_D*(C0) = 1 − 2τ (e.g. 524287/524288 at k = 2²⁰) with g/τ ≈ 4, A ≈ 2/τ,
  ℓ/τ = 2τ < 1/2. NOT a leaf-hypothesis entrant: the attempted selection
  A = {u} is not ρ-far, far-G_v and ω masses are 0, H < 16τ, so the pinned
  ultra antecedent is unavailable and 𝓓_leaf > 0 (not a refuter).
- **A-esc:** the priority guards fail upstream (C0 mass already ≈ 1);
  carrierwise ℓ − τ/2 < 0 exactly, so the tested D mass routes BEFORE the
  actorization window. No tall growing-rank exact completion found (cloning /
  transient extensions excluded by the brief).
- **T-esc:** the W55 A₀ = 5 completion exhibits the complementary
  residual/tail shape locally (actor residual 65537/2147483648 < 3δ;
  Tail₁ = 257/65536 > δ) but carries its exact order-one finance-row
  negativity ν_f = 21475229695/4294967296 ≫ τ², so it is rejected before any
  tallness or D-cap inference; the legal-negativity plateau routes to C0.
- **Mandatory panel** printed per candidate (m_D* > 1/16 PASS; R2 sup ≤ e_δ
  PASS; P_v⁺(𝓛_v) < ℓ_T FAIL with P_v⁺(𝓛_v) = 1; P_v⁺(𝓔_*) ≥ τS/8 FAIL at 0;
  exactly one B5 overlay label = D_gap, definition-level, with all six (B5.3)
  targets and the D_near guard printed; 𝓓_leaf > 0).

**HONEST SCOPE:** bounded search over named families; no emptiness claim; the
C0 entrant is definition-level only (its parent theorem antecedents fail) and
is recorded as an adverse fixture, not a counterexample to any proved shard.
**Decision take-away:** the pre-creative decider program for the W65 D-cap
leaves is complete with zero class entrants and zero refuters; the decisive
wall is tallness TOGETHER WITH producing a legal far selected mass and a
nonempty ultra ω package. Non-proof green light for the creative queue
(aism-72zn: A-esc first, then T-esc, G<4, C0, N), with the exact local C0
cell as the adverse fixture for the C0 leaf.

## Command

```bash
cd runs/2026-07-14-w66-dcap-five-leaf-decider/scripts
PYTHONDONTWRITEBYTECODE=1 python3 -u search.py   # deterministic, no seed; exact Fractions
```

## Invariant (checkable)

Every claim is an exact `fractions.Fraction` assertion in `search.py`:
P = LB and BL = I entrywise (hence P² = P), row sums, per-row negativities,
the exact visible set and exposedness margins, primal+dual height
certificates, the fixed reduced display and its singleton exposedness LP, all
R1 boundary assignments and the 1/80 priority arithmetic, the true ray value
via the proved `lem-l5-top-face-ray-formula`, the corrected six-target B5
overlay panel, the leaf deficit (4.3), and both regression fixtures. Exits
nonzero on any mismatch; prints the five verdict lines + two unit-test lines +
"exact checks passed; 0 genuine I-base data and 0 genuine refuters; L3
evidence only, never a proof" (orchestrator-reproduced 2026-07-14, exit 0).
Unit tests: the W63 diagonal plateau routes to D (M_I = 0, M_D = 1023/1024)
and fails tallness; the W55 A₀ = 5 completion reproduces
ν_f = 21475229695/4294967296 and is rejected. `data/certificates.json`
freezes all exact values (sha256
7625e929b15b4fbac62b69b04c5881f0e0de2e998f83d4ead9fae3291b9816c9).

## Next

Green-lights the W65 creative queue (aism-72zn §4.3 order): A-esc first (no
tested family even reaches the actorization window — a refuter must beat the
ℓ ≥ τ/2 gate on constant D mass while tall), then T-esc (the residual/tail
shape exists only with order-one negativity — the creative proof should target
the incompatibility), then G<4, C0 (with the exact local C0 cell as adverse
fixture), N. Completes the §4.2 pre-creative program for the W65 tree,
extending `runs/2026-07-11-w63-ihorn-six-shape-decider/`.
