# Run bundle: W63 decider — six-shape I-horn pre-creative batch: ALL BLOCKED (one X-cell by-catch); tallness binds for the FIFTH consecutive exact batch (2026-07-11, session 17)

## Hypothesis

bd `aism-t20p` / DECOMPOSITION-W63-I.md §4 item 2: can any of the six creative
leaves of the W63 I-horn decomposition (D natural-drift, W natural-width, Sh
tall shallow counterweight, X off-diagonal freight, I-cap ultra sign cube, D-cap
high-rank slab escape) be refuted — or their hypothesis classes even ENTERED —
by an exact family, before creative spend? A genuine candidate must be an I-base
datum: exact P^2 = P, row sums 1, delta -> 0, nonempty W, hidden top with
H > 16*tau, the full all-local-center inequalities, S >= 1/4, true ray value
Z_v(q_A)/tau -> 0. (Rigour tag: `numerical` — exact rational L3 evidence, never
proof.)

## Finding

**All six shapes BLOCKED in the tested families (X: PARTIAL definition-level
by-catch); ZERO genuine I-base data; tallness (H > 16*tau) binds for the FIFTH
consecutive independent exact batch.** Fresh codex xhigh worker, exact rationals,
orchestrator-reproduced:

- **D / W:** the coordinate corank-one family reaches the drift equality
  ||r_omega - p_v||_1 = b*tau and (for W) an exactly PAID weighted chord through
  one common receiver, with true ray value Z_v(q_A) = 2*tau^2 (exhibited
  minimizing pair = feasible top-face vector) — but its exact scalar width
  Omega = (3+4*tau^2)/(4*(1+tau^2)^2) -> 3/4 violates the parent I ceiling 1/16,
  and H = 2*tau^2 << 16*tau. The old wide fan in both cases.
- **Sh:** the W61 receiver attachment binds exactly at the top-row negativity
  budget (m_sh - (tau^2 - q) > 0 at tau = 1/256) and the legal seed stays
  cubic-height; rejected before theta or a tallness sweep can be claimed.
- **X (PARTIAL):** first exact definition-level selected-corner ledger with
  M_X = 12582911/16777220 > 1/8 (eta(1) = 4194304/4194305; large-gap freight
  mass 4194303/16777220 vs near-freight 2097152/4194305) built on the W61 graft
  at tau = 1/2048 — but NOT a lem-ihorn-selected-corner-extraction output: no
  I-base/L0 input exists (short height). Useful adverse fixture for leaf X only.
- **I-cap (the decisive search):** the attempted diagonal plateau yields an
  exact ledger with M_X = 0, M_I = 0, M_D = 1023/1024 — it MISSES the I cell by
  exactly 1/16 and routes to D; also short. The predicted sign-cube I-cell has
  STILL never been entered by any exact construction.
- **D-cap:** the D-routed ledger is rank three and short (g_u/tau one-point at
  ~4.0, mass 1023/1024), inside — not outside — the proved rank-three
  obstruction class; the canonical W55 A0 = 5 completion fails all-row
  negativity by an exact order-one margin (nu_f ~ 5 vs tau^2).

**HONEST SCOPE:** bounded search over named families; no emptiness claim; the
ED-level-set diagnostic at the D calibration is recorded as an
inapplicable-contract note (parent hypotheses fail), not a counterexample to the
proved shard. **Decision take-away:** the pre-creative decider program for ALL
NINE creative surfaces of the W62+W63 trees (S/C/I horns + six I-horn leaves) is
complete with zero class entrants; every failure names tallness and/or the
negativity budget as the wall. Non-proof green light for the creative queue
(aism-72zn: I-cap first), with the X-cell ledger and the routes-to-D plateau as
adverse fixtures.

## Command

```bash
cd runs/2026-07-11-w63-ihorn-six-shape-decider/scripts
PYTHONDONTWRITEBYTECODE=1 python3 -u search.py   # deterministic, no seed; exact Fractions
```

## Invariant (checkable)

Every claim is an exact `fractions.Fraction` assertion in `search.py`: P^2 = P
entrywise, row sums, per-row negativities, visibility/hiddenness and
primal+dual height certificates, the true ray value bracketed by an exhibited
minimizing pair and a feasible top-face vector (via the proved
`lem-l5-top-face-ray-formula`), exact scalar width by extreme-sign enumeration,
the weighted chord and its payment, the vertex kernel / corner ledger / radial
block / eta type masses for the by-catch ledgers, and the exposedness LP at u
solved algebraically for the I-cap/D-cap classification. Exits nonzero on any
mismatch; prints the six verdict lines + "exact checks passed; 0 genuine I-base
data; L3 evidence only" (orchestrator-reproduced 2026-07-11).
`data/certificates.json` freezes all exact values.

## Next

Green-lights the W63 creative queue (aism-72zn): I-cap first (a refuter must now
BEAT the exact 1/16 routing margin to enter the I cell — or the cell is
plausibly rigid), then D-cap, X (with the by-catch ledger as fixture), Sh, W, D.
Completes the §4.2 pre-creative program begun in
`runs/2026-07-10-w62-i-horn-refuter/` and `runs/2026-07-10-w63-sc-decider/`.
