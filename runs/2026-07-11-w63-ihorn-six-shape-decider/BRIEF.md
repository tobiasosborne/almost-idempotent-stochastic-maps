# W63 L3 decider batch — the six I-horn refuter shapes (exact arithmetic)

You are a fresh, independent worker. Your workspace is this directory (registry
snapshot `argument/` + `definitions/` + `context/`). Everything you produce stays
inside it. This is an L3 (constructive/numerical evidence) job: **nothing you
produce is a proof**, and your report must say so.

## Target

`context/DECOMPOSITION-W63-I.md` §4 item 2 — the six pre-creative decider shapes
gating the creative leaves D, W, Sh, X, I-cap, D-cap of the I-horn decomposition.
Read that artifact's §1.1 shared notation IN FULL (I-base datum, b = c_m/128 with
c_m = 1/4 as working threshold, k_b, G_v, Sh_v, E_c, K_v^loc, omega, r_omega,
scalar width Omega, theta and the depth rim R_48, lambda_A, the selected-corner
certificate and M_X/M_I/M_D from `definitions/def-selected-corner.md`). The TEN
routine nodes are now PROVED registry shards `argument/lem-ihorn-*.md` — consult
them; your candidates must live WITH those theorems (esp. the universal exterior
package and tall halo saturation), and compute the true dual value Z_v(q_A)
exactly via the proved `lem-l5-top-face-ray-formula`.

## The gate every genuine candidate must pass (exact rationals)

P^2 = P entrywise, all row sums 1, delta_k = delta(P_k) -> 0, nonempty visible
set W, hidden top vertex v with H > 16*tau (tau = sqrt(delta)), the FULL I-base
all-center inequalities (for every c in K(P) with ||c - p_v||_1 <= 1/4:
shallow-exterior mass < tau*S/16 AND far-exterior G_v mass >= tau*S/16), S >= 1/4,
and normalized true ray value Z_v(q_A)/tau -> 0. Tallness has been the binding
wall in FOUR consecutive searches (see context/seeds/*REPORT.md) — attack it
head-on; a short gadget without tallness is evidence only.

## The six shapes (DECOMPOSITION-W63-I.md §4 item 2 — implement each)

1. **Natural-drift completion (D):** additionally b*tau <= ||r_omega - p_v||_1
   < 1/8 and Omega(omega) < 1/16; test whether the ED payer set F_chi (construct
   it exactly per `lem-ihorn-drift-payer-extraction`) can remain co-top for the
   actual R1 ray certificate.
2. **Natural-width bouquet (W):** drift < b*tau and b*tau <= Omega(omega) < 1/16;
   record the exact weighted chord s_+*s_-*||q_+ - q_-||_1 and whether one common
   receiver pays it. The old fan is NOT a candidate (its width -> 3/4).
3. **Tall shallow counterweight (Sh):** both stats < b*tau and rim mass
   theta >= tau/D_0; sweep H/tau downward toward 16 preserving universal exposer
   shadow and all-row negativity <= tau^2.
4. **Off-diagonal freight family (X):** ultra + theta < tau/D_0 + an extracted
   corner certificate with M_X > 1/8; separately measure the mass at
   ||p_x - p_u||_1 >= b*tau. This is the exact tall completion of the W61
   graft/financer to try (seed matrices in context/seeds/).
5. **Ultra sign cube (I-cap) — THE DECISIVE SEARCH:** both statistics < b*tau,
   theta < tau/D_0, extracted corner with M_X <= 1/8 and M_I >= 1/16. This is
   the predicted high-dimensional sign-cube/dual-simplex plateau: many alpha-free
   diagonal carriers, common mean near p_v, no scalar direction with large width.
   Compute the TRUE R1 value, not pointwise deficits. A realization here kills or
   sharply restates the principal creative threat.
6. **High-rank slab escape (D-cap):** start from the W55 A0 = 5, g = 5*tau
   starvation scaffold, but require all row negativities <= tau^2, H > 16*tau,
   and support OUTSIDE the proved rank-three slab
   (`lem-starvation-completion-obstruction` pins the rank-3/slab class — read its
   contract; your family must escape its hypotheses, not fight the theorem).
   Record the distribution of g_u/tau (g_u = dist_1 of the always-tight hulls).

For the corner extraction in shapes 4-5: construct the certificate exactly per
`lem-ihorn-selected-corner-extraction` (score-selector row, a legal vertex
kernel, corner ledger, radial block, eta = Gamma_f|_B) and verify eta(1) >= 1/4
and the cell inequalities exactly.

## Deliverables (all inside this directory)

1. `search.py` — self-contained exact-rational (fractions.Fraction) construction
   + verification; EVERY claim an exact assertion; exit nonzero on any mismatch.
   Include per-shape verdict lines and one final summary line.
2. `certificates.json` — exact rational matrices + all quantities for any hit or
   best near-miss per shape.
3. `REPORT.md` — verdict per shape: REALIZED (show the certificate) / BLOCKED
   (name the exact binding inequality per attempted family, with margins) /
   PARTIAL. State explicitly this is L3 evidence, never proof. Record where
   tallness binds if it does, and any by-catch entrant to ANY leaf's hypothesis
   class (no genuine I-base datum has EVER been constructed — one exact entrant
   to any cell is the single most valuable output).

## Discipline

- `context/FINDINGS.md` dead routes are absolute; all quantities clone-invariant
  (full fibers, row points, l1); signed picture; no probabilistic readings.
- Timebox: prefer six honest BLOCKED-with-named-margins verdicts over one
  half-verified REALIZED. If time forces triage, prioritize shapes 5, 3, 4.
- Final answer: six verdict lines + one sentence.
