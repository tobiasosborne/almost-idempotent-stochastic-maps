# Run bundle: W63 decider — S/C pre-creative shapes: both BLOCKED; the C width bouquet is a complete near-refuter failing ONLY tallness (4th consecutive tallness bind) (2026-07-10, session 17)

## Hypothesis

bd `aism-3yyz` / DECOMPOSITION-W62-L5.md §4.2 items 2–3: can the S or C horn of the
proved S/C/I trichotomy for L5-GAP-1 be refuted (or their constants calibrated) by
an exact family, BEFORE creative spend? Two named shapes: (i) shallow-counterweight
completion (attach the W54 shallow payer to the W61 dyadic leak-financer in
factorized coordinates P = LB, BL = I, force the receiver into E_c ∩ Sh_v, sweep
H/tau ↓ 16 — attacks S, `conj-w62-shallow-exterior-payer-exclusion`); (ii)
two-prong engine-financing bouquet (opposing co-top actors sharing one reusable
exterior payer with q_A on the summit axis — attacks C,
`conj-w62-scalarizable-cotop-web-exclusion`, at the same-carrier alignment step
after the now-proved `lem-l5-universal-exterior-payer`). A genuine refuter must be
an L5 datum: exact P² = P, row sums 1, δ → 0, nonempty W, hidden top with
H > 16·τ. (Rigour tag: `numerical` — exact rational L3 evidence, never proof.)

## Finding

**Both shapes BLOCKED (in the tested factorized families).** Fresh codex xhigh
worker, exact rationals throughout:

- **S (shallow-counterweight completion): BLOCKED at hiddenness + negative-mass,
  exactly.** The legal W61 factorized seed has H = 2εt*/(1+ε−εt*) with t* < τ/4 —
  cubic height, H/τ → 0, never 16. The receiver attachment binds at the top-row
  negativity budget ν_A = q + m_sh + S ≤ τ²: for S = 1/4 the shallow floor
  m_sh = τS/16 alone exceeds the spare budget τ² − q for every tested τ ≤ 1/64
  (exact margins in REPORT.md). Formally dialing the face equation to H/τ = 16..20
  drives the closest-face D-weight negative and makes the proposed top VISIBLE
  (exact exposer with far margin 1/64 exhibited per dial) — the tall dials are not
  heights at all.
- **C (two-prong bouquet): the width branch is a COMPLETE near-refuter failing
  only tallness.** The m-fan family (m = 4, τ = 1/32..1/256) satisfies C's ENTIRE
  center-uniform antecedent (shallow mass 0, co-top floor ≥ 1/4 ≫ τS/16, uniform
  metric certificate over all local centers), enters the width branch exactly
  (Ω(ω) = (3+4δ)/(4(1+δ)²) → 3/4 > 1/16, drift 2δ < 1/8), realizes the weighted
  chord s₊s₋‖q₊−q₋‖₁ = Ω/2 ≥ 1/32, PAYS the C(b) engine demand through one
  reusable ballast fiber (exact payment margin exhibited), and has true ray value
  Z_v(q_A)/τ = 2τ → 0 — while H = 2δ = 2τ² << 16τ. **Tallness is the sole failed
  gate.** The separate drift routing is blocked by exact factorization forcing
  ν_b = u/4 ≤ τ², so only O(τ²) mass reaches the near transient row and the drift
  branch ‖r_ω − p_v‖₁ ≥ 1/8 is never entered.

**HONEST SCOPE:** bounded search over the named factorized families; no emptiness
claim; no genuine L5 datum was realized (S/C/I hypothesis classes still never
entered); no node-I by-catch. **Decision take-away:** TALLNESS (H > 16τ) binds for
the FOURTH consecutive independent refuter search, and now against a family that
satisfies every other C clause including the engine payment — the strongest
non-proof evidence yet that (a) the C mechanism's missing step is exactly the
chord-demand-to-ray-certificate coupling under tallness, and (b) any winning S/C/I
mechanism must consume H > 16τ quantitatively. Green light (non-proof) for the C
creative wave with the width bouquet as its adverse fixture; S's creative wave
should treat the visibility flip under tall dials as a structural hint (tall +
shallow-payer configurations fight hiddenness itself).

## Command

```bash
cd runs/2026-07-10-w63-sc-decider/scripts
PYTHONDONTWRITEBYTECODE=1 python3 -u search.py   # deterministic, no seed; exact Fractions
```

## Invariant (checkable)

Every claim is an exact `fractions.Fraction` assertion in `search.py`: P² = P
entrywise, row sums, per-row negativities, BL = I factorization identities, full
fibers, visibility/hiddenness certificates (primal + dual height certificates;
exact exposers for the rejected tall dials), the quotient submeasure and q_A, the
ray-formula minimizing pair bracketed by a feasible top-face vector (via the proved
`lem-l5-top-face-ray-formula`), r_ω, the exact scalar width by complete
extreme-sign enumeration, the uniform local-center inequalities (metric
certificate, not sampling), the weighted chord, and the engine payment ledger.
Exits nonzero on any mismatch; prints the two BLOCKED verdict lines +
"EXACT CHECKS PASSED; L3 EVIDENCE ONLY — this computation is not a proof."
(orchestrator-reproduced 2026-07-10). `data/certificates.json` freezes all exact
values; rejected completions are labeled rejection certificates, never L5 data.

## Next

Feeds the S and C creative waves (DECOMPOSITION-W62-L5.md §4.3 order: I first —
running as W63's parallel worker — then C, then S): the C prover gets the width
bouquet as its adverse-calibration fixture; the S prover gets the
visibility-flip-under-tall-dials obstruction. Together with
`runs/2026-07-10-w62-i-horn-refuter/` this completes the §4.2 pre-creative decider
program for all three horns.
