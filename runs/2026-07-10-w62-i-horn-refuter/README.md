# Run bundle: W62 decider — I-horn refuter batch: all three shapes BLOCKED; tallness binds for the third consecutive independent search (2026-07-10, session 16)

## Hypothesis

bd `aism-xeqx` / DECOMPOSITION-W62-L5.md §4.2: can the isotropic co-top-web horn I
(`conj-w62-isotropic-cotop-web-exclusion`, proposed) — or the shared S/C/I output
interface — be refuted by an exact family? Three named shapes: (i) heavy
summit-axis spike (one far-deep fiber, P_v^+ mass >= 1/4, Z_v(p_Q)/tau -> 0);
(ii) growing low-width dual-simplex fan (true ray-LP value Z_v(q_A)/tau -> 0,
for-all-local-centers co-top floor, drift < 1/8 AND scalar width < 1/16);
(iii) tall completions of the W61 graft/financer and W55 A0=5 plateau seeds.
A genuine refuter must be an L5 datum: exact P^2 = P, delta -> 0, nonempty W,
hidden top with H > 16*tau. (Rigour tag: `numerical` — exact rational L3 evidence,
never proof.)

## Finding

**All three shapes BLOCKED (in the tested families/completion classes).** Fresh
codex xhigh worker, exact rationals throughout: (i) the corank-one spike family
realizes mass 1/4, far/deep, and Z_v/tau = 2*tau -> 0 but has exact height
H = 2*delta = 2*tau^2 << 16*tau; (ii) the simplex-fan family realizes the true ray
value Z_v(q_A) = 2*delta, the local-center floor, and drift 2*delta, but fails
BOTH remaining gates: H = 2*delta < 16*tau and scalar width
Omega(omega) = (3+4delta)/(4(1+delta)^2) -> 3/4 instead of < 1/16; (iii) the W61
seeds stay cubic-height under the tested completions, and the direct W55
rank-three left inverse violates the all-row-negativity target by an exact
order-one margin (the broader W55 rank-3/slab class is excluded by the registered
bounded-slab theorem). HONEST SCOPE: bounded search over named families; no
emptiness claim; no genuine L5 datum was realized, so node I's hypothesis class
has still never been entered. Decision take-away: TALLNESS (H > 16*tau) binds for
the THIRD consecutive independent refuter search (after both W61 deciders), and
the low-width gate (Omega < 1/16) independently repels the natural fan — the
standard (non-proof) green light for spending creative effort on I, with tallness
and width as the load-bearing resources.

## Command

```bash
cd runs/2026-07-10-w62-i-horn-refuter/scripts
PYTHONDONTWRITEBYTECODE=1 python3 -u search.py   # deterministic, no seed; exact Fractions
```

## Invariant (checkable)

Every claim is an exact `fractions.Fraction` assertion in `search.py`: P^2 = P
entrywise, row sums, row negativities, full fibers, visible/hiddenness
certificates, primal+dual height certificates, the quotient submeasure, the
ray-formula minimizing pair (via the proved `lem-l5-top-face-ray-formula`),
r_omega, the exact scalar width, and the universal-local-center inequalities.
Exits nonzero on any mismatch; prints "EXACT CHECKS PASSED: 3 BLOCKED-family
verdicts; 0 genuine L5 refuters" (orchestrator-reproduced 2026-07-10).
`data/certificates.json` freezes all exact values.

## Next

Feeds the I-first creative wave (DECOMPOSITION-W62-L5.md §4.3): the I prover gets
this bundle as its adverse-calibration fixture set; the S/C pre-creative shapes
(shallow-counterweight completion, two-prong bouquet) remain to be run before
those horns are funded.
