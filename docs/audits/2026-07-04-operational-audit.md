<!--
ROLE: canonical record of the 2026-07-04 four-lane operational audit (user-mandated sober review).
PROVENANCE: 4 parallel read-only sonnet auditors (DAG census / fr process / mathematical gap map /
  evidence-and-gates), each instructed to verify against primary shards, not HANDOFF claims. Full
  transcripts live outside the repo — THIS FILE is the durable summary.
UPDATE POLICY: append corrections; items graduate out as they are fixed (link the fixing commit/issue).
-->

# 2026-07-04 — Operational audit: where the campaign actually stands

## 1. Headline

The campaign has a genuinely rigorous toolbox (~6 substantive af-validated results) and a well-factored
rank-3 attack skeleton, but **exactly ONE validated result (`lem-classical-equiv`) sits on the
machine-checked path to `op-classical`**; the frontier gate (`conj-kernel`/`conj-ex`) is completely open;
and the two largest risks are not the B-lemma — they are unpriced: **rank-3-only scoping** of the entire
G-skeleton and the **unaudited inherited downstream chain**.

## 2. Rigorous core (independent census, corroborates HANDOFF's honesty note)

- Registry: **44** results (HANDOFF said 46 — stale, fix queued). 15 `proved/af-validated`.
- Substantive 6: `lem-classical-equiv`, `obs-height-collapse`, `lem-factorization`,
  `lem-fan-payment-restricted`, `lem-residual-lower`, `lem-residual-upper`. Borderline 2:
  `conj-halo-collapse` (assembly), `lem-fan-payment` (two trivia stapled). Near-definitional 7, including
  `lem-collateral-import` (auditor would discount it too — HANDOFF's #14 not explicitly flagged there).
- The other 14 validated results live in DAG **islands** connected to the frontier by PROSE ONLY (empty
  `deps:` on `conj-ex`/`conj-kernel`); the linker gives them zero credit toward the goal today.

## 3. The full gap (what remains even if the B-lemma falls tomorrow)

B-lemma ≈ **1–5% of remaining work**. Still open after it: (V)/(Ψ) branch charging (not started, only
realized); (SC) assembly; the fan-lift `conj-degenerate-transport` (self-flagged "genuine-gap abort
predicted"); a never-written master decomposition into `conj-ex`; `conj-ex ⟺ conj-kernel` (asserted in
prose, empty deps — same profile as the retired `lem-dual-localization` tautology); **rank ≥ 3
generalization of everything** (sits exactly on the cloning/anti-splitting wall; plateau-vs-growth
UNDECIDED; if Φ/δ grows, conj-ex is FALSE — strategist credence (EX) true ~80/20); and the **entire
inherited downstream re-audit** (`thm-classical-factorization` incl. the DEFERRED JB↔stochastic
identification, HLC assembly — which upstream needed an open DMF/Baake–Sumner layer never reproduced
here —, `thm-cluster`, `prop-approx-simplex`, `lem-exposed-circuit`, `lem-leakage`).

Top mod-audit risks (load × thinness): 1. `thm-classical-factorization`/JB identification;
2. rank-3-only scoping (unflagged in HANDOFF's not-here list); 3. HLC assembly edge; 4. ex⟺kernel
equivalence; 5. `conj-degenerate-transport`.

## 4. Process findings (fr log cycles 1–106)

- **83% of wave-pulls (30/36) chased one mechanism** relabeled A→D→G; arms C/E never ran; B parked
  (successor `conj-skinny-shadow-cap` zero pulls since creation); F (cheap-decider arm) ran twice then
  abandoned while G-waves rebuilt equivalent scratch checkers.
- **The stall breaker is structurally defeated**: every narrowing wave self-tags `progress`; the string
  "stalled" appears in 0/106 cycles; the G7–G11 five-wave wall broke only on a manual user audit.
- **Reactive budget-patching confirmed ×3** (D3→R_D^ν, G5→SIGMA, G6→silent rows). The pending `+C_δ·δ`
  amendment (aism-z98) pattern-matches; its "arrives with financing proof" defense is a self-assessment
  from the same authorial line — needs independent review.
- Bookkeeping: 49% of all cycles produced no pull (70% in the latest session). Genuine new-arm EXPLORE
  events: 3 in the whole campaign.

## 5. Evidence blind spots (worst first)

1. **B-lemma data all at δ ∈ {0.233, 0.2498, 1/4}** — the domain's extreme boundary; zero certified
   argmin data at small/mid δ; 6 instances, one construction style. The lone `B=0` refuter sits at δ=1/4.
2. **(BN) is not even a registered shard**; `conj-sc`/`conj-rh`/`conj-skinny-shadow-cap`/degenerate-* have
   ZERO gated runs/ bundles — their numerics live in ungated wave prose (fails the repo's own L3 bar).
3. **Kernel/σ-cap numerics are dimension-capped at n ≤ 9** (~73k instances, 7 families) for a
   dimension-free claim.
4. Higher-rank plateau evidence varies rank but essentially not δ (fixed a=1/100).
5. Zero `cited` leaves in the DAG; the two pinned refs sources gate nothing (L1 muscle never exercised).

## 6. Mechanical defects queued for fixing

HANDOFF "46 results" vs registry 44 · `lem-dual-localization` still `kind: open-problem` (renders as live
open in DAG) · 10/15 validated af trees violate the >12-node brittleness cap (one self-waived) · chart
apparatus (Φ, E, β, λ, μ, SIGMA, FanRes) has NO def-shards and drifts across ~8 shards (r/q vs s/t index
conventions) — L2 risk directly in af-prover-facing contracts · HLC named in PRD/op-exposed-hull but has
no registry shard (invisible to the linker) · no wired finisher edges from either route into
`conj-kernel`/`op-exposed-hull`.

## 7. Recommended sequencing (adopted 2026-07-04 with the literature sweep; see bd issues)

Banking (docs/notes/FINDINGS/issues; refs-staging fetches) → **deciders before wave 13**: rank-4/5
skeleton stress test; small-δ certified argmin sweep; ex⟺kernel adversarial codex audit; + two
literature probes (SBD reset-trick transfer; ex-hume vs SBD sharpness family) → branch: wave 13 (B-lemma)
if deciders survive, else pivot (B-side / arm E / downstream re-audit become the frontier). Arm E
(error-bound route) activates as the portfolio's second genuine arm (see
`docs/lit-review/2026-07-04-literature-sweep.md` §1.3). `aism-z98`: recommend DEFER.
