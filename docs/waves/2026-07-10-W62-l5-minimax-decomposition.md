<!--
ROLE: W62 wave record — the L5 dual-face mass minimax (aism-vuc) decomposition wave.
STATUS DISCIPLINE (L0): a process record; the banked tree is a STRATEGY artifact;
every proposed node is `conjecture`; nothing is promoted.
-->

# W62 — L5-GAP-1 decomposition (the dual-face mass minimax)

**Session 16, 2026-07-10 (same session as W61). Target: `aism-vuc` — L5-GAP-1, the
W54 Branch-I closing premise and the unregistered premise of the huddle assembly
bridge (aism-pus). Objective function: decomposition into lower-complexity pieces.**

## Dispatch

One codex ultra strategist, isolated workspace (registry snapshot + W54
l5-answer.md + W54 decomposition doc + sketch v25 + the W61 wave doc). Deliverable
banked verbatim: `docs/waves/2026-07-10-W62-artifacts/DECOMPOSITION-W62-L5.md`
(+ the brief).

## The tree (headline)

**Binding-gap verdict: the gap is an engine-payer mass-transport dual, NOT a
finite cover of Y_v.** The W54 conditional cover lemma is correct but strictly
stronger than needed and retains the wrong combinatorial object. Aggregating the
A-mass on full fibers gives ONE owned synthetic barycenter q_A in K(P); L5 asks for
one top-dual direction separating it. Seven nodes:

- **R0 `conj-w62-mass-barycenter-dualization` (routine):** sup_y sum_A (P_vj)_+
  z_y(j) = S * Z_v(q_A) — the mass-to-one-point conversion.
- **R1 `conj-w62-top-face-primal-ray-formula` (routine):** finite LP duality
  collapses Z_v(q) to a constant-complexity outward visible-ray certificate
  min_{Lambda>=0, c in C_W} ( ||p_v - q + Lambda(p_v - c)||_1 - Lambda*H ).
- **R2 `conj-w62-positive-flow-foldback` (routine):** one aggregate application of
  P^2 = P: selected actors' one-step positive outflow into any fiber set F cannot
  exceed P_v^+(F) + 2*delta*(1+delta)*max(g).
- **R3 `conj-w62-universal-exterior-payer` (routine-hard):** THE new step —
  `lem-hx-forced-exterior-coupling` (W61, T0) charged over the submeasure + R2
  foldback forces ROW v ITSELF to pay P_v^+(E_c) >= tau*S/8 for EVERY center c.
  This is the engine-demand pairing W54 lacked.
- **Fork (S/C/I, creative-hard), disjoint and exhaustive after R3:**
  - **S `conj-w62-shallow-exterior-payer-exclusion`:** some local half-ball has a
    shallow (depth <= H-8tau) exterior payer => Z_v(q_A) >= gamma_S*tau. Interface
    to SL1b named in the fallback.
  - **C `conj-w62-scalarizable-cotop-web-exclusion`:** co-top payer web with
    barycenter drift >= 1/8 OR scalar width >= 1/16 => explicit weighted-chord
    engine pair.
  - **I `conj-w62-isotropic-cotop-web-exclusion` (highest info):** the isolated
    dual-simplex package — low drift AND low width; the whole-measure transport
    dual; must NOT call the financing floor at vanishing separation.
- **Assembly (quantifiers threaded in the artifact):**
  R0+R1+R2+R3+S+C+I => L5-GAP-1 with c_5 = min(gamma_S, gamma_C, gamma_I).

Kill-list audit: all nodes PASS (cover-as-main-target, summit-cylinder+averaging,
unfolded pairwise E5 sums, witness=P_v^+ identification, favorable-minimizer
selection, lex-minimal/censoring/second-web all killed pre-entry). The W61
tallness signal is consumed as evidence in all three creative horns; the only
tallness budgets permitted are the two exact height inequalities
(obs-height-collapse, lem-halo-collapse).

## Decision-relevant notes

- The tree CONSUMES the W61-validated engine bank (floor/coupling/ledger/moment) —
  the first structural payoff of the elevation investment beyond H-X itself.
- The L5 premise of the huddle bridge (aism-pus) should be registered as the
  L5-GAP-1 statement R0 converts to; hold aism-pus premise wording until the
  routine batch lands.
- Five concrete L3 refuter shapes are named in §4 for pre-creative deciders
  (summit-axis spike; shallow-counterweight completion; two-prong bouquet;
  growing low-width fan; tall completions of the W61/W55 seeds).

## Outcomes

*(Filled as results land; a dispatch is not an outcome.)*

- Strategist tree: BANKED (this wave's §The tree). Status: strategy artifact,
  promotes nothing.
- Routine batch R0-R3: prover dispatched (codex, batched-verify pattern) — see
  below.
