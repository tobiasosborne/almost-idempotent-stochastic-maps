# W64 creative wave — ATTACK the I-cap leaf (intersection-diagonal corner exclusion)

You are a fresh, independent proof strategist-prover. Your workspace is this
directory: the full registry snapshot (`argument/`, `definitions/`) + context
docs (`context/`). Everything you produce stays INSIDE this directory.
Deliverable: `ICAP-ATTACK.md` (+ optional `APPENDIX-icap-proofs.md`). Status
discipline (L0): everything you produce is proposed/`conjecture` until verified
elsewhere; you promote nothing.

## THE OBJECTIVE FUNCTION (user mandate, binding)

**Decomposition into lower-complexity pieces.** A full proof of I-cap is welcome
but NOT the success criterion. Success is EITHER:
(a) a decomposition of I-cap into routine nodes (near-mechanical on the proved
    interface) + strictly smaller creative residual(s), each a PROPER subclass or
    a constant-complexity package (the W56 one-hard-leaf wall is dead — no
    funnelling into one residual restatement); OR
(b) a proof of a quantitatively weakened I-cap (larger tallness threshold
    H > K*tau, or gamma depending on H/tau) with the exact loss recorded; OR
(c) a precise reduction of I-cap to a NAMED, isolated completion problem (the
    "internally closed sign-cube completion package") whose statement is
    strictly smaller than I-cap and comes with its own refuter shape.

## Target — verbatim pinned contract (DECOMPOSITION-W63-I.md §1.3, node I-cap)

`conj-w63-I-intersection-diagonal-corner-exclusion`. Read
context/DECOMPOSITION-W63-I.md §§0-1.1 FIRST and adopt ALL its notation (I-base
datum, b = c_m/128, k_b, delta_rt, theta, lambda_A, the selected-corner
certificate (phi, h, f, eta) and M_X/M_I/M_D from def-selected-corner):

> There are universal gamma_cap > 0 and delta_cap in (0, delta_rt] such that
> every I-base datum d with 0 < delta <= delta_cap, ||r_omega - p_v||_1 < b*tau,
> Omega(omega) < b*tau, theta < tau/D_0, for which there exists an exhibited
> selected-corner certificate C in P(d) with M_X(C) <= 1/8 and M_I(C) >= 1/16,
> satisfies Z_v(q_A) >= gamma_cap * tau.

By `lem-ihorn-priced-ray-package` (PROVED), Z_v(q_A) <= delta*D_0/S on the
I-base class — so proving I-cap means proving the hypothesis class is EMPTY
below a ceiling: an exact, tall, ultra-isotropic, thin-rim idempotent carrying
an intersection-diagonal corner certificate CANNOT EXIST. Argue from that
emptiness framing whenever it is cleaner.

## The proved interface (ALL consumable as proved; shards in `argument/`)

The W63 batch (10/10 hostile-verified): `lem-ihorn-priced-ray-package`,
`lem-ihorn-tall-halo-saturation` (T: 1 - sigma_g < (4*tau/63)*(D_0 + tau/4)),
`lem-ihorn-dual-cotop-geography` (V: witness mass > 13/16 in G_v),
`lem-ihorn-universal-exterior-package`, `lem-ihorn-drift-payer-extraction`,
`lem-ihorn-width-payer-extraction`, `lem-ihorn-ultra-compression`,
`lem-ihorn-rim-sl1b-package`, `lem-ihorn-cotop-sl1a-package`,
`lem-ihorn-selected-corner-extraction`. Plus the W62 batch (`lem-l5-*`), the
af-validated engine bank (`lem-hx-*`, incl. the corrected A > 0 financing floor
and `lem-hx-robust-scalar-starvation`), the SL1a machinery
(`lem-sl1a-score-selector`, `lem-sl1a-corner-ledger`,
`lem-radial-horn-partition`), `lem-starvation-completion-obstruction` (the
rank-3/slab completion obstruction — the closest proved relative of what I-cap
needs), `lem-optimal-face-conic-reduction`, `lem-always-tight-dual-support`,
`lem-cotop-witness-pinning`, `lem-top-deficit-price` (UPPER budget only),
`obs-height-collapse`, `lem-halo-collapse`.

The two exact tallness budgets (the ONLY ones): H(1-sigma_v) <= nu_v*D_0 and
H(1-sigma_g) <= (sigma_v-sigma_g)*tau/4 + nu_v*D_0.

## What the tree already says about I-cap (engage with it, then go deeper)

DECOMPOSITION-W63-I.md node I-cap (b)-(e): the creative task is a whole-measure
transport dual on the coefficient measure eta|_I — produce ONE common bounded
receiver statistic whose Omega(tau) demand contradicts R2 + P + T. The recorded
fallback statistic: eta_I({u}) = eta{(x,u) : p_x = p_u, u type I} is a
full-fiber submeasure of P_f^+, and
F_I := sup_{0<=g<=1} { sum_u eta_I({u}) P_u^+(g) - P_f^+(g) } is capped by
2*delta*(1+delta) via R2. A common-test lower mechanism must BEAT that cap; the
strict reverse is the isolated internally-closed sign-cube completion package.

## Fresh evidence you must consume (context/fixtures/)

The five refuter batches' convergent signal: tallness binds ALWAYS; and the W63
six-shape batch (w63-six-shape-REPORT.md) shows the natural diagonal plateau
construction gets M_I = 0 EXACTLY and routes to the D cell (disjoint hulls) —
the I cell (intersecting always-tight hulls at diagonal carriers) has NEVER been
entered by any exact construction, missing by exactly 1/16. Diagnose WHY:
what does intersecting-hulls-at-a-type-I-carrier structurally COST an exact tall
idempotent? If you can prove a lemma of the shape "an exact idempotent with
[tall + ultra + thin-rim] forces every extracted diagonal carrier's always-tight
hulls to be disjoint / forces M_I < 1/16" — that would prove I-cap VACUOUSLY
and is fully in scope (register it as the candidate hard core). Equally
valuable: an explicit exact family that DOES enter the I cell (even short) —
that sharpens the leaf instead.

## Hard constraints (interface discipline + walls; violations = instant reject)

- Signed picture; clone-invariant full-fiber quantities; frame-free constants.
- No 1/t*(v) or 1/t*(u); alpha-free displays used existentially as geography;
  the reduced witness NEVER identified with P_v^+ or eta (no lambda*P = p_v).
- No witness/y_c averaging, no Jensen, no W37 dual reversal, no raw-index paths,
  no favorable minimizer/kernel/tie, no coefficient-only LP cleanup, no
  finite-cover main target, no summed pairwise engine demands without one R2
  foldback on a common nonnegative test, no freight censoring without a norm
  gap, no second-generation web recursion, no conic recurrence.
- Every mechanism must show EXPLICITLY where T (or a parent height budget) is
  consumed — a ledger-only argument is rejected before review.
- context/FINDINGS.md dead routes are ABSOLUTE.

## Deliverable format — `ICAP-ATTACK.md`

0. **Verdict first** (<= 1 page): which of (a)/(b)/(c) you achieved, and the
   single sentence naming the hard core as you now see it.
1. **The tree / the argument**: per node — (a) pinned contract (single minimal
   statement, af-elevation-shaped); (b) mechanism naming exact banked tools;
   (c) honest price (difficulty, likeliest death, evidence both ways);
   (d) interface check (quantifier order, boundary ownership, no selectors);
   (e) fallback.
2. **Assembly**: nodes => I-cap (or the weakened/reduced statement), quantifiers
   and constants threaded explicitly.
3. **Kill-list check**: PASS/FAIL per node vs every wall above + FINDINGS.
4. **Dispatch order**: routine batch, L3 decider shapes (concrete, with exact
   target inequalities), creative residual(s) last.

Do not touch `argument/` or `definitions/`. Write ONLY the two named files.
