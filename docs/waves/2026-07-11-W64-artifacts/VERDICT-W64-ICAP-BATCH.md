B0: VALID — The diagonal Dirac property gives a full-fiber source submeasure of (P_{f^*}^+), and one legal R2 application yields exactly (e_\delta=2\delta(1+\delta)) and the stated positive-variation maximizer.
S: VALID — The fixed score is nonnegative with strict mean (<6\tau/7), so the equality-owning (12\tau/13) sublevel has mass (>1/14), and scaled ownership gives (a_A(F)>c_m/16).
C: VALID — For a kernel fixed arbitrarily before inspection, the registered corner and radial partitions classify every score-good root, and the guarded (3/42) priority census has the stated strict and weak boundaries.
G: VALID — Each (g_J) is one root-independent test, the raw foldback floors are (c_m/384) and (c_m/768), the routine ceiling pays the error, and diagonal Diracness makes the I/D statistics intrinsic vertex-set indicators.
T+: VALID — Tall-halo saturation gives the exact shallow mass bound, whose quotient is (8/63+(64/63)\tau+(16/63)\tau^2<2/15), while the corner deficit forces every cell receiver into (d>H-4\tau>\tau/4).
IC: VALID — Both foldbacks use the same arbitrary (0\le g\le1), produce overflow ((1+a_I)e_\delta), and after the explicit T-spend and receiverwise truncation leave more than (c_m/1024) in the outer halo (likewise for D).
A: VALID — Type-I hull intersection supplies an alpha-free display, giving (\rho_T\le t^*D_0<(1/2+\delta)\tau) and the strict ((7/2-\delta)\tau) dispersion bound without division by (t^*).
R: VALID-WITH-CORRECTION — The three displayed two-bin routings are exhaustive but not pairwise disjoint because the second line in each pair omits failure of the first line.

## R defect

The exact claim fails at (1.15)--(1.17). For example, in X let each of the gap and near pieces have mass (3c_m/2048). Then their total (3c_m/1024) exceeds the G floor (c_m/512), while simultaneously

\[
 \Xi_X(\mathrm{gap})\ge c_m/1024,
 \qquad
 \Xi_X(\mathrm{near})>c_m/1024.
\]

Thus both (X_{\rm gap}) and (X_{\rm near}) hold. The identical overlap occurs for I and D by assigning each piece mass (3c_m/6144): both exceed (c_m/3072), and the total exceeds (c_m/1536). Nothing in G supplies an upper bound or exclusivity that rules these distributions out.

The minimal honest correction is to make each split priority-guarded: add (\Xi_X(\mathrm{gap})<c_m/1024) to (X_{\rm near}), add (n_I(\mathrm{far})<c_m/3072) to (I_{\rm near}), and add (n_D\{g_u\ge\tau\}<c_m/3072) to (D_{\rm near}). Equivalently, state each routing as “take the first line if it holds; otherwise take the second.” This preserves all constants and assigns equality to the first line as claimed.

ASSEMBLY: GAP — Section 2 repeatedly concludes “exactly one” creative leaf from unguarded R alternatives that can overlap; the conclusion can be repaired by the priority guards above, and the separate emptiness ceiling (2.3) is otherwise correctly quantified and computed.

CALIBRATIONS: VERIFIED — I recomputed (P_I) in (A.26), (P_S), and (P=\operatorname{diag}(P_S,P_I)) in (A.33): each displayed matrix satisfies the claimed projection and row-sum identities; the factorization, negativity vectors, (A.27)--(A.32), visible sets, exposedness margins, height dual certificates, scores, cell masses, (A.34)--(A.37), and zero top ownership of the I block all agree exactly.

## What I checked hardest

I re-ran the clone audit with split and partially selected fibers; checked that S, C, G, IC, and R use only full-fiber measures; fixed the census kernel before classification; verified that I/D kernel-independence follows only from diagonal vertex Diracness; matched every R2 source domination and common-test quantifier; recomputed the (6/7), (1/14), (1/42), (c_m/512), (c_m/1536), (2\tau/15), and (c_m/1024) chains; and checked the actual registered dependency contracts, including the corrected (A>0) financing floor and the upper-only top-deficit price.
