# Wave W45 — trunk <2>7 hostile audit: thm-classical-factorization step classification (2026-07-07, session 12)

**Node:** trunk <2>7 (bd `aism-23b`, the gap-map top mod-audit risk). **Design:** single
fresh-codex hostile auditor (worker AV; classification pass, not a proof wave — findings are
audit verdicts, not theorems). Prompt + raw answer in the session-12 scratchpad (`W45/`).

## Verdict (verbatim first line)

- Worker AV: `AUDIT (steps: 18; routine: 12; needs-def: 2; gap-risk: 4)`

## Results

1. **The W27 <2>6 interface fear does NOT bind (auditor verdict, absence claim):** the <2>7
   consumer never uses the projected hull matrix as stochastic/idempotent/exact — it needs
   only `dist_1(p_i, conv W_{rho,kappa}) <= C0*sqrt(eta)`, then a 4rho-separated skeleton +
   thm-cluster. The Q-not-stochastic flag of `lem-hlc-implies-exposed-hull` transfers no risk
   at this interface. (Recorded as the auditor's verdict; the residual risk moves to G2.)
2. **G1 (moderate, THE binding interface risk): the loose-delta mismatch is a two-scale
   problem.** The registered `op-exposed-hull` wording is loose-delta (W27 finding 3), AND
   the <2>7 contract demands `kappa >= c*sqrt(eta)` while the W27-proved pinned form delivers
   `kappa ~ sqrt(d)`, `d = delta(P)` — with `d << eta` possible. Pinned-d restatement or a
   robustness lemma is REQUIRED (W46/AW is deciding exactly this).
3. **G2 (moderate):** the exposed-hull → separated-skeleton → thm-cluster instantiation is
   NOT in the local ingest (lives in upstream sibling notes only; mathematically routine per
   the auditor but unproved in-repo). Factoring target.
4. **G3 (moderate):** `thm-cluster` consumed as proved-mod-audit; no in-repo proof tree.
5. **G4 (moderate, USER DECISION):** the JB-algebra clause (J = E(l-inf), Effros–Størmer) is
   out of scope per PRD and undefined in-repo. Auditor's proposed pure-stochastic restated
   contract: "If an exact signed idempotent P with d = delta(P) <= d0 has rows within
   gamma = O(sqrt d) of the convex hull of (rho,kappa)-exposed representatives with
   rho = O(sqrt d), kappa >= c*sqrt(d), then there is a stochastic idempotent E with
   ||P − E||_{inf->inf} <= C*sqrt(d); via lem-classical-equiv this gives op-classical."
   Contract restatement = escalation; queued for the user.
6. **G5 (small):** W-nonemptiness hygiene — explicit wherever conv W is used (standing).

## Trunk ledger effect

<2>7 stays **proved-mod-audit** with its risks now NAMED and PRICED (G1–G5); the recommended
path is: restate in pure stochastic vocabulary pinned to d = delta(P) (user decision), factor
into three in-repo lemmas (pinned exposed hull ⇒ skeleton; skeleton ⇒ thm-cluster hypotheses;
thm-cluster ⇒ stochastic idempotent), leave JB external. No status change (honest tiers).

## Banking (orchestrator)

Wave doc only; bd: aism-23b updated (classification pass done), factoring + USER-DECISION
issues filed. No registry shard changes (audit wave).
