# Hostile verification of the W56 extraction shards

The ten shards are judged independently.  A `VALID-WITH-CORRECTIONS` verdict below means that the displayed mathematical statement and proof survive, but the registry-ready shard needs the listed mechanical repair before promotion.

## Preliminary audit — `extraction/DEAD-ROUTES.md`

1. **NOTE — lines 5–9: all five entries accurately report their cited verdict findings.**  Line 5 is exactly the r1-F1/r2-F1 restatement obstruction; line 6 combines r2-F1's unused-minimality defect, r2-F3's transient-extension failure, and r2-F11's confirmation that integer attainment itself is sound; lines 7–9 respectively match r2-F4's missing censoring norm gap, r2-F5's center/band/score failures in the proposed second generation, and both parts of r2-F6's max-principle/same-carrier failure.  No entry overstates the cited finding.

## `lem-affine-barycenter-identity.md` — VALID

1. **NOTE — lines 15–33: the proof is complete and exact.**  Writing the affine function as `L+c` and using total mass one gives `integral(L(p_x)+c)=L(b)+c`; no nonlinear statistic is asserted or used.
2. **NOTE — lines 4 and 35–37: the contract is standalone, dimension-free, and clone-invariant.**  It has no registry dependencies or numerical constants.  The note expressly removes the false nonlinear-interchangeability claim identified in W56-R2 Finding 7.

## `lem-clone-invariant-row-complexity.md` — VALID

1. **NOTE — lines 15–30: both finiteness and lexicographic attainment re-derive correctly.**  A finite nonempty row-point set has a finite nonempty vertex set, and two applications of well-ordering to the nonempty image in `N^2` produce an attained lexicographic minimum.
2. **NOTE — lines 15, 24, and 34: the invariant is geometric rather than a raw index count.**  Under the stated natural coordinate identification, clone splitting changes multiplicities only, hence neither the distinct-point set nor its extreme points.  The shard does not claim that minimality has a valid consumer or is transient-extension stable; its final note correctly records the certified dead route.

## `lem-radial-horn-partition.md` — VALID

1. **NOTE — lines 15–41: the two alternatives are disjoint and exhaustive with all boundaries owned.**  The predicates `distance >= 4 tau` and `distance < 4 tau` partition `C`; failure of far mass `>=1/4` makes the near mass strictly greater than `1/2-1/4=1/4`.  Distance equality and mass equality are correctly assigned to (F).
2. **NOTE — line 45: the Proposition-E red-test wording is the corrected wording.**  The standalone bookkeeping lemma can admit the shallow diagonal lift, while the assembled corner excludes it through `z<4 tau => depth>H-4 tau`; the shard does not falsely claim standalone exclusion.

## `lem-sl1a-score-selector.md` — VALID-WITH-CORRECTIONS

1. **NOTE — lines 35–49: the selector proof is correct and uses one genuinely proved dependency.**  `argument/lem-top-deficit-price.md:4,7` has `status: proved` and contains the quoted existence/`ANY phi` clause.  Its hypotheses hold because `delta>0`, `W` is nonempty, and `v` is a hidden top.  The quote gives `0<=z` and one-Lipschitzness; row diameter gives `z<=D`, so `z/D` is admissible.  Applying the all-exposer mean bound to `z/D` twice and to `h` once gives exactly `12 tau/13`, and finite-support averaging selects `f` with the weak inequality.
2. **NOTE — lines 21–30 and 51–53: all SL1a constants and strict support properties are preserved.**  `(16/13)kappa=4 tau/13`; the selected support point remains both `4 tau`-far and strictly deeper than `H-4 tau`.  The barycenter is deliberately unused, and no antipode or nonlinear measure inference occurs.
3. **MINOR — front-matter contract line 4: `putative SL1a counterexample datum` is a dangling parent shorthand.**  The body statement repairs the ambiguity, but the canonical one-line contract required by `argument-README.md` must itself spell out `P`, `delta,tau,D,W,v,H,lambda`, the support/depth conditions, the barycenter radius, and the all-exposer mean bound.

## `lem-zero-face-one-sixteenth-capacity-kill.md` — VALID

1. **NOTE — lines 22–30: the dependency, hypotheses, strictness, and constant are all correct.**  `argument/lem-zero-face-capacity-kill.md:4,7` is `status: proved` and contains the quoted clause.  With `c_r=1/16`, shipping at least `1/16` gives `tau/64<=delta=tau^2`, hence `tau>=1/64`; but `delta<=2^-16` gives `tau<=2^-8=1/256`.  Thus equality at `1/16` is excluded and the conclusion is strictly `<1/16`.
2. **NOTE — lines 15–18 and 32–34: the statement is standalone, dimension-free, and clone-invariant.**  It uses a value-defined full coefficient-mass sum, invokes no disjoint-hull or horn route, and does not divide by the possibly tiny exposedness margin.

## `lem-sl1a-corner-ledger.md` — VALID-WITH-CORRECTIONS

1. **NOTE — lines 55–98: all three imports are genuinely established and quoted faithfully.**  `argument/lem-top-deficit-price.md:4,7`, `argument/lem-harmonic-affine-bridge.md:4,7`, and `argument/lem-mass-split.md:4,7` all have `status: proved`.  Their hypotheses hold: `delta>0`, `W` is nonempty, `v` is a hidden top, `z,h` are affine row observables, and `f` is an actual row point.  Full-fiber aggregation commutes with each index sum.
2. **NOTE — lines 65–115: the reproduction and two-marginal ledger is correct.**  Harmonic reproduction gives `S_f=z_f+h_f+sum P^-_{fx}(z_x+h_x)`.  The score and `D>=2` give `z_f+h_f <= (D/2)(2z_f/D+h_f) <= 6D tau/13`; hence `S_f<=6D tau/13+nu_f(D+1)`.  Each bad coordinate costs at least `4 tau` in its own `q=z+h` moment, so the two-coordinate union bound is exactly `S_f/(2 tau)`.  Using total mass `1+nu_f`, dropping the favorable `+nu_f`, and applying `nu_f<=tau^2` gives `1-3D/13-tau(D+1)/2`.
3. **NOTE — lines 117–129: the constant and depth conversion check exactly.**  The lower bound is decreasing for `0<tau<=1/256`; at the endpoint `D=32769/16384` it is exactly `58079731/109051904=1/2+3553779/109051904`.  The support inequality `phi(p_y)<=d_y` gives `H-d_y<=z_y`, so strict `z_y<4 tau` gives strict depth `d_y>H-4 tau`; since this is positive, the vertex coordinate is outside `conv W` and hence hidden.
4. **NOTE — lines 27–41 and 131–133: the kernel construction does not violate the W55 carrier wall.**  It retains the coefficient coordinate `x` and vertex coordinate `u` jointly and never calls the `u`-marginal transition mass.  A finite row polytope supplies the stated vertex representation, Dirac behavior at vertices is forced, and full-fiber masses make the statement clone-invariant.  No certified dead route is used.
5. **MINOR — front-matter contract line 4: `SL1a top datum (v,phi,h)` is undefined canonical-contract shorthand.**  The body supplies the missing hypotheses, but the registry contract itself must replace that phrase by the explicit nonempty-visible-set, hidden-top, `H>16 tau`, top-support-functional, and admissible-exposer clauses and must define `d_f`, `D`, the legal kernel, and `C_f` rather than relying on body-local notation.

## `conj-sl1a-off-diagonal-cell.md` — VALID-WITH-CORRECTIONS

1. **NOTE — lines 34–41: this is one quantified exclusion and its numerical cell is the correct first residual branch.**  For a fixed selected datum and fixed owned radial cell, the condition `M_X(B)>1/8` owns the strict side; equality `M_X(B)=1/8` is left to the diagonal branch.  The constants are universal and independent of dimension, and full-fiber `P^+_{fx}` plus row-point kernels make the condition clone-invariant.
2. **NOTE — line 47: the refuter criterion is correctly asymptotic.**  It requires exact instances along `delta_k -> 0`; it does not incorrectly claim that one fixed positive-scale instance refutes an existential ceiling.
3. **MINOR — lines 20 and 36, and front-matter line 4: `top support functional` and the canonical phrases `rho-far co-top`, `resulting doubly-low coupled corner`, and `owned radial cell` are not defined in the shard's imported definitions.**  The local notation defines the kernel, `Gamma_f,C_f,B_F,B_N`, and the body expands far/co-top numerically, but it never defines a top support functional as affine with `phi(p_v)=H`, `phi<=0` on `C_W`, and one-Lipschitz.  The front-matter contract must also replace its descriptive shorthands with the displayed definitions so the canonical statement itself is standalone.

## `conj-sl1a-intersection-diagonal-cell.md` — VALID-WITH-CORRECTIONS

1. **NOTE — lines 41–48 and 54: the intended type-I exclusion is quantified, dimension-free, clone-invariant, and has the correct `delta_k -> 0` refuter criterion.**  Hull tangency/equality is owned by type I through nonempty intersection, and mass equality at `1/16` is correctly owned by this branch.
2. **MAJOR — lines 43–47: as stated this is not the minimal residual cell and is not disjoint from the off-diagonal cell.**  A datum can have both `M_X(B)>1/8` and `M_I(B)>=1/16`, so it satisfies both conjecture antecedents.  This overlap does not break the ordered proof—either exclusion already contradicts the datum—but it fails the requested literal/minimal three-cell partition.  Add the preceding-branch guard `M_X(B)<=1/8` to this statement.  That weakening is exactly what the reduction knows on entering the type-I test.
3. **MINOR — lines 20, 34–39, and front-matter line 4: essential vocabulary is only descriptive, not defined.**  Define a top support functional explicitly.  Also define the exposedness optimal face and give formulas such as `T(u)={r:dist(p_r,p_u)>=4 tau and h(p_r)=t*(u) for every optimal h}` and `O(u)={i:h(p_i)=1 for every optimal h}`; merely saying “constraint families tight on the whole optimal face” leaves the LP face and its variables implicit.  The canonical contract must expand its `rho-far co-top`, `legal kernel`, `resulting corner`, `owned radial cell`, and hull shorthands or import dedicated definitions for them.

## `conj-sl1a-deep-diagonal-cell.md` — VALID-WITH-CORRECTIONS

1. **NOTE — lines 41–48 and 54: the intended type-D exclusion is quantified, dimension-free, clone-invariant, and has the correct asymptotic refuter criterion.**  Hull disjointness is the exact complement of type-I intersection for each carrier, and the mass condition is correctly strict `>1/16`.
2. **MAJOR — lines 43–47: as stated this is not the minimal final residual cell and overlaps both earlier cells.**  Data with `M_D(B)>1/16` may simultaneously have `M_X(B)>1/8` or `M_I(B)>=1/16`.  The ordered reduction remains logically valid, but a literal residual partition requires the guards `M_X(B)<=1/8` and `M_I(B)<1/16`.  Those are precisely the assumptions available when the proof reaches the type-D branch; adding them weakens the conjecture without changing the assembly.
3. **MINOR — lines 20, 34–39, and front-matter line 4: the same standalone-definition defect occurs here.**  Explicitly define top support functionals, the exposedness LP's optimal face, and the always-tight sets `T(u),O(u)`, and expand or formally import all body-local kernel/corner/radial/hull notation used by the canonical contract.

## `lem-sl1a-three-cell-reduction.md` — VALID-WITH-CORRECTIONS

1. **NOTE — lines 59–106: the SL1a constants and selection route are coherent.**  Fixing `delta_0=min(2^-16,delta_D,delta_I,delta_X)` gives `tau<=1/256` and `D<=32769/16384`; `(16/13)kappa=4 tau/13` and `2.2 tau=11 tau/5` are exact.  `argument/lem-top-deficit-price.md:4,7` and `argument/lem-positive-exposedness-margin.md:4,7` are `status: proved` and are quoted faithfully with their hypotheses satisfied.  The finite exposedness LP does attain its optimum.  Applying the all-exposer bound to `z/D` twice and to `h` once produces a support point with score at most `12 tau/13`, still `4 tau`-far and strictly co-top.
2. **NOTE — lines 110–147: the coupled-corner computation re-derives correctly for every legal kernel.**  Finite-polytope disintegration supplies the kernel without identifying its vertex marginal with transition mass.  `argument/lem-harmonic-affine-bridge.md:4,7` and `argument/lem-mass-split.md:4,7` are proved and quoted exactly.  Reproduction gives `S_f<=6D tau/13+nu_f(D+1)`; the two coordinate failures cost `S_f/(2 tau)` in total, hence `Gamma_f(C_f)>=1-3D/13-tau(D+1)/2`.  At the ceiling this is exactly `58079731/109051904>1/2`.
3. **NOTE — lines 149–167: every diagonal carrier is legally typed using established shards only.**  `phi(p_y)<=d_y` implies `d_u>H-4 tau>12 tau`, so `u` is hidden and every visible row is more than `4 tau` away.  Positive exposedness gives `0<t*(u)<kappa`.  `argument/lem-always-tight-dual-support.md:4,7` is proved, its quoted clause is present, and its hypotheses hold; therefore `T(u),O(u)` are nonempty.  Compact-hull intersection versus disjointness is exhaustive, with tangency owned by type I.
4. **NOTE — lines 169–193: the ordered three-cell routing has no numerical seam.**  The radial split owns distance equality on `B_F` and mass equality at `1/4` on the far choice.  If `M_X>1/8`, X fires; otherwise diagonal mass is at least `1/8`.  If `M_I>=1/16`, I fires; otherwise `M_D>1/16`.  Thus off-diagonal equality belongs to the diagonal continuation and type-I equality belongs to I.  Although the unguarded conjecture antecedents overlap as sets, the ordered proof is sound; after the minimal guards specified in the two conjecture findings are added, the contracts also become a literal partition.
5. **NOTE — lines 193 and 215–217: existence-plus-universality routing is handled correctly.**  The proof needs only one legal choice of `phi,h,f,xi,B`, while each “no datum admits” conjecture rules out any tuple satisfying its antecedent.  Noncanonical choices and the possibility that both radial cells have mass at least `1/4` therefore create no gap.  No conjecture is treated as established; all three are explicit conditional premises.
6. **NOTE — lines 195–213: the parent-verdict constant corrections are genuinely applied.**  Markov gives a score-subweb of mass at least `1/2`, not `7/13`.  On it the recomputed corner floor is exactly `7746547/109051904>7/100`; the text correctly distinguishes the web proportion from the per-row corner floor.  No nonlinear barycenter interchangeability is claimed, and the corrected Proposition-E logic remains strict co-top exclusion rather than standalone exclusion by a bookkeeping leaf.
7. **MINOR — lines 59–73: the proof silently replaces a target measure “on rows” by a measure on geometrically distinct row points.**  This is valid but must be stated: push the index measure forward under `i -> p_i`; barycenters, affine-exposer means, support inequalities, and total mass are unchanged.  Without that one sentence the clone-invariant routing step is implicit.
8. **MAJOR — lines 219–223: the transient-row extension note falsely infers that `W` and `H` are unchanged merely because the new row is a convex combination.**  The row polytope and its vertices are unchanged, but exposedness tests all rows at distance at least `rho`.  A newly appended nonvertex convex combination can itself be `rho`-far and adds a new constraint `h(p_new)>=t` to the exposedness LP; it can lower `t*(u)`, change visibility, and hence change `W` and `H`.  This note is unused by the implication.  Delete the unconditional `W,H` invariance claim and restrict the lift assertion to extensions for which the recomputed exposedness data are proved unchanged (or supply a separate lemma imposing a sufficient far-row condition).
9. **MINOR — line 61: `\delta_0,qquad` is a malformed TeX token.**  Replace it by `\delta_0,\qquad`; this is presentation-only.
10. **MINOR — front-matter contract line 4 and conditional-assumption summary line 48: the canonical contract is a meta-reference rather than a standalone quantified sentence.**  Expand the three conditional cell assumptions and the exact pinned SL1a conclusion (as the body already does), or formally list/import the three conjecture contracts; “the conjectures ... hold with ceilings” is not self-contained registry-contract text.  When the minimal residual guards are added to the I and D contracts, line 48 must quote those guards too; the ordered proof already supplies them.

## Count

Four shards are valid as written, six are valid after the enumerated mechanical contract/note corrections, and none has a fatal mathematical proof defect.
