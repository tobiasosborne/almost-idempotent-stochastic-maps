# Hostile verification of `DECOMPOSITION.md`

1. **FATAL — §3 H-CCO, lines 524–529: “This is a strictly smaller configuration-data space than SL1a.”**  The claimed removal is not a restriction to a smaller configuration class.  H-CCO retains the full exact idempotent `P` and tall hidden-top geometry, drops the existence of `lambda`, its barycenter constraint, and its all-exposer constraint, and adds an arbitrary all-row vertex kernel.  Thus its antecedent ranges over a *broader* class of matrices/tuples than the SL1a antecedent; projecting away `lambda` does not make that class a strict subspace.  The promised one-line implication is exactly the prohibited pattern: from an SL1a object, mean-select `f,e`, reproduce at `f` to get `Gamma_f(C_f)>1/2`, choose its horn, and contradict H-CCO.  The new `Gamma_f` is an always-available repackaging of one row of the same full matrix, not a reduction of rank, row-point geometry, carrier family, or matrix configuration space.  Therefore the sole HARD-leaf permission (“strictly smaller configuration space”) is not met, and H-CCO is the residual exclusion restated after routine preprocessing.

2. **FATAL — §3 H-CCO next-level sketch, lines 420–440 and 500–522: “`H-DI` … `H-DD` … `H-MX`.”**  None of these three descendants is stated as a quantified lemma with hypotheses, conclusion, or constants.  `H-MX` is only the off-diagonal part of H-CCO renamed “factorization obstruction”; lines 509–522 then say a dimension-free certificate “must” be produced.  `H-DI` and `H-DD` likewise name “completion” problems without stating what they assert or how they close.  The sole proved progress inside the sketch is the `c_r=1/16` capacity rejection of the high-shipping subcase.  Partitioning the remaining hard configuration and assigning names to every residual cell is not a one-level-deeper decomposition path.  This violates the mandatory non-vacuous further-decomposition condition for the one permitted HARD leaf.

3. **MAJOR — §2.1 exhaustiveness, lines 111–114, 128–143, and 174–176: “every counterexample … follows one and only one path.”**  The path is not unique.  `phi` and the relative-interior optimal exposer are chosen noncanonically; L-S only existentially selects `f,e`; and the vertex kernel `xi` is also chosen.  Different legal choices can yield different corner measures and opposite horn labels.  No clone-invariant tie-breaking rule is stated, and a raw ordering would violate the clone wall.  The horn split is disjoint only *after all these choices are fixed*.  The assembly needs merely “at least one path, and every possible selected tuple is killed,” but the claimed exactly-once routing/exhaustiveness is false as written.

4. **MAJOR — §3 H-CCO grading/mechanism, lines 400–413 and 524–529: the advertised reductions are not used by the proposed proof program.**  The antipode `e` and its `9*tau/5` separation never occur anywhere in the H-CCO sub-DAG or completion mechanism after the statement.  Likewise the labels `F,N` are immediately treated through the same diagonal/off-diagonal program; no descendant uses which horn occurred.  Consequently the target's `2.2*tau` barycenter information and the radial horn split are syntactic decorations on the hard leaf rather than resources that reduce its hardness.  This makes the “strictly smaller object” and HARD-tier justification materially misleading: the proposed closer is the same all-row completion wall even after deleting `e` and `sigma`.

5. **MAJOR — §3 H-CCO red test and §5 non-vacuity, lines 531–550 and 713–719: “may satisfy the local corner” / “mutually consistent locally.”**  This does not run the required W55 test on H-CCO's hypotheses.  The `A0=5, g=5*tau` tableau is explicitly not a full exact idempotent with tall visible-hull geometry, whereas those are H-CCO hypotheses.  No “obvious co-top analogue” satisfying the full antecedent is constructed; the text only says one *may* satisfy its local scalar pattern.  Hence the recorded PASS is not a yes/no hypothesis check, and the hard antecedent class could be empty.  Local consistency of selected scalar ledgers does not establish non-vacuity of the full all-row completion class.

6. **MINOR — §3 L-P Proposition-E red test, lines 357–365 and 380–382: “PASS—L-P does not alter `C`.”**  As a standalone leaf L-P has no co-top/depth hypothesis at all.  The Proposition-E two-point counterweight measure can be diagonally lifted to a pair measure and `C` can be chosen with mass `>1/2`, so it does satisfy L-P's hypotheses.  The leaf's conclusion is harmless bookkeeping and the assembly supplies a co-top `C_f`, but the recorded statement that this leaf does not re-admit the shallow object is false under the task's leaf-by-leaf red-test wording.

7. **MINOR — §1 transient-row test, lines 92–102: “if the recomputed visible/depth data invalidate that antecedent, the extension is outside that leaf rather than a refutation.”**  This is not a stability verification; it excuses failure of the antecedent.  For the displayed standard extension the repair is available and should be stated: the appended row is a convex combination of the embedded old rows, so the row polytope, its geometrically distinct vertices, admissible-exposer values, `W`, and `H` are unchanged (with one zero coordinate appended), and old coefficient fibers acquire a zero new column.  The conclusions then lift.  As written, the red-test argument does not prove the requested hypothesis stability, although the omission is mechanically correctable.

8. **NOTE — §4 assembly arithmetic, lines 570–665: no arithmetic defect found.**  Independently: `delta_H<=2^-16` gives `tau<=1/256`; `D<=2+2^-14=32769/16384`; `(16/13)kappa=4*tau/13`; `2.2*tau=11*tau/5`; `4*tau-11*tau/5=9*tau/5`; and the score mean is `12*tau/13`.  In L-C, `S_f<=6D*tau/13+nu_f(D+1)`, the two-coordinate union bound gives `Gamma_f(C_f)>=1+nu_f-S_f/(2*tau)>=1-3D/13-tau(D+1)/2`, and at the ceiling this is exactly `58079731/109051904=1/2+3553779/109051904`.  The strict implication `z<4*tau => d>H-4*tau`, the unchanged `rho=4*tau`, and `H>16*tau` are all correctly carried.  The optional high-shipping calculation is also correct: `(1/16)kappa<=delta` gives `tau/64<=tau^2`, hence `tau>=1/64`, contrary to `tau<=1/256`.

9. **NOTE — quoted-clause fidelity, §§1, 3, and 4: the cited registry clauses checked out.**  I opened all ten consumed or wall shards.  Each has `status: proved`, and the quoted parts of `lem-top-deficit-price`, `lem-harmonic-affine-bridge`, `lem-mass-split`, `lem-positive-exposedness-margin`, `lem-always-tight-dual-support`, `lem-optimal-face-conic-reduction`, `lem-separator-zero-face-obstruction`, `lem-zero-face-capacity-kill`, `lem-intersection-witness-confinement`, and `lem-l2-core-collapse` are present with the used hypotheses.  The assembly satisfies those hypotheses where it invokes them.  `conj-straddling-web-exclusion` is quoted only as the target, and no `status: conjecture` shard is used as a premise.

10. **NOTE — wall audit, §§2–4: no direct Proposition-D, W55-carrier, B6, or raw-count violation found in the formal front end.**  The only `lambda` pairings select `f`; L-C then uses exact reproduction at the non-top row `f`.  `Gamma_f(x,u)` retains the actual coefficient coordinate and does not identify its vertex marginal with transition mass; dual multipliers are not treated as flow; no mutually-`rho`-far family is quantified; and all displayed thresholds are row-point mass sums.  The hard leaf's proposed basis coordinates still owe the dimension-free certificate they promise, but no such certificate is claimed as an established assembly step.

11. **NOTE — routine-leaf grading and vacuity.**  L-S is a correct finite averaging/dual-norm argument, L-C is correct bounded measure bookkeeping plus vertex disintegration, and L-P is the stated exact dichotomy; EASY/EASY/MEDIUM are honest for those three.  Each is usable whenever the previous pipeline antecedent exists.  H-CCO alone has neither a demonstrated full antecedent nor a non-vacuous deeper closer, which is the substantive HARD-leaf failure in Findings 1, 2, 4, and 5.

12. **NOTE — mandatory W55 gadget outcomes, leaf by leaf.**  The literal local `A0=5, g=5*tau` tableau satisfies none of L-S, L-C, or H-CCO's full matrix antecedents because it is not a globally completed exact idempotent with the required visible-hull geometry; it can satisfy the scalar score/corner ledgers used by L-C, so L-C correctly makes no exclusion.  A pair-measure built from the tableau can satisfy the abstract L-P antecedent and is merely assigned a horn.  A globally completed co-top analogue would satisfy H-CCO precisely when it also has the selected score, corner, and horn data, and would refute that leaf along a sequence `tau->0`; no scalar ledger in the document excludes it.  Thus the only claimed closer is the unresolved global completion statement itself.

13. **NOTE — restatement tests.**  L-S alone yields only selected row points; L-C alone yields only a coefficient/disintegration corner; and L-P alone yields only a two-cell partition, so none of those implies SL1a, L2-core, or the huddle charge.  H-CCO fails for the reasons in Finding 1: the complete derivation of SL1a from it is the single routine sentence “select by the two means and barycenter, reproduce at the selected row, partition the corner, contradict H-CCO,” while the alleged smaller configuration is not a restricted matrix class.

14. **NOTE — known-family coverage, §5 lines 688–711.**  The numerical routing statement itself is arithmetically honest: `H>16*tau` implies `H^2/delta>256`, while the banked census reports ratios below `16`, so the named W52 families are outside the SL1a root rather than evidence for an internal horn.  Proposition E exits the strict co-top root and the W55 tableau is only a local H-CCO refuter target.  This covers the listed data without using it as a proof premise, but it supplies no exact in-class witness for the non-vacuity concern in Finding 5.

15. **NOTE — boundary ownership after choices are fixed.**  The numerical splits themselves have no seam: far `u` owns equality at `4*tau`; the far horn owns mass equality at `1/4`; the diagonal branch owns equality at half the horn mass; hull tangency belongs to intersection; and high shipping owns equality at `1/16`.  The exhaustiveness defect is therefore not a missing numerical boundary but the unacknowledged nondeterministic selection before those splits (Finding 3).
