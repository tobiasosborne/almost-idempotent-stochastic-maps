# W56 round-2 repair map — VERDICT-W56.md findings 1-15 → DECOMPOSITION-v2.md

| # | Severity | Finding (short) | Repair in DECOMPOSITION-v2.md |
|---|---|---|---|
| 1 | FATAL | H-CCO ranged over a BROADER class (dropped lambda, added a free all-row kernel) and was derivable back to SL1a in one routine sentence | Hard leaf replaced by H-W (§3): its antecedent contains SL1a's (h1)-(h2) VERBATIM (nothing dropped, constants untouched), restricted further by (h3) lex-minimality of `(V(P), R(P))` — a genuine matrix-configuration-space restriction supplied by the new EASY leaf L-M — and by the forced carrier structure (h5)-(h7), where the kernel's corner property is a hypothesis (forced for EVERY kernel by L-C), not free data.  The logical direction is reversed and stated three ways (§3 H-W (a)-(d)): H-W is implied by SL1a, does not imply it in any number of routine lines, and the excluded class is a strict refinement (tuples), the lex-minimal stratum (matrices), and a weaker statement (logic). |
| 2 | FATAL | H-DI/H-DD/H-MX were named cells, not quantified lemmas | The one-level-deeper program (§3 H-W) now consists of: SD0 (proved inline, 4 lines), SD1/SD2 (owned mass splits), H-D1 (PROVED capacity kill, verbatim shard composition at `c_r = 1/16`), and three FULLY-QUANTIFIED descendants H-D(sigma), H-I(sigma), H-X(sigma) — each with hypotheses (PRE(sigma) + a displayed mass inequality), conclusion, explicit constants (`1/16`, `1/8`, normalizations `||ell||_inf <= 1`, `|m| <= 3+4*delta`, `osc <= 5+8*delta`, band `9*tau`), a concrete closing mechanism (H-D: three exact ledgers + per-horn coupling channel; H-I: the proved-inline H-I0 banded-second-web brick + generation recursion with its named termination residual; H-X: the explicit clone-invariant moment system with the required Farkas-certificate shape and non-locality condition, plus the minimality-surgery route with its censoring-inflation obstruction priced), and an exact-instance kill criterion.  A checkable sub-assembly (H-D1+H-D+H-I+H-X ⇒ H-W) with constant read-order closes the level. |
| 3 | MAJOR | "Exactly one path" routing is false; nondeterministic selections (phi, h*, f, xi) unhandled; no clone-legal tie-breaking | §2.1 reformulated to exactly what is provable: (R-a) AT LEAST one complete legal tuple exists per counterexample; (R-b) H-W excludes EVERY legal tuple (its antecedent packages all selections existentially, horn-parameterized).  The exactly-once claim is withdrawn explicitly.  No canonical choice or tie-breaking exists anywhere; the §1.0 barycenter identity shows all lambda-selections are equivalent through `b`; the kernel is handled by universal quantification (L-C is kernel-universal), chosen over canonicalization precisely because any tie rule would risk the clone wall (§6 pruned list). |
| 4 | MAJOR | The `9*tau/5` antipode and the far/near horn label were unused decorations | Antipode: DROPPED, with the account required (L-S section): it was a two-line corollary of the barycenter clause, and the hard leaf now retains the barycenter clause itself verbatim in (h2) — nothing lost, `9*tau/5` eliminated from the document.  Horn label: made load-bearing — it routes the sub-assembly and each of H-D/H-I carries a per-horn mechanism that consumes it (far horn: row `v` lies in every carrier's far set, so `h_u*(p_v) >= t*(u)` and `psi_u(p_v) = sum_j P_vj psi_u(p_j)` couples to v's own coefficients, the Proposition-D-permitted channel; near horn: carriers huddle in the `4*tau`-ball, and H-I compares the second-generation barycenter `b_u` with the web barycenter `b` inside a `7*tau` ball — consuming the `11*tau/5` clause).  A hypothesis-to-consumer table (§3 H-W) maps every carried hypothesis to its consumer, with one honest flag on the all-exposer clause's residual strength. |
| 5 | MAJOR | The W55 gadget was not run as a yes/no against the full hypotheses; non-vacuity of the hard class unaddressed | H-W's red test runs the gadget item-by-item against (h1)-(h7): definite NO with the failing clauses named ((h1): all-row exactness, W, H undefined; (h2): no web), and a definite record of what it DOES satisfy (the local scalar ledgers), converted into a binding requirement on H-D/H-X mechanisms (must consume a resource the gadget lacks; a Farkas certificate realizable by the gadget's local data is declared self-refuting in H-X's kill criterion).  Non-vacuity: stated exactly — the hard class is empty IFF SL1a holds (the pipeline equivalence), so H-W IS the emptiness lemma, not a footnote; §5 (W1)-(W5) enumerates exactly what an in-class witness must exhibit, with the completion-LP route named and the census obstruction quoted as L3 evidence only. |
| 6 | MINOR | L-P's Proposition-E red test wording false (the diagonal lift DOES satisfy standalone L-P) | Rewritten per the verifier's own wording (L-P red tests): the shallow counterweight's diagonal pair-measure lift satisfies standalone L-P, whose conclusion is exclusion-free bookkeeping; re-admission into the assembly is blocked because there L-P is fed `C_f`, whose `z < 4*tau` clauses force depth `> H - 4*tau` by (1.1). |
| 7 | MINOR | Transient-row stability was excused, not verified | §1.3 spells out the verifier's own repair verbatim: the appended row point is a convex combination of the embedded old row points; the row polytope, geometrically distinct vertices, admissible-exposer value profiles, `W`, all `d_j`, and `H` are unchanged (one zero coordinate appended, `l1`-isometric); old fibers acquire a zero column; conclusions lift.  Two additions: `delta(P') = delta(P)` computed, and the honest one-way note (lift only; deletion need not preserve a web) which also disarms a false minimality shortcut. |
| 8 | NOTE | Assembly arithmetic independently confirmed | No change needed — all confirmed constants and the (3.7) fraction are reused verbatim; the `9*tau/5` line is deleted along with the antipode (its only consumer). |
| 9 | NOTE | All quoted shard clauses checked out | No change to the surviving quotes; four NEW shard quotes added for the sub-DAG (`lem-separator-zero-face-obstruction`, `lem-zero-face-capacity-kill`, `lem-optimal-face-conic-reduction`, `lem-always-tight-dual-support` — the last two were already quoted in round 1), each verbatim from the shard contract at point of use.  `lem-psi-corner-trap` is deliberately NOT quoted (its preamble pins the top vertex); its constants are re-derived inline at carriers (H-D statement). |
| 10 | NOTE | No wall violations in the front end; the hard leaf still owed its certificate | Front end preserved; the owed certificate is now a stated REQUIREMENT with shape and non-locality conditions inside H-X (not claimed as an established step), and the B6/pairwise audit is now trivial: no pairwise-separation clause remains in the document. |
| 11 | NOTE | L-S/L-C/L-P grades honest; H-CCO lacked antecedent and closer | Grades kept (L-S EASY now score-only, L-C MEDIUM, L-P EASY, plus L-M EASY); the H-CCO defect is addressed by findings 1/2/5 repairs above. |
| 12 | NOTE | Gadget outcomes leaf-by-leaf; only closer was the unresolved completion | Incorporated: each leaf's red test now records the gadget yes/no in the verifier's terms; the completion statement is no longer "the only claimed closer" — it is quantified (H-X) with its certificate obligations, and H-D/H-I carry independent mechanisms (max-principle ledgers; second-web recursion). |
| 13 | NOTE | Restatement tests: routine leaves pass; H-CCO fails | Re-run for all five leaves (each §3 subsection): L-M/L-S/L-C/L-P pass as before; H-W passes with the logical direction stated (it cannot imply SL1a, L2-core, or the huddle charge because it is silent off its restricted class — the round-1 failure is structurally impossible). |
| 14 | NOTE | Coverage honest; no in-class witness supplied | Table kept; the non-vacuity gap is now handled by the §5 witness-requirement list (W1)-(W5) and the emptiness equivalence (finding-5 repair), rather than left open. |
| 15 | NOTE | Numerical boundaries seamless; the defect was pre-split nondeterminism | Boundary ownerships kept and extended (diagonal at half horn mass, type-I at tangency and at `1/16`, shipping at `1/16`); the pre-split nondeterminism is resolved by the finding-3 repair (existence + universality), stated in §2.1 as "boundaries of predicates evaluated AFTER a tuple is fixed; no seam claim across tuples". |

Status discipline: everything in DECOMPOSITION-v2.md is AUTHOR-CLAIM; H-D1 is the
only descendant claimed as a proved composition (of `status: proved` shards, with the
verifier-confirmed arithmetic); H-I0 and SD0 are proved-inline candidate bricks
pending independent review; H-D, H-I, H-X, and H-W are open conjecture-grade
statements; no status is promoted.
