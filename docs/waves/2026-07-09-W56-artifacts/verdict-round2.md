# Hostile verification of `DECOMPOSITION-v2.md`

1. **FATAL — §3 H-W, lines 530–537, 857–863, and 882–886: “H-W does NOT imply SL1a at all” / “the class ... is empty IF AND ONLY IF SL1a holds.”**  The document contradicts itself, and the latter assertion is the correct one.  From H-W alone, using only the established/inline facts already proved in this document, SL1a follows in two lines: assume an SL1a counterexample; well-order the nonempty set of integer pairs `(V,R)` and choose a lex-minimal one, then choose `phi,h*,f,xi,sigma` by the proved margin/support shards, scalar averaging, finite-polytope disintegration, the re-derived L-C ledger, and the literal horn partition; this produces an H-W tuple, contradiction.  L-M, L-S, L-C, and L-P add no open mathematical premise: §§3.1–3.4 prove them from well-ordering, proved shards, and arithmetic.  Thus H-W is logically equivalent to SL1a at its ceiling, exactly as lines 857–863 admit.  Minimality does no established work in any descendant: its only advertised consumer is H-X Route 2, which immediately says the required negativity-thrifty deletion is a “NEW ... open sub-problem” (lines 796–810).  The lex-minimal label is therefore a decoration on the same residual emptiness statement, not a cured hard leaf.

2. **FATAL — §3 H-W sub-DAG, lines 629–826, and §6 lines 1021–1035: “H-D ... HARD (open),” “H-I ... HARD (open),” “H-X ... HARD (open).”**  The round-1 named residual cells have been expanded into quantified residual exclusions, but they have not been decomposed into plausibly routine leaves.  H-D stops at “the program must show” that its ledgers cannot be completed (lines 666–671); H-I explicitly says its required termination functional “is not yet constructed” (lines 745–749); H-X says the Farkas proof object is “to be produced” (lines 786–795) and that its alternative needs a “NEW ... open sub-problem” (lines 796–810), while Route 3 “cannot terminate alone” (lines 811–817).  Their conclusions merely ban the three mass-partition cells whose union is PRE, so the sub-assembly closes only by assuming every residual cell empty.  The sole proved descendant H-D1 is logically unused: H-D's statement already bans every type-D mass `>1/16` without a shipping hypothesis, so lines 830–838 derive H-W from H-D/H-I/H-X whether H-D1 is present or not.  This is the round-1 defect with quantified names, and it also violates the target's “at most one leaf harder than MEDIUM” rule: the document itself labels H-W plus all three terminal descendants HARD.

3. **FATAL — §1.3 lines 166–170 and H-W red test lines 864–868: “the extension maps the class into the class-or-its-non-minimal-shadow.”**  That is an admission that H-W and every PRE-descendant fail the mandatory transient-row stability test.  Given any H-W tuple, its web contains a row `f` distinct from `v` because `||p_f-p_v||_1 >= 4*tau>0`.  In the standard extension (1.4), choose `mu` so `mu P=(1-theta)p_v+theta p_f` is not one of the finitely many old row points (all but finitely many `theta in (0,1)` work).  The document's own correct extension calculation preserves exactness, `delta`, `W`, `H`, all old web data, and `V`, while the new nonvertex point makes `R(P')=R(P)+1`.  The original `P` remains in the same counterexample class with smaller `(V,R)`, so `P'` necessarily fails (h3).  Hence H-W's hypothesis is not stable under a transient-row extension; saying the assembly can re-select a different representative does not repair the leaf-by-leaf red test required by `target.md`.  Clone splitting preserves `(V,R)`, but transient extension does not, exactly as the requested red test warned.

4. **MAJOR — H-X Route 2, lines 796–810, especially “the banked exact deletion is censoring: `lem-censoring-exactness` ... gives `S^2=S`.”**  The cited proved shard applies only to a block decomposition with `a=||A||_{inf->inf}<1`.  No such bound is stated or derived for the block of freight rows; indeed signed row blocks need not have norm below one.  Therefore the shard does not supply an exact freight deletion at this point, and the ensuing inflation discussion is premature.  The citation is also absent from H-W's purported exhaustive May-consume list at lines 506–509 and from the “only ... established inputs” list in §1.1.  This is a quoted-clause hypothesis failure, not merely an unfinished estimate, and it removes the only concrete banked operation behind the advertised minimality consumer.

5. **MAJOR — H-I mechanism, lines 722–749: “run the L-C corner computation at a second-generation web row `r`.”**  H-I0 itself re-derives the advertised display bounds correctly, but those bounds do not satisfy L-C's hypotheses.  A row `r in T(u)` is `rho`-far from the carrier `u`, not necessarily from the original top `v`; `u` has depth merely `>H-4*tau` and is not a top vertex of height `H`; and `z_r<9*tau` is not the L-C score inequality `2z_r/D+h(p_r)<=12tau/13` for an exposer at a hidden top.  Thus L-C cannot be recursively invoked with either center.  The text supplies no replacement two-observable corner estimate, no value of `s_2`, and no recurrence showing that the next band exceeds `16tau`; “the recursion terminates in at most two generations” is unsupported.  The later admission that no decreasing termination functional exists confirms that the asserted second-generation route does not compose.

6. **MAJOR — H-D mechanism, lines 651–683: “since `psi_u>0` on `T(u)` ... the maximal slab is far-side.”**  Positivity on `T(u)` implies only that the global maximum is positive; it does not imply that a maximizing or near-maximizing row belongs to `T(u)` or is `rho`-far from `u`, because `psi_u` is unconstrained in sign on all other rows.  The claimed far-side return channel therefore does not follow from the max-principle ledger.  The proposed web-of-corners repair also has a same-carrier gap: applying the corner estimate to many web rows produces different kernels, carriers, types, and horns, with no lemma forcing those rows to finance the particular type-D carrier selected from `Gamma_f`.  This is precisely the W55 carrier-coincidence obstruction, not a concrete closing mechanism for H-D.

7. **MINOR — §1.0 lines 39–47: “two webs with the same barycenter and support set are interchangeable in every statement of this document.”**  The three-line calculation proves equality only for affine integrals.  It does not determine nonlinear sublevel masses used later, such as `lambda{q>24tau/13}`.  For example, on four collinear support points `0,1,2,3`, the positive weight vectors `(1/4,1/4,1/4,1/4)` and `(2/5,1/10,1/10,2/5)` have the same support and barycenter `3/2`, but different mass above the affine-score threshold `2`.  The routing repair needs only `integral h=h(b)` and existence of a support point below an affine mean, both valid; the stronger “interchangeable in every statement” consequence is false and should be deleted.

8. **MINOR — H-W hypothesis-to-consumer table line 552 versus the proved-inline calculation at lines 603–611: “a `7/13`-mass sub-web of reusable corner-builders.”**  Markov gives only web mass at least `1/2`, not `7/13`; the number `7/100` is the lower bound on each selected row's corner mass.  At `tau=1/256`, the recalculated corner floor is `7746547/109051904 ≈ 0.0710354 > 7/100`, while the web fraction remains `1/2`.  No downstream displayed inequality uses `7/13`, so this is mechanically correctable, but the consumer table conflates two different constants.

9. **MINOR — H-X risk criterion, lines 824–826: “A completed exact instance carrying `>1/8` off-diagonal freight refutes H-X.”**  H-X asserts existence of some positive ceiling `delta_X`; one instance at `delta=d>0` can be evaded by choosing `delta_X<d`.  A refutation requires a family with `delta -> 0` satisfying PRE and (H-X.1), as H-D correctly states at lines 686–690.  H-I's analogous “family” criterion at lines 751–754 also needs this limiting condition made explicit.

10. **MAJOR — repair audit for round-1 Finding 4, H-W table lines 552–559 and the per-horn prose at lines 672–683 and 735–744: “the label is consumed, not decorative.”**  The antipode was genuinely removed and L-C never needed it, but the horn and barycenter repairs remain aspirational.  In H-D(F), `v in F_u` gives an exposer inequality at `v`, yet no displayed argument turns row-`v` reproduction into the claimed contradiction; in H-D(N), the web-of-corners rows are not tied to the selected carrier (Finding 6).  In H-I(N), the `7tau` barycenter comparison is followed only by the phrase “targeted overdetermination,” with no inequality it violates; H-I(F) likewise only says the generations are “coupled.”  H-X has no per-horn mechanism at all beyond naming the horn cell.  Consequently the label and `11tau/5` datum do no proved work in the descendants; they merely index three assumed branch-emptiness statements.  This renames, rather than cures, the round-1 decoration defect.

11. **NOTE — L-M and §1.3, lines 141–170 and 247–285: the integer minimization itself is well-defined.**  For each finite matrix, `V` and `R` are finite positive integers; a nonempty subset of `N^2` attains its lexicographic minimum by two applications of well-ordering; clone splitting preserves geometrically distinct row points; and no numerical constant depends on either count.  Conditional on a counterexample existing, the minimal stratum is even a proper matrix subclass because the transient construction in Finding 3 produces nonminimal counterexamples.  The defect is not existence or attainment of the minimum; it is that minimality is unused in any valid descendant and violates the required transient stability.

12. **NOTE — §§3–4 arithmetic and the deleted antipode: all surviving front-end constants recheck, apart from Finding 8.**  At `delta<=2^-16`, `tau<=1/256` and `D<=32769/16384`; `(16/13)kappa=4tau/13`, `2.2tau=11tau/5`, and the L-S mean is `12tau/13`.  L-C gives `S_f<=6Dtau/13+nu_f(D+1)` and `Gamma_f(C_f)>=1-3D/13-tau(D+1)/2=58079731/109051904>1/2` at the ceiling.  The doubled-score family gives the exact floor in Finding 8.  H-D1 correctly turns `kappa/16<=delta` into `tau>=1/64`, contradicting `tau<=1/256`.  The implication `z<4tau => d>H-4tau`, the strict `H>16tau`, and the unchanged radius `rho=4tau` are carried correctly.  No downstream proof uses the deleted `9tau/5` antipode; only historical prose mentions it.

13. **NOTE — §2.1 and §4 routing: the round-1 nondeterministic-choice defect is otherwise repaired.**  L-S is universal in `phi,h`, L-C is universal in the legal kernel, L-P partitions each fixed tuple, and H-W's “for each horn, no tuple” statement excludes every legal choice; the assembly needs only one.  No canonical tie-breaker or raw index order is used.  The far/near, diagonal/off-diagonal, I/D, and high/low-shipping equalities all have coherent owners.  The affine identity `integral h=h(b)` is correct and suffices for mean selection; only the overstrong interchangeability assertion in Finding 7 fails.

14. **NOTE — quoted-shard audit:** every registry result cited as an input has `status: proved`, and no conjecture shard is used as a premise.  The quoted contracts of `lem-top-deficit-price`, `lem-harmonic-affine-bridge`, `lem-mass-split`, `lem-positive-exposedness-margin`, `lem-always-tight-dual-support`, `lem-optimal-face-conic-reduction`, `lem-separator-zero-face-obstruction`, `lem-zero-face-capacity-kill`, `lem-intersection-witness-confinement`, and `lem-l2-core-collapse` contain the used clauses and have their hypotheses at the stated front-end uses.  `lem-psi-corner-trap` and `lem-genuine-disintegration` are mentioned but not consumed.  The sole failed proved-shard use is `lem-censoring-exactness` in Finding 4.

15. **NOTE — mandatory red tests and coverage.**  The revised L-P Proposition-E record is correct: the shallow diagonal lift satisfies standalone L-P but cannot enter the assembled corner because `z<4tau` forces strict co-top depth.  The literal W55 `A0=5, g=5tau` tableau definitely fails H-W's (h1)–(h2) because it is not a completed all-row exact projection and supplies no web; it does satisfy the scalar ledgers, which is why Findings 2 and 6 reject those ledgers as closers.  Clone splitting is harmless, while transient extension fails exactly at minimality (Finding 3).  The named W52/banked families all have `H^2/delta<16`, whereas the root requires `>256`, so their pre-root routing is arithmetically honest; it is evidence only and supplies no hard-class witness.  L-M/L-S/L-P are honestly EASY and L-C honestly MEDIUM, but H-W's three HARD terminal descendants make the claimed one-HARD tier grading false (Finding 2).
