<!--
ROLE: verbatim harvest artifact of fr arm B, wave 3 (2026-07-02). Worker: opus:prover (subagent).
STATUS: HEURISTIC exploration report (L0). Verdict: OBSTRUCTION (the one-sided ledger) + REDUCTION
(sigma-cap => conj-kernel is rigorous via the af-validated collapse; the reverse is open/probably
false, so the cap may be STRICTLY STRONGER than the kernel). The R3 decider (low-height sigma->1
exact search) was already in flight as arm F wave 2 when this report landed.
Referenced by .frontier/log.jsonl (arm B wave 3) and bd aism-yxq.
-->

# Arm B · wave 3 · the σ̃-cap — harvest (verbatim report)

(verbatim deliverable follows — see .frontier log c-record for the summary)

## 1. READ + the DMF ⇔ σ̃-cap logical map

(Full source list with loci in the fr log; key map:)

Two **parallel, independent sufficient conditions** for the kernel height cap `H ≤ B·τ`:

| | picture | bridge to `H ≤ Bτ` | bridge status |
|---|---|---|---|
| **σ̃-cap** `1−σ̃_v ≥ cτ` | **primal**: row v's coefficients on `C_W` vs outside | obs-height-collapse: `H ≤ ν_v(2+4δ)/(1−σ̃_v) ≤ (2+4δ)τ/c` | **af-VALIDATED** |
| **existential-DMF** | **dual**: an optimal failed-exposedness witness with deep mass `≥ m*` | W2 sharp exchange `m*(H−E) ≤ t*Ω < κΩ` (`08-residual-dmf.tex:53–80`) | proved-mod-audit |

- σ̃-cap ⇒ conj-kernel [solid, rigorous in-repo]. **Its bridge is the rigorous one; DMF's is only mod-audit.**
- Neither implies the other (DMF needs the optimal dual face; σ̃-cap is pure primal).
- Reverse (kernel ⇒ σ̃-cap) OPEN, probably FALSE ⇒ **σ̃-cap ≥ kernel in strength, possibly strictly stronger**.

## 3. THE ARGUMENT (structure)

**Mass bookkeeping [solid, clone-invariant]:** `a_j := P_vj`; `S = Σa_j⁺ = 1+ν_v` (export.md 1.1.2). Three pots:
Pot 1 `π_v` = C_W-mass; Pot 2 `σ^dpo_v` = deep-but-outside; Pot 3 `σ^sh_v` = shallow-outside.
`σ̃_v = σ^dpo+σ^sh`, `π_v + σ̃_v = 1+ν_v`, hence **`1−σ̃_v = π_v − ν_v`**; **σ̃-cap ⟺ `π_v ≥ ν_v + cτ`**.

**Step A [solid]:** if `σ̃_v ≤ τ` the cap holds with c=1 (`1−σ̃ ≥ 1−τ ≥ τ` for δ≤¼). The cap's whole content sits in the kernel-antecedent regime `σ̃_v > τ`.

**Step B [the obstruction]:** the harmonic identity `0 = g_v = Σ a_j g_j` gives `M_{≥H} := Σ_{g_j≥H} a_j⁺ ≤ δ(2+4δ)/H`, and `π_v ≤ M_{≥H}` — pots 1 AND 2 are upper-bounded by O(δ)/H; the residual `1+ν_v − O(δ)/H` is FORCED into pot 3. **Every campaign tool (validated collapse, deep-leakage, W2 exchange) produces only UPPER bounds on pot 1; the target needs a LOWER bound on the identical quantity. NAMED OBSTRUCTION: the one-sided ledger.**

**Step C [solid reduction]:** the only lower-bound route = excluding a self-sustaining shallow web (pot 3 ≈ 1) = the recorded anti-splitting / signed quantitative Baake–Sumner frontier (`kernel:318–322`, `08:198–209`), with a critical-scale coincidence at `H ∼ τ` (upper bound `(2+4δ)τ` vs needed `cτ` — a constant-factor fight, no slack). Aggregate forms do NOT escape the class-counting need.

**Step D [check → risk]:** a kernel-safe-but-cap-false config is conceivable: low height `H = O(δ)` with `σ̃_v → 1` (mass in the thin O(δ)-halo just outside C_W). The collapse bound is permissive at low H. No such instance is known (F1: σ̃→1 forces δ>¼ in all tried families) — but if one exists, the right target is the HEIGHT-CONDITIONAL kernel, not the height-free cap.

## 4. RESIDUALS (contract candidates)

- **R1**: `π_v − ν_v ≥ c√δ` for hidden top vertices (⟺ the σ̃-cap) — implies conj-kernel rigorously; open at anti-splitting; possibly strictly stronger than kernel.
- **R2**: shallow-outside mass `σ^sh_v ≤ 1 − c√δ` (the shallow-web-exclusion core = signed quantitative Baake–Sumner).
- **R3 (cheap decider)**: compute `sup σ̃_v` over hidden top vertices at ALL heights, esp. low-H thin-halo straddling configs. Decides R1-vs-height-conditional retarget. [= arm F wave 2's territory]

## 5-6. DEAD-ROUTE AUDIT + VERDICT

All binding constraints respected (clone-invariant pots; harmonic identity used linearly; existential-DMF only; no pushed-witness). **VERDICT: OBSTRUCTION (the one-sided ledger) + REDUCTION (σ̃-cap ⇒ kernel is rigorous; the missing half IS the anti-splitting wall; two-sided-bound sharpening: the campaign has the upper side, needs the lower).**

## 7. NEXT PULL

1. R3 decider FIRST (in flight as arm F wave 2). Outcome A (sup σ̃ ≤ 1−Θ(τ)): σ̃-cap survives, R2 becomes the attack target. Outcome B (low-H σ̃→1 instance): cap false-but-kernel-safe → retarget arm B to the height-conditional kernel + death certificate.
2. Do NOT open af on R1/R2 (would balloon into the genuine anti-splitting gap — the §6 tripwire would fire).
3. The live sub-question remains: dimension-free bound on the number of distinct shallow classes hit by P_v⁺.
