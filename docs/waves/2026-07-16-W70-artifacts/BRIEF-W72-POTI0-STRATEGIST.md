# W72 creative wave — ATTACK the POTI-0 residual (zero oriented-surplus synchronization)

You are a fresh, independent proof strategist-prover. Your workspace is this
directory: the full registry snapshot (`argument/`, `definitions/`) + context
docs (`context/`). Everything you produce stays INSIDE this directory.
Deliverable: `POTI0-ATTACK.md` (+ optional `APPENDIX-poti0-proofs.md`). Status
discipline (L0): everything you produce is proposed/`conjecture` until
verified elsewhere; you promote nothing.

## THE OBJECTIVE FUNCTION (user mandate, binding)

**Decomposition into lower-complexity pieces.** A full proof of POTI-0 is
welcome but NOT the success criterion. Success is EITHER:
(a) decomposition into routine nodes on the proved interface + strictly
    smaller creative residual(s), each a proper subclass or constant-
    complexity package; OR
(b) a proof of a quantitatively weakened POTI-0 (an extra hypothesis
    STRICTLY WEAKER than actorization or than positive overlap, with the
    exact loss recorded; or constants depending on H/tau); OR
(c) a precise reduction of POTI-0 to a NAMED, isolated problem strictly
    smaller than POTI-0 with its own refuter shape.

## Target — the registered conjecture (verbatim in the registry)

`conj-dtr-zero-oriented-surplus-exclusion` (open, registered W70; read the
shard in `argument/` for the full inline contract): every pinned DTR datum
with exactly G_phi = 0 obeys (EC):
  Z_v(q_A) >= (1/8)*P_v^+(E_*) - (c_m/16)*P_v^+(L_v).
Read `context/DTR-ATTACK-W69.md` IN FULL FIRST (§1.5 is POTI-0; §1.1 pins
the datum from context/AESC-ATTACK-W67.md §1.6, which adopts
DCAP-ATTACK-W65.md §1.1 and DECOMPOSITION-W63-I.md §§0-1.1).

## The verified interface you stand on (NEW since the attack doc)

The W70 pipeline (4/4 VALID, zero corrections) proved and codified:
- `lem-dtr-canonical-overlap` (COV): rho = min{m_A, eta_D*|_B} is a
  clone-invariant common submeasure on the full row-point quotient.
- `lem-dtr-oriented-tail-ray-conversion` (POTI-R): S*Z_v(q_A) >= G_phi.
- `lem-dtr-tail-coherent-conversion` (TC): the actor-free weakened
  conversion with exact loss.
- `lem-dtr-poti-assembly`: POTI-0 + POTI+ => exact (EC) on every pinned
  DTR datum (conditional; you may treat its case split as settled — your
  job is ONLY the G_phi = 0 case).
Consult each shard's OWN hypothesis block. Also available: lem-aesc-* (5),
lem-dcap-* (7), lem-ihorn-* (10), lem-l5-* (4 — esp.
mass-barycenter-dualization, top-face-ray-formula, positive-flow-foldback,
universal-exterior-payer), lem-top-deficit-price, lem-top-support-dual-face,
the af-validated lem-hx-* engine bank, the two exact tallness budgets.
CAUTION: lem-icap-* carry the incompatible M_I >= 1/16 block — do NOT
consume. The conditional lem-huddle-charge-assembly and
lem-intersection-branch-production are NOT consumable here (circularity:
POTI-0 feeds conj-l5-gap-1's reduction tree).

## What POTI-0 actually asks (the hard core, stated honestly)

G_phi = 0 means the canonical overlap carries no oriented surplus: either
(i) rho(1) = 0 — the root-owned population eta_D*|_B and the top-selected
population m_A are mutually singular on the row-point quotient — or
(ii) rho(1) > 0 but every overlapped carrier moment t_phi(u) <= D_0*delta
(orientation starvation). In either case the proved POTI-R conversion
yields nothing, and (EC) must come from somewhere else: a top-facing charge
NOT seen by the pinned deficit. §1.5(b)'s two axes: force positive common
ownership with positive z-surplus (contradicting G_phi = 0 on the pinned
class), or price another legal top-face direction through the SAME L5 mass
objective with an arbitrary attained ray certificate.

## Fresh evidence you MUST consume (context/fixtures/)

- `2026-07-16-w71-poti0-zero-overlap-decider/` (REPORT.md + README.md +
  data): THE named candidate mechanism. In the exact anchor/probe
  growing-rank family, max_i nu(P_i) = beta*a exactly (beta = public-root
  mass on probe fibers, a = 1 - 1/m + tau/20): R0 carrier ownership
  (eta_D* <= P_f*^+) needs beta >= 1/8 while the negativity budget needs
  beta <= tau^2/a — support disjointness (rho(1) = 0) survives ONLY outside
  the gate, and the ownership repair cost tends to 1/8, rank-UNDISTRIBUTABLE.
  Diagnose whether this is the general mechanism: does carrier ownership
  (the R0 output eta_D* <= P_f*^+ at the public center) + the delta
  negativity budget FORCE either rho(1) > 0 or an order-one cost, on the
  WHOLE pinned class? If yes, decompose that statement; if no, name the
  exact escape and its price. Orientation starvation (ii) is UNTOUCHED
  territory — no exact family has ever realized it; treat it as a separate
  case with its own residual.
- `2026-07-14-w69-dtr-growing-rank-decider/`: the local-vs-global lesson
  (local DTR geometry free at zero finance negativity; every global gate
  fails rank-uniformly; H/tau = 0 the first wall).
- Both deciders' seventh-consecutive tallness bind: no tested family has
  EVER entered the tall class. W55/W57/W58/W66 remain L3 calibration only.

## Hard constraints (violations = instant reject)

- Signed picture; clone-invariant full-fiber quantities; frame-free
  constants; row-point ell^1 geometry.
- No 1/t*; conic coefficients never transitions; the synthetic actor never
  vertexized; no favorable row/minimizer/kernel/tie/certificate/atom
  selection; no witness/y_c averaging, Jensen, W37 reversal, raw-index
  paths, coefficient-only cleanup, finite-cover main target, summed
  pairwise demands without one R2 foldback on a common nonnegative test,
  freight censoring without a norm gap, second-generation web, conic
  recurrence.
- ONE common nonnegative test per foldback; TU's union is the only owned
  DTR aggregation; POTI-R's z/D_0 conversion is proved — a NEW foldback
  needs its own common test and source-mass-scaled error.
- The B5 population is NOT eta_D*'s; no carrierwise alignment.
- Boundary ownership: G_phi = 0 belongs to YOU (POTI-0); D_POTI = 0
  belongs to the routine close; h = 3*delta to D-tail; actor-residual
  equality 3*delta to T-esc; mass equality 1/160 to HES; coherence
  equality to the TC coherent class.
- **Every mechanism must show EXPLICITLY where T (or a parent height
  budget) is consumed, AND the exact line producing the (1/8)*P_v^+(E_*)
  main term (E at p_f*, once), AND the exact subtraction of
  (c_m/16)*P_v^+(L_v) BEFORE B4.1 is used. A ledger-only argument is
  rejected before review.**
- `lem-l5-universal-exterior-payer` may be used only at one fixed center
  with one common test; it does not place populations outside a required
  half-ball for free (DTR-ATTACK-W69.md §1.5(b)).
- context/FINDINGS.md dead routes are ABSOLUTE.

## Deliverable format — `POTI0-ATTACK.md`

0. **Verdict first** (<= 1 page): which of (a)/(b)/(c) you achieved; one
   sentence naming the hard core as you now see it.
1. **The tree**: per node — (a) pinned contract (single minimal statement,
   af-elevation-shaped, no 'hence'); (b) mechanism naming exact banked
   tools; (c) honest price (difficulty, likeliest death, evidence both
   ways); (d) interface check; (e) fallback. Handle the rho(1) = 0 and
   orientation-starvation cases explicitly (one split is legal — it is the
   §1.5(e) fallback and you own it).
2. **Assembly**: nodes => POTI-0 (or the weakened/reduced statement),
   quantifiers and constants threaded explicitly, ending at the exact (EC)
   line then (via B4.2 once at p_f*, B4.1 last) the c_m*tau/64-derived
   close.
3. **Kill-list check**: PASS/FAIL per node vs the K1-K14 codes of
   DTR-ATTACK-W69.md §3 + every FINDINGS wall.
4. **Dispatch order**: routine batch, L3 decider shapes (exact target
   inequalities; D_POTI/D_EC/D_leaf distinct — they are proved ORDERED,
   D_leaf >= D_EC >= D_POTI/S), creative residual(s) last.

Do not touch `argument/` or `definitions/`. Write ONLY the two named files.
