# Wave W32 — the CS low-slab pincer + the theta-flexible frontier statement (2026-07-07, session 11)

**Node:** sketch v7 ledger item 5 (the coupling), bd `aism-n7i`/`aism-2fi`. **Design:** single
fresh-codex prover (worker Z; brief carried the orchestrator's theta-flexible re-scoping and
the untouched CS channel from W29's harvest) + SEPARATE fresh hostile verifier VZ. Prompts +
raw answers in the session-11 scratchpad (`W32/`). Paper wave.

## Verdicts (verbatim first lines)

- Worker Z: `PARTIAL (proved: complementary-slackness low-slab pincer and aggregate-exposer
  conditional cap; gap: no theorem couples the hiddenness witness/low-exposer slab to the
  actual row coefficients `P_vj^+` defining `sigma_a`)`
- Verifier VZ: `VALID (minimal hypotheses: exact signed idempotent `P`, row index `v`, affine
  `h` with `h(p_v)=0` and `0<=h(p_j)<=1` on all rows, and `s>0`; the `G_a` corollary only
  needs `G_a` as a chosen subset with `sigma_a` defined.)`

## Results

1. **`lem-cs-low-slab-pincer` (codified, proved/af:none).** For ANY admissible-at-v affine h:
   P_v⁺{h ≥ s} ≤ ν_v/s. VZ established the MINIMAL hypotheses (no hiddenness, no vertex, no
   top, no W, no δ > 0) and exact fixture EQUALITY at s = t* on the W29 frontier instance —
   the bound is sharp. Four-line proof (row reproduction + row sums + sign split + Markov).
2. **`conj-low-slab-cap` (registered conjecture — THE frontier statement, theta-flexible).**
   Some hidden top's optimal exposer has low-slab coefficient mass ≤ 1 − θ − 4τ, for ANY
   universal (a, θ): composition with the pincer gives σ_a ≤ 1 − θ there, and the af-validated
   parametric collapse closes the height bound at B = K_a/θ. conj-min-a-w4's 1/2 is the
   (4, 1/2) calibration point, NOT load-bearing (the session's theta-flexibility re-scoping,
   confirmed [T0] by worker Z).
3. **Theta-flexibility confirmed + the cohabitation picture:** hiddenness pins > 94% of the
   witness mass in the deep low-exposer slab (depth-Markov); the pincer forbids row mass in
   the high slab; a counterexample must make row mass and witness mass cohabit the deep low
   slab totally. VZ confirmed the gap honesty: CS alone can NEVER cap the low slab (mass at
   h = 0, incl. all self-mass, is invisible to the identity).

## Banking (orchestrator)

Registry: the two shards above (VZ as reviewer for the lemma; the conjecture enters as
`conjecture`, promoting nothing). FINDINGS 2026-07-07 (W31+W32 entry) records the unified-
mechanism convergence. Honest tiers: reviewed paper proof (L5) + a registered conjecture; NOT
af-validated, NOT L0-rigorous; the cap remains OPEN.
