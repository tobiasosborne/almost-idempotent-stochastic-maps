# Wave W52 — the pre-registered tall-entry experiment: BLOCKED, binding constraints named (2026-07-07, session 12)

**Node:** sketch v15's (M1)-vs-(M2) mode-decider (FP1 pre-registration). **Design:** one
fresh-codex exact-search worker (BH) executing and extending the FP1 lab2.py
instrumentation with its own independent scripts. Bundle:
runs/2026-07-07-w52-tall-entry/.

## Verdict (verbatim first line)

`BLOCKED (binding constraints: no tried exact perturbation reached H^2/delta > 16; C4
stayed empty; when top/top-cluster behavior was restored, the always-tight hulls
intersected)`

## Effect on the mode choice (v16)

(M2) TALL-EMPTINESS is now the favoured mode: the class is unreachable along every tried
exact family, and the THREE binding constraints are the scaffolding of an emptiness proof
(delta inflation; forced visibility of the deep row; top reversion forcing intersection).
(M1)'s reach conjecture remains the rank-3 fallback — its quantified domain is exactly what
BH could not populate. Honest scope: bounded search over named families; evidence, never
proof (L3).
