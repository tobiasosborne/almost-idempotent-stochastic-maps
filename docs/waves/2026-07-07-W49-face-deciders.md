# Wave W49 — the Tier-1 face deciders: both faces reduce to TIGHTNESS PROMOTION; in-class disjointness never realized (2026-07-07, session 12)

**Node:** the two faces of conj-zero-face-elimination (sketch v13 (F1)/(F2)), user directive
"eliminate unknown steps" / "all creativity and all means". **Design:** three fresh-codex
workers — BD ((F2) prove) ∥ BE ((F2) refute) ∥ BF ((F1) prove) — + hostile verifier VBD over
the BD/BF proved pieces (in flight at doc time); PLUS the Fable-grade lane: FP1/FP2 (deep
proof attempts, in flight), FP3 (bypass audit, delivered), LIT (opus literature strike,
delivered → W50). Prompts + raw answers in the session-12 scratchpad (`W49/`, `W49-fable/`).

## Verdicts (verbatim first lines)

- BD: `PARTIAL (proved: bridge only needs m_kappa(z) > 4*tau, and the harmonic ledger has an
  exact thin-blocker obstruction; gap: tallness must exclude/select away O(delta)-depth
  zero-face blockers)`
- BE: `NOT-REFUTED (binding constraint: disjoint T/O gaps persist only while G_4=C_4=empty
  and H<4*tau; frontier: W41 HEIGHT+A / W29 append rays)`
- BF: `PARTIAL (proved: harmonic row-coordinate balance + row-support-to-interval criterion;
  gap: row-positive far support is not promoted to always-tight T under tall/heavy)`
- FP3 (Fable bypass audit): `NO-CHEAPER-BYPASS` (docs/plans/2026-07-07-bypass-audit.md)

## Results

1. **The bridge requirement pinned (BD):** any m_kappa(z) > 4*tau closes (F2) — constant c0
   is overkill; capacity caps every blocker at exactly 4*tau, so disjointness must DEMAND
   what capacity FORBIDS.
2. **The thin-blocker gadget (BD; codification pends VBD):** an exact graft
   z = (1+eps(1-t))u + t*eps*o - eps*a produces a universal zero-face blocker with kappa-high
   mass t*eps — ledger-only proofs of (F2) are DEAD; tallness must do real work
   (anti-thin-blocker selection).
3. **The tightness-promotion convergence (BF + BD):** (F1)'s residual (promote row-positive
   far balance-witnesses into always-tight T) and (F2)'s residual (select fat blockers) are
   the SAME difficulty — whole-optimal-face tightness under tall/heavy, exactly the W42 hard
   stop. BF's row-balance + spread-criterion pieces pend VBD.
4. **In-class disjointness has never been realized (BE, exact frontier):** starving blockers
   exist ONLY out-of-class (G_4 = C_4 = empty, H < 4*tau); restoring topness makes the
   intersection RETURN. Weighted clone lifts do not fatten the width-4 cluster. Suggests
   (F1) may hold with (F2) vacuous in-class — the faces are two shots at one truth.
5. **The bypass audit (FP3):** NO-CHEAPER-BYPASS across six axes (each >= 2 face-sized
   unknowns, or U8 = op-classical verbatim). (F2) = softer proof target (rank-free, any
   bound > 4*tau, lower-bound tools now banked: return-flow + Gamma-budget); (F1) = softer
   decision target but RANK-3-SCOPED — **v13 AUDIT FLAG: the rank-3 -> general-rank step of
   (F1) was unpriced; v14 must name it** (done). Reserve dual-use deciders: the two-level
   splitting wave (Gamma + heaviness => <= 2 harmonic levels; output = (F2)'s mass floor);
   the B1 door-ratio kill.

## Banking (orchestrator)

BD/BF shards pend VBD; BE's frontier extends the census record (bundle extension deferred —
scripts in scratch); FP3 audit banked at docs/plans/2026-07-07-bypass-audit.md; sketch v14
carries the (F1) rescope + the tightness-promotion convergence. Honest tiers: reviewed-
pending-verification (BD/BF), exact-search NOT-REFUTED (BE), strategy audit (FP3, promotes
nothing).
