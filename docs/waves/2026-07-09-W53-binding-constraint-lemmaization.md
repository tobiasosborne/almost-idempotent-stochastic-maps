# Wave W53 — the (B1)-(B3) lemma-ization wave: the constraints COLLAPSE into the huddle charge (2026-07-09, session 13)

**Node:** sketch v16's opening move (the three W52 binding constraints, each targeted as a
universal lemma) + (M1) `conj-rank3-cluster-zero-face-reach` in parallel. **Design:** four
fresh-codex provers (isolated workspaces: definitions + full lemma registry + W52 anatomy;
status discipline: only `proved` shards as inputs; dimension-free + clone-invariant
mandatory; labelled gaps) + one Opus strategist (the (M2) assembly skeleton, interface
contracts + fallback lattice) + four SEPARATE fresh hostile codex verifiers, one per prover.
bd `aism-asc`. Scratchpad: session-13 w53/ (prompts + answers + verdicts; perishable — this
doc carries the verbatim verdict lines; the shards carry the corrected contracts).

## Verdicts (verbatim first lines)

**Prover B1 (delta inflation):**
`VERDICT: PARTIAL — A universal coefficient-deficit price is proved, but the rho-near top-slab huddle remains uncharged and no unconditional B1 height cap follows.`

**Prover B2 (forced visibility):**
`VERDICT: PARTIAL — A hidden tall top universally forces rho-far near-top rows, but I cannot prove the requested two-hidden-vertex exclusion without a new zero-face/low-slab coupling lemma.`

**Prover B3 (top reversion):**
`VERDICT: PARTIAL — The requested B3 intersection theorem is not proved here; what is proved is the sharp reduction that disjointness forces a local hidden-vertex huddle, and in rank 3 the bounded-display horn is exactly a missing reach lower bound.`

**Prover M1 (rank-3 reach):**
`VERDICT: PARTIAL — The rank-3 reach conjecture is reduced to one explicit huddle reach/exclusion gap, but the transverse reach lower bound itself is not proved or refuted here.`

**Verifier VB1:**
`VERDICT: VALID-WITH-CORRECTIONS — Lemma 1 is valid after explicit positivity hypotheses are added to its threshold corollaries, and Lemma 2 is repairable but currently invokes a shard beyond its contract.`

**Verifier VB2:**
`VERDICT: VALID-WITH-CORRECTIONS — Propositions A and B survive the contract audit under the canonical small-beta hiddenness-witness reading, but that reading and the GAP bookkeeping must be made explicit.`

**Verifier VB3:**
`VERDICT: VALID-WITH-CORRECTIONS — Statements A and B are contract-faithful after explicit domain/wording fixes, but Statement B is false as written for nonpositive A_0.`

**Verifier VM1:**
`VERDICT: VALID-WITH-CORRECTIONS — The proved huddle reduction is supported by proved shards, but the answer needs precise contract-level wording for C_4, the R_2/chi_2 identification, and GAP-1's always-tight output.`

## Banked (corrections applied; L5 = fresh-hostile-verified, NOT L0)

- `lem-top-deficit-price` (B1 Lemma 1): the affine pairing sum a_j^+ z_j <= nu_v(2+4delta)
  with the sign-corrected threshold corollaries. First-principles; the (i) handle.
- `lem-disjointness-huddle-reduction` (B3 Statement A): in-class disjointness at a
  mass-carrying ball-cluster vertex FORCES the huddle pair, with explicit depths
  (H-4tau, H-8tau); contrapositive = no huddle => intersection horn. Top reversion is
  NOT an independent wall.
- `lem-top-witness-third-actor` (B2 Prop A): >13/16 of any canonical hiddenness witness of
  a tall top sits rho-FAR at depth > H-4tau — the huddle is never two-body; a forced
  third actor spans >= 4tau inside the top slab.
- `lem-bounded-alpha-forced-far-slab` (B2 Prop B): a hidden near-top vertex with a
  small-beta witness of alpha-mass <= A_0 forces its own rho-far top-slab row.
- Contract daylight closed: `conj-zero-face-elimination` and
  `conj-rank3-cluster-zero-face-reach` now define their cluster sets INLINE (the C_4
  ambiguity found by VM1).
- NOT banked as shards (already covered / negative space): B3 Statement B (contained in
  `lem-optimal-face-conic-reduction` + `lem-rank3-zero-face-min-mass` clauses); B1 Lemma 2
  (a corollary of `lem-rho-near-residual-cancellation` needing an S>=1 case split; its
  content is the FINDINGS blind-spot entry).

## New conjectures (the huddle-charge handles, codified)

- `conj-top-deficit-coupling` (GAP-B1): Z_v(phi) >= c_def*H on tall heavy tops — with
  `lem-top-deficit-price` this alone yields tall-emptiness.
- `conj-tall-bounded-alpha` (GAP-B2-1): near-top hidden vertices of tall tops admit
  small-beta witnesses with bounded alpha — activates `lem-bounded-alpha-forced-far-slab`.
- (The web-incompatibility statement (GAP-B2-2) is deliberately NOT yet codified — VB2
  required GAP-1+GAP-2 joint bookkeeping and the statement needs W54 sharpening first.)

## Effect on the map (sketch v17)

The three W52 binding constraints are NOT three walls. Verified anatomy: (B3) reduces to
huddle exclusion + (at rank 3) the reach floor; (B2)-as-stated is subsumed by
`lem-ball-cluster-exposure-void` (Opus skeleton finding G7) and its real content is the
third-actor web; (B1)'s affine pairing is blind exactly on the low top-deficit slab where
the huddle lives. ONE Tier-1 question remains: **THE HUDDLE CHARGE** — tall heavy top +
forced huddle partner + forced rho-far top-slab web => contradiction, via (i)
`conj-top-deficit-coupling`, (ii) `conj-tall-bounded-alpha` + web incompatibility, or
(iii) the rank-3 transverse reach floor. Assembly interface gaps G1-G8 (Opus skeleton,
scratch) recorded in the sketch; E1 (node => conj-low-slab-cap) is PROSE and needs a
registered conditional lemma.

## Honest scope

All W53 banked results are compositions/corollaries of existing `proved` shards plus one
first-principles pairing lemma — reviewed L5, none af-validated (L0 claimed for NONE). The
huddle charge itself remains OPEN. No numerical fact used as proof anywhere (VB1-VM1
audited).
