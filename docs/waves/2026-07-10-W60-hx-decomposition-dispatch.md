<!--
ROLE: W60 wave record — H-X generalization wave, DISPATCH leg (harvest appended on return).
STATUS DISCIPLINE (L0): a strategy/process artifact; promotes nothing.
-->

# W60 — H-X generalization wave: decomposition dispatch (2026-07-10)

**Bead:** aism-bgh (claimed). **Arm:** B (primary). **User directive (this session):**
the objective function of Tier-1 attacks is **decomposition into lower-complexity
pieces** — task decomposition, case-splits, probabilistic/averaging method,
multi-lemma assembly whose implications compose to the Tier-1 statement.

## Target and anchor

- **Tier-1 target:** `conj-sl1a-off-diagonal-cell` (H-X cell of the three-cell SL1a
  surface, sketch v24 item 0).
- **T0 anchor:** `lem-starvation-completion-obstruction` (af-validated, W55–W59 arc).
- **The gap = the four §HONEST LIMITS scope limits of PAPER-PROOF-w59.md:**
  1. rank > 3 (extra transverse components may cancel `X_Q·D`);
  2. slab confinement (`0 ≤ Y_Q ≤ 1` makes the `p_v,p_o` combination convex);
  3. fiberwise zero-top exterior support (`c_Q = 0` needed by the sign-union budget);
  4. the pinned tableau (`‖p_z−p_v‖₁ = τ`, `A ∈ [4,6]`, the coefficient display) —
     the upstream reduction from a selected-corner datum to SOME such tableau.

## Dispatch (this leg)

Workspace: `build-workspace.sh` snapshot (153 registry shards, 21 defs, CONVENTIONS,
FINDINGS, sketches v23+v24, W56/W59 wave docs, PAPER-PROOF-w59.md) + `BRIEF-W60.md`
(the decomposition brief: ≤7 nodes, each with pinned one-line contract / mechanism /
honest price / statement-level interface check / fallback; FINDINGS dead routes
absolute; clone-invariant quantities only; mechanism separation mandated).

Two **independent** background strategists (author-only; neither sees the other):
- codex `gpt-5.6-sol`, effort **ultra**, via `codex-dispatch.sh` → `DECOMPOSITION-W60.md`;
- Fable subagent (creative ideation, memory-authorized for proof ideation)
  → `DECOMPOSITION-W60-FABLE.md`.

## Planned harvest protocol (next leg)

Compare the two trees; select/merge as orchestrator (strategy-level judgment, not
proof verification); then a **fresh hostile codex interface check** (batched) on the
selected assembly implication BEFORE any conjecture shards enter the registry.
Nothing from this wave promotes any status.
