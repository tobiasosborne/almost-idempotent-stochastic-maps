# Wave W50 — literature strike: the unknown math is NOT known math (2026-07-07, session 12)

**Node:** the Tier-1 elimination directive (user, 2026-07-07: "all creativity and all
means"). **Design:** one opus web-research agent (WebSearch/WebFetch over arXiv/ar5iv/SIAM),
primary-source confirmation where reachable. **STATUS DISCIPLINE (L0/L1): every external
statement below is `stated` — NOTHING is byte-matched into refs/; ingest (aism-5de) remains
the promotion gate. [fetched] = confirmed from primary source this strike; [prior-scout] =
rests on the 2026-07-04 sweep, could not be re-extracted this session.**

## Verdict (verbatim first line)

`HITS (A: 0, B: 0, C: 6)` — no paper states or proves op-classical or either open face;
the strongest external results are a solved SIBLING (Salzmann–Bergh–Datta: fixed-VECTOR
robustness, sqrt(eps), dimension-free, sharp) and the named OPEN PROBLEM (Kitaev); the
decomposition machinery adjacent to (F2) is uniformly dimension-DEPENDENT.

## A/B — closures: NONE

- Fourth independent pass confirming op-classical is not accidentally-known; corroborates
  Kitaev's own "I do not know of a solution."
- The only B-candidate (SBD Thm 5.2) fixes a VECTOR (TP ~ P); ours fixes a MAP (Q^2 ~ Q).
  Reassembling per-vector fixes into one globally consistent partition IS the Kernel/(EX)
  content — not bounded adaptation.

## C — imports (6, ranked)

1. **C1 [fetched] SBD arXiv:2405.01532 Thm 5.2 (the solved sibling):** P in Prob(X),
   T in Stoch(X), (1/2)||TP−P||_1 <= eps ==> exists Q, S with SQ = Q EXACT, Q ~sqrt(eps) P,
   S ~sqrt(eps) T, NO |X| factor; Remark 5.4: sqrt(eps) essentially optimal via an explicit
   3-state family (ex-hume cross-check TODO). **TRAP (newly documented): the abstract
   foregrounds the dimension-DEPENDENT QMC application; the dimension-free win is buried in
   §5.** Import: Lemma 5.5 reset-trick (mix a reset channel at rate lambda ==> strict
   contraction; optimize lambda ~ sqrt(eps)) — one of the two genuinely different proof
   shapes on the table.
2. **C2 [abstract fetched; §1.2/Prop 3.1/Ex 1.3 prior-scout — ar5iv fatal-error, PDF
   compressed-stream, arxiv/html 404] Kitaev arXiv:2405.02434:** the named open problem
   (O(sqrt(eta)) dimension-free approximation of eta-idempotent UCP maps); Prop 3.1
   sign-function exact-idempotent fix (linear O(delta), no positivity) = our signed-linear
   anchor; the §§5–9 incremental merge-and-extend construction = the second different proof
   shape. Solving op-classical resolves the COMMUTATIVE case of Kitaev's problem. The
   cb-norm-on-commutative-domain ==> inf->inf reduction is an in-repo verification TODO.
3. **C3 Meyer, SIAM Review 31(2):240–272 (1989), stochastic complementation:** the exact
   censored-chain algebra — the right LANGUAGE for (F2)'s zero-face states (and the W48-BC
   brick is exactly this operation kept exact under signedness). Formulation tool only.
4. **C4 Dayar–Stewart, SIAM JMAA (1997), lower-bounding coupling matrices:** the closest
   object to (F2)'s mass floor — but ill-conditioned, fixed-partition, fixed-n. Study the
   form; rebuild the constant natively.
5. **C5 parametric-LP optimal-partition invariancy (Ghaffari-Hadigheh et al.,
   arXiv:2008.02305):** confirms the standard structure behind (F1)'s interval picture;
   1-D Helly is the citable capstone once pairwise overlap is shown; no overlap-forcing
   supplied.
6. **C6 ultrametric l-inf nearest-projection (arXiv:2210.12288):** speculative
   hierarchical-rounding tool; low priority.

## D — negative space (9 items; full list in the strike report — the decisive one:)

**D1. Hartfiel–Meyer, LAA 272 (1998) 193–203:** subdominant eigenvalue near 1 implies
near-uncoupling ONLY at fixed n — the eigenvalue "must, in general, be remarkably close to
1 before uncoupling occurs", threshold degrading with n. **KILLS the spectral-gap ==>
structure import route for (F2) as dimension-free.** Banked in FINDINGS as a dead-route
certificate. Also: metastability (reversible, dimension-dependent), Courtois–Semal
(forward-only, fixed-n), lumpability (partition is an input), Davis–Kahan family (wrong
norm), Baake–Sumner (delta = 0 only, no quantitative stability — reconfirmed), discrete
Bakry–Émery (curvature-keyed, no partition rigidity in inf->inf).

## Net assessment (strategist)

(F2)'s literature adjacency is all the right language and all dimension-dependent — exactly
why the v13 framing (any bound > 4*tau wins) can succeed where the literature cannot, and
why the floor must be built natively and clone-invariantly. (F1) is standard parametric-LP
structure finished by a bespoke overlap-forcing step. The two importable proof SHAPES
(reset-trick; incremental merge-and-extend) go to the bypass strategist's dossier.

## Banking (orchestrator)

Wave doc (this file, the agent's report verbatim-condensed) + FINDINGS dead-route entry
(D1) + fr pull. NO registry/refs changes (L1: nothing byte-matched; ingest = aism-5de).
