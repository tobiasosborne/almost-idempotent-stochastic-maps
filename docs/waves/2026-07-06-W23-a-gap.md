# Wave W23 — THE A-GAP: parametric halo collapse closes it; small-a Lemma A stays open-both-sides (2026-07-06)

**Node:** sketch v4 M1 step 2', THE A-GAP `(29τ/8, 4τ]`, bd `aism-sg6`. **Design:** two mutually-blind
adversarial workers on the two closure routes — worker H on route (i) (parametric collapse), worker I
on route (ii) (small-a Lemma A prove-or-refute) — plus a SEPARATE fresh hostile verifier (worker L) on
H's derivation. Prompts + raw answers in the session scratchpad (`W23/PROMPT-{H,I,L}.md`,
`W23/ANSWER-{H,I,L}.md`); worker I's exact certificates in `runs/2026-07-06-w23-a-gap/` (L3 bundle).

## Verdicts (verbatim first lines)

- Worker H (route i): `ROUTE-I-CLOSES (T(4) = 13, assembly: H > 13τ forces hidden-top g^(4) > 1/2 - δ
  while Lemma A gives visible-row g^(4) <= 4τ; remaining open piece is the step-4 band-web
  contradiction)`
- Worker L (verifier): `VALID (no error found; checks: expanded residual split, constants/algebra,
  exact rank-5 and small exact tests)`
- Worker I (route ii): `OPEN-BOTH-SIDES (prove blocked at: shell S_w inside rho where the visible
  exposer has no lower bound; refute frontier: no exact family here enters G_{15/4}, shallow-corner
  ansatz blocked by delta-inflation/exposedness absorption)`

## The result (codified as `lem-parametric-halo-collapse`, status: proved, af: none)

For a hidden top vertex v of height H and ANY halo width a > 0:
**H·(1 − σ_a) ≤ (σ − σ_a)·a·τ + ν_v·(2+4δ)** — the af-validated conj-halo-collapse is exactly the
a = 1/4 case (coefficient = halo radius; no off-by-4, verifier-checked). Consequences (shard body):
forced-mass curve **T(a) = 5a/2 + 3** (T(1/4) = 29/8 recovering the W18 constant exactly;
**T(4) = 13**); MIN-A-at-width-4: `H > 13τ ⇒ every hidden top has g^{(4)} > 1/2 − δ`, pairing with
the proved Lemma A cap `g|_W ≤ 4τ` into a genuine numerical gap iff `δ < (17 − 12√2)/2 ≈ 0.0147`
(orchestrator re-verified the window algebra by hand). Verifier L independently expanded the
compressed pricing into the lem-residual-lower/-upper split, checked the σ_a ≥ 1 branch (no
division), and passed exact tests (banked rank-5 at a ∈ {1/4,1,2,4}; constructed 4×4).

## Route (ii) — honest state (worker I; L3 bundle)

Prove side blocked structurally: for a ∈ (29/8, 4) the annulus `aτ < ‖p_j − p_w‖₁ < 4τ` is priced by
NO available tool (the exposer starts at ρ = 4τ; shell-row reproduction controls outgoing, not
incoming mass; the collapse tools speak of hidden tops, not visible-row load). Refute side: no exact
family entered G_{15/4} (Hume s=1/100, the W19 anchors, duplicate splits — all G empty at 15/4; the
shallow-corner constant-mass ansatz dies by δ-inflation 1903/1000000 vs formal 1/1000000 + exact
absorption). NOT an emptiness claim. Route (ii) is MOOT for the bootstrap while route (i) stands.

## Wave outcome (orchestrator, [T2] strategic)

1. **THE A-GAP IS CLOSED (mod-review it now carries):** the bootstrap's contradiction surface is
   width-compatible at a = 4 — harmonic g^{(4)}, visible rows ≤ 4τ, hidden tops > 1/2 − δ whenever
   H > 13τ, valid for δ < (17−12√2)/2. MIN-A's antecedent updates from H > 29τ/8 to **H > 13τ**
   (the tall threshold the step-4 argument must beat; anything below 13τ is Kernel-acceptable
   B = 13 territory via the W18/D1-style assembly — D1 codification aism-yxa should now target the
   parametric form directly).
2. The step-4 residual is unchanged in kind but sharper in statement (see W24's ledger): rule out
   the band-web at width 4 under the numerical gap.
3. Honest tiers: parametric collapse = reviewed paper proof (L5); route-(ii) block = structured
   worker read [T2] + L3 certificates; nothing af-validated; nothing L0-rigorous.
