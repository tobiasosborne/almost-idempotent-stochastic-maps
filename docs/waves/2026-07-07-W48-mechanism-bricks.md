# Wave W48 — the ideation-portfolio bricks re-established: Gamma-calculus, return-flow, deflation, censoring (2026-07-07, session 12)

**Node:** the ideation portfolio's four near-certain first lemmas (docs/plans/
2026-07-07-ideation-mechanism-portfolio.md, candidates 1-4), bd `aism-2fi`. **Design:** four
independent fresh-codex provers — AZ (Gamma) ∥ BA (return-flow) ∥ BB (deflation) ∥ BC
(censoring) — + ONE SEPARATE fresh-codex joint hostile verifier VBW over all four. Prompts +
raw answers in the session-12 scratchpad (`W48/`).

## Verdicts (verbatim first lines)

- AZ: `PROVED-WITH-CORRECTIONS (the algebra is exact; constants in (c),(d),(e) sharpen/correct the ideation bookkeeping)`
- BA: `PROVED-WITH-CORRECTIONS (corrected constants: cluster-uniformity has L=1, not 1+delta; return-flow has sharper exact slack ...)`
- BB: `PROVED-WITH-CORRECTIONS (NO added rank hypothesis; the precise clone model is the weighted clone lift ...)`
- BC: `PROVED-WITH-CORRECTIONS (exactness proved; requested negativity constant corrected LOUDLY: signed powers of A require a resolvent-squared term)`
- VBW: `VALID-WITH-CORRECTIONS (BA: L=1 sharp is false; sharp constant is 1/2, strengthening the r slack)`

## Results (four shards codified, VBW-approved contracts)

1. [[lem-signed-carre-du-champ]] — PGamma = 0 with SHARP constants; the tau^2-native budget;
   ONE fixed vector budgets every harmonic observable (cluster-uniform).
2. [[lem-cluster-return-flow]] — uniformity at the sharp 1/2; the return-flow lower bound
   (an (F2)-type output).
3. [[lem-wedderburn-deflation]] — exact rank descent, trace = rank clone-invariant, exposer
   vectors D-fixed. CONVENTIONS gap flagged: the weighted clone lift needs a formal home.
4. [[lem-censoring-exactness]] — Meyer complements survive signedness exactly; the
   one-resolvent constant is FALSE for signed A (exact fixture); resolvent-squared necessary.

Honest tiers: reviewed (L5); NOT af-validated. Quota halt #4 interrupted AZ/BC once
(relaunched verbatim after the 18:57 reset).
