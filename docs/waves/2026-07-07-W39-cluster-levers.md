# Wave W39 — levers (a), (c), (d) on near-cluster absorption: all reduce to TWO primitives (2026-07-07, session 11)

**Node:** conj-near-cluster-absorption (sketch v9 levers), bd `aism-2fi` (P0). **Design:** two
independent fresh-codex provers — AH (levers a: conditioning + c: deepest vertex) ∥ AI
(lever d: anchor tension / aggregated witness). Both PARTIAL with structural yields; NO new
registry shards (every provable piece is a corollary of banked material or a blocked identity
— codifying would be L2 drift; recorded here + FINDINGS at worker-T1). Killed once by the
codex quota (2026-07-07 ~01:00) and relaunched verbatim at the 04:05 reset. Prompts + raw
answers in the session-11 scratchpad (`W39/`).

## Verdicts (verbatim first lines)

- Worker AH: `PARTIAL (proved: deepest-vertex collapse specialization + a precise conditional
  LP-continuity obstruction; gap: no coefficient-vs-hiddenness-witness coupling for the
  rho-far deep rows)`
- Worker AI: `PARTIAL (proved: far-set shift/annulus lemma, cluster barycenter proximity, and
  exact aggregated-witness circuit; gap: no theorem controls the witness `alpha`
  top-slab/anchor side or converts the resulting feasible circuits into the lower bound
  `t*(.) >= kappa` needed for exposure.)`

## Results (all worker-T1; the value is the REDUCTION)

1. **Lever (a) CLOSED honestly (AH):** value-normalized exposedness admits no uniform gradient
   bound along thin directions of the row hull — t* does NOT transfer across 4τ-near rows
   (basepoint shift breaks the box constraints). The thin directions are the cloning escape.
2. **Lever (c) inherits the wall (AH):** the deepest-vertex specialization of the depth-d
   collapse (C_a ≡ 0) is a one-line COROLLARY of banked lemmas (not codified — duplicate);
   it forces TOTAL deep mass near 1, not ρ-near deep mass; the W38 pure-cluster mode stays
   consistent.
3. **Lever (d) blocked at two named primitives (AI):** the far-set shift fails at scale
   (cluster vertices near the ρ-boundary have incomparable far sets); the aggregated-witness
   circuit is exact but carries center coefficients (1 + A_u − B_u) with A_u UNBOUNDED (the
   dual's free ray), and even a low-payment circuit only UPPER-bounds t* (the W37
   dual-direction wall).
4. **THE REDUCTION (the wave's payload): conj-near-cluster-absorption needs exactly (i) an
   ALPHA-GAUGE lemma** — an optimal/feasible hiddenness witness with controlled total alpha
   (LP gauge-fixing: alpha is objective-free, so a minimal-alpha optimal solution exists —
   what does complementary slackness force on its support and size?) — **and/or (ii) a PRIMAL
   EXPOSER construction** (the W36 anatomy route: build the margin-κ exposer directly from
   the cluster/anchor data instead of arguing through duals). W40 attacks both.

## Banking (orchestrator)

No registry change. FINDINGS entry (the two primitives). fr progress pull. Honest tiers:
worker-T1 derivations, no promotion; the conjecture remains OPEN.
