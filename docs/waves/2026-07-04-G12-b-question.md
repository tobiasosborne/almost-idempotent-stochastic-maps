<!--
worker: codex
arm: G wave 12
answers: bd aism-4uh
Tier legend: T0 = exact repo-file fact or exact fractions.Fraction arithmetic
in this wave; T1 = elementary derivation from T0/validated inputs; T2 =
plausible target with a live gap; T3 = speculation.
Scope discipline: repo files only; no prior conversation trusted.
Mission override: the user explicitly forbade fr/bd commands, so fr and bd
were not run. No existing repo file was edited.
Scratch checker: /tmp/aism_g12_b_question.py, pure fractions.Fraction
arithmetic; no checker output file was written to the repo.
Verdict: B_{r,s}>0 is REALIZED at a capped theta-half Phi-argmin.
The realized argmin example is small: delta=1/4, B=2/57, C=0.
I did not realize B>>delta at a capped maximal-pivot argmin; non-argmin
charts amplify B/delta easily, then minimality pivots away.
Assembly has no rate obstruction below O(delta); the only caveat is whether
a naked delta term is admitted or reabsorbed into the stated SC/RH budget.
-->

# Arm G Wave 12: The B-Question

## Q1. Certified B>0 At A Capped Argmin

Rows are ordered `(c0,c1,c2,j,k)`, chart `U=(c0,c1,c2)`, pivot `s=2`, transverse pivot `r=1`.

The exact certified construction is:

```text
L =
[1     0     0]
[0     1     0]
[0     0     1]
[3/5  -1/3   11/15]
[1/10  19/20 -1/20]

B =
[1      0      0      0     0]
[-4/57  1/3    2/57   0     40/57]
[-1/4   5/36   25/36  5/12  0]
```

Thus `P=L B` is:

```text
P =
[1       0       0        0      0]
[-4/57   1/3     2/57     0      40/57]
[-1/4    5/36    25/36    5/12   0]
[301/684 -1/108  1021/2052 11/36 -40/171]
[11/240  223/720 -1/720   -1/48  2/3]
```

The scratch checker asserted exactly:

```text
B L = I_3,        P^2 = P,        P*1 = 1,
delta(P) = 1/4.
```

Complete theta-half enumeration:

```text
chart        volume   Phi vector                         max Phi
(0,1,2)      1        (0, 0, 1/36)                       1/36
(0,1,3)      11/15    (0, 2/209, 13273/22572)            13273/22572
(0,2,4)      19/20    (0, 1/36, 223/4560)                223/4560
(0,3,4)      17/25    (0, 133751/209304, 10481/146880)   133751/209304
(1,2,3)      3/5      (64/513, 0, 5117/6156)             5117/6156
(2,3,4)      181/300  (0, 23177/30951, 5263/65160)       23177/30951
```

So `U=(0,1,2)` is a certified capped theta-half `Phi`-argmin, and `s=2` is the unique maximal pivot.

For `r=1`, `s=2`, the cross-pivot masses are:

```text
A_{1,2} = 2/57
B_{1,2} = 2/57
C_{1,2} = 0
D_{1,2} = 0
```

The validated cancellation is exact here:

```text
A = B + C - D = 2/57.
```

The B-carrying row is `k`:

```text
a(k) = (1/10, 19/20, -1/20),
beta_1(k) = 40/57,
beta_1(k)^+ * a_2(k)^- = (40/57)*(1/20) = 2/57.
```

Pivot-`s` budget:

```text
G_class^- = 1/4
S_-^mu    = 0
SIGMA     = 2495/24624
total     = 8651/24624
B/delta   = 8/57
(B+C)/budget = (2/57)/(8651/24624) = 864/8651.
```

### Clean (G) Coexistence

No clean high-self non-fan `(G)` branch coexists in this certified example.

The active non-fan row is `j`:

```text
a(j) = (3/5, -1/3, 11/15),
beta_2(j) = 5/12,
E_2(j) = 1/15,
beta_2(j)E_2(j) = 1/36 = M.
```

But it is not high-self:

```text
P_jj = 11/36,        kappa_j = 25/36 > 1/2.
```

Its pivot-removing chart `(0,1,j)` is Psi-blocked, not clean `(G)`:

```text
Psi_j = 13273/22572 > M = 1/36,
Gamma_j = 2/209 < M.
```

The B-carrying row `k` is high-self (`P_kk=2/3`) but is not a non-fan demand row for pivot `s=2`:

```text
W_s(k)=0,       E_s(k)=0,       a_s(k)=-1/20 < 0.
```

Also, the validated collateral-import lemma covers `c=a_s(j)>0`; it does not cover the `c<0` move using the B-carrying row itself.

## Amplification Probe

I did not realize `B/delta -> infinity` at capped maximal-pivot argmins.

There is a useful contrast: away from the argmin/maximal-pivot setting, amplification is easy. For `0<eps<1/4`, set

```text
L =
[1 0 0]
[0 1 0]
[0 0 1]
[eps, 3/2-eps, -1/2]

d = (1-eps)/(3/2-eps)

B =
[1 0 0 0]
[-d*eps, eps, d/2, d]
[0 0 1 0].
```

At the base chart `(0,1,2)` and coordinate pair `(r,s)=(1,2)`,

```text
delta(P) = d*eps,
B_{1,2} = d/2,
B_{1,2}/delta(P) = 1/(2 eps).
```

For example, at `eps=1/100`:

```text
delta = 99/14900,
B_{1,2} = 99/298,
B/delta = 50.
```

But this base chart is not a `Phi`-argmin. The theta-half chart `(0,2,k)` has max `Phi=0`, while the base chart has max `Phi=9801/14900`. This is exactly the minimality obstruction: the large `B` row pivots in and kills the bad chart.

## T1 Output And Gap

The realized example refutes the strongest possible T1 target:

```text
B_{r,s}=0 at capped theta-half Phi-argmins.
```

What remains open is the useful branch-sensitive target:

```text
At a capped theta-half Phi-argmin, for a clean high-self non-fan Gamma branch,
B_{r,s} <= K*delta(P).
```

I do not have this proof. The honest gap is converting minimality for the B-carrying rows, especially the `c<0` pivot-removing move, into a bound on excess positive pivot-mass under `beta_r^+`. The validated `(CI)` statement is only for `c>0`.

## Q2. Assembly Tolerance

Answer: **NO rate obstruction found.** The intended assembly does not require the collateral term to be `o(delta)` or smaller than `O(delta)`.

Exact loci:

```text
G6 "What Would Prove RH", eq. (6):
P_r^O <= Gamma_r^- + N_r^O + (-H_r)_+.
```

This is a linear ledger. It names class, own-negative, signed, and fan sources; it does not require sublinear collateral.

```text
G7 "(RH) Assembly Status":
If (SC) is proved with constant C_SC, G5/G6 bookkeeping combines to (RH),
and the G5 family forces C_RH >= 4.
```

A forced constant floor `4` is direct evidence that the assembly is constant-factor, not sublinear.

```text
G8 "High-Self Branch Accounting" / display (PRT):
sum_high beta_j W_j <= C*(G_class^- + S_-^mu + SIGMA) + C_fan*FanRes.
```

Again the interface asks for a universal constant, not a better rate.

The only bookkeeping caveat is contract-level: `conj-sc` and `conj-rh` as registered do not literally include a naked `+ C_delta*delta(P)` term. So a `2*delta` C-part is rate-tolerated, but a formal proof must either reabsorb it into the stated budget in the branch, or record a delta-repaired variant. No source I read requires `o(delta)`.

## Pre-Factored PRT Skeleton

For the clean Gamma branch with `c=a_s(j)>0`:

```text
1. Pivot-removing disjunction:
   M <= Gamma_j, and for some transverse r,
   M - Phi_r(U) <= Phi_r(V_j) - Phi_r(U).

2. Collateral import:
   Phi_r(V_j) <= Phi_r(U) + I_{r,j}.
   Therefore M - Phi_r(U) <= I_{r,j}.

3. Import reduction:
   I_{r,j} <= alpha_B B_{r,s} + alpha_A A_{r,s}.
```

Under theta-half admissibility, `c>=1/2`; with the theta-half Cramer box, the coefficients are universal.

```text
4. Cross-pivot cancellation:
   A_{r,s} = B_{r,s} + C_{r,s} - D_{r,s} <= B_{r,s}+C_{r,s}.

5. B-lemma target:
   B_{r,s} <= K_B * delta(P)
   for clean high-self Gamma branches.

6. C-financing:
   C_{r,s} <= 2*delta(P)
   because sum_i beta_r(i)^- <= nu_{u_r} <= delta(P)
   and a_s(i)^+ <= 2 by the theta-half Cramer box.

7. Collateral conclusion:
   M - Phi_r(U) <= K_G * delta(P)
   with universal K_G.
```

Then the G8 high-self branch ledger uses the three blockers:

```text
volume-inadmissible rows -> harmonic/class ledger,
Psi-blocked rows         -> transformed same-pivot import ledger,
Gamma-blocked rows       -> the collateral conclusion above.
```

This gives the high-self `(PRT)` input for `(SC)`. Then G7’s assembly note says `(SC)` plus the G5/G6 bookkeeping yields `(RH)`, with constants only.

## Verdict

```text
(Q1) B at capped argmins:  REALIZED (B=2/57, B/delta=8/57);
                           amplification at argmins OPEN;
                           non-argmin B/delta is unbounded and pivots away.

(Q2) assembly tolerance:   YES-tolerant (skeleton given);
                           no step found requiring smaller than O(delta).

(PRT)/(SC)/(RH):           still OPEN. B=0 is false; the remaining useful
                           target is branch-sensitive B <= K*delta, plus
                           contract-level handling of any naked delta term.
```