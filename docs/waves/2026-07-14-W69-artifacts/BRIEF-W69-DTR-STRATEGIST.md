# W69 creative wave — ATTACK the DTR residual (diffuse-tail ray conversion)

You are a fresh, independent proof strategist-prover. Your workspace is this
directory: the full registry snapshot (`argument/`, `definitions/`) + context
docs (`context/`). Everything you produce stays INSIDE this directory.
Deliverable: `DTR-ATTACK.md` (+ optional `APPENDIX-dtr-proofs.md`). Status
discipline (L0): everything you produce is proposed/`conjecture` until
verified elsewhere; you promote nothing.

## THE OBJECTIVE FUNCTION (user mandate, binding)

**Decomposition into lower-complexity pieces.** A full proof of DTR is
welcome but NOT the success criterion. Success is EITHER:
(a) decomposition into routine nodes on the proved interface + strictly
    smaller creative residual(s), each a proper subclass or constant-
    complexity package; OR
(b) a proof of a quantitatively weakened DTR (e.g. under an extra
    tail-coherence or incidence-coherence hypothesis STRICTLY WEAKER than
    actorization, with the exact loss recorded; or gamma depending on
    H/tau); OR
(c) a precise reduction of DTR to a NAMED, isolated problem strictly smaller
    than DTR with its own refuter shape.

## Target — verbatim pinned contract (AESC-ATTACK-W67.md §1.6)

`conj-w67-aesc-diffuse-tail-ray-conversion`:

> Every pinned target datum carrying (1.22) obeys (EC):
> Z_v(q_A) >= (1/8)*P_v^+(E_*) - (c_m/16)*P_v^+(L_v).

where (1.22) is, simultaneously: eta_D*(D_tail) > 1/160; h_u <= 3*delta;
min_f ||p_f - x_u||_1 > 3*delta; Tail_1(u) > tau/8 for every carrier;
P_f*^+(U_tail) > tau/2560. Read `context/AESC-ATTACK-W67.md` IN FULL FIRST
(notation §1.1: D_u, a_u, x_u, h_u, chi_u, Tail_1, c_{u,Q}, d_{u,Q}; it
adopts DCAP-ATTACK-W65.md which adopts DECOMPOSITION-W63-I.md §§0-1.1). The
pinned datum carries the FULL D-cap antecedent + all seven proved lem-dcap-*
outputs (esp. B4 = lem-dcap-tall-same-center-packet: exact T-spend
P_v^+(L_v) < 2*tau/15 AND the E packet P_v^+(E_*) >= tau*S/8 at the ONE
public center p_f*) + the realized B5 overlay (top-owned but carrierwise
UNRELATED to eta_D* — treating them as the same population fakes the missing
theorem) + the W67 proved interface (lem-aesc-*: the tau/8 pointwise tail,
the tau/2560 common union, the guarded split, separator geography).

## What DTR actually asks (the hard core, stated honestly)

The proved chain already gives: a root-owned common receiver set U_tail with
P_f*^+(U_tail) > tau/2560, each of whose witnessing carriers has a tau/8
tail crossing the +-1 level of its own normer chi_u, plus B4's T/E packet at
the same center p_f*. What is MISSING is root-to-top synchronization: the
target (EC) is a lower bound on the TOP ray value Z_v(q_A), and the only
legal top conversion is through lem-l5-mass-barycenter-dualization + an
arbitrary attained certificate from lem-l5-top-face-ray-formula. The known
threats: the incidence directions rotate with u (the union remembers
incidence, loses the scalar sign); the B5 overlay cannot be carrierwise
aligned with eta_D*; eta_D* <= P_f*^+ but the top ray lives at v, not f*.
Candidate axes (not mandates): a second foldback from f* to v through the
proved universal exterior payer (lem-l5-universal-exterior-payer gives row v
tau*S/8 outside EVERY half-ball — can U_tail's receivers be placed outside
the right half-ball at v?); split U_tail once by a fixed clone-invariant
radial statistic at p_f* vs at p_v (retain both pieces); exploit that E_* is
ALSO at p_f* — the same-center structure means (EC)'s main term needs only
E_*'s mass to reach the ray, so the true residual may be only the
L_v-shallow interaction; quantify over the retained tail-value and
incidence distributions (R1's fallback keeps them).

## The proved interface (consult each shard's OWN hypothesis block)

lem-aesc-* (5, W67), lem-dcap-* (7, W65), lem-ihorn-* (10, W63), lem-l5-*
(4, W62 — esp. mass-barycenter-dualization, top-face-ray-formula,
positive-flow-foldback, universal-exterior-payer), lem-icap-* (8, W64 —
CAUTION: I-cap-scoped hypothesis blocks), the af-validated lem-hx-* engine
bank, the SL1a machinery, the zero-face bank, the two exact tallness
budgets, and NEW this week: lem-intersection-branch-production +
the repaired conditional lem-huddle-charge-assembly (NOT consumable
unconditionally — it rests on four open conjectures incl. conj-l5-gap-1;
DTR feeds conj-l5-gap-1's reduction tree, so consuming the bridge here
would be circular. Do not touch it).

## Fresh evidence you must consume (context/fixtures/)

w66-dcap-decider-REPORT.md: no exact family reaches even the A-esc window;
tallness + legal far selection + nonempty ultra omega is the wall; W55's
completion pays order-one finance negativity when it concentrates the
finance vector — DTR's whole threat is that growing rank DISTRIBUTES it.
w57/w58: low-rank completions are exactly-infeasible (Farkas). Diagnose:
what does distributing the finance vector across many rows cost against the
tau/8 pointwise tail and the tau/2560 root-owned union? If you find the
distribution is FREE in some regime, say so — that sharpens the refuter.

## Hard constraints (violations = instant reject)

- Signed picture; clone-invariant full-fiber quantities; frame-free
  constants; row-point ell^1 geometry.
- No 1/t*; alpha-free displays as geography only; conic coefficients never
  transitions; the synthetic actor never vertexized; no favorable
  row/minimizer/kernel/tie/certificate/atom selection.
- No witness/y_c averaging, Jensen, W37 reversal, raw-index paths,
  coefficient-only cleanup, finite-cover main target, summed pairwise
  demands without one R2 foldback on a common nonnegative test, freight
  censoring without a norm gap, second-generation web, conic recurrence.
- ONE common nonnegative test per foldback; TU's union is the only
  aggregation currently owned — a second foldback needs its own common test
  and must scale errors by source mass.
- The B5 population is NOT eta_D*'s; no carrierwise alignment.
- Boundary ownership per W67: h = 3*delta owned by D_tail; residual
  equality 3*delta owned by T-esc; HES mass equality 1/160 owned by HES.
- **Every mechanism must show EXPLICITLY where T (or a parent height
  budget) is consumed, AND the exact line producing the (1/8)*P_v^+(E_*)
  main term (E at p_f*, once), AND the exact subtraction of
  (c_m/16)*P_v^+(L_v) — the shallow subtraction lives in (EC) itself,
  BEFORE B4.1 is used. A ledger-only argument is rejected before review.**
- context/FINDINGS.md dead routes are ABSOLUTE.

## Deliverable format — `DTR-ATTACK.md`

0. **Verdict first** (<= 1 page): which of (a)/(b)/(c) you achieved; one
   sentence naming the hard core as you now see it.
1. **The tree**: per node — (a) pinned contract (single minimal statement,
   af-elevation-shaped, no 'hence'); (b) mechanism naming exact banked
   tools; (c) honest price (difficulty, likeliest death, evidence both
   ways); (d) interface check; (e) fallback.
2. **Assembly**: nodes => DTR (or the weakened/reduced statement),
   quantifiers and constants threaded explicitly, ending at the exact (EC)
   line then (via B4) the c_m*tau/64 form.
3. **Kill-list check**: PASS/FAIL per node vs every wall + FINDINGS.
4. **Dispatch order**: routine batch, L3 decider shapes (exact target
   inequalities; distinguish the D_EC and D_leaf diagnostics — they are NOT
   interchangeable), creative residual(s) last.

Do not touch `argument/` or `definitions/`. Write ONLY the two named files.
