# Wave W47 — the sharpened terminal node attacked on both routes: the rank-3 line collapse and the conditional (T2) capacity bridge (2026-07-07, session 12)

**Node:** conj-zero-face-elimination (sketch v12), bd `aism-2fi` (P0). **Design:** two
independent fresh-codex provers — AX (route (a): rank-3 interlacing) ∥ AY (route (b): charge
the blocker with the L0 capacity primitive) — + separate fresh hostile verifiers VAX (done)
and VAY (relaunched after quota halt #4; AY codification PENDS its verdict). Prompts + raw
answers in the session-12 scratchpad (`W47/`).

## Verdicts (verbatim first lines)

- Worker AX: `PARTIAL (proved: rank-3 optimal-face line/interval reduction plus endpoint
  interlacing and separator-blocker classification; gap: no theorem forcing the interval
  interlacing from tall-heavy near-cluster hypotheses)`
- Verifier VAX: `VALID-WITH-CORRECTIONS (tier: independent hostile verifier; exact rational
  rank-3 interval reduction, fixtures, edge probes)`
- Worker AY: `PARTIAL (proved: local exposer/zero-face capacity, separator perturbation
  collapse, and a conditional T2 bridge under an explicit blocker lower-mass hypothesis;
  gap: no current lemma lower-bounds blocker high-set mass or ties blocker charges to v's
  outside-C shipping)`
- Verifier VAY: (in flight)

## Results

1. **`lem-rank3-optimal-face-interval-reduction` (codified; VAX corrections verbatim).**
   At rank 3 both always-tight hulls lie on the exposer level line — (T1) at rank 3 ==
   interval overlap == scalar ordering. VAX's exact endpoint table: disjoint exactly at
   HEIGHT+A (out-of-class), overlap at both in-class census instances.
2. **AY's conditional (T2) bridge (NOT yet codified — pends VAY):** the L0 composition
   [affine exposer => P-fixed box vector (bridge #22)] + [row-zero capacity #27] charges any
   zero-face blocker's kappa-high positive mass against nu_z <= delta; hence IF
   (conj-downhill-zero-face-lower-mass) every disjoint-hull blocker carries a universal c0
   of positive mass into the kappa-high slab (under: tall heavy near-cluster, no outside
   shipping, no cluster exposure), THEN c0*kappa <= delta gives c0*tau/4 <= tau^2 —
   CONTRADICTION for delta < (c0/4)^2, closing (T2). THE SCALE GAP WORKS FOR US: any
   universal c0 wins; no constant-fight.
3. **AY's honest negative (exact certificate):** the naive sliding family h* + s*psi is
   DEAD — the blocker makes the feasible s-interval length zero (4x4 instance, eps = 1/100).
   The orchestrator's dispatch framing was wrong on this point; the certificate corrects it.
4. **The theatre check (user, this round):** conj-downhill-zero-face-lower-mass could be
   the anti-splitting wall renamed. Distinguishers, both testable: the tau-vs-tau^2 scale
   advantage (no prior formulation had it) and the cheap refutation path (hunt a starving
   blocker at tall-heavy). If the next wave returns "needs a new mechanism", pivot the arm —
   recorded here so the exit is pre-committed.

## Banking (orchestrator)

Registry: one shard now (AX/VAX); AY's three pieces (capacity corollary, perturbation
collapse obs, conditional bridge) + the conjecture shard follow VAY's verdict. Honest
tiers: reviewed (L5) / conditional-on-conjecture; NOTHING promoted to L0.
