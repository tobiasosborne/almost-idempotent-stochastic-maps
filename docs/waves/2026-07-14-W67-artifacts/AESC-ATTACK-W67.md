# A-esc attack: hull defect, synthetic finance, and the rotating-tail cost

> **Status discipline (L0).** Every node introduced in this file is proposed / `conjecture`,
> including the two short derivations labelled routine. Nothing here is promoted. The seven
> `lem-dcap-*` inputs and the other registry shards retain their own recorded statuses. All
> measures below are clone-invariant measures on full row-point fibers in the exact signed
> picture.

## 0. Verdict first

I achieve objective **(a)** and isolate two residuals of type **(c)**; I do not prove A-esc.
For a carrier set

\[
 D_u:=\widetilde q_u-p_u,\quad a_u:=\widetilde A_u,\quad
 x_u:=p_u-a_uD_u=p_u+k_{T,u}-k_{O,u},                               \tag{0.1}
\]

so the pinned residual is \(\|p_f-x_u\|_1>3\delta\). With
\(h_u:=\operatorname{dist}_1(x_u,K(P))\), split

\[
 \mathsf H_{\rm out}:=\mathsf A_{\rm esc}\cap\{h_u>3\delta\},\qquad
 \mathsf D_{\rm tail}:=\mathsf A_{\rm esc}\cap\{h_u\le3\delta\}.   \tag{0.4}
\]

Either the first class has mass \(\ge1/160\), or, under its strict failure guard, the second has
mass \(>1/160\). These are proper subclasses. In the hull-near class an arbitrary nearest
\(r\in K(P)\) is a synthetic fixed row; the starvation moment calculation gives

\[
 \operatorname{Tail}_1(u)>\tau/8,                                   \tag{0.5}
\]

and one common-set R0 foldback gives

\[
 P_{f^*}^+(\mathcal U_{\rm tail})>\tau/2560.                         \tag{0.6}
\]

The creative residuals are **hull-exterior separator synchronization (HES)** and
**diffuse-tail ray conversion (DTR)**. HES has strict separation from the entire row hull; DTR has
\(3\delta\)-convexifiability, no actual actor, the pointwise \(\tau/8\) tail, and the single
root-owned package (0.6). They have different refuter shapes.

Both residuals target the stronger same-center inequality

\[
 \boxed{Z_v(q_A)\ge {1\over8}P_v^+(\mathcal E_*)
                  -{c_m\over16}P_v^+(\mathcal L_v).}                 \tag{EC}
\]

Only after (EC) is obtained is B4 applied:

\[
 {1\over8}P_v^+(\mathcal E_*)\ge{\tau S\over64}
 \ge{c_m\tau\over64}.                                               \tag{0.7}
\]

The shallow subtraction is already in (EC); B4.1 then yields
\(Z_v(q_A)>7c_m\tau/960\).

**Hard core:** convert either a rotating hull-separator field or the unoriented root-owned tail
union into the top ray without identifying the fixed D population with B5's unrelated overlay.

W66 reaches C0 rather than A-esc and its leaf deficit stays positive; W57/W58 obstruct low-rank
shapes, while W55 pays order-one finance negativity. This is evidence only.

## 1. The tree / the argument

### 1.1 Pinned interface, signs, and the exact claim idempotence does make

Adopt `context/DCAP-ATTACK-W65.md` in full and
`context/DECOMPOSITION-W63-I.md` §§0--1.1 verbatim. In particular

\[
 b={c_m\over128},\qquad k_b={c_m^2\over8192},\qquad
 D_0=2+4\delta,\qquad e_\delta=2\delta(1+\delta),                    \tag{1.1}
\]

\[
 \delta_{\rm rt}=\min\left\{2^{-16},(c_m/4)^2,(c_mb/120)^2\right\},
 \qquad \tau=\sqrt\delta.                                           \tag{1.2}
\]

Fix an arbitrary datum carrying the complete pinned D-cap antecedent, all seven proved W65 D-cap
outputs, and the A-esc priority package

\[
 \eta_D^*(\mathsf A_{\rm esc})\ge {1\over80}.                        \tag{1.3}
\]

The exhibited certificate
\(\mathscr C^*=(\phi,h,f^*,\eta^*)\), the arbitrary legal kernel, and the arbitrary reduced-display
field were fixed in the W65 quantifier order and are never replaced. The realized B5 overlay is
retained, but it is not identified with \(\eta_D^*\).

For a carrier in (1.3), W65 supplies

\[
 {\tau\over2}\le\|D_u\|_1\le2\tau,\qquad a_u\ge4,\qquad
 a_uD_u=k_{O,u}-k_{T,u}.                                              \tag{1.4}
\]

Consequently

\[
 \|x_u-p_u\|_1=a_u\|D_u\|_1\ge2\tau.                               \tag{1.5}
\]

The sign in (0.1) is forced: the pinned residual is
\(p_f-p_u+a_uD_u=p_f-x_u\). No claim below reverses it.

Here is the exact idempotence interface. For every
\(r=\sum_i\lambda_i p_i\in K(P)\), \(\lambda_i\ge0\),
\(\sum_i\lambda_i=1\),

\[
 rP=r,\qquad r\mathbf1=1,\qquad
 \nu(r)\le\sum_i\lambda_i\nu(p_i)\le\delta.                        \tag{1.6}
\]

The first equality uses \(p_iP=p_i\), i.e. the row identities in \(P^2=P\); the last uses convexity
of the negative-part norm. This does **not** assert that \(x_u\in K(P)\), that \(x_u\) is an actual
row, or that a conic coefficient is a transition. If \(r\in K(P)\) obeys
\(\|r-x_u\|_1\le K\delta\), it gives only the honest synthetic finance identity

\[
 r-p_u+a_uD_u=e_u,\qquad \|e_u\|_1\le K\delta.                       \tag{1.7}
\]

On a full fiber \(Q\), put

\[
 c_{u,Q}:=\sum_{j\in Q}P_{uj},\quad
 d_{u,Q}:=\sum_{j\in Q}(\widetilde q_{u,j}-P_{uj}),\quad
 \widetilde q_{u,Q}:=\sum_{j\in Q}\widetilde q_{u,j},\quad
 r_Q:=\sum_{j\in Q}r_j,\quad e_{u,Q}:=\sum_{j\in Q}e_{u,j}.          \tag{1.8}
\]

Then the exact fiber identity is

\[
 r_Q-c_{u,Q}+a_ud_{u,Q}=e_{u,Q}.                                    \tag{1.9}
\]

Equations (1.6)--(1.9), rather than “idempotence realizes convex combinations by rows,” are the
only reproduction claims used below.

The proposed tree is

```text
fixed A-esc priority population (mass >= 1/80)
  -> row-hull defect h_u = dist(x_u,K(P))
  -> H_out: h_u > 3 delta
       -> if mass >= 1/160: HES creative residual
  -> D_tail: h_u <= 3 delta
       -> synthetic-finance tail amplification: Tail_1(u) > tau/8
       -> if H_out mass < 1/160: D_tail mass > 1/160
       -> one common union test + one R0 foldback
       -> P_f*^+(U_tail) > tau/2560
       -> DTR creative residual
  -> either creative residual proves (EC)
  -> E at p_f* produces c_m tau/64
  -> the shallow subtraction was already present; B4 completes the spend
```

### 1.2 SF — synthetic-finance tail amplification

**(a) Pinned contract — `conj-w67-aesc-synthetic-finance-tail-amplification`.** For every carrier
with (1.4), if

\[
 \operatorname{dist}_1(p_u-a_uD_u,K(P))\le3\delta,
\]

then

\[
 \operatorname{Tail}_1(u)>\tau/8.                                   \tag{SF}
\]

This is a single scalar conclusion. It is proposed until separately verified.

**(b) Mechanism and exact banked interface.** Compactness of \(K(P)\) gives a nearest
\(r\in K(P)\). The argument works for every nearest point, so no favorable tie is chosen. Use (1.6)
and (1.9) with \(K=3\). Let

\[
 \chi_u(x)={\operatorname{sgn}(D_u)\cdot(x-p_u)\over\|D_u\|_1},
 \qquad C_u:=\operatorname{Tail}_1(u).
\]

`lem-hx-transverse-moment-identity` gives

\[
 1=\sum_Qd_{u,Q}\chi_u(p_Q).                                        \tag{1.10}
\]

This is the af-validated engine identity. The rest is the sign split used by
`lem-hx-robust-scalar-starvation`, with the actual finance row replaced by the synthetic fixed row
\(r\). The core \(|\chi_u|\le1\) contributes at most

\[
 \sum_{|\chi_u|\le1}|d_{u,Q}|
 \le\|D_u\|_1\le2\tau.                                               \tag{1.11}
\]

Every row point satisfies \(\|p_Q-p_u\|_1\le D_0\), hence, using
\(\|D_u\|_1\ge\tau/2\),

\[
 |\chi_u(p_Q)|\le {D_0\over\|D_u\|_1}\le {6\over\tau}.              \tag{1.12}
\]

On \(\chi_u<-1\), only \(d_{u,Q}<0\) can contribute positively. Since
\((-d_{u,Q})_+\le(c_{u,Q})_++(-\widetilde q_{u,Q})_+\), its total coefficient is at most
\(C_u+\delta\). On \(\chi_u>1\), only \(d_{u,Q}>0\) can contribute positively. From (1.9),

\[
 a_u(d_{u,Q})_+
 \le(c_{u,Q})_++(-r_Q)_++|e_{u,Q}|,
\]

so the total coefficient is at most
\((C_u+\delta+3\delta)/a_u\le C_u/4+\delta\). Therefore (1.10)--(1.12) give

\[
 1\le2\tau+{6\over\tau}\left({5C_u\over4}+2\delta\right)
   =14\tau+{15C_u\over2\tau}.                                      \tag{1.13}
\]

Thus

\[
 C_u\ge {2\tau\over15}(1-14\tau)
 \ge {121\over960}\tau>{\tau\over8},                              \tag{1.14}
\]

because \(\delta\le\delta_{\rm rt}\le2^{-16}\) gives \(\tau\le1/256\). This is the promised
near-mechanical extension. It uses the proof ingredients of the af-validated starvation shard, but
does not claim that shard's literal actual-row contract already contains synthetic finance rows.

No T budget is spent here because SF is a pointwise reduction, not a leaf lower bound. The complete
B4 T/E packet remains an explicit premise of both creative consumers; its spend is (EC), (0.7), and
(2.5) below.

**(c) Honest price.** The most likely technical death is that an independently reconstructed proof of
the af shard is found to use actual-row status beyond fixedness, mass one, and negative mass
\(\le\delta\). Equations (1.10)--(1.14) display every use, so that objection is locally decidable.
Positive evidence is that `lem-hx-signed-variation-ledger` already records the same three synthetic-row
facts. Adverse evidence is the af verifier's rank-two near-example: without a tail cap its tail is
order one.

**(d) Interface check.** The fiber positive part in \(C_u\) is taken after aggregation, exactly as in
R1. The normer is the pinned sign normer, not a selected direction. No actual row, conic coefficient,
hiddenness witness, or reduced witness is identified with \(r\). There is no \(1/t^*(u)\).

**(e) Fallback.** For every fixed \(K\ge3\), the same calculation with
\(h_u\le K\delta\) gives

\[
 \operatorname{Tail}_1(u)
 \ge {2\tau\over15}\left(1-{3K+19\over2}\tau\right).                \tag{1.15}
\]

In particular, if additionally \(\tau\le1/(3K+19)\), the tail is at least \(\tau/15\). The exact
loss is a ceiling
\(\delta\le(3K+19)^{-2}\), and the complementary creative class becomes the strictly smaller
\(h_u>K\delta\) hull-exterior class. This is the clean larger-escape-distance fallback requested in
the brief.

### 1.3 HS — the guarded hull split

**(a) Pinned contract — `conj-w67-aesc-guarded-hull-split`.** Under (1.3),

\[
 \eta_D^*(\mathsf H_{\rm out})<1/160
 \quad\Longrightarrow\quad
 \eta_D^*(\mathsf D_{\rm tail})>1/160.                               \tag{HS}
\]

The priority declaration assigns equality in the failed premise to HES.

**(b) Mechanism.** The two predicates in (0.4) partition
\(\mathsf A_{\rm esc}\), with equality at \(3\delta\) assigned to
\(\mathsf D_{\rm tail}\). Hence

\[
 \eta_D^*(\mathsf D_{\rm tail})
 \ge {1\over80}-\eta_D^*(\mathsf H_{\rm out})>{1\over160}
\]

under the failure guard. SF then supplies (0.5) throughout the second class.

This is bookkeeping only. It retains the parent T/E packet without claiming a lower bound.

**(c) Honest price.** Routine. Its only likely death is a sign error in the target point or assigning
\(h_u=3\delta\) to the wrong class. Both are explicit in (0.1) and (0.4). The two leaves are proper:
neither can contain the other.

**(d) Interface check.** The display field was fixed before \(h_u\) was measured. Hull distance is
clone-invariant and frame-free. The mass equality \(1/160\) belongs to HES; DTR receives a strict
mass floor when HES fails.

**(e) Fallback.** Retain the full distribution of \(h_u/\delta\). For any fixed \(K\ge3\), split at
\(K\delta\), equality on the hull-near side, and use (1.15). No dyadic recursion is needed.

### 1.4 TU — one common tail-union foldback

On a DTR population \(B\subseteq\mathsf D_{\rm tail}\), define the receiver set

\[
 \mathcal U_B:=\left\{R:\exists u\in B\text{ with }
            c_{u,R}>0\text{ and }|\chi_u(p_R)|>1\right\}.             \tag{1.16}
\]

This is a union of full fibers, not a union of directions or a finite cover.

**(a) Pinned contract — `conj-w67-aesc-common-tail-union`.** If
\(\eta_D^*(B)>1/160\), every carrier in \(B\) has \(h_u\le3\delta\), and SF holds, then

\[
 P_{f^*}^+(\mathcal U_B)>\tau/2560.                                  \tag{TU}
\]

**(b) Mechanism and exact banked tools.** For each \(u\in B\), the positive part of a signed fiber
aggregate is bounded by the positive coefficient mass before aggregation. Therefore

\[
 P_u^+(\mathcal U_B)\ge\operatorname{Tail}_1(u)>\tau/8.              \tag{1.17}
\]

Restrict \(\eta_D^*\) to \(B\) and apply `lem-l5-positive-flow-foldback` once, at the fixed row
\(f^*\), to the one common nonnegative test \(1_{\mathcal U_B}\). R0 verifies the submeasure
hypothesis. Thus

\[
 P_{f^*}^+(\mathcal U_B)
 \ge\int_B P_u^+(\mathcal U_B)\,d\eta_D^*(u)-e_\delta
 >{\tau\over1280}-e_\delta.                                         \tag{1.18}
\]

The third component of \(\delta_{\rm rt}\) gives
\(\tau\le c_m^2/15360<1/15360\). Hence

\[
 {e_\delta\over\tau}=2\tau(1+\tau^2)<{1\over3840},
\]

and (1.18) is stronger than (TU).

TU does not itself make a leaf exclusion and therefore does not spend E. Its creative consumer DTR
must produce (EC), where the exact E and shallow spends are visible.

**(c) Honest price.** The gain is real but unoriented. Every receiver in \(\mathcal U_B\) is at
distance \(>\|D_u\|_1\ge\tau/2\) from at least one witnessing carrier, so there is a norm gap and no
freight censoring. However the witnessing carrier and sign direction can depend on the receiver.
Passing from the incidence relation in (1.16) to a common affine direction would reintroduce the
forbidden dimension-dependent cover. TU deliberately retains only one common set.

**(d) Interface check.** Exactly one R2 foldback is used. The test is chosen canonically from the
entire already classified population, not from a favorable atom. The positive part in (1.17) is used
only in the safe direction. No B5 overlay actor is identified with a carrier in \(B\).

**(e) Fallback.** Under the fixed-\(K\) version (1.15), mass \(>1/160\) yields
\(P_{f^*}^+(\mathcal U_B)>\tau/4800\) after the same foldback and the correspondingly smaller ceiling.
Retain the full receiver marginal and incidence relation if the coarse union loses too much geometry.

### 1.5 HES — hull-exterior separator synchronization

For every \(u\in\mathsf H_{\rm out}\), finite-dimensional \(\ell^1/\ell^\infty\) separation gives an
affine functional \(\psi_u\), with linear part of \(\ell^\infty\)-norm at most one, such that

\[
 \psi_u(x_u)-\sup_{r\in K(P)}\psi_u(r)=h_u>3\delta.                  \tag{1.19}
\]

Since \(p_u\in K(P)\) and \(x_u=p_u-a_uD_u\),

\[
 -a_u\,\psi_u(D_u)\ge h_u>3\delta.                                  \tag{1.20}
\]

The separator may be chosen after fixing \(u\); it is geography and is never called a transition.

**(a) Pinned contract — `conj-w67-aesc-hull-exterior-separator-synchronization`.** Every pinned target
datum satisfying

\[
 \eta_D^*(\mathsf H_{\rm out})\ge1/160
\]

obeys (EC).

**(b) Proposed mechanism and exact banked tools.** Equations (1.19)--(1.20) convert actor absence into
genuine convex-hull separation, a stronger fact than the absence of nearby rows. The intended proof
has to compress this separator field into one clone-invariant nonnegative receiver test before using
R0's `lem-l5-positive-flow-foldback`. The resulting root demand must then be paired with
`lem-l5-mass-barycenter-dualization` and the arbitrary attained certificate from
`lem-l5-top-face-ray-formula` to prove exactly (EC). The realized B5 overlay is allowed only as an
independent top-owned background package; it cannot supply the missing overlap with
\(\eta_D^*\).

The mandatory last two lines are not negotiable:

\[
 Z_v(q_A)\ge {1\over8}P_v^+(\mathcal E_*)
                 -{c_m\over16}P_v^+(\mathcal L_v),                   \tag{1.21}
\]

followed by the B4 E spend (0.7). Only after that is the T/shallow estimate
\(P_v^+(\mathcal L_v)<2\tau/15\) used. A proof that obtains a ledger at \(f^*\) but not (1.21) has not
proved HES.

**(c) Honest price.** Creative-hard. The likely death is a rotating-normal crown: every carrier has a
legal separator, but no dimension-free common nonnegative test retains constant separator moment.
The margin in (1.20) can be only just above \(3\delta\), the same order as an R2 error. A finite
directional cover, a sum of carrier-dependent tests, or a favorable separator would violate the
interface. Positive evidence is the K-free rank-three
`lem-starvation-completion-obstruction`, whose discovery was guided by W57/W58, and the absence of
any tall in-class completion. Adverse evidence is that hull-exterior escape does not itself furnish an
actual or synthetic finance row.

The refuter shape is precise: a growing-rank exact tall family with HES mass \(\ge1/160\), separator
margins \(>3\delta\), normals escaping every one-test compression, all D/root/B5/T/E ledgers valid,
and negative leaf deficit. This is strictly narrower than an A-esc refuter because every target must
stay outside the entire row hull.

**(d) Interface check.** No separator is averaged, selected for a favorable value, or transferred
between nearby rows. No finite cover is asserted. Equality \(h_u=3\delta\) is excluded from HES.
The public center remains \(p_{f^*}\).

**(e) Fallback.** For fixed \(K>3\), replace HES by the proper subclass \(h_u>K\delta\). Its complement
has the quantitative tail floor (1.15). A second legal one-time split is macroscopic separation
\(h_u\ge\tau/32\) versus \(K\delta<h_u<\tau/32\), with equality on the macroscopic side; retain both
pieces and do not recurse.

### 1.6 DTR — diffuse-tail ray conversion

This branch has, simultaneously,

\[
 \begin{gathered}
 \eta_D^*(\mathsf D_{\rm tail})>1/160,\qquad
 h_u\le3\delta,\qquad
 \min_f\|p_f-x_u\|_1>3\delta,\\
 \operatorname{Tail}_1(u)>\tau/8\quad\hbox{for every carrier},
 \qquad P_{f^*}^+(\mathcal U_{\rm tail})>\tau/2560.                  \tag{1.22}
 \end{gathered}
\]

Thus \(x_u\) is approximated by a convex combination of row points but by no single row point. This
is the exact high-rank convexification phenomenon.

**(a) Pinned contract — `conj-w67-aesc-diffuse-tail-ray-conversion`.** Every pinned target datum
carrying (1.22) obeys (EC).

**(b) Proposed mechanism and exact banked tools.** SF uses
`lem-hx-transverse-moment-identity`; TU uses R0 and one
`lem-l5-positive-flow-foldback`. What remains is the named **same-center tail-incidence-to-ray
conversion problem**: convert the one common root-owned set \(\mathcal U_{\rm tail}\), together with
its carrier incidence and the B4 packet at the same center \(p_{f^*}\), into (1.21). The only legal
top conversion is through the mass objective
`lem-l5-mass-barycenter-dualization` and an arbitrary top-face ray certificate. B5 can provide its
realized overlay, but its carriers may not replace those in (1.22).

Again the exact finance and height lines are

\[
 Z_v(q_A)\ge {1\over8}P_v^+(\mathcal E_*)
                 -{c_m\over16}P_v^+(\mathcal L_v),                   \tag{1.23}
\]

\[
 {1\over8}P_v^+(\mathcal E_*)
 \ge{\tau S\over64}\ge{c_m\tau\over64},                            \tag{1.24}
\]

and only then
\(P_v^+(\mathcal L_v)<2\tau/15\). The term in (1.24) is financed by E at the one public center,
not by TU's smaller \(P_{f^*}^+\)-mass.

**(c) Honest price and the role of growing rank.** Creative-hard, but materially smaller than A-esc.
Growing rank buys three things for a prospective refuter:

- it can put \(x_u\) within \(3\delta\) of a convex hull of many row points while keeping every row
  farther than \(3\delta\);
- it can rotate the normers \(\chi_u\), so the common union (1.16) remembers incidence but loses a
  common scalar sign;
- it can distribute the finance vector among many rows instead of concentrating W55's order-one
  negativity in one finance row.

The exact costs are (1.5), the pointwise \(\tau/8\) tail, and the root-owned
\(\tau/2560\) common union. In addition, R0 keeps all charged zero-face geography above
\(H-8\tau\), while B4 says top-row shallow mass is \(<2\tau/15\). What is **not** presently priced is
root-to-top synchronization: \(\eta_D^*\le P_{f^*}^+\), whereas B5's closed overlay is top-owned but
explicitly unrelated carrierwise. Treating those populations as the same would fake the missing
theorem.

The decisive DTR refuter is therefore a growing-rank exact tall completion satisfying all of (1.22),
with the incidence directions rotating so that (1.23) fails and with negative W65 leaf deficit. W55
shows what happens without rank-distribution: the local actor scaffold is available, but the finance
row pays order-one negativity. W66 is the sixth consecutive exact batch in which tallness binds;
its opposite cheap escape has exact small negativity but collapses to \(\ell/\tau=2\tau\), reaches
C0 before this window, and lacks the legal far selected mass and nonempty ultra \(\omega\) package.

**(d) Interface check.** The nearest hull point is synthetic, never vertexized. Its convex
representation is not used as transition mass. TU is the only aggregation and uses one common test.
No tail sign is summed after the union forgets it. No B5 carrier is substituted for an R0 carrier.

**(e) Fallback.** The recorded actor-residual weakening fits here exactly. If a weakened branch theorem
assumes

\[
 \eta_D^*\left\{u:\ g_u\ge\tau,\ A_u\ge4,\ \ell_u\ge\tau/2,
  \ \exists f\ \|p_f-x_u\|_1\le3\delta\right\}\ge1/160,             \tag{1.25}
\]

then A-esc need not be solved on that mass. There is no rank-three or bounded-slab hypothesis. The
loss is exactly \(1/80\to1/160\). The actual row in (1.25) is in \(K(P)\), so SF strengthens the
banked R1 conclusion from \(\operatorname{Tail}_1>\delta\) to
\(\operatorname{Tail}_1>\tau/8\), and TU supplies the same
\(\tau/2560\) root-owned package. This fallback removes the actorization leaf but still leaves DTR;
it is not advertised as a proof of (EC).

## 2. Assembly

Assume SF, HS, TU, HES, and DTR at their stated ceilings, all still proposed. Put

\[
 \delta_A:=\min\{\delta_{\rm rt},\delta_{\rm HES},\delta_{\rm DTR}\}. \tag{2.1}
\]

The SF arithmetic itself needs no shrink beyond \(\delta_{\rm rt}\). All constants are fixed before
the datum, certificate, kernel, display field, carrier classification, or nearest-hull points.

Take an arbitrary target datum with \(0<\delta\le\delta_A\) and
\(\eta_D^*(\mathsf A_{\rm esc})\ge1/80\). HS gives exactly one guarded branch.

- If \(\eta_D^*(\mathsf H_{\rm out})\ge1/160\), HES gives (EC).
- Otherwise \(\eta_D^*(\mathsf D_{\rm tail})>1/160\). SF gives
  \(\operatorname{Tail}_1(u)>\tau/8\) for every carrier, TU gives
  \(P_{f^*}^+(\mathcal U_{\rm tail})>\tau/2560\), and DTR gives (EC).

Thus in either branch

\[
 Z_v(q_A)\ge {1\over8}P_v^+(\mathcal E_*)
                 -{c_m\over16}P_v^+(\mathcal L_v).                   \tag{2.2}
\]

Now consume E, once, at the same fixed center \(p_{f^*}\):

\[
 {1\over8}P_v^+(\mathcal E_*)
 \stackrel{\mathrm{B4.2}}{\ge}{\tau S\over64}
 \ge{c_m\tau\over64}.                                               \tag{2.3}
\]

Substitution into (2.2), with the shallow term already subtracted, is exactly the pinned W65 A-esc
contract:

\[
 Z_v(q_A)\ge {c_m\tau\over64}
                 -{c_m\over16}P_v^+(\mathcal L_v).                   \tag{2.4}
\]

Only now spend the tallness bound from B4.1:

\[
 \begin{aligned}
 Z_v(q_A)
 &>{c_m\tau\over64}-{c_m\over16}{2\tau\over15}\\
 &=\left({15\over960}-{8\over960}\right)c_m\tau
 ={7c_m\over960}\tau.                                               \tag{2.5}
 \end{aligned}
\]

This is the requested explicit T/E accounting. TU's \(\tau/2560\) floor is structural input to DTR;
it is never misreported as the source of the larger \(c_m\tau/64\) term.

The quantifier order is

\[
 c_m\longrightarrow(\delta_A,\hbox{universal constants})
 \longrightarrow\mathfrak d\longrightarrow\mathscr C^*
 \longrightarrow\hbox{arbitrary kernel and arbitrary display field}
 \longrightarrow\hbox{forced hull labels}
 \longrightarrow\hbox{arbitrary nearest points used only inside SF}. \tag{2.6}
\]

No selector is moved to the left of the datum or certificate.

## 3. Kill-list check

This is an interface audit, not verification of HES or DTR. `OPEN` below means a mathematical
residual, not permission to cross a wall.

- **K1 — signed / fibers / frame.** Exact signed rows, full row-point fibers, \(\ell^1\) row geometry,
  and frame-free constants only; no raw indices or clone-count floors.
- **K2 — exact target sign.** \(a_uD_u=k_O-k_T\),
  \(x_u=p_u-a_uD_u\), and the A-esc residual is \(p_f-x_u\).
- **K3 — hiddenness / conic discipline.** No \(1/t^*\), no identification of a reduced witness with
  \(P_v^+\), \(\eta\), or \(\lambda_A\), no \(\lambda P=p_v\), and no conic coefficient treated as a
  transition or recurrence.
- **K4 — common-test R2.** TU uses exactly one common nonnegative test. HES and DTR stop rather than sum
  carrier-dependent tests; any future proof must produce its common test first.
- **K5 — selectors / averaging.** The certificate, kernel, and displays are arbitrary and pre-fixed.
  SF is uniform over nearest hull points. There is no favorable row, minimizer, tie, witness average,
  \(y_c\) average, Jensen step, or finite cover.
- **K6 — T/E/center.** Both creative contracts target (EC). Equation (2.3) is the sole line producing
  \(c_m\tau/64\) from E at \(p_{f^*}\); the shallow subtraction occurs in (2.2), before B4.1 is used in
  (2.5).
- **K7 — boundaries.** W65 owns \(g=\tau\), \(A=4\), and \(\ell=\tau/2\) on the A-esc window;
  actor residual equality belongs to T-esc. Here \(h=3\delta\) belongs to DTR, HES mass equality
  \(1/160\) belongs to HES, and the guarded complement is strict.
- **K8 — freight / recursion.** Every TU incidence has the norm gap
  \(\|p_R-p_u\|_1>\|D_u\|_1\ge\tau/2\). There is no censoring, second web, conic recurrence,
  lexicographic minimality, or max-principle far-side return.
- **K9 — hypothesis honesty.** Only the seven D-cap shards are consumed on the D-cap class. No
  `lem-icap-*` shard is invoked. The literal actual-row contract of robust starvation is not silently
  enlarged; SF gives its own displayed derivation.
- **K10 — ownership separation.** \(\eta_D^*\le P_{f^*}^+\) and the B5 top-owned overlay coexist but are
  never aligned carrierwise. TU closes only to \(f^*\).
- **K11 — FINDINGS dead routes.** No legal-leak/orphan exclusion, broad NSC charge, unnormalized class
  sum, quadratic cleanup, generic spectral import, exposer transfer across near rows, unconditional
  alpha bound, witness averaging, coefficient-only LP cleanup, finite-cover main target, failed-census
  emptiness claim, or one-hard-leaf-after-free-preprocessing is used.
- **K12 — evidence discipline.** W55/W57/W58/W63/W66 are L3 evidence or regression shapes only. No
  numerical infeasibility or failed search is promoted.

| Node | K1 | K2 | K3 | K4 | K5 | K6 | K7 | K8 | K9 | K10 | K11 | K12 | Mathematical status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| SF | PASS | PASS | PASS | PASS: pointwise | PASS | PASS: retains B4 for consumer | PASS | PASS | PASS: fresh derivation | PASS | PASS | PASS | proposed routine |
| HS | PASS | PASS | PASS | PASS | PASS | PASS: no exclusion claim | PASS | PASS | PASS | PASS | PASS | PASS | proposed routine |
| TU | PASS | PASS | PASS | PASS: one union test | PASS | PASS: retains E/T | PASS | PASS: explicit norm gap | PASS | PASS: closes only to \(f^*\) | PASS | PASS | proposed routine |
| HES | PASS | PASS | PASS | PASS: common test is the stated gap | PASS | PASS: (1.21), (0.7), then T | PASS | PASS | PASS | PASS | PASS | PASS | OPEN creative residual |
| DTR | PASS | PASS | PASS | PASS: TU only | PASS | PASS: (1.23)--(1.24), then T | PASS | PASS | PASS | PASS: mismatch explicit | PASS | PASS | OPEN creative residual |
| Assembly | PASS | PASS | PASS | PASS | PASS | PASS: (2.2)--(2.5) | PASS | PASS | PASS | PASS | PASS | PASS | conditional |

There is no interface FAIL in the proposed tree. HES and DTR remain mathematical gaps. In
particular, “B5 supplies top ownership” is not a proof of either residual, because B5 itself says its
overlay need not use the original \(\eta_D^*\) carriers.

## 4. Dispatch order

### 4.1 Routine hostile batch

Verify these before funding either creative residual:

1. **SF synthetic invariants.** Recheck \(rP=r\), \(r\mathbf1=1\),
   \(\nu(r)\le\delta\), the fiber identity (1.9), and that fiber aggregation cannot increase any
   negative-part or \(\ell^1\) budget.
2. **SF moment arithmetic.** Recompute the negative-tail coefficient \(C_u+\delta\), the
   positive-tail coefficient \(C_u/4+\delta\), the global lever \(6/\tau\), and the exact close
   \(C_u\ge(2\tau/15)(1-14\tau)>\tau/8\).
3. **Fixed-\(K\) fallback.** Verify (1.15) and the ceiling
   \(\tau\le1/(3K+19)\).
4. **HS boundaries.** Check the sign in (0.1), hull-distance equality ownership, and
   \(1/80-1/160=1/160\) with the strict failure guard.
5. **TU common test.** Verify (1.17), one and only one R0 foldback, and
   \(\tau/1280-e_\delta>\tau/2560\) from the actual \(\delta_{\rm rt}\) ceiling.
6. **Separation geography.** Verify (1.19)--(1.20) for \(\ell^1\) distance to the compact convex row
   hull, without choosing a preferred normal.

The highest-value hostile check is SF: if it passes, high rank has a new unavoidable \(\tau\)-scale
cost; if it fails, the failure will identify the exact actual-row property absent from a synthetic
fixed row.

### 4.2 L3 decider shapes and exact targets

Every candidate family must be an exact rational factorization

\[
 P_k=L_kB_k,\qquad B_kL_k=I,\qquad P_k^2=P_k,
\]

with \(\tau_k\to0\), \(\delta_k=\tau_k^2\), every row negativity at most \(\delta_k\), the full
I-base/all-center package, \(H_k>16\tau_k\), a legal far selected mass, nonempty ultra \(\omega\),
\(\theta_k<\tau_k/D_{0,k}\), the fixed D certificate, all R0/B1--B5/R1 outputs, and the true ray value.
Cloning and transient rank inflation do not count as rank growth.

Every run prints the mandatory W65 panel and also

\[
 \mathcal D_{\rm EC}:=
 Z_v(q_A)-{1\over8}P_v^+(\mathcal E_*)
             +{c_m\over16}P_v^+(\mathcal L_v),                       \tag{4.1}
\]

\[
 \mathcal D_{\rm leaf}:=
 Z_v(q_A)-{c_m\tau\over64}
             +{c_m\over16}P_v^+(\mathcal L_v).                       \tag{4.2}
\]

A negative \(\mathcal D_{\rm EC}\) refutes the stronger residual contract; a genuine refuter of the
pinned A-esc leaf must make \(\mathcal D_{\rm leaf}<0\). These are not interchangeable diagnostics.

**HES target.** Fix the display field before measurement and require

\[
 \eta_D^*\{u:h_u>3\delta\}\ge1/160.                                  \tag{4.3}
\]

Print exact hull-distance certificates and separator margins

\[
 \psi_u(x_u)-\sup_{K(P)}\psi_u=h_u,qquad -a_u\psi_u(D_u)\ge h_u.     \tag{4.4}
\]

The adversarial shape is growing rank with rotating separators, negative (4.2), and no one common
nonnegative receiver test surviving R0 with enough margin.

**DTR target.** Guard (4.3) by strict failure and require

\[
 \eta_D^*\{u:h_u\le3\delta,\ \min_f\|p_f-x_u\|_1>3\delta\}>1/160.    \tag{4.5}
\]

The run must print, carrierwise and in aggregate,

\[
 \operatorname{Tail}_1(u)>\tau/8,\qquad
 P_{f^*}^+(\mathcal U_{\rm tail})>\tau/2560,                         \tag{4.6}
\]

plus the exact receiver incidence, its intersections with \(\mathcal E_*\) and
\(\mathcal L_v\), the realized B5 label, and an explicit warning that the B5 population is different.
The decisive refuter has convex hull distance \(\le3\delta\), actual-row distance \(>3\delta\), rank
and genuine support complexity tending to infinity, all negativities \(O(\tau^2)\), and negative
(4.2). This directly tests whether rank can distribute W55's finance negativity while paying only the
unoriented cost (4.6).

**Regression fixtures.** The exact fractions frozen in `context/fixtures/w66-certificates.json` and
the W66/W63 plateau must continue to route to C0 with
\(\ell/\tau=2\tau\), fail tallness/far-selection/ultra-\(\omega\), and keep
\(\mathcal D_{\rm leaf}>0\). The W55 \(A_0=5\) fixture must reproduce its small actor residual and
large tail but be rejected at its order-one finance-row negativity. W57/W58 remain exact low-rank
infeasibility evidence only.

### 4.3 Creative order

1. **Verify SF and TU.** They are short, quantitative, and decide whether the proposed decomposition
   is real.
2. **Attack DTR first.** It has one root, one canonical common receiver set, a
   \(\tau/2560\) floor, one B4 center, and one top ray. The exact target is (1.23), not an informal
   ledger.
3. **Run the DTR growing-rank completion decider in parallel with the proof attempt.** The target is
   (4.5)--(4.6) plus negative (4.2), not merely an actorless local tableau.
4. **Attack HES next.** First test the macroscopic subcase \(h_u\ge\tau/32\); leave the thin rotating
   separator band as its own retained residual.
5. **Use the fixed-\(K\) fallback if HES stalls.** Increasing \(K\) shrinks the hull-exterior residual
   and gives the exact tail/ceiling loss in (1.15), without rank, slab, or a second web.

No dispatch is complete until the proposed proof prints the exact (EC) line, the E multiplication by
\(1/8\) producing \(c_m\tau/64\), and the pre-existing shallow subtraction before invoking B4.1.
