<!--
WAVE: wave 16b (D_J/B floor decider for conj-b-restricted) — 2026-07-05, session 8, bd aism-cpn.
WORKER: fresh codex exec (prompt: session scratchpad PROMPT-w16b-dj-floor.md). Answer VERBATIM below.
ORCHESTRATOR: script rerun clean (exit 0); headline point's recorded exact quantities
  cross-checked (D = S; D/B = 23/1000; D/delta consistent). LIMITATION: the fresh-point
  certificate JSON lacks full L,B matrices, so the matrix-level independent recheck (DC2/W15
  standard) was not possible — noted in the bundle README; follow-ups must emit full matrices.
READING (orchestrator): the direct-FE route to conj-b-restricted looks FRAGILE — the floor is
  family-dependent (1 - alpha) and collapsed 13x under one adversarial design; the alpha -> 1
  continuation is the kill/rescue question. Combined with W17b's Route-A slack, session 9 should
  lead with the Route-A wall re-read, keeping conj-b-restricted's amplifier wall (0.77764) as
  the soft-evidence anchor that the CONJECTURE itself is still plausibly true.
TIER: T0 certificates (arithmetic-level orchestrator check); T1 identity use; T2 reads.
-->

# Wave 16b D_J/B Floor Decider

Verdict: **UNDECIDED**.

Status tags: T0 exact rational certificates for the listed instances; T1 use of the direct-FE identity; T2 adversarial read; no rigorous theorem.

Rerun commands:

```bash
python3 waves-scratch/w16b-dj-floor/dj_floor_decider.py
python3 -m py_compile waves-scratch/w16b-dj-floor/dj_floor_decider.py
```

## Bundle Recompute

All 9 certified clean-block bundle points were rebuilt from `L,B`: `B L=I_3`, `P=L B`, `P^2=P`, row sums `1`, and `delta<=1/4` were hard-asserted.

For every theta-half argmin, every maximal pivot, and every transverse row, the script recomputed `D_J`, `B_{r,s}`, `D_J/B`, and `D_J/delta` and hard-asserted `D_J=S_J`.

Bundle positive-`B` infimum: `157/500`. The tied charts are included: `(0,1,3)` gives `203/400`; `(0,2,4)` gives `157/500`. No sign kill occurred: every certified positive-`B` row had `D_J>0`.

## Adversarial Minimization

The seed value `lambda=157/500` is not stable. A fresh exact two-carrier-plus-insert sequence pushes the same single-carrier anatomy downward to:

`D_J/B = 23/1000`

Best certified fresh point parameters:

```text
p=43/200, e=11/100, q=11/1000, g=17/1000
v=977/895, w=61/1000, a=1/125, y=2/125
```

It certifies:

```text
alpha=(1-p+e)v = 977/1000
D_J/B = 23/1000
B/delta = 1343375000/2640001033 > 1/2
delta = 5248511/22375000 <= 1/4
```

So this is not a fake kill from `B` vanishing faster than `delta`.

Clean block:

```text
U=(0,2,4), s=2, r=1, clean row j=1, carrier J={3}
```

Best-point margins:

```text
delta cap slack = 345239/22375000
M-Psi = 5907707501/1800740000000
Gamma-M = 1394474279/1800740000000
```

The binding anatomy is clear but not closed: in this family `D_J/B = 1-alpha` and `B/delta` stays near `1/2`, while the search approaches the `delta<=1/4` and maximal-pivot/clean-branch constraints.

## Upper Side

Across all certified rows in this worker, the largest exact `D_J/delta` is:

```text
142110462155611150607479752/581992834553676370814493935
```

So the certified data are consistent with `D_J <= C delta` for that finite-set `C`; no upper-side blow-up was observed.

## Verdict

FLOOR HOLDS is too strong: the certified infimum dropped from `157/500` to `23/1000`.

FLOOR DEAD is also too strong: I did not certify `D_J/B -> 0` and found no point with `D_J <= 0 < B`.

**UNDECIDED residual:** prove or refute whether the fresh alpha-family can be continued with `alpha -> 1` while preserving the clean block, capped theta-half argmin status, maximal pivot `s=2`, and `B/delta` bounded below.

Artifacts:

```text
waves-scratch/w16b-dj-floor/dj_floor_decider.py
waves-scratch/w16b-dj-floor/REPORT.md
waves-scratch/w16b-dj-floor/floor_table.csv
waves-scratch/w16b-dj-floor/floor_certificates.json
```