# Wave W49F — the Fable deep proofs: the faces MERGE; the rank-3 anatomy; the tall class has never been realized (2026-07-07, session 12)

**Node:** the tightness-promotion wall (sketch v14 ledger item 1), user directive "all
creativity and all means". **Design:** two Fable-grade deep-proof authors — FP2 ((F2)) and
FP1 ((F1)) — each hostile-verified by a SEPARATE fresh codex verifier (VFP2 done; VFP1 in
flight). Author drafts + labs in the session-12 scratchpad (`W49-fable/FP1`, `FP2`).

## Verdicts

- FP2: `PARTIAL` — six exact fragments; VFP2: `VALID-WITH-CORRECTIONS (A-G survive;
  corrections are scope/wording, not mathematical invalidations)`.
- FP1: `PARTIAL` — the complete rank-3 zero-face anatomy chain; VFP1:
  `VALID-WITH-CORRECTIONS (core anatomy chain and exact fixtures validate; statement-level
  corrections required)` — five corrections adopted (W nonempty in the setting; the
  positive-transverse-component strictness; the quotient-distance normalization; the
  fixture count 4 disjoint + 5 overlap; the width-4 conjecture form). CODIFIED:
  [[lem-rank3-zero-face-anatomy]], [[lem-rank3-zero-face-min-mass]] (A_min = gap/reach,
  reproducing the banked 100 exactly), [[lem-rank3-supporting-functional-pinning]],
  [[lem-rank3-downhill-dichotomy]], and THE rank-3 closer candidate
  [[conj-rank3-cluster-zero-face-reach]] (width-4 form).

## Results (FP2 side, codified per VFP2's split)

[[lem-zero-face-localization]], [[lem-zero-face-vertex-support]] (the huddle anatomy),
[[lem-ball-cluster-exposure-void]] (the bridge trichotomy is ship-or-intersect),
[[lem-disjoint-hulls-forced-alpha]], [[lem-zero-face-exchange-identity]],
[[prop-f2-t1-equivalence]] (THE STRATEGIC COLLAPSE: (F2) == the (T1) in-class intersection
horn on the restricted domain — the two faces are ONE), [[obs-gamma-capacity-scale-blind]].
Fragment G (shallow blockers, psi(p_z) in (-4*tau, 0) for gap-attaining normalized
separators) verified but deferred from codification (wording-scoped; lives here).

## The decision-grade audit fact (VFP2-recomputed)

ALL banked instances are OUT of the tall class: H^2/delta = 101183481/985900000 (HEIGHT+A),
5/49 (TOP-preserving), 5/99 (W29) — every one < 16. **(T1)'s quantified domain has never
been realized.** FP2's belief: TRUE, very likely VACUOUSLY — tall-emptiness IS the Kernel
height clause. The pre-registered decisive experiment (perturb HEIGHT+A toward tall entry,
FP1's lab2.py instrumentation) is running as W52-BH.

## The pivot (per the pre-committed theatre guard)

The guard's criterion fired ("the mechanism is (T1)'s"): the map pivots from two faces to
ONE face with TWO attack modes — (i) the FP1 rank-3 reach inequality
(lem-rank3-cluster-zero-face-reach candidate, closes the node at rank 3 via the anatomy);
(ii) TALL-EMPTINESS (exclude the huddle: two mutually-exempt deep hidden vertices within
4*tau). Sketch v15 executes the redraw after VFP1.
