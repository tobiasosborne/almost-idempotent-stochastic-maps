# Wave W43 — the optimal-face selection question ANSWERED: the alpha-free condition is a convex intersection (2026-07-07, session 11)

**Node:** sketch v11 (T1) core, bd `aism-2fi` (P0). **Design:** single fresh-codex prover
(worker AQ) + SEPARATE fresh hostile verifier VAQ. Prompts + raw answers in the session-11
scratchpad (`W43/`). Paper wave (VAQ reran the W41 exact verifier: PASS).

## Verdicts (verbatim first lines)

- Worker AQ: `CHARACTERIZED (alpha-free optimal witness exists iff the objective normal is
  generated by the always-tight far constraints and always-tight upper-row constraints, with
  no lower/zero-row normals; tall-mode: PARTIAL/GAP)`
- Verifier VAQ: `VALID (tier: independent hostile verifier; finite-LP duality check plus
  exact-rational fixture recomputation; no repo writes)`

## Results

1. **`lem-optimal-face-alpha-free-characterization` (codified).** Alpha-free optimal
   witnesses exist iff conv{d_f : f ∈ T} ∩ t*·conv{d_i : i ∈ O} ≠ ∅ (T/O = the always-tight
   far/upper sets). VAQ strengthened the dual-face step (any dual optimum complements EVERY
   primal optimum) and resolved the O-empty edge (t* > 0 forces O nonempty for alpha-free).
   All four W41 families recomputed exactly — the condition predicts every blow-up and every
   cancellation in the record, residual by residual.
2. **The terminal node, final form:** prove the intersection nonempty CLUSTER-UNIFORMLY at
   tall heavy near-cluster tops (or the (T2) bridge). The W42 hard stop preserved: near-zero
   exposer values do not certify membership in T/O-complements.

## Banking (orchestrator)

Registry: one shard (VAQ as reviewer). Honest tiers: reviewed (L5); NOT L0; (T1) remains
OPEN in its final geometric form.
