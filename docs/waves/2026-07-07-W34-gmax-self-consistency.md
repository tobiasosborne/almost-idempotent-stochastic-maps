# Wave W34 — g-max self-consistency: the depth-d collapse lands; the cap needs the OPPOSITE direction (2026-07-07, session 11)

**Node:** conj-low-slab-cap attack channel 1 (sketch v8), bd `aism-2fi` (P0). **Design:** single
fresh-codex prover (worker AB; brief carried the depth-d sub-target) + SEPARATE fresh hostile
verifier VAB. Prompts + raw answers in the session-11 scratchpad (`W34/`). Paper wave.

## Verdicts (verbatim first lines)

- Worker AB: `PARTIAL (proved: g-max hidden/web concentration and a depth-d halo collapse with
  exact deeper-row correction; gap: no available lemma charges the self-sustaining high-g deep
  web or transfers the g-max vertex to a hidden top/optimal exposer)`
- Verifier VAB: `VALID-WITH-CORRECTIONS (both claims valid after making the base hypotheses
  explicit: exact signed idempotent, `0<delta(P)`, `W(P) nonempty`; W29 has no non-top hidden
  vertex, and `a=1/4` still has empty `G_a` there)`

## Results

1. **`lem-depth-d-halo-collapse` (codified, proved/af:none).** The af-validated collapse
   generalized to EVERY row index with the exact deeper-row correction C_a(v); vanishes at
   tops (calibration recovers the af contract VERBATIM — the load-bearing anchor). VAB
   legitimized the convex split (negative mass lives inside the residual q) and ran a genuine
   non-top exact fixture (W19 rank-3).
2. **`lem-gmax-web-concentration` (codified, proved/af:none).** In tall theta-flexible windows
   the g-max row vertex is hidden with g_r > 1 − θ − δ and its harmonicity ledger concentrates
   its positive mass on near-max-g rows up to δ(1+2δ)/t.
3. **No-new-squeeze ([T0], banked as body note):** the CS pincer at the g-normalized exposer
   IS the ledger — the two channels coincide at the g-max; do not re-derive.
4. **THE DIAGNOSIS (the wave's strategic payload):** every banked collapse-family tool is
   ONE-DIRECTIONAL (pushes sigma_a/deep mass UP); high-g deep rows make the depth-d collapse
   easier, not contradictory; and the g-max vertex need not be a top (the transfer is open).
   The cap requires the OPPOSITE-direction mechanism — exposedness ABSORPTION — which is
   exactly W35's target (blind pair dispatched: prover AC formalizes; refuter AD measures the
   threshold with exact certificates).

## Banking (orchestrator)

Registry: the two shards above (VAB as reviewer). FINDINGS: the one-directionality diagnosis
folds into the 2026-07-07 entries; sketch v9 at the next redraw. Honest tiers: reviewed paper
proofs (L5); NOT af-validated, NOT L0-rigorous; conj-low-slab-cap remains OPEN.
