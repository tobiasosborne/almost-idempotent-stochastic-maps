# Wave W42 — the two v10 questions: residual cancellation is the mechanism; the cluster-uniform correction (2026-07-07, session 11)

**Node:** sketch v10's two open mechanism questions, bd `aism-2fi` (P0). **Design:** two
independent fresh-codex provers — AO (radial thickness) ∥ AP (the primal conversion) — + ONE
SEPARATE fresh hostile verifier VAO over both. Prompts + raw answers in the session-11
scratchpad (`W42/`). Paper wave (VAO reran the W41 exact verifier: PASS).

## Verdicts (verbatim first lines)

- Worker AO: `PARTIAL (proved: top residual is trapped in a small nonnegative height-deficit
  slab, and heavy near-cluster mass only forces near-zero exposer values; gap: no available
  LP-geometric step turns “near zero” into zero-face convex-hull reach, so no universal `mu`
  follows.)`
- Worker AP: `GAP (missing: a primal lower-bound/row-to-circuit bridge: cheap witness circuits
  for cluster vertices are not controlled by the top row’s positive coefficient ledger, and
  alpha can still absorb the geometric mismatch unless a cluster-uniform alpha bound is
  added.)`
- Verifier VAO: `VALID-WITH-CORRECTIONS (the bricks survive, but AO-2/AP-1 need explicit
  affine linear-part bookkeeping; AO-3 is duplicate; no brick proves the tall
  radial-thickness conjecture or AP’s primal conversion.)`

## Results

1. **`lem-bounded-alpha-top-slab-reduction` (codified — the ONE new shard; VAO-corrected
   constant).** Bounded-alpha cheap witnesses of rho-near cluster vertices are confined to
   the top slab — the graph-building half of the primal conversion.
2. **NOT codified (L2 discipline, VAO's flags):** AO's heavy-cluster low-slab statement
   (duplicate of the banked pincer corollary) and the top-residual-slab scalar (the engine
   already inside alpha-slab-leakage/depth-Markov).
3. **The TERMINAL-NODE REFORMULATION (the wave's payload):**
   (a) the thickness route has a hard stop — value normalization never turns "h* small on
   the cluster" into "h* zero" (the exact missing LP statement: min over optimal h* of
   h(p_j) = 0 for a useful near-deep nonclone row);
   (b) the W41 certificates acted by RESIDUAL CANCELLATION (R = 0 on the optimal face;
   VAO-reconfirmed alpha = 0 in the TOP-preserving family) — the right target is
   optimal-face selection, not measured thickness;
   (c) AP's correction: any such statement must hold CLUSTER-UNIFORMLY (v's own datum is not
   enough); the alternative single statement is the row-to-circuit absorption bridge
   (if every mass-carrying cluster vertex has a cheap bounded-alpha witness, then v ships
   >= c mass outside C or some cluster vertex is exposed).

## Banking (orchestrator)

Registry: one shard (VAO as reviewer). Sketch v11 carries the reformulated terminal node.
Honest tiers: reviewed (L5); NOT L0; both v10 questions remain OPEN in sharpened form.
