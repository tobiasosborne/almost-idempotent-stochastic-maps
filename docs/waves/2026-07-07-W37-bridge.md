# Wave W37 — the bridge: the row-far dual certificate lands; the dual direction is a wall (2026-07-07, session 11)

**Node:** THE BRIDGE (row mass ↔ circuit mass; W36's final form), bd `aism-2fi` (P0).
**Design:** single fresh-codex prover (worker AF; brief carried the own-row decomposition and
the λ·P hybrid) + SEPARATE fresh hostile verifier VAF. Prompts + raw answers in the session-11
scratchpad (`W37/`). Paper wave.

## Verdicts (verbatim first lines)

- Worker AF: `BRIDGE (proved: the row-mass dual certificate `t*(v) <= nu_v / L_F(v)`; cap: gap
  is the wrong dual direction plus `rho`-halo/near-mass escape)`
- Verifier VAF: `VALID (minimal hypotheses: R needs exact signed idempotent + row vertex/t*
  program + L_F(v)>0; H needs a normalized dual witness, with “hidden” only supplying
  B=sum beta=t*(v)<kappa; O is directionally correct.)`

## Results

1. **`lem-row-far-dual-certificate` (codified, proved/af:none).** t*(v) ≤ ν_v/L_F(v) —
   normalizing v's own row reproduction is a feasible dual point; SHARP on the W29 frontier
   (ν₃/L_F = 1/81 = t*₃ EXACTLY). Visible corollary: visible vertices ship ≤ 4τ to their
   ρ-far set. The first exact row-mass ↔ exposedness-margin inequality.
2. **`lem-hybrid-dual-certificate` (codified, proved/af:none).** The λ·P substitution circuit
   is feasible but NON-AMPLIFYING (L_μ ≤ 1 + δ; bound near-tautological) — the hybrid
   direction is closed honestly.
3. **THE DUAL-DIRECTION WALL (FINDINGS):** feasible circuits UPPER-bound t*; hiddenness
   (t* < κ) cannot be run backward through them; and ρ-NEAR mass contributes zero to L_F.
   The surviving counterexample mode = the ρ-near deep cluster — W38's target.

## Banking (orchestrator)

Registry: the two shards (VAF as reviewer). FINDINGS: the dual-direction wall folded into the
2026-07-07 entries. Honest tiers: reviewed (L5); NOT L0; the cap remains OPEN.
