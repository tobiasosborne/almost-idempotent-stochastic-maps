<!--
ROLE: W56 wave close record — SL1a Tier-2 decomposition: two INVALID rounds certify a
  wall; the twice-verified routine material extracted, per-shard hostile-verified, and
  BANKED (registry 140 -> 150). Supersedes the -interrupted record for outcome purposes;
  that file remains the interruption record.
STATUS DISCIPLINE (L0): banked lemmas are L5 (fresh-hostile-verified), NOT af-validated.
  SL1a remains OPEN (now = the three sigma-cells). Promotes nothing to L0.
-->

# Wave W56 CLOSE — SL1a decomposition: the wall, the harvest, the new surface

**Target:** `conj-straddling-web-exclusion` (SL1a). **Method:** decomposition-first,
serial workers, reviewer != author throughout. Workers: resumed codex architect
(gpt-5.6-sol ultra) -> fresh codex hostile verifier r1 (xhigh) -> Fable repair author
-> fresh codex hostile verifier r2 (xhigh) -> codex extractor (xhigh) -> fresh codex
per-shard verifier (xhigh) -> codex corrections worker (high). All artifacts in
`2026-07-09-W56-artifacts/` (prompts, drafts, session logs, verdicts verbatim).

## Verdict first-lines (verbatim)

- r1 (on the resumed architect's DECOMPOSITION.md): `VERDICT: INVALID — H-CCO is a
  broader restatement of the residual exclusion, and its purported one-level sub-DAG
  merely renames unresolved completion cells.`
- r2 (on the Fable repair's DECOMPOSITION-v2.md): `VERDICT: INVALID — H-W is a
  lex-minimal restatement of SL1a, and its purported sub-DAG leaves three hard
  residuals unresolved.`
- extraction (per-shard): `VERDICT: 4/10 VALID` — plus 6 VALID-WITH-CORRECTIONS,
  0 INVALID; all prescribed corrections applied (`CORRECTED: 6/6`) and the corrected
  shards banked.

## The wall (the round-1/round-2 convergence — now a FINDINGS certificate)

The routine pipeline (selector, kernel corner, partition) is FREE: provable from
standing shards. Hence any terminal hard leaf retaining the full counterexample class
is derivable back to SL1a in one routine sentence — a restatement by construction.
Round 2's lex-(V,R) minimal-counterexample stratum was additionally free preprocessing
(minimality had no valid consumer) and failed transient-row stability. The
"one hard leaf with a strictly smaller configuration space" objective is DEAD for SL1a.

## The harvest (banked; registry 140 -> 150)

7 proved L5 lemmas: `lem-sl1a-score-selector`, `lem-sl1a-corner-ledger`,
`lem-radial-horn-partition`, `lem-zero-face-one-sixteenth-capacity-kill`,
`lem-affine-barycenter-identity`, `lem-clone-invariant-row-complexity`, and the prize
`lem-sl1a-three-cell-reduction` (conditional: three cells => SL1a,
existence-plus-universality routing, disjoint+exhaustive guarded cells).
3 conjectures (the new SL1a surface): `conj-sl1a-deep-diagonal-cell` (H-D),
`conj-sl1a-intersection-diagonal-cell` (H-I), `conj-sl1a-off-diagonal-cell` (H-X) —
single-contract, af-elevation shape, delta->0 refuter criteria. SL1a is EQUIVALENT to
the conjunction (reduction lemma + the r2-exhibited converse); the value is mechanism
separation, not strict progress — stated honestly, exactly the W54 close shape.

5 new dead-route certificates appended to FINDINGS.md (2026-07-10): the
one-hard-leaf-after-free-preprocessing objective; lex-(V,R) stratification;
freight-censoring without a norm gap; second-generation L-C recursion; the
max-principle far-side return channel (= the W55 carrier-coincidence obstruction).

## Process notes

- The codex usage limit interrupted verification once (Jul 10 00:49 reset); a delayed
  dispatcher bridged it. gpt-5.6-sol tiering (ultra/xhigh/high) worked as configured.
- The user creativity directive was exercised: both architects redesigned freely (the
  Fable round replaced the whole lower DAG); the hostile gates caught both overreaches.
  Creativity + hostile verification behaved as designed: bold drafts, honest bank.

## Next (see sketch v20 Tier-1 order)

H-X first (shares the completion-obstruction shape with the W55 large-gauge wall — one
exact `P=L*B, B*L=I` LP wave serves both), then H-D / H-I; E1-E5 codification; the
small-gauge bridge; SL1b unchanged as the easiest sibling; L5 minimax independent.
