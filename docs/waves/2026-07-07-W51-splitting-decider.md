# Wave W51 — the two-level splitting decider: the Gamma-lumping bypass dies at the class-count wall (2026-07-07, session 12)

**Node:** the FP3 bypass audit's best-value dual-use decider (B4), bd `aism-2fi`. **Design:**
one fresh-codex prover (BG) + SEPARATE fresh-codex hostile verifier (VBG). Prompts + raw
answers in the session-12 scratchpad (`W51/`).

## Verdicts (verbatim first lines)

- BG: `PARTIAL (proved: sharp Gamma-to-straddling inequalities plus a rank-3 class-count
  obstruction to any Gamma-only two-level theorem; gap: no tall-heavy tightness-promotion
  input forcing an F2 blocker)`
- VBG: `VALID-WITH-CORRECTIONS (core claims hold; sharpen the straddle bound with nu_j, and
  scope the obstruction to Gamma-budget-only splitting)`

## Results

1. **[[obs-gamma-two-level-class-count-wall]] (codified):** Gamma-only two-level splitting
   is FALSE at rank 3 (exact P_eps family; three levels on zero-Gamma mass rows). The B4
   Gamma-lumping bypass dies at the recorded class-count wall — as the bypass audit
   predicted. Scope honest: no hidden/tall claim.
2. **[[lem-positive-row-straddle-gamma-lower]] (codified):** the straddle-to-budget
   converter with its honest (F2)-scale limit (tau^3 vs tau^2 at kappa).
3. **The consolidated verdict:** budget-class attacks on the tightness-promotion wall are
   PROVABLY insufficient. The wall needs LP-tightness structure. Live attacks: FP1/FP2
   (Fable, non-budget); pre-committed pivot if both return mechanism-needed.

## Banking (orchestrator)

Two shards (VBG as reviewer); honest tiers reviewed (L5). The B4 axis of the bypass audit
is now CLOSED by certificate (not just priced).
