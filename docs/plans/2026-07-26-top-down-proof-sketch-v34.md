<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 34 (sessions 27+28
  delta: phase-4 safe leaves closed + GAP-EA discharged + Stage-1 topology
  ground truth L0). Supersedes v33; everything not restated here is unchanged
  from v33 (and, through it, v32/v31).
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing.
  op-classical remains OPEN. T0 = 82; nothing below is af-validated unless
  said so.
-->

# Top-down proof sketch v34: op-classical (2026-07-26, sessions 27–28 delta — all six §D safe/gateway leaves T0; GAP-EA discharged; five of seven Stage-1 topology sources af-validated)

## UNCHANGED from v33

The signed-geometry trunk and its whole surface (PAUSED per standing directive
viii); Route X as the registered fallback; the phase-2/3 process laws; the
`lem-routef-k-ledger` DO-NOT-REWIRE-OR-SEED guard (parent stays blocked until
GAP-LEDGER-DOMAINS forms a transcribable local-domain DAG); the
`docs/LEARNINGS.md` 2026-07-26 partial retraction of the "Route F
proved-mod-audit COMPLETE" headline (15 rows demoted; fresh proofs with
derived local domains are needed — no new conjecture, no new idea).

## Map change 1 (session 27): ALL SIX v4.1 §D safe/gateway phase-4 leaves are T0 (70 → 76)

`lem-stage1-quantitative-inverse-function` (71st, 14 nodes),
`lem-stage1-exact-unit-rectification` (72nd, 6), `lem-routef-prh-finish`
(73rd, 22), `lem-kitaev-almost-idemp-audit` (74th, 24 — the FH gateway),
`lem-routef-functional-calculus-closeness` (75th, 11),
`lem-routef-ai-defect-linearization` (76th, 13). All first-run, all taint
clean. The banking precedent (user-ratified 2026-07-26) is in force: a
validated root expansion replaces the registry contract VERBATIM at banking.

## Map change 2 (session 28): GAP-EA IS DISCHARGED at L0 (the first of the four GAP families falls)

The design cycle ran exactly as prescribed (codex design job →
`DESIGN-GAP-EA.md` option (a) → verbatim landing → seeding → elevation):

- **`lem-extcb-exact-target-correction` af-VALIDATED (82nd, 6 nodes 6/6
  clean, first-pass, zero challenges)** — the standalone transcription of the
  validated `conj-extcb` node-1.2 subtree: for finite-dimensional
  C*-algebra B and finite-dim H, a linear dagger-preserving T with
  amplification-uniform multiplicative/unit defects ≤ a ≤ a_corr admits ONE
  level-one unital dagger-homomorphism μ with ‖μ_n−T_n‖ ≤ C_corr·a at every
  n. C_corr = 57 lives in the proof body, NOT the contract (over-banking
  guard). The tree landed exactly on the designed 6-node budget (diagonal /
  Newton K_N=18 / degenerate-safe convergence + the two endpoint-repair
  nodes).
- **`lem-extcb-exact-target-approximation`** landed as the M_r-specific
  BRIDGE row (v4.1 contract unchanged, `stated`, deps: the correction row).
  Seeding plan: 2–3 nodes after banking, external = the validated correction
  contract.
- **IMPROVE-CB (`lem-maincb-error-improvement`) consumes the GENERAL row**,
  not the bridge. Its contract narrowing (add finite-dim source +
  ε ≤ ε_max^cb per the pinned source, DESIGN-GAP-EA §2.3) is ESCALATED TO
  THE USER — outside the verbatim delegation. It must not seed unnarrowed
  (§4.8: the overbroad form repeats the local-domain failure pattern v4.1
  quarantines).

## Map change 3 (session 28): Stage-1 topology ground truth — 5 of 7 rows are L0

af-validated first: `lem-topology-lefschetz-hopf` (77th, 2 nodes; the
maximal-simplex-restricted Arkowitz–Brown Thm 1.2 — the consumer obligation
on `lem-stage1-extra-fixed-class` stands), `lem-topology-kunneth-cross-product`
(78th, 7), `lem-topology-orientable-top-cohomology` (79th, 14; Thm 3.26 +
field-UCT + Cor 3.39 composed), `lem-topology-quotient-manifold` (80th, 4;
Lee Thm 21.10), `lem-topology-hopf-structure` (81st, 13; Thm 3C.4 + the
Hopf-algebra definition, finite-dim corollary derived in-tree).

Two rows are PARKED with precise diagnoses (ledgers preserved, work ~90%
validated):

- `lem-topology-finite-triangulation` (19/23 validated): af inference-rule
  bookkeeping thrash at node 1.5 (modus_ponens declared without P, P→Q
  premise shape). Process repair, no math risk.
- `lem-topology-local-index-sign` (21/23 validated): **GENUINE contract/def
  scope gap** — the contract asserts the index formula for an unqualified
  C¹ self-map, but `def-lefschetz-fixed-point-data` is harmonised to smooth
  self-maps of compact orientable manifolds. USER DECISION: narrow the
  contract to the def's scope (checking the `lem-stage1-extra-fixed-class`
  consumer still gets what it needs) vs. re-scope the def (ripples; L2).

## Map change 4 (session 28): orchestration-infrastructure laws (process, banked to bd memory)

(i) af orchestrations are strictly SEQUENTIAL per checkout — the overreach
guard's porcelain snapshot makes parallel runs mutually aborting; (ii) a
codex job writing a NEW repo path mid-run trips live guards (pre-create the
dir); (iii) the Stop-hook-forced fr logging appends to `.frontier/log.jsonl`
and kills a live run whose baseline was committed-clean — commits land ONLY
in zero-live-run windows. None of the seven aborts today was mathematical;
2,386+ adversarial jobs remain at zero route-level refutations.

## The open surface after this delta (phase 4)

1. **GAP-LEDGER-DOMAINS** (now the largest open family): the 14 local-radius
   producers per v4.1 §D ordering; `lem-routef-k-finiteness` last.
2. **GAP-S1-POLAR-CONTRACT + GAP-MAIN-STRUCTURE**: formula-level polar
   contracts; the split/merge/reset induction into the eight MAIN `stated`
   targets — MAIN seeding gated on the user's IMPROVE-CB narrowing decision.
3. **The assemblies**: `lem-thmainext-conditional`, `lem-routef-k-ledger`
   (guard intact), F0/F2/F3 root composition, phase 5 `aism-y81y`.
4. **Small closers**: the bridge-row elevation (2–3 nodes); the two parked
   topology trees (one process repair, one user decision).

## Controller note

The fr circuit-breaker fired on FH (stalled ×2 under the frontier-move
criterion despite +6 T0 today — elevation waves bank rigour but do not move
the OPEN line). Decision logged: next cycle EXPLOREs XE (the Route X
deciders: aggregate-peak, QCMP ratio, quotient-refinement/cross-financing)
before further FH exploitation.
