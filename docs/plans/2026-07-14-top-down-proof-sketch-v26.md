<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 26 (W66-W68 session-19 delta).
  Supersedes v25; everything not restated here is unchanged from v25 (hence v24/v23).
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing.
-->

# Top-down proof sketch v26: op-classical (2026-07-14, W66-W68 delta — the bridge is repaired)

## UNCHANGED from v25

The trunk (op-classical <= op-exposed-hull <= HLC <= Kernel/(EX)), the
three-cell SL1a surface, all dead routes, the H-X two-route fork (aism-ur9,
USER DECISION pending), H-D, H-I, SL1b, L6.5, E1-E5 codification, the
small-gauge bridge. T0 count 34 af-validated. Registry now 194.

## Map change 1: the ASSEMBLY BRIDGE IS REPAIRED (W68; registry 192 -> 194 + 1 rewrite)

The 2026-07-10 INVALID verdict on `lem-huddle-charge-assembly` is discharged
by executing its own repair recipe (artifacts in
`docs/waves/2026-07-14-W68-artifacts/`; fresh xhigh prover -> fresh xhigh
hostile verifier, 3/3 VALID-WITH-CORRECTION, zero mathematical defects):

- **`conj-l5-gap-1` is REGISTERED** — the dual-face mass minimax (universal
  c_5 before c_m; A at depth H - 8*tau; charge >= c_5*c_m*tau), verbatim the
  W62 pinned target. The entire W62-W67 reduction tree (lem-l5-*,
  lem-ihorn-*, lem-icap-*, lem-dcap-*, lem-aesc-*) now has its parent
  formally in the DAG.
- **`lem-intersection-branch-production` is PROVED (L5)** — the previously
  missing Branch-II implication: the L2-core intersecting-hulls
  configuration produces either the SL1a-forbidden probability measure or
  the SL1b-forbidden sub-probability measure. Genuine dependency repair en
  route: the prose "B5" co-top clause is supplied by the proved
  `lem-top-witness-third-actor` (13/16 at c = 4), not by the confinement
  shard.
- **`lem-huddle-charge-assembly` is now PROVED (conditional)** — explicitly
  two-branch on {SL1a, SL1b, cotop-web-coupling, conj-l5-gap-1} with
  delta_0 = min{delta_a, delta_b, delta_c, delta_5(c_*/2), 1/4,
  (c_5*c_*/6)^2}. DO-NOT-CONSUME is lifted; unconditional consumption of its
  conclusion remains illegal. lem-l2-core-collapse is no longer a dep.

**Consequence — the conditional surface of the tall near-cluster charge is
now EXACTLY four named conjectures:** SL1a (== the three conj-sl1a-* cells
via the proved three-cell reduction), SL1b, conj-cotop-web-coupling (L6.5),
and conj-l5-gap-1. Closing those four closes the tall near-cluster charge
through a proved chain. The L5 minimax campaign no longer feeds a broken
link.

## Map change 2: A-esc decomposed on a proved tau/8-tail interface (W67; registry 187 -> 192)

The first W65 creative leaf `conj-w65-dcap-actorization-escape-exclusion` is
DECOMPOSED (AESC-ATTACK-W67.md; routine batch 5/5 hostile-verified, SEP in
verifier-corrected linear-part form — 4th genuine defect caught upstream):

- **KEY MECHANISM EXTENSION (`lem-aesc-synthetic-finance-tail-amplification`,
  proved L5):** the starvation engine now prices SYNTHETIC finance rows. On
  the A-esc window, a missing actor within 3*delta of the row HULL (not of
  any actual row) forces Tail_1(u) > tau/8 — a tau-scale cost, rank- and
  slab-free, consuming only rP = r, mass 1, nu(r) <= delta and the
  af-validated transverse-moment identity. Fixed-K fallback: residual
  <= K*delta forces Tail_1 >= tau/15 at delta <= (3K+19)^(-2).
- Plus proved: guarded 1/160 hull split, the one-foldback common tail union
  (P_f*^+(U_tail) > tau/2560), separator geography.
- **A-esc == exactly TWO strictly smaller creative residuals:** HES
  (hull-exterior separator synchronization; refuter = growing-rank
  rotating-separator crown) and DTR (diffuse-tail ray conversion; refuter =
  growing-rank tall completion with rotating incidence). Both target the
  stronger same-center inequality (EC) with the exact E line (c_m*tau/64 at
  p_f*) and the shallow subtraction before B4. Weakened-theorem fallback
  removes A-esc at 1/80 -> 1/160 loss with no rank/slab hypotheses.

## Map change 3: the D-cap decider program is complete (W66; L3, no status change)

All five W65 D-cap leaves BLOCKED in exact families
(runs/2026-07-14-w66-dcap-five-leaf-decider/): zero I-base entrants, zero
refuters, tallness binds for the SIXTH consecutive batch. First exact local
C0 cell entrant (definition-level only; adverse fixture). No tested family
reaches the A-esc window (ell < tau/2 routes to C0 first); the T-esc
residual/tail shape has only ever coexisted with order-one finance
negativity. Both unit tests pass.

## Tier-1 order (updated)

0. **DTR creative attack** (aism-72zn; W67 §4.3: one root, one canonical
   common receiver set, tau/2560 floor, one B4 center, one top ray — the
   best-conditioned creative target so far) + its growing-rank L3 decider in
   parallel; then HES (macroscopic h_u >= tau/32 subcase first).
1. **The route fork (aism-ur9, USER DECISION)** — Route A vs Route B on H-X;
   decider-informed since W60.
2. **af-elevation queue (aism-88r)** — prime candidates now:
   lem-aesc-synthetic-finance-tail-amplification (single minimal contract,
   engine-adjacent), lem-intersection-branch-production (bridge-critical),
   the D-cap spine (root closure + five-way split), lem-hx-robust-scalar-
   starvation elevation follow-ups. The L5:T0 ratio (now ~60:34) must not
   keep widening.
3. **The remaining L5-GAP-1 creative leaves** in decider-informed order
   (W64 six, then T-esc/G<4/C0/N, then X/Sh/W/D, then S/C).
4. **SL1b, conj-cotop-web-coupling (L6.5), H-D/H-I** — the other three
   conjectures of the bridge's conditional surface + the sibling faces.
5. E1-E5 codification + small-gauge bridge (batched); parked items
   unchanged (aism-l1a, aism-cei, aism-5de, aism-nlg, aism-z98).
