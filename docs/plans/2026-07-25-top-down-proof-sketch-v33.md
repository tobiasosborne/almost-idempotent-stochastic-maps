<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 33 (session-26
  phase-3-closure delta). Supersedes v32; everything not restated here is
  unchanged from v32 (and, through it, v31).
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing.
  op-classical remains OPEN. T0 = 70; nothing below is af-validated unless
  said so.
-->

# Top-down proof sketch v33: op-classical (2026-07-25, phase-3 closure delta — EXT-CB is L0-rigorous; the quarantine repair is landed; phase 4 is single-gated on the GAP families)

## UNCHANGED from v32

The signed-geometry trunk and its whole surface; Route X as the registered
fallback; the phase-2 process laws (dep alignment, default first-class set,
cumulative def kit, tripwire factoring) — now validated across TWO phases;
the `lem-routef-k-ledger` DO-NOT-REWIRE-OR-SEED guard (per the v4.1 design:
the registered F2/F3 rows close the stochastic-retract bridge and compose
literally with `lem-routef-prh-finish`, but the parent stays blocked until
GAP-LEDGER-DOMAINS forms a transcribable local-domain DAG).

## Map change 1: PHASE 3 EXECUTED AND CLOSED (aism-fgr7 closed; T0 69 → 70)

The **parent `conj-extcb` is af-validated (L0)**: root EXT-CB extension
theorem — one map `v₊:M_{r+1}→A` whose every amplification is a
`C_ext·e`-isomorphism, the same level-one unitary and the same four corner
maps carrying all amplification levels, constants independent of `r`, `n`,
`dim A`. 46 nodes (40 validated + 6 archived), taint clean 46/46; the
user-stopped session-25 run resumed with zero ledger loss and closed in 21
rounds at routine tier. Banked ▣: external oracle `af-conj-extcb`
registered, `fr verify proofs/conj-extcb/export.md` passes. With `conj-hcb`
(v32) this closes **both named gaps of `th_main_ext`** — the entire
COMP/H-CB/EXT-CB tower under the Route F chain is now L0-rigorous.

## Map change 2: the aism-0163 quarantine repair is DESIGN-COMPLETE (v4.1)

The V4 cycle (fresh-codex repair → separate fresh-codex hostile verify
VALID-WITH-CORRECTIONS → mechanical correction application) landed as
`DESIGN-FUDW-DECOMP-v4.1.md`: the v3 verdict's Registry impact A/B/C applied
byte-faithfully — new upstream `lem-maincb-reset-constant-ledger` (transcribing
the hostile-verified constant ledger, PROOF-W74F-H-STAGE1:389-423), the three
raw-reset rows + `lem-maincb-uniform-reset-chain` re-contracted, the polar row
and fourteen ledger-domain ids reduced to uncontracted GAP inventory
reservations, and the registered F2/F3 bridge rows folded in. Inventory
self-checked: 79 contracted = 57 proved-mod-audit / 15 stated / 7
cited-candidate, 15 GAP reservations, 20 proposed defs. **Phase 4 (aism-5byv)
is now single-gated on genuine mathematics**: GAP-EA, GAP-S1-POLAR-CONTRACT,
GAP-MAIN-STRUCTURE, GAP-LEDGER-DOMAINS — plus def sign-offs and the topology
byte-match below. No process debt remains between phase 3 and phase 4.

## Map change 3: Stage-1 topology ground truth 3/7 acquired (legal OA only)

In `refs-staging/` (promotion to `refs/` is the deliberate per-def L1 step):
Hatcher AT (rows: top-cohomology / Künneth / Hopf structure — NOTE the
canonical PDF numbers Künneth as **Thm 3.15**, not the design's 3.16, and the
top-cohomology locus should cite Cor 3.39; design-owner flag recorded in
ACQUIRED.md), Cairns 1935 Bull. AMS (finite triangulation, in place of the
JSTOR-gated Whitehead 1940), Arkowitz–Brown 2004 (Lefschetz–Hopf, OA journal).
**Lee (quotient manifold) and Granas–Dugundji (local index sign) have no legal
open copy — USER escalation: purchase/institutional access.** These two gate
only their own `cited candidate` leaves.

## Report surface (waves 3+3b)

Eleven prose shards authored (the phase-2/3 harvest: `conj-hcb`,
`lem-hcb3-offdiagonal-inverse`, the hcb4-canonical trio, the seven EXT
lemmas over 6 shards, revised status outlook); batched hostile
prose-vs-export review (W56 pattern): 7/11 VALID first pass, all 12 contract
quotes byte-matched; the four non-VALID shards all failed in ONE direction —
prose importing material outside the export/registry ground truth — and are
under verdict-specified correction with a fresh re-verify to follow. Two
review catches already landed as registry hygiene (the
`lem-hcb4-canonical-inverse` defs-line gap — a recorded linker blind spot for
workspace-provisioned defs — and the stale `lem-extcb-four-corner-norm`
provenance sentence). Landing follows the re-verify.

## The live campaign frontier: PHASE 4 (aism-5byv) preliminaries

Elevation order unchanged (Stage-1 packet → `lem-thmainext-conditional` →
`lem-routef-k-ledger`), but per v4.1 §D nothing in MAIN/degree/telescope/
threshold seeds until its named GAP closes. The actionable preliminaries:
(i) the v4.1 def-provisioning register sign-offs (USER), (ii) the two
missing topology sources (USER), (iii) GAP-EA and the ledger-domain
local-radius producers as the first genuine phase-4 mathematics. Route X
deciders (aism-ea2f) remain the priced fallback; phase 5 (aism-y81y: F0
codification + root composition) unchanged behind phase 4.
