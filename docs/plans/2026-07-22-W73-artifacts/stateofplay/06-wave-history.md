# Attack Narrative W55–W72 (chronological ledger + meta-pattern)

## Ledger
- W55 (conj-cotop-web-coupling): OPEN, decomposed. Exact local starvation gadget A0=5, g=5τ survives every scalar ledger; both hostile reviewers REJECTED the large-gauge thin/thick closure (dual conic multipliers are not transition mass). Insight: decisive question is GLOBAL completion/refutation in P=LB, BL=I coordinates, not a local scalar ledger.
- W56 (SL1a decomposition): two hostile INVALIDs before the wall was certified; extraction banked, registry 140→150; SL1a collapsed to three σ-cells H-D/H-I/H-X on lem-sl1a-three-cell-reduction.
- W57/W58 (starvation gadget completions): minimal rank-3 actor-hull family INFEASIBLE (exact Farkas, stable A0∈[4,6], τ≤1/256); extra-vertex family also INFEASIBLE; K-parametric.
- W59: completion obstruction PROVED K-FREE (lem-starvation-completion-obstruction, τ≤1/256), then af-VALIDATED (T0 29). Mechanism: idempotence demands one unit of transverse moment vs O(τ) supply.
- W60: H-X engine bank banked L5 (5 lemmas: transverse-moment-identity, signed-variation-ledger, financing-floor, robust-scalar-starvation with ceiling δ_R=min(2^-16,1/(4H²)), forced-exterior-coupling). Route fork escalated (aism-ur9).
- W61: all five W60 lemmas af-VALIDATED (T0 34). Retraction: financing-floor "all reals A" → A>0. X2 graft refuter NOT refuted (everything except tallness, H=O(τ³)); a genuine financing instance found (ledger-only close dead). Convergent signal: tallness is the binding wall in both searches.
- W62: L5 minimax reframed as engine-payer mass-transport dual on owned barycenter q_A (old cover framing retired). R0–R3 proved 4/4 (registry 162): lem-l5-mass-barycenter-dualization, top-face-ray-formula, positive-flow-foldback, universal-exterior-payer (row v pays τS/8 outside every half-ball). I-horn refuter: ALL THREE SHAPES BLOCKED (tallness, 3rd bind).
- W63a/b/c: S/C deciders BLOCKED (4th bind); I-horn DECOMPOSED (10 nodes 10/10 VALID, registry 172), routes into the same X/I/D selected-corner trichotomy — structural unification; six-shape decider ALL BLOCKED (5th bind), diagonal plateau has M_I=0 exactly, first exact M_X>1/8 ledger.
- W64: I-cap decomposed (8 lem-icap-*, registry 180). Exact 4×4/8×8 calibrations: tall TOP OWNERSHIP, not intersection, is the real obstruction. (2nd genuine defect caught.)
- W65: D-cap decomposed (registry 187): 7 routine nodes + 5 creative leaves N, G<4, C0, A-esc, T-esc. Sharpening: robust-scalar-starvation is rank/slab-free once actorized — higher-rank slab escape collapses into A-esc + T-esc. (3rd defect: undefined Ξ_X.)
- W66: D-cap five-leaf decider ALL BLOCKED; C0 gets partial definition-level entrant; 6th tallness bind; A-esc window never reached.
- W67: A-esc decomposed (lem-aesc-* ×5, registry 192). Starvation engine prices SYNTHETIC finance rows (hull-near missing actor forces Tail₁(u)>τ/8, rank/slab-free). A-esc = HES + DTR. (4th defect: affine vs linear in SEP.)
- W68: conj-l5-gap-1 REGISTERED; lem-intersection-branch-production proved; lem-huddle-charge-assembly → proved-conditional on exactly {SL1a, SL1b, L6.5, conj-l5-gap-1}. Registry 194.
- W69: Route A chosen (named H-X: X2/X3F/X3N/X4). DTR growing-rank decider PARTIAL: ranks 4..32 realize local DTR geometry at exactly zero finance negativity (local D_EC=−7/64<0 threat is real) but every global gate fails by exact rank-uniform margins (R0 ownership excess 1/8, H/τ=0, shallow mass 1, empty ultra ω). DTR proof must price ROOT-TO-TOP SYNCHRONIZATION, not local negativity. DTR reduced (raw) to POTI.
- W70: DTR→POTI reduction VERIFIED 4/4 VALID zero corrections (cleanest of campaign). Registry 200. Diagnostics ordered D_leaf ≥ D_EC ≥ D_POTI/S.
- W71: POTI-0 zero-overlap decider BLOCKED; exact trade-off law max_i ν(P_i)=β·a (ownership β≥1/8 vs negativity β≤τ²/a, incompatible at every rank/τ); 7th consecutive tallness bind.
- W72 (UNVERIFIED): POTI-0 = S0 (exact cause split Z:{r=0} vs O:{r>0, t_φ(u)≤D₀δ}) + RX (zero-overlap exchange ledger σ_B ≥ w_*·M_B − e_δ, e_δ=2δ(1+δ), M_B>1/160, via one foldback application) + O48 (fixed-level starvation ledger on V₄₈={z<48τ}: H₄₈>τ/16) + ASM2 (assembly: Z_v(q_A) > (7c_m/960)τ) + creative residuals RDSE + LDHR-48. KEY NEGATIVE: W71 law is family-specific; whole-class escape is selected-root dilution w_*→0, unbounded below on the pinned interface. Routine batch proved standalone, hostile verifier INTERRUPTED — nothing codified.

## Current front detail
- COV: ρ(Q):=min{m_A(Q), η_D*(B∩Q)}. Surplus G_φ := Σ_{u∈B} ρ(u)·[t_φ(u) − D₀δ]_+, D₀=2+4δ.
- POTI-R (proved): S·Z_v(q_A) ≥ G_φ, where Z_v(q_A):=sup_{y∈Y_v} y·(p_v−q_A).
- D_POTI := G_φ − (S/8)P_v⁺(E_*) + (c_m S/16)P_v⁺(L_v); if ≥0 routine close (EC) holds; else POTI-0 (G_φ=0) vs POTI+ (0<G_φ<gap).
- RDSE (OPEN, creative): every pinned datum with r=0 + forced RX package (C_B,w_*,M_B,σ_B) obeys (RAY-EC): R_A(Λ,c) ≥ (1/8)P_v⁺(E_*) − (c_m/16)P_v⁺(L_v). Exact escape: w_*→0 with genuine rank while σ_B is mass outside m_A, not obviously convertible.
- LDHR-48 (OPEN, creative): every pinned datum in O + forced O48 package obeys (RAY-EC). Likely death: rotating near-top huddle — carrier tail lands in V₄₈ while the needed top-face direction varies/cancels. Adverse: W54 simplex-minimax obstruction, W53 top-deficit blind spot.
- ASM2 close reuses the fixed T/E spend: B4.2 once at p_{f*} (P_v⁺(E_*) ≥ τS/8 ≥ c_mτ/8), B4.1 last (P_v⁺(L_v) < 2τ/15) → (15−8)/960·c_mτ = 7c_mτ/960.

## Meta-pattern
1. Single dominant wall = tallness, hit independently ≥7 times. No decider has ever entered the tall class with the needed pathology. (Non-entry never treated as theorem — dead-route pattern F22/K12.)
2. LOCAL ledgers are repeatedly free; the wall appears exactly when local financing must become globally/rank-uniformly synchronized. Whatever quantity a decider bounds away from zero within one family turns out to be driveable to zero across the whole class (w_* dilution). THE load-bearing open pattern: turning a per-instance ledger into a class-wide bound is where every attack stalls.
3. Hostile verification catches real defects at a steady rate (4–5 across 18 waves), always upstream of codification.
4. Accreting engine bank, never re-derived: l5-* ray/foldback machinery (W62), dcap B4 T/E spend (W65), ihorn selected-corner extraction (W63, supplies w_*>0), aesc tail amplification τ/8 floor (W67), top-deficit-price. RDSE and LDHR-48 are the FIRST places the accreted bank is insufficient as-is — both need a genuinely new mechanism (root/top synchronization theorem, or anti-cancellation device for a rotating low-deficit huddle).
5. 18 waves of adversarial search never found a counterexample (always BLOCKED, never REFUTED).
