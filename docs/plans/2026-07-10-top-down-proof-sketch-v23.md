<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 23 (W59 obstruction-lemma delta).
  Supersedes v22; everything not restated here is unchanged from v22.
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing.
-->

# Top-down proof sketch v23: op-classical (2026-07-10, W59 obstruction-lemma delta)

## UNCHANGED from v22

Everything: 28 af-validated, the three-cell SL1a surface, SL1b, L6.5 + the W55 map,
the L5 minimax, all dead routes, the W57/W58 exact-infeasibility record.

## W59 map change: the completion obstruction is PROVED (L5), K-free

Registry 150 -> 151: **`lem-starvation-completion-obstruction`** (status: proved, L5;
fresh hostile verdict VALID-WITH-CORRECTIONS, the single notation correction applied).
STRONGER than the W58 candidate: K-FREE (any finite number of exterior zero-top
support fibers in the canonical slab 0 <= y <= 1), universal ceiling tau <= 1/256,
first-principles proof independent of the LP computation. Mechanism: exact idempotence
at the pinned display demands one unit of transverse moment; the actor hull and the
aggregated exterior budgets supply only O(tau).

This is the FIRST PROVED MECHANISM on the H-X / large-gauge front. The whole W55
starvation-gadget threat (which survived every scalar ledger) is now excluded at rank
three with slab-confined exteriors — by paper proof, not just certificates.

## What must generalize (the honest gap to H-X / L6.5)

The lemma's hypothesis class is the pinned gadget display. To close
`conj-sl1a-off-diagonal-cell` (or the L6.5 large-gauge wall) the same
moment-vs-budget mechanism must be run: (i) without the slab confinement
0 <= y_Q <= 1 (or with it derived from the H-X hypotheses); (ii) at general rank >= 3;
(iii) at the H-X cell's actual tableau (majority-horn off-diagonal freight > 1/8)
rather than the gadget constants. Each is a sharply-scoped extension question.

## Revised Tier-1 order

0. **Generalization wave on the obstruction mechanism** toward H-X: formulate the
   moment-vs-budget ledger at the H-X tableau; decide which of (i)-(iii) is the
   binding gap (strategy pass first, then prover). ALSO: af-elevate
   `lem-starvation-completion-obstruction` (single minimal contract, prime shape).
1-5. Unchanged (E1-E5 codification; small-gauge bridge; H-D/H-I; SL1b; L5 minimax).
