<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 16 (session 12 close) — THE
  CANONICAL STRATEGIC MAP. Supersedes v15 (kept for line citations) after W52 (45256cf).
  DELTA redraw: everything not restated here is UNCHANGED from v15.
STEWARDSHIP CONTRACT (binding): as in v10-v15. STATUS DISCIPLINE (L0): a SKETCH — promotes nothing.
-->

# Top-down proof sketch v16: op-classical (2026-07-07, session 12 close)

## UNCHANGED from v15

Theorem; trunk; the endgame stack's lower half; the pivot (ONE face — the in-class
intersection horn of conj-zero-face-elimination — merged by prop-f2-t1-equivalence);
28 af-validated; registry 119; the closed-by-certificate list; USER DECISIONS aism-nlg,
aism-z98; parked Tier-2/3 farm-out queue.

## THE MODE CHOICE, DATA-INFORMED (W52): (M2) TALL-EMPTINESS IS FAVOURED

The pre-registered tall-entry experiment is BLOCKED: 4 exact families + a 1296-record
exact grid produced ZERO tall entries and ZERO nonempty width-4 clusters; best frontier
H^2/delta ~ 0.228 vs threshold 16 (a ~70x gap). THREE binding constraints are named —
the scaffolding of an emptiness proof:
  (B1) delta inflation (pushing height inflates negative mass faster than H^2);
  (B2) forced visibility (the deep append row turns visible before tallness);
  (B3) top reversion (the top returns to base rows and the hulls INTERSECT).
**(M2) target statement (rank-free):** no exact signed idempotent with delta <= delta_0 has
a hidden top v with H > a*tau carrying the heavy width-4 top-slab cluster — equivalently,
exclude the HUDDLE (lem-zero-face-vertex-support: two mutually-exempt deep hidden vertices
within 4*tau). This IS the Kernel height clause. Attack: turn (B1)-(B3) into lemmas; the
huddle anatomy + exchange identity + co-top forcing (lem-rank3-supporting-functional-pinning
clause) + the L0 collapse/pincer/concentration engine are the tools.

**(M1) fallback (rank-3): [[conj-rank3-cluster-zero-face-reach]]** via the anatomy chain —
unchanged; its quantified domain is exactly what W52 could not populate.

## THE OPEN LEDGER v16 (session close)

1. The in-class intersection horn — (M2) tall-emptiness favoured (three named constraints
   to lemma-ize) / (M1) the reach inequality (rank-3 fallback). THE only unscoped math.
2. The rank-3 -> general-rank step (VOID if (M2) lands — tall-emptiness is rank-free).
3. Parked Tier-2/3 (farm-out once Tier 1 falls): Kernel(i) rank >= 3, trunk <2>7 factoring
   (aism-ik6), elevation queue (the W44/W47/W49F reviewed families), report labels.
4. USER DECISIONS: aism-nlg (pinned-delta contract re-wordings), aism-z98.

## Next session opening move

W53: the (B1)-(B3) lemma-ization wave — three provers (one per binding constraint, each
"prove the constraint is UNIVERSAL, not family-specific") + the standing hostile-verifier
discipline; decisive because (B1)-(B3) universal + the huddle anatomy = tall-emptiness =
the Kernel height clause, rank-free, closing op-classical modulo the parked Tier-2/3 grind.
