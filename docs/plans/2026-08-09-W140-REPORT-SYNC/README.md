# W140 report sync (2026-08-09) — protocol record

92 af-validated registry results anchored into report/sections/52-72 (21 new shards).
Pipeline: 21 fresh codex authors (high; op-classical flagship xhigh) -> mechanical
validation (byte-verbatim contract check, labels, caps) -> wiring (main.tex, README,
SHARD_CATALOG, PROVENANCE +94 claim rows +184 source rows, UNWIRED -92) -> 4-batch
fresh-codex hostile faithfulness audit (xhigh) -> 26 findings / 10 FIX shards ->
fixer codex applied all 26 verbatim -> fresh-verifier re-audit: 10/10 LAND.
NOTE: the FIRST audit pass was VACUOUS (empty shard-list substitution, caught via
missing per-shard verdicts, 4-minute runtime tell) and was re-run in full — kept
here as the honest record (audit-*.md are the REAL second-pass files).
Stats layer: dedicated xhigh codex analyst rebuilt scripts/gen-report-stats.py into
a retraction-aware post-discharge census (answer-stats.md).
