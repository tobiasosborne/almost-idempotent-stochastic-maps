# BRIEF — aism-fudw decomposition REPAIR (v3): apply the v2 verdict's mechanical corrections

You are a FRESH REPAIR ARCHITECT (codex, independent context — you are none of the
four prior workers; their written artifacts are your only inputs). Design v2 was
hostile-reviewed: `VERDICT: INVALID`, but the reviewer CONFIRMED the overall shape
(row/status counts correct, GAP-EA + F2/F3 honestly blocked, no literal cycles, no
status inflation) and recorded EXACT mechanical corrections for every remaining
defect. Your job is the narrowest possible v3: apply the v2 verdict's corrections
faithfully, change nothing the verdict did not fault, and produce a complete
standalone v3 design.

## Read first

1. `BRIEF-FUDW-DESIGN.md` then `BRIEF-FUDW-REPAIR.md` (all binding rules and hard
   boundaries carry over verbatim: envelope, single minimal contracts, no
   parent-contract changes, faithful transcription, GAPs flagged never filled).
2. `VERDICT-FUDW-DECOMP-V2.md` — your work order. Every finding gets a
   disposition row in v3.
3. `DESIGN-FUDW-DECOMP-v2.md` — the object under repair.
4. `VERDICT-FUDW-DECOMP.md` (v1 verdict) — to ensure no v1 repair regresses.
5. The verified sources (per `BRIEF-FUDW-DESIGN.md` §Read-first items 4–5) for the
   loci the corrections cite — especially wherever COMP-CB's amplified structure,
   almost-containment, and corner-algebra inheritance clauses, the H-CB children's
   closed data, the MAIN-CB refinement/transitivity/disjoint-merging factoring,
   and the C_split producer actually live in the proofs.

## The v2 findings to repair (disposition each; use the verdict's exact correction text where given)

1. COMP-CB: add the amplified-structure, almost-containment, and corner-algebra
   inheritance content the verdict names.
2. H-CB children: give each a closed, non-circular datum (self-contained
   hypothesis package; no reference to an unstated parent datum).
3. MAIN-CB: genuinely factor refinement, transitivity, and disjoint merging into
   their own rows (not relocation).
4. Reset chain: add the producer row for C_split (or, if no verified section
   produces it, a `stated` GAP-flagged producer — per the verdict's disposition).
5. Ledger: fix the η_A overuse, add the degree-two producer, replace the naked
   def_3 usage with a defined term (propose the def shard if needed).

## Output (ONLY these two files; no other repo edits, no git)

1. `DESIGN-FUDW-DECOMP-v3.md` — complete standalone v3, same required sections as
   v2 (disposition table covering BOTH verdicts' findings, proposal table,
   assembly wiring, def-provisioning + Stage-1 external register, phase map, risk
   register).
2. `ANSWER-REPAIR-V3.md` — ≤15-line summary (counts; v2 findings dispositioned
   n/n; remaining GAPs).

Hard boundaries: identical to the prior briefs. Only the two files above; no
registry/proofs/refs edits; no git; transcribe faithfully; never invent
mathematics to close a GAP.
