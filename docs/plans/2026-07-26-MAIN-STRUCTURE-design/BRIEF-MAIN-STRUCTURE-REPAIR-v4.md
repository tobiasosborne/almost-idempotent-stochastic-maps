# BRIEF — MAIN-STRUCTURE fourth repair (prescribed; audit-v3 binding; NARROW)

You are a fresh, independent design mathematician executing a PRESCRIBED
narrow repair. `AUDIT-MAIN-STRUCTURE-v3.md` is BINDING. It REFUTED
`DESIGN-MAIN-STRUCTURE-v3.md` on four fatal closure defects (its §1 A–D)
plus one dependency defect and a set of exact per-row corrections — while
CONFIRMING the architecture: the M16 C_s2 absorption is exact and universal
(conj-extcb at e = C_s2·t, D₂ ≥ C_ext·C_s2, C_s2·e₂ ≤ e_ext), M07/M09 and
their imports, M05/M06/M10/M15/M16/M17/M18, the R19/R21 measures, the G-S1
placement, and dimension-freeness (NO route alarm). Apply the corrections
EXACTLY as the audit states; invent nothing new.

## The prescribed corrections (each stated verbatim in the audit — read it)

1. **Defect A (M19-S1):** replace the "non-unital reset inclusion"
   hypothesis: assume A is an extended ε-C*-algebra and w is an extended
   c₀^cb·ε-INCLUSION (unit clause included), or the equivalent explicit
   unit bound. (The audit's M₄ counterexample shows the non-unital form is
   FALSE at t₁ = 0.) Restore the unit-preserving v2 form for the global
   Stage-1 map.
2. **Defect B (M19-S2/S3, M26, M27):** every one of these rows must
   quantify A as an extended ε-C*-algebra and w as an extended
   c₀^cb·ε-inclusion (the v2 hypotheses that v3 silently weakened). With
   M04 imported, that licenses d_U ≤ c₀^cb·ε_U ≤ c₀^cb·L·ε ≤ K₂ε and the
   Stage-3 analogue.
3. **Defect C (M19-R):** add the hypothesis that the raw call's literal
   output map is an extended d_raw-inclusion (or isomorphism) from the
   named finite-dimensional C*-algebra source into the extended
   ε_R-C*-algebra A_R — or instead assume the full conclusion of an
   M18-admissible call. Then M03 applies and RI follows.
4. **Defect D (M20 + M25):** add the compressed-corner scalar call as a
   DISTINCT literal call type with its own scale t_atom = K_call·ε (or
   another earlier universal multiple dominating L·ε); M20 must explicitly
   prove ε_{j} ≤ t_atom ≤ r_reset; M25 must import M04 directly and use
   t_atom at its atomic base call.
5. **Dependency defect (M11/M12/M13):** M11 must state the original
   ambient defect bound (ε_A ≤ t) explicitly; M12 AND M13 must import M07
   directly (the quantitative nested target-projection export — a dep's
   proof-internal imports are not exported facts) and carry the original
   ambient bound; M13's contract must put P_U^R, P_j^R into a closed EXT
   datum per `definitions/def-extcb-datum.md:13-17`.
6. **Per-row exact fixes from §4:** M03 — copy the landed contract line
   `argument/lemmas/lem-maincb-error-improvement.md:4` BYTE-FOR-BYTE
   (v3 wrote "from finite-dimensional B" instead of "from a
   finite-dimensional C*-algebra B"), changing only deps; M04 — state
   "A is a finite-dimensional extended ε-C*-algebra" inside the contract
   (no reliance on §2 prose); M14 stays conditional on the corrected
   operator-space shard.
7. **P0 schema completion (§2):** for each of the four definition
   proposals supply EXACT shard frontmatter per `definitions/README.md`
   (id, term, kind, status, and for the cited shard source/locus/SHA):
   `def-operator-space` = byte-verbatim Definition block ONLY from
   `approximate_algebras.tex:1453-1464` (source kitaev-2405.02434, SHA
   prefix e7eb512a2ec2438d; the rectangular construction at 1467–1475 goes
   in provenance/notation, NOT as primitive fields); the three original
   shards get schema fields + draft status + consensus-pending note; the
   partition-state shard must state whether it records the global ambient
   defect and w's defect/unit tag (per the audit: if not recorded, every
   consuming row quantifies them explicitly — you are already doing that
   via corrections 1–2); note that M12/M19-S3 need TWO separately supplied
   reset states (no single "current union" field supplying both).
8. Keep the M28 target contract, the M28-only join, M27's complete-family
   hypothesis, G-S1's placement, and every row the audit marked VALID,
   verbatim.

## Read (all binding)

`AUDIT-MAIN-STRUCTURE-v3.md` (binding; §1 defects, §2 P0, §4/§6 per-row
verdicts, §8 retained-contract diff, §9 disposition audit),
`DESIGN-MAIN-STRUCTURE-v3.md` (the base), `AUDIT-MAIN-STRUCTURE-v2.md`,
`DESIGN-MAIN-STRUCTURE-v2.md` (the hypotheses v3 silently weakened —
restore them), the landed shards cited above, `definitions/README.md` and
`definitions/def-extcb-datum.md`, `def-four-corner-merging-datum.md`, and
the TeX loci (1054–1082, 1162–1187, 1239–1359, 1414–1450, 1453–1475,
1508–1557).

## Deliverable — write `docs/plans/2026-07-26-MAIN-STRUCTURE-design/DESIGN-MAIN-STRUCTURE-v4.md`

Full standalone document (same structure as v3): §0 exact delta vs v3
(each change against the audit item forcing it); the schema-complete P0
package; the corrected row table (contracts self-contained; exact deps by
id; provenance; honest budgets); the scale discipline with t_atom added;
hazards; dimension-freeness; the serial landing order (P0 first, G-S1
unchanged); the complete escalation ledger; and a disposition table
covering EVERY finding of `AUDIT-MAIN-STRUCTURE-v3.md` (CLEARED-BY /
unchanged-VALID).

## Hard constraints

Design only; write ONLY inside
`docs/plans/2026-07-26-MAIN-STRUCTURE-design/`; no registry/definitions
mutation; no status promotion; no guessed radii; retained contracts
verbatim; NOT IN LOCAL REFS discipline; an honestly escalated gap beats a
papered-over one.
