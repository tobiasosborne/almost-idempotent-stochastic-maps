# BRIEF — MAIN-STRUCTURE third repair (fresh designer; re-audit binding)

You are a fresh, independent design mathematician. Prior state:

- `DESIGN-MAIN-STRUCTURE-v2.md` was REFUTED by the fresh hostile re-audit
  `AUDIT-MAIN-STRUCTURE-v2.md` — but narrowly. The re-audit CONFIRMED the
  mathematical core: M07's two-sided nested-corner comparison is derivable
  (fixed telescope, its §3), M09 is the genuinely distinct outer/nested
  compression transfer, M10–M13 are genuinely conditional, the R19/R21
  measures are valid, the R22 zero-datum math is cleared by M11→M12, G-S1 is
  correctly placed, and there is NO dimension alarm and NO obstruction to the
  source route.
- Exactly TWO contract-architecture defects killed v2 (its §1):
  **Defect A** — M19 is not a closed acyclic producer (it hides the Stage-2/3
  current block maps in "constructed from w", creating a semantic
  M19→M20→M25/M27→M19 cycle or missing hypotheses; and M13's output scale
  C_s2·t is never absorbed: M16 accepts master error t, M20 checks only
  K_call·ε ≤ r_reset, not C_s2·K_call·ε ≤ r_reset).
  **Defect B** — the definition-provisioning gate was silently dropped
  (`def-operator-space`, `def-maincb-reset-state`, `def-maincb-raw-call`,
  and the partition/current-union state are used as naked prose notions in
  M15–M20; none is landed; the landing order and escalation ledger omit
  them).

`AUDIT-MAIN-STRUCTURE-v2.md` is BINDING. Its §10 lists eight exact
requirements for this third repair; treat them as your specification:

1. put the datum/notation provisioning + user-ratification gate BEFORE M01;
2. replace M19 by closed, call-type-specific conditional envelopes that name
   the current block maps and their reset invariant, without asserting their
   later existence;
3. separate the base geometric scale from the post-M13 EXT-datum scale, then
   EITHER make M16 accept defect ≤ C_s2·t and absorb C_s2 into D_2, e_2, OR
   put the enlarged Stage-2 master error explicitly into M20's minimum;
4. prove (state as a row with a proof plan) the stronger reset invariant the
   M25 induction actually uses — e.g. defect controlled by the current
   corner's ambient defect — instead of hiding it in "constructed from w";
5. add exact direct deps to M05/M07/M09 (the amplified compression,
   compression identities, almost-containment, and rectangular-product rows
   BY ID — no category phrases like "landed compression rows");
6. retain G-S1 exactly where it is;
7. retain M27's complete-family hypothesis and the M28-only join;
8. add the missing R22 subsection and the definition/dependency items to the
   escalation ledger (including the future `lem-thmainext-conditional` deps
   rewire, which the v2 landing order mentions but its ledger omits).

Also apply the re-audit's per-row VALID-WITH-CORRECTIONS items (its §4):
M01/M02 definition gate; M03 dependency rewire to M02 (contract stays
verbatim); M05/M07/M09 exact deps; M11/M12 canonical partition/raw-call
data; M13's C_s2 threading; M15's explicit hypotheses; M18's common-t
caveat; rebuild M20/M21/M25/M26/M28's domains from the corrected ledger.

## What NOT to redo

Do NOT redesign M07–M13, the two-induction architecture (M25 one-class
measure |C|−r; M27 cross-class measure q−r; M28-only join), the R19 measure,
or the G-S1 gate — the re-audit confirmed all of them. Change ONLY what
defects A and B and the §4/§10 corrections require. Keep every row you
retain verbatim unless a named correction touches it, and mark each change
against the audit item that forced it.

## Read (all binding)

`AUDIT-MAIN-STRUCTURE-v2.md` (the binding re-audit), `BRIEF-MAIN-STRUCTURE.md`
(original constraints), `BRIEF-MAIN-STRUCTURE-REPAIR.md`,
`DESIGN-MAIN-STRUCTURE-v2.md` (the base you are repairing),
`AUDIT-MAIN-STRUCTURE.md` (first audit), the landed shards both audits cite
(esp. `lem-maincb-error-improvement.md`, `lem-extcb-four-corner-merge.md`,
`conj-extcb.md`, `lem-compcb-*` rows, `lem-thmainext-conditional.md`), the
v4.1 register (`DESIGN-FUDW-DECOMP-v4.1.md` §2.4, §3.3, §4.1 incl. the
datum-only definition specs at lines ~403–428, R17–R23, R36), and
`refs/kitaev-2405.02434/approximate_algebras.tex` (esp. 1054–1082,
1162–1187, 1239–1359, 1414–1450, 1508–1557). Cite exact loci for every
claim.

## Deliverable — write `docs/plans/2026-07-26-MAIN-STRUCTURE-design/DESIGN-MAIN-STRUCTURE-v3.md`

Same discipline as v2, plus:

- a §0 verdict stating exactly what changed vs v2 and which audit item
  forced each change;
- a definition-provisioning section specifying the four datum-only
  definition shards (fields only, NO existence/estimate content — R35),
  placed as step 0 of the landing order, ESCALATED for user ratification;
- the corrected M19-replacement rows and the C_s2 scale resolution (state
  which of the two §10-item-3 options you take and why);
- per-row one-line contracts (self-contained), exact deps by id (acyclic;
  existing shards or earlier rows only), provenance loci, node budgets
  (≤12 / depth ≤3), per-row feasibility verdicts;
- dimension-freeness audit (the C_s2 absorption must stay universal);
- a serial landing order that starts with the provisioning/ratification
  gate and is a genuine topological sort;
- a COMPLETE escalation ledger (the re-audit's §9 list is the floor);
- a disposition table covering EVERY finding of `AUDIT-MAIN-STRUCTURE-v2.md`
  (CLEARED-BY / REFUTED-WITH-LOCUS / ESCALATED).

## Hard constraints

Design only; write ONLY inside `docs/plans/2026-07-26-MAIN-STRUCTURE-design/`;
no registry or definitions/ mutation; no status promotion; no guessed radii
or thresholds; NOT IN LOCAL REFS discipline; an honestly escalated gap beats
a papered-over one.
