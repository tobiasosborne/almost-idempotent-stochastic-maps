# BRIEF — MAIN-STRUCTURE repair round (fresh designer; audit binding)

You are a fresh, independent design mathematician. Prior state:
- `DESIGN-MAIN-STRUCTURE.md` correctly refuted the v4.1 MAIN factoring
  (all five defect claims CONFIRMED by the hostile audit) but proposed a
  repair the audit REFUTED.
- `AUDIT-MAIN-STRUCTURE.md` is BINDING: its four repair-failures (§1 items
  1–4, elaborated in its later sections with exact loci) must each be cleared
  or refuted with exact loci.

Your job: the second repair design — an ACYCLIC, well-founded factoring of
the MAIN-CB structural argument (Kitaev TeX ~1180–1450, esp. 1325–1359 the
merging lemma and its direct-sum corollary, 1414–1450 the main induction)
into registry-shape rows, clearing all four audit failures:

1. **Well-founded thresholds:** every constant a row imports must be produced
   by an EARLIER row of the plan or an already-landed shard. No forward
   imports, no cycles. If the reset-constant package must land first, place
   it explicitly in the landing order with its complete dependency list
   (coordinate with the W77 finding: `lem-maincb-reset-constant-ledger` is
   NOT landed and the v4.1 version misses the ratified IMPROVE-CB
   ε_max^cb guard — your plan must produce the corrected package as a row).
2. **Close-corner transport must be proved, not assumed:** design the row so
   its proof obligation (the original-corner vs compressed-ambient-corner
   comparison) is explicit, with the exact source mechanism or an elementary
   derivation plan; if the needed comparison is not derivable from local
   sources, mark GAP and stop on that point.
3. **Correct compression instantiation:** distinguish the ideal-unit-image
   compression in the validated `lem-compcb-single-compression-transfer`
   from the outer compression Stage 2/3 actually use; if a new producer row
   is needed for the outer form, design it.
4. **Complete induction data:** the recombination induction's initial data
   must include the conditional family of one-class maps; state the
   induction's well-founded measure and its data flow explicitly (R19/R21
   discipline).

Read: `BRIEF-MAIN-STRUCTURE.md` (original constraints bind),
`DESIGN-MAIN-STRUCTURE.md`, `AUDIT-MAIN-STRUCTURE.md`, the landed shards both
cite, and the TeX loci. The audit's SHA256 pin of the TeX is recorded; cite
exact loci for every claim.

## Deliverable — write `docs/plans/2026-07-26-MAIN-STRUCTURE-design/DESIGN-MAIN-STRUCTURE-v2.md`

Same discipline as before: per-row one-line contracts (self-contained, no
free witnesses — inline formulas or witness-package rows), deps (acyclic,
existing shards or earlier rows only), provenance loci, node budgets
(≤12 / depth ≤3), per-row feasibility verdicts, dimension-freeness audit,
the corrected reset-constant package as an explicit row, a serial landing
order, and a "disposition of audit findings" table (CLEARED-BY / REFUTED /
ESCALATED per finding). All contract corrections remain ESCALATED for user
ratification.

## Hard constraints

Design only; write ONLY inside `docs/plans/2026-07-26-MAIN-STRUCTURE-design/`;
no registry mutation; no status promotion; no guessed radii or thresholds;
NOT IN LOCAL REFS discipline; an honestly escalated gap beats a papered-over
one.
