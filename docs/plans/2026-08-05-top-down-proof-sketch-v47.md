# Top-down proof sketch v47: op-classical (2026-08-05, session 43 — the ledger family RE-SCOPED onto a formation backbone and SEVEN rows banked: T0 169 → 176; the rows 5–14 status cap DISSOLVED)

## UNCHANGED from v46

`op-classical` is OPEN. The overall Route-F architecture (F0 seam → MAIN →
ledger → K-ledger → F2/F3/PRH finish), the sharp-exponent ground truth
(`ex-hume`), the dead routes (FINDINGS Rule 13), and the root-rewire-LAST
discipline all stand. The strengthened `lem-routef-k-ledger` replacement and
the F0-assembly landing remain the next structural moves; the
DO-NOT-REWIRE guard on the parent stays on.

## Map change 1: the af verifiers refuted the ledger contracts AS WRITTEN — and the repair produced a new backbone

First elevations of rows 1/3 validated the *mathematics* node-by-node but
correctly blocked both roots: all 16 landed contracts used the tilde-map
vocabulary and scalar ledger without binding the ambient finite-dimensional
UCP/cb setting (it lived only in the design preamble); `rho_AI := eta_A` was
stated nowhere; `def-almost-idempotent` (row-stochastic picture) could not
supply the UCP context. Repair per the twice-audited, USER-RATIFIED
LEDGER-SETTING-RESCOPE v2 package (v1 was DESIGN-REJECTED by its hostile
audit for definition-as-theorem laundering — the deletion test works):

- **`def-routef-raw-factor-setting`** — theorem-free witness-package
  definition (data/typing/notation only; the `(1.1)`–`(1.8)` scalar ledger is
  derived notation of the header `W_RF`). Locked on user sign-off; one
  verifier-caught type fix (`I_B` = identity map, not unit) user-ratified.
- **`lem-routef-raw-factor-setting-formation`** — NEW row (registry 365) and
  now the family's existence backbone: `exists ONE global W_RF, then for
  every admissible (H, Phi, eta) on rho_id^corr, exists a datum S with B, v,
  u = v^(-1)`. Deps: Kitaev audit + AI linearization + thmainext.
- All 16 rows re-scoped by prefix-only binding (15 suffixes byte-identical;
  row 14 revised to a purely scalar threshold interface — its F2/F3/PRH
  application moved to the future strengthened K-ledger); defs/deps repaired;
  the retained generator reproduces the landed state; independent
  transcription audit TRANSCRIPTION-CONFIRMED after 5 exact corrections.

## Map change 2: seven af banks (T0 169 → 176)

| # | row | tree | note |
|---|---|---|---|
| 170 | `lem-routef-raw-factor-setting-formation` | 10/10 first pass | exactly on budget; global-header-outside-quantifiers explicitly verified |
| 171 | `lem-routef-raw-factor-norms` | 23/23 | verifier refuted a strict `dim(A) < d^2` overclaim (counterexample `Phi=I, eta=0`); repaired to `<=` |
| 172 | `lem-routef-raw-factor-identities` | 11/11 first pass | clean re-seed after the I_B fix |
| 173 | `lem-routef-raw-product-estimate` | 6/6 | product-defect clause grounded in byte-verbatim `GT-kitaev-def-delta-homomorphism` (tex:443–456) |
| 174 | `lem-routef-raw-factor-units` | 8/8 | verifier unit-clause obstruction dissolved by the same GT external (`hom_unit`: `||v(I)-I|| <= delta` is definitional) |
| 175 | `lem-kitaev-diagonal-repair` | 20/20 | BOTH clauses: printed formula refuted against PROVENANCE-ONLY externals (tex:1254, 2780–2783) + phase-balanced diagonal with exact projective norm 1 |
| 176 | `cor-kitaev-diagonal-cpization` | 22/22 | centrality-only CP without multiplicativity; `q_t >= 0` dependence made explicit; ratified `Phi:B(H)->B(H)` typing scope |

Every bank: fresh codex prover, separate fresh verifier per node, oracle
registered, external `fr verify` PASS, mechanical flip. Honest scope: rows
170–174 re-establish the *designed* ledger mathematics at T0; 175–176
re-establish inherited proved-mod-audit rows (first Kitaev-paper material of
the campaign to clear af with a refutation clause).

## Map change 3: the rows 5–14 status cap is GONE

With the Kitaev pair T0, no non-rigorous dep remains under any of rows 5–14.
The ENTIRE remaining LEDGER-DOMAINS queue (rows 5, 6, D2, 7, D3, 8–13 and the
revised scalar row 14) is elevatable, in the design's §D order, under the
re-scoped contracts. Budget guidance from this session: fresh builds run
1.5–3x the pre-rescope projections (formation-instantiation scaffolding);
pre-amend caps accordingly and flag (ceiling 26).

## Map change 4: what the verifiers taught us (new obstruction instances)

1. *Ambient-binding under-specification* — a contract family can pass design
   audits yet be undischargeable because its quantification context lives in
   prose outside the contracts. Remedy now worked end-to-end: theorem-free
   setting definition + formation row (the witness-package/producer split,
   third instance in the repo).
2. *Interface projections hide load-bearing clauses* — thmainext deliberately
   hid M28's unit estimate; row 2 needed it; the Kitaev `hom_unit` clause
   supplied it definitionally. When an interface row hides a clause, check
   which consumers still need it before routing everything through the
   interface.
3. *Worker-discipline drift* — the Kitaev-pair auditor committed and pushed
   its own audit file (content verified benign); a design-round codex may
   read the repo but must not write outside its one deliverable. Watch this.

## The open surface after this delta

1. **Ledger rows 5–14** (bead `aism-3fjg`): serial elevation, all deps T0.
2. **F0-assembly landing** + the strengthened `lem-routef-k-ledger`
   replacement (W78 §5 step 6; releases the DO-NOT-REWIRE guard). Note the
   rescope audit: the strengthened parent must add the formation row and
   rows 5/6/8/9 as direct deps (packet construction), budget ~17/4/cap 22.
3. **Root rewire LAST** — unchanged.
4. Report sync `aism-9kmt` (now larger: 7 new banks + formation id).

## Controller note

All work committed and pushed at T0 176; wave W136 logged bank-by-bank on arm
FH (7 banked pulls + design/audit progress pulls). The `fr` FRONTIER text
should advance to: ledger rows 5–14 under the re-scoped family, then
F0-assembly + strengthened K-ledger, then the root rewire.
