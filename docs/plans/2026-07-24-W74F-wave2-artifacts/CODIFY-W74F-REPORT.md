STATUS: UNAUDITED TRANSCRIPTION

# W74F wave-1 registry codification report

This report records a faithful transcription of the four hostile-verified
W74F wave-1 artifacts.  It does not prove or verify any mathematical claim.

## Files created or edited

Created:

- `definitions/def-positive-approximate-retract.md`
- `definitions/def-extended-epsilon-cstar-algebra.md`
- `definitions/def-ha-map.md`
- `definitions/def-fd-cstar-diagonal.md`
- `argument/lemmas/lem-prh.md`
- `argument/lemmas/lem-prh-sharpness.md`
- `argument/lemmas/lem-kitaev-diagonal-repair.md`
- `argument/lemmas/cor-kitaev-diagonal-cpization.md`
- `argument/lemmas/conj-hcb.md`
- `argument/lemmas/conj-extcb.md`
- `argument/lemmas/lem-thmainext-conditional.md`
- `argument/lemmas/lem-kitaev-almost-idemp-audit.md`
- `docs/plans/2026-07-24-W74F-wave2-artifacts/CODIFY-W74F-REPORT.md`

Edited or regenerated:

- `definitions/INDEX.md` — regenerated by `check-defs.py`.
- `argument/INDEX.md` — regenerated by `argument.py`.
- `argument/DAG.md` — regenerated by `argument.py`.
- `report/sections/13_discussion.tex` — eight non-rigorous status-ledger
  anchors; shard header preserved; 68 lines after the edit.
- `report/main.pdf` — rebuilt by the required `cd report && make`.

`report/UNWIRED.md` was not changed because every new result was anchored once
in the status ledger.

## Registry shards

- `lem-prh` — `proved-mod-audit` — Positive-retract hardening: positive
  unital \(A,M\) with
  \(\lVert MA-I_k\rVert_{\infty\to\infty}\le\varepsilon<1/2\) admit a
  stochastic idempotent \(E\) with
  \(\lVert AM-E\rVert_{\infty\to\infty}\le2\sqrt{2\varepsilon}\).
- `lem-prh-sharpness` — `proved-mod-audit` — For every
  \(0<\lambda<1/2\), the W74F-A family has
  \(\varepsilon_\lambda=2\lambda^2\downarrow0\) and every stochastic
  idempotent lies at distance at least
  \(\lambda=\sqrt{\varepsilon_\lambda/2}\) from \(AM\).
- `lem-kitaev-diagonal-repair` — `proved-mod-audit` — Kitaev's printed
  direct-sum formula is noncentral, while finite phase balancing gives an
  exact diagonal of every finite-dimensional \(C^*\)-algebra with
  projective norm and coefficient sum exactly one, independently of block
  data.
- `cor-kitaev-diagonal-cpization` — `proved-mod-audit` — Averaging an
  involution-preserving \(\widetilde\Delta\) against the repaired exact
  diagonal and a UCP \(\Phi\) is completely positive at every level;
  exact multiplicativity of \(\widetilde\Delta\) is not used.
- `conj-hcb` — `conjecture` — Universal \(C_H,e_H\) give the adjoint,
  product, unit, norm, inverse, homomorphism, and identity-closeness
  estimates for every \(1_{M_n}\otimes\mathrm{Ha}^{Q}_{P,R}\) under
  COL-HILB, uniformly in \(n\) and all dimension/block data.
- `conj-extcb` — `conjecture` — Universal \(C_{\rm ext},e_{\rm ext}\)
  give the amplified extension lemma through one map, the same level-one
  unitary, and the same four corner maps at every amplification, uniformly
  in \(r,n,\dim\mathcal A\).
- `lem-thmainext-conditional` — `proved-mod-audit` — Assuming
  `conj-hcb` and `conj-extcb`, `th_main_ext` has full amplified universal
  strength; the corrected squared COL-HILB estimate holds and the
  downstream constants have the conditional \(K\)/\(\eta_K\) ledger shape
  of W74F-C §§4–5.
- `lem-kitaev-almost-idemp-audit` — `proved-mod-audit` — After the
  four operator-domain fixes and \(V_{1+k}\) index fix, both associativity
  identities hold at every amplification with error
  \(10\eta\lVert X\rVert\lVert Y\rVert\lVert Z\rVert\), and the explicit
  \(\varepsilon_{\rm AI}(\eta)=O(\eta)\) extended interface is
  dimension-free.

Every shard has `af: none`.  Both conjecture shards have empty `deps`.
`lem-thmainext-conditional` names `conj-hcb` and `conj-extcb` in both its
contract and dependency list.

## RATIFICATION NEEDED

All new definitions are deliberately `status: draft`:

- `def-positive-approximate-retract`
- `def-extended-epsilon-cstar-algebra`
- `def-ha-map`
- `def-fd-cstar-diagonal`

## Gate outputs

Final prescribed sequence:

```text
check-defs: 23 shards, 0 errors, 14 warnings
wrote definitions/INDEX.md (23 terms)
argument: 208 results, 32 ready, 96 blocked, 0 errors, 6 warnings
check-refs: 23 externals, 0 failed, 23 skipped
check-provenance: 208 registry results, 62 claim rows, 110 tex labels — 0 errors, 147 warnings
[check-all] OK
```

The 14 definition warnings are draft-status notices, including the four new
definitions as required.  The six argument warnings are pre-existing af-tree
refactor warnings.  Provenance warnings comprise the existing UNWIRED
whitelist, one pre-existing unavailable af external, and the eight expected
missing per-claim rows for these status-only non-rigorous anchors.  No gate
reported an error.

The separately required report build also completed:

```text
Latexmk: All targets (main.pdf) are up-to-date
```

## Defect register

1. **Atomic split from the suggested seven-shard layout.**
   W74F-B's entrywise CP-ization was split into
   `cor-kitaev-diagonal-cpization`, depending on
   `lem-kitaev-diagonal-repair`.  It is a distinct corollary and the
   registry requires one result per shard.  The total is therefore eight
   result shards.
2. **No D split.** Only the hostile-batch-verified corrected
   `th_almost_idemp` core and explicit extended interface were registered.
   Claims outside the audit's scope were excluded rather than added as a
   `stated` shard.  In particular, no claim about `th_main_ext` is carried
   by the D shard.
3. **Conditional dependency judgment.**
   `lem-thmainext-conditional` has the required conjecture dependencies and
   additionally imports `cor-kitaev-diagonal-cpization` and
   `lem-kitaev-almost-idemp-audit`, because its contract includes the
   downstream conditional \(K\)/\(\eta_K\) ledger that consumes those
   verified interfaces.
4. **Definition status.** All four new definitions are `draft`, as
   required.  The three source-defined terms are `cited` with
   byte-verbatim TeX and SHA256 prefix `e7eb512a2ec2438d`; manual
   `grep -F` line checks matched every quoted source line.
   `def-positive-approximate-retract` is `original`, since it is the
   project-introduced packaging requested by the brief.
5. **Contract notation.** One-line frontmatter contracts use ASCII
   spellings such as `l-infinity`, `sqrt`, and `tensor`, following the
   existing flat-YAML pattern; the mathematical quantities and constants
   are unchanged and the bodies retain typeset notation.
6. **Owner field.** The schema requires an `A`/`B` owner.  The transcription
   assigns A to the A/C artifacts and B to the B/D artifacts as an
   editorial default; this field is not a mathematical-status claim.
7. **Report anchoring.** All eight ids were anchored in the non-rigorous
   status ledger rather than whitelisted.  The ledger remains below the
   line cap and the LaTeX build succeeded.  The provenance checker emits
   expected coverage warnings because these status-only anchors have no
   per-claim `PROVENANCE.md` rows; it reports zero errors.
8. **Unaudited transcription.** No mathematical correction or independent
   verification was attempted.  Fidelity remains for the orchestrator's
   line-by-line audit, so this report deliberately retains
   `STATUS: UNAUDITED TRANSCRIPTION`.
