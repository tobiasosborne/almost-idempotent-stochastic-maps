<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 37 (session 33
  late delta: the repo's FIRST RETRACTION of af-validated results — T0
  107 -> 105; the 13e repair campaign widened to an explicit-binder
  re-derivation of the inversion-derivative pair). Supersedes v36;
  everything not restated here is unchanged from v36.
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing.
  op-classical remains OPEN. T0 = 105; nothing below is af-validated unless
  said so.
-->

# Top-down proof sketch v37: op-classical (2026-07-28, session 33 late delta — first retraction: inversion-derivative pair demoted, T0 105; 13e repair scope widened)

## UNCHANGED from v36

Everything except the items below; in particular the Route-F chain
through F3, polar analytic rows 1–7 and 9–12, transports 13a–d and 13f,
the report catch-up state, and the ratified serial order.

## Map change 1: FIRST RETRACTION — `lem-stage1-inversion-derivative-control` (was 97th) and `lem-stage1-inversion-derivative-transport` (was 107th) are DEMOTED to stated/seeded (T0 107 → 105)

The W97 design-audit chain caught a defect the elevating verifier cohorts
had accepted: both proof bodies identify an anaphorically-bound polar
inverse (the group-laws u_grp; the parent's u_I) with the explicitly
typed `lem-stage1-polar-retraction` inverse WITHOUT a typed preimage
witness h_X — the same missing-h_X obstruction three W93 cohorts had
(correctly) established for transport 13e. Chain of custody: hostile
audit of `DESIGN-13E-BINDER.md` raised it (finding 2, MAJOR); an
independent fresh adjudication primed to refute it CONFIRMED it per-locus
(T1 export node 1.3; T2 nodes 1.3-second-half/1.5.5/1.6) and bounded the
cascade to exactly these two (linker-verified: T2 was T1's only validated
dependent). Full record: `docs/LEARNINGS.md` 2026-07-28;
`docs/plans/2026-07-28-13E-BINDER-design/ADJUDICATION-T0-ALLEGATION.md`;
bead `aism-e1qs`. Both CONTRACTS stand undisputed as `stated` candidates;
workspaces/ledgers retained as re-elevation bases; report shards 48/51
carry retraction notes (statements demoted to conjecture environments).

**Process law (new, recorded in LEARNINGS):** per-node verification does
not enforce cross-workspace consistency of "the same map". Every map a
root binds by definite description must have a provider external that
supplies the TYPED WITNESS, not merely a same-named conclusion — the 13g
external-provider lesson, strengthened.

## Map change 2: the 13e repair campaign (W97) now covers the pair

The R1 design (two explicit-binder bridge rows; 13e contract
byte-unchanged) was REJECTED by its hostile audit for exactly the defect
that forced the retraction, plus the reuse of anaphoric
smooth-unitary-operations downstream. Design round v2 must deliver, on an
explicit-binder spine: the two bridge rows (membership, closeness), an
explicit-binder smooth-operations bridge, and re-derivations of
`lem-stage1-inversion-derivative-control` and its transport; coherence-
naturality drops out of the repaired 13e deps (audit finding 4). Row 13
and rows 14+ remain byte-unchanged consumers per the (audit-corrected)
consumer trace.

## The open surface after this delta

1. **Design round v2** (fresh codex; audit findings 1/3/4 + the
   retraction scope folded in) → hostile audit → land → serial
   elevations: bridges → smooth-operations bridge → control → 13e → 13g.
2. Then row 13 (`lem-stage1-polar-constant-ledger`) and the queue of v36.
3. The `aism-b5hz` user delegation (2026-07-28) covers this campaign; the
   retraction itself is reported to the user with this file.
