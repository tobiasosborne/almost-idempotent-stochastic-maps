<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 27 (W69-W70 delta).
  Supersedes v26; everything not restated here is unchanged from v26 (hence v25/v24).
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing.
-->

# Top-down proof sketch v27: op-classical (2026-07-16, W69-W70 delta — DTR is verified-reduced to POTI)

## UNCHANGED from v26

The trunk (op-classical <= op-exposed-hull <= HLC <= Kernel/(EX)), the
three-cell SL1a surface, all dead routes, H-D, H-I, SL1b, L6.5, E1-E5
codification, the small-gauge bridge, the repaired conditional assembly
bridge and its four-conjecture surface (SL1a cells, SL1b, L6.5,
conj-l5-gap-1). T0 count 34 af-validated. Registry now 200.

## Map change 1: DTR == POTI, verified (W69 attack + W70 pipeline; registry 194 -> 200)

The second W67 creative residual DTR (diffuse-tail ray conversion,
`conj-w67-aesc-diffuse-tail-ray-conversion`) is REDUCED to the named POTI
problem (pinned-deficit oriented-tail-incidence) on a fully verified
interface (fresh routine prover high -> fresh batched hostile verifier
xhigh, 4/4 VALID, ZERO corrections — the cleanest batch verdict of the
campaign; `docs/waves/2026-07-16-W70-artifacts/`):

- **`lem-dtr-canonical-overlap` (proved L5):** the canonical root/top
  overlap rho = min{m_A, eta_D*|_B} — atomwise on the full row-point
  quotient, clone-invariant, dominated by both parents. The overlap is the
  new geometric object: it measures root-to-top synchronization, exactly
  what the W69 growing-rank decider showed is unpriced by the local ledger.
- **`lem-dtr-oriented-tail-ray-conversion` (POTI-R, proved L5):** S*Z_v(q_A)
  >= G_phi — the entire pinned-direction oriented tail surplus reaches the
  top ray through exact row reproduction + mass-barycenter dualization at
  the pinned y_phi + an arbitrary attained ray certificate. Verifier
  confirmed: the deficit bounds 0 <= z <= D_0 hold at EVERY row index
  (lem-top-deficit-price literal scope — no extension), and the dualization
  is literally about the un-normalized m_A.
- **`lem-dtr-tail-coherent-conversion` (TC, proved L5) — the first PROVED
  quantitatively-weakened theorem on the A-esc front:** under a
  tail-coherence hypothesis strictly weaker than actorization (an
  alpha-fraction of r_0-much overlap tail on the level z >= lambda),
  Z_v(q_A) > gamma_coh*tau with gamma_coh = r_0*alpha*lambda/(16*(1+delta_coh))
  and exact recorded loss r_0*alpha*lambda/(16S).
- **`lem-dtr-poti-assembly` (proved L5, conditional):** assuming the two
  registered residuals, every pinned DTR datum obeys exact (EC), then the
  strict 7*c_m*tau/960 close (E once at p_f*, B4.1 last). Conclusion NOT
  consumable unconditionally.
- **The two residuals are REGISTERED (open conjectures, empty deps):**
  `conj-dtr-zero-oriented-surplus-exclusion` (POTI-0: G_phi = 0 => (EC);
  the rho(1) = 0 mutual-singularity regime is the whole danger) and
  `conj-dtr-positive-oriented-surplus-gap-exclusion` (POTI+: positive but
  insufficient surplus => (EC); only the quantified shortfall Delta_POG
  remains). Diagnostics D_POTI, D_EC, D_leaf are proved ORDERED
  (D_leaf >= D_EC >= D_POTI/S), not interchangeable.

**Creative surface shrink:** A-esc == HES + DTR (W67); now DTR == POTI-0 +
POTI+ through a proved conditional chain, and POTI+ is strictly smaller
than DTR (the surplus already reaches the ray; only the gap needs pricing).
The W69 decider's lesson stands as the POTI-0 target: growing rank realizes
the LOCAL geometry at zero finance cost but every GLOBAL gate fails
rank-uniformly — the open question is exactly root-to-top synchronization
(rho(1) = 0 vs > 0), now isolated as a single registered conjecture.

## Map change 2: the H-X route fork is DECIDED — ROUTE A (session 20, absorbed)

aism-ur9 decided ROUTE A (codex named-H-X via X2 microfreight exclusion /
X3F/X3N far-near actor selection / X4 top-tail regularization, all on the
T0 engine bank) under explicit user delegation; Route B (gamma
renegotiation) is the recorded fallback. The issue remains open as the
Route A execution item.

## Tier-1 order (updated)

0. **POTI deciders + creative attacks (the DTR successor surface):** the
   POTI-0 growing-rank decider FIRST (exact G_phi = 0 refuter hunt in the
   rho(1) = 0 regime, full W67 gate + the (4.5) diagnostic table), then the
   POTI-0 synchronization attack (root/top common-ownership) and the POTI+
   z-level-distribution attack; then HES (macroscopic h_u >= tau/32 subcase
   first).
1. **Route A execution (aism-ur9):** the named H-X wave (X2 prove-or-refute
   with the W61 graft fixture as refuter shape; X3F/X3N; X4).
2. **af-elevation queue (aism-88r):** L5:T0 now ~66:34 and MUST not keep
   widening; prime candidates unchanged
   (lem-aesc-synthetic-finance-tail-amplification,
   lem-intersection-branch-production, the D-cap spine) + NEW:
   lem-dtr-oriented-tail-ray-conversion (single-conclusion, engine-adjacent)
   and lem-dtr-canonical-overlap (small, purely measure-theoretic).
3. **The other three bridge conjectures:** SL1b, conj-cotop-web-coupling
   (L6.5), H-D/H-I creative waves.
4. Remaining L5 leaves in decider-informed order (W64 six, then
   T-esc/G<4/C0/N, then X/Sh/W/D, then S/C). Tallness-consumption and
   c_m*tau/64-production lines mandatory in every proof attempt.
5. Parked: aism-l1a, aism-cei, aism-5de, aism-nlg, aism-z98 (user
   decisions), rank>3/unbounded-K gadget LPs.
