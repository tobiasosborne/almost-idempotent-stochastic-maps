<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 22 (W58 extra-vertex delta).
  Supersedes v21; everything not restated here is unchanged from v21.
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing. W57/W58 results
  are L3 (exact-rational computation) — evidence and mechanism-seed, NEVER proof.
-->

# Top-down proof sketch v22: op-classical (2026-07-10, W58 extra-vertex delta)

## UNCHANGED from v21

Everything: registry 150, 28 af-validated, the three-cell SL1a surface, SL1b, L6.5 +
the W55 map, the L5 minimax, all dead routes, the W57 minimal-family kill.

## W58 map change: the extra-vertex escape is dead too (L3, exact); the candidate lemma is now K-parametric

`runs/2026-07-10-w58-starvation-completion-extra-vertex`: all three first-extra-vertex
cases INFEASIBLE (exact Farkas; stability uniform over A0 in [4,6], tau <= 1/256,
Y in [0,1]; independent check OVERALL PASS, orchestrator-reproduced). A column-local
seven-entry multiplier pattern extends the obstruction to EVERY fixed K exterior
zero-top support fibers below a K-dependent ceiling (tau <= min(1/256, 1/(12(K+1)))).

Honest residual at the gadget: unbounded-K (the ceiling degrades with K) and rank > 3.
The refuter pressure on H-X via the literal gadget is now substantially spent; the
CONSTRUCTIVE direction (the paper proof of the obstruction) is the main line.

## Revised Tier-1 order (v21 order, 0a resolved)

0. **Paper-proof wave on the K-parametric completion-obstruction candidate lemma**
   (bd aism-cq2): the column-local multiplier pattern is the guide; a dimension-free
   paper proof must either accept the K-ceiling honestly (fixed-K lemma, still enough
   to anchor the H-X mechanism) or remove it (the strong form). Fresh hostile
   verification, codify, af-elevate.
1-5. Unchanged (E1-E5 codification; small-gauge bridge; H-D/H-I; SL1b; L5 minimax;
   elevation queue). Rank > 3 / unbounded-K gadget LPs are parked unless the paper
   proof stalls.
