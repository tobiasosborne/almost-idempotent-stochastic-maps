<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 19 (W55 strategy delta).
  Supersedes v18; everything not restated here is unchanged from v18.
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing.
-->

# Top-down proof sketch v19: op-classical (2026-07-09, W55 strategy delta)

## UNCHANGED from v18

The theorem, trunk, 28 af-validated results, verified W54 huddle-charge assembly, four
Tier-1 leaves, parked Tier-2/3 queue, and user decisions remain unchanged. Registry remains
140. `conj-cotop-web-coupling`, the Kernel Conjecture, and `op-classical` remain OPEN.

## W55 map change: L6.5 has an exact front end and a global completion wall

The direct lambda-vs-`P_v^+` attack is abandoned. Under failure of L6.5 with target mass
`<2^-10`, exact idempotence forces a **high-return near corner**: more than `15/16` of the
top's positive mass funds near rows that each return more than `3/4` positive mass to a
slightly broader near/top/low-exposer corner. Disjointness makes that corner nontrivial by
planting a second hidden zero-face vertex. E1-E5 passed two hostile reviews but remain
uncodified, non-L0 paper-level results.

For a reduced optimal display with conic gauge `A0=sum a_z`:

1. **Small gauge `A0<=3/32`:** hostile-checked conditional bridge
   `SL1a+SL1b => impossible`; the co-top conditioning constants are `484/223<2.2` and
   `256/223<16/13`.
2. **Moderate gauge `3/32<=A0<=1`, conditionally on SL1b:** new OPEN `mixed co-top
   straddle exclusion` — a
   co-top probability within `2*tau` of the top, universally shadowed, with definite far
   mass and definite near zero-face mass. It is cleaner than L6.5 but not implied by SL1a.
3. **Large gauge `A0>=1`:** the wall is a **global projection-completion obstruction**,
   not a harmonic or separator ledger. The dual conic weights are not transition mass.
   The proof must use `P=L*B`, `B*L=I`, and the all-row negativity budget to show a
   starved completion pays `Omega(tau)` negativity, contradicting `delta=tau^2`. One
   reduced optimal display is fixed before this exhaustive gauge split.

An exact local gadget with `A0=5`, `g=5*tau`, exact top-row reproduction, and zero far
positive inflow satisfies every current scalar ledger. It is not a counterexample until a
full low-negativity tall completion exists. Exact completion LPs are now the decisive
prove/refute instrument.

## New dead-route certificates

- No direct identification or comparison of `lambda*P` with `p_v`.
- No “conic recurrence”: `a_z` are LP multipliers, not `P`-flow.
- No thin/thick split from the single average `g/A0`; recurrence, transverse displacement,
  and vertexhood may occur on different carriers.
- “Some web member is exposed” is ill-typed until nonvertex/pairwise-separation and
  same-carrier vertexization are solved.

## Revised Tier-1 order

1. Codify E1-E5 after a fresh standalone prover/verifier pass.
2. Codify the small-gauge SL1a/SL1b bridge after the same discipline.
3. Run the exact `P=L*B`, `B*L=I` completion/refutation wave on the starvation gadget.
4. Attack mixed co-top straddle jointly with SL1a; SL1b remains the easiest sibling.
5. Formulate the large-gauge completion theorem only from an exact LP dual certificate or
   a first-principles factorization proof. L5 minimax remains independent and open.

Canonical detail: `docs/waves/2026-07-09-W55-cotop-web-coupling-strategy.md`.
