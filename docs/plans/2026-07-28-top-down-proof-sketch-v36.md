<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 36 (sessions
  31 + 32 + 33 delta: T0 85 -> 107; F2/F3 closed, ALL 12 Stage-1 polar
  analytic rows T0, six of seven transports T0; 13e paused on a
  ratified-contract interface defect = the live user decision). Supersedes
  v35; everything not restated here is unchanged from v35 (and, through it,
  v34/v33/v32/v31).
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing.
  op-classical remains OPEN. T0 = 107; nothing below is af-validated unless
  said so.
-->

# Top-down proof sketch v36: op-classical (2026-07-28, sessions 31–33 delta — F2/F3 T0, polar analytic rows complete, transports 6/7, row 13 blocked on the 13e decision)

## UNCHANGED from v35

The signed-geometry trunk (PAUSED); Route X/XE as registered fallback only;
D1–D4 as the ratified envelope; the F0 seam (T0, map change 4 of v35); the
de-risk campaign designs (S1-POLAR-v6 LAND, MAIN-STRUCTURE-v5
REPAIR-CONFIRMED, LEDGER-DOMAINS-v2 LAND-14, F0-ASSEMBLY LAND); the
banking sequence; the LEARNINGS 2026-07-26 partial retraction. Registry
295, defs 45.

## Map change 1 (session 31): F2 + F3 af-VALIDATED — the Route-F row chain is L0-complete through F3 (package §5 step 1 DONE)

The F2 typing repair ran exactly as v35 map change 5 mandated (fresh design
job → fresh hostile check → corrected contract landed VERBATIM → re-seed →
re-elevate): **`lem-routef-f2-positive-unital-compression` af-VALIDATED
(86th)** on the corrected real/complex typing, Φ end exactly on the T0 F0
seam. **`lem-routef-f3-retract-defect` af-VALIDATED (87th)** — no typing
issue, as predicted. With F0 (v35) this closes the stochastic↔Kitaev
interface chain F0→F2→F3 at L0.

## Map change 2 (sessions 31–32): ALL 12 Stage-1 polar analytic rows are T0 (87 → 101)

Per `DESIGN-S1-POLAR-v6.md` §3/§9, elevated serially and banked
(88th–101st): `lem-stage1-rectified-cstar-control`,
`lem-stage1-unitary-graph-control`, `lem-stage1-maurer-cartan-trivialization`,
`lem-stage1-polar-retraction`, `lem-stage1-polar-coherence-naturality`,
`lem-stage1-group-domain-membership`, `lem-stage1-group-closeness`,
`lem-stage1-approximate-group-laws` (session 31);
`lem-stage1-polar-path-admissibility`,
`lem-stage1-inversion-derivative-control` (row 8),
`lem-stage1-smooth-unitary-atlas` (row 9; Lee C.40 byte-matched),
`lem-stage1-smooth-polar-inverse` (row 10; Lee C.34/C.36 byte-matched),
`lem-stage1-smooth-unitary-operations` (row 11),
`lem-stage1-polar-scalar-arithmetic` (row 12) (session 32). Most first-pass
at tier routine; every challenge repaired in-run. The C¹→smooth Kitaev
prose seam is now rigorous in-repo, not just designed.

## Map change 3 (sessions 32–33): transports 13a–g — SIX of SEVEN are T0 (101 → 107); 13e is PAUSED on a ratified-contract interface defect

- **T0:** 13a `lem-stage1-rectified-cstar-transport` (102nd), 13b
  `lem-stage1-unitary-graph-transport` (103rd), 13c
  `lem-stage1-maurer-cartan-transport` (104th), 13d
  `lem-stage1-polar-retraction-transport` (105th), 13f
  `lem-stage1-polar-path-transport` (106th), 13g
  `lem-stage1-inversion-derivative-transport` (107th, session 33).
- **13g needed two runs.** Run 1 ABORTED [STUCK]: the workspace's sole
  external (the parent control lemma) binds u_delta/g_{sJ} as bare
  anaphors, leaving three root premises underivable (E1 the Pi_delta-inverse
  characterization of u_delta; E2 the f_{sJ}/C¹ characterization of g_{sJ};
  E3 sigma globally C¹). All three are carried verbatim by existing T0
  results, so the fix was a 13e-precedent DEPS-ONLY widening (contract
  byte-unchanged): + polar-retraction (E1), unitary-graph-control (E2),
  smooth-unitary-operations (E3) + its three antecedents; re-seed; run 2
  validated 13/13 clean. LESSON (transport seeding): a transport workspace
  must import the providers of every definite description its root binds
  explicitly, not only its direct parent.
- **13e `lem-stage1-approximate-group-laws-transport` is PAUSED — USER
  DECISION `aism-b5hz`, and it BLOCKS row 13.** Three STUCK runs (last with
  prover xhigh) converged on a genuine ratified-contract interface defect:
  the group-laws family binds u_delta by the ELLIPTICAL description "the
  inverse u_delta of the polar map", while the ratified 13e contract binds
  (u_delta, h_delta) EXPLICITLY as the Pi_delta inverse; the identification
  u_grp = u_pol needs a typed polar datum the parent contract never
  exposes — formally underivable from ANY validated externals. Unlike 13g,
  NO existing T0 result supplies the missing datum: the repair requires a
  contract change (options in the bead: (A) anaphoric restatement of 13e's
  binder — deviates from ratified text, needs ratification; (B)
  explicit-binder amendment of the group-laws family + re-elevation of 3
  af-validated contracts; (C) fresh codex design round). Do NOT re-run 13e
  unchanged.

## Map change 4 (sessions 32–33): the report is (nearly) caught up; orchestration-law amendment

Session 32 anchored all 18 then-unanchored T0 results (shards 42–48 +
provenance; UNWIRED −18). Session 33 wave W96 (in flight) anchors the
remaining nine (99th–107th: the smooth-polar upgrades + the transport
family). LAW AMENDMENT (supersedes the v35 wording): the prover-overreach
guard now EXEMPTS `.frontier/` — mid-run fr-log appends are safe;
`definitions/`, `argument/`, `report/`, `scripts/` stay protected. The
rest of the v35 orchestration laws stand (fr/bd writes first, commit,
launch last; commits only in zero-live-run windows).

## The open surface after this delta (the ratified §5 serial order, W80)

1. **`aism-b5hz` — the 13e decision (USER).** Row 13
   (`lem-stage1-polar-constant-ledger`, the 8-way conjunction) consumes ALL
   SEVEN transports and is blocked until 13e closes; the polar queue's
   remaining rows queue behind it.
2. After row 13: **maximal-simplex + the 5 downstream rows**, then polar
   §9 steps 28–29 (three separately-designed trace rows + corrected
   `lem-stage1-extra-fixed-class` — blocked on their own audited designs).
3. **Package §5 step 3 (G-S1 split producers)** once rows 1–13 are T0 —
   still the one remaining critical-path design gap.
4. **MAIN** per `DESIGN-MAIN-STRUCTURE-v5.md` §10.
5. **The ledger 14-row campaign** (decoupled; can interleave).
6. **The strengthened k-ledger** (D4 guard releases here), then
   **f0-assembly**, then the **root rewire LAST** (D1 OR-route wiring).
7. Carried small closers: `aism-j5t9` (Munkres C^r-triangulation def
   external); the parked finite-triangulation tree; the 29-node
   polar-retraction REFACTOR warning (cosmetic).

## Controller note

FH remains the primary arm (236+ pulls); the polar elevation cadence is
~1 result/run at tier routine with the verified banking sequence. The
codex usage limit is the external resource wall; the 13e decision is the
only non-resource blocker on the critical path.
