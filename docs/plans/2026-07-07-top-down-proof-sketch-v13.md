<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 13 (session 12) — THE
  CANONICAL STRATEGIC MAP. Supersedes docs/plans/2026-07-07-top-down-proof-sketch-v12.md
  (kept intact for line citations) after W47 (1bdea5a + 7363884) and the ideation portfolio
  (a7e0a26). v13 is a DELTA redraw: everything not restated here is UNCHANGED from v12.
STEWARDSHIP CONTRACT (binding): as in v10-v12. STATUS DISCIPLINE (L0): a SKETCH — promotes nothing.
-->

# Top-down proof sketch v13: op-classical (2026-07-07, session 12, round 3)

## UNCHANGED from v12

Theorem; trunk; the endgame stack's lower half; Kernel(i) rank >= 3; the W44 conic
reformulation (conj-zero-face-elimination = THE terminal node); elevation ladder (27
af-validated; #28 lem-always-tight-dual-support orchestrating); USER DECISIONS aism-nlg,
aism-z98; <2>7 factoring aism-ik6.

## THE TERMINAL NODE NOW HAS TWO SINGLE-STATEMENT OPEN FACES (W47)

Either face closes [[conj-zero-face-elimination]] (and with it the Kernel height clause via
the rigorous pincer #21 => collapse #17 chain):

- **(F1) Rank-3 interval interlacing** (from [[lem-rank3-optimal-face-interval-reduction]],
  VAX-verified): at rank 3 both always-tight hulls are intervals on the exposer level line;
  force their overlap from the tall/heavy/near-cluster hypotheses. Exact endpoint fixtures
  banked (disjoint ONLY at out-of-class HEIGHT+A).
- **(F2) The blocker lower-mass trigger** ([[conj-downhill-zero-face-lower-mass]], the sole
  open input of the VAY-verified [[lem-blocker-capacity-bridge]]): every disjoint-hull
  separator blocker carries a universal c0 of positive mass in the kappa-high slab — then
  c0*kappa <= nu_z <= delta is a CONTRADICTION for delta < (c0/4)^2. **Scale advantage: any
  universal c0 wins; no constant-fight.** Refutation path: a kappa-high-STARVING blocker at
  an exact tall-heavy instance. Sliding-family repair is certified dead
  ([[obs-zero-face-perturbation-collapse]]).

**Theatre guard (pre-committed, W47 wave doc + the conjecture shard):** if the (F2) decider
wave returns "needs a new mechanism" (not proof, not counterexample), PIVOT the arm.

## New supporting bricks (W47/W48, reviewed or harvested)

[[lem-affine-exposer-row-capacity]] (L0-composition charging tool, VAY-verified);
[[lem-rank3-optimal-face-interval-reduction]] (VAX-verified). Harvested pending joint
verification (W48, ideation bricks): BA cluster return-flow package (uniformity sharp at
L = 1; return-flow slack r(1+d)+d(1+3d)); BB Wedderburn deflation package (rank drop exact,
trace = rank clone-invariant, exposer vectors D-fixed). In flight: AZ (Gamma-calculus
package — the tau^2-native budget), BC (censoring exactness). The ideation portfolio
(docs/plans/2026-07-07-ideation-mechanism-portfolio.md) prices all five mechanisms.

## THE OPEN LEDGER v13

1. (F1) or (F2) — THE terminal node, now two named single statements (was: one conic
   conjecture; before that: prose). The ONLY unscoped mathematics.
2. Kernel(i) rank >= 3 (rides (F1)'s machinery).
3. Trunk <2>7 factoring (aism-ik6) + thm-cluster in-repo re-establishment — priced,
   routine-per-auditor.
4. Elevation queue: #28 in flight; then the W44/W47 lemma family (conic reduction,
   characterization, separator obstruction, affine-exposer capacity), geography last (dep
   chain). W48 bricks join after their joint verifier.
5. USER DECISIONS: aism-nlg (contract re-wordings), aism-z98.
