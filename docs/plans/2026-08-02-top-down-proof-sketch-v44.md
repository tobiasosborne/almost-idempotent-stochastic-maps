# Top-down proof sketch v44: op-classical (2026-08-02, session 41 — THE MAIN CAMPAIGN IS COMPLETE: T0 165 → 168; the M24 gap repaired end-to-end by the audited option-(a) provider; M28 capstone banked; every MAIN row M01–M28 is af-validated T0)

## UNCHANGED from v43

The global architecture (Route F via positive approximate retract), the
complete Stage-1 record, the parked arms, the parallel-af discipline
(worktrees, serial banking, scoped cap amendments, pre-launch
root==ratified-text checks), and the honest headline (`op-classical`
OPEN) all stand.

## Map change 1: the M24 gap repaired end-to-end by process (T0 165 → 167)

The aism-twpa P0 (v43 map change 4) was resolved by the full prescribed
loop in one session — design round → hostile audit → landing → elevation
— with the user's standing in-session ratification (2026-08-02):

- **Design** (fresh codex, xhigh): option **(a)** — ONE new ledger-bound
  provider `lem-maincb-corner-nontriviality`; M24's contract
  byte-UNCHANGED, deps-only amendment. Option (b) (`dim <= 1`) REJECTED:
  the frozen T0 consumers M10/M25/M19-S3/M26/M27 literally require
  one-dimensional atomic images and `def-maincb-partition-state`'s class
  relation loses reflexivity. Option (c) collapses into (a): M22/M23/M18
  are frozen T0. (`DESIGN-M24-NONTRIVIALITY-v2.md`.)
- **Hostile audit** (separate fresh codex): DESIGN-CONFIRMED, zero
  substantive corrections (3 editorial, applied verbatim in v2). Key
  confirmations: M04 (`lem-maincb-direct-corner-envelope`) applies to the
  singleton corner WITHOUT a partition-state or near-unit premise; the
  frozen witness-arithmetic chain gives `W.r_reset < 1/2` so the corner
  unit is nonzero; the paper's unproved tex:1066 sentence ("It is clear
  that S_P = 0 iff …") is EXCLUDED as an external — the provider derives
  nonzeroness from the unit-norm axiom + the ledger inequalities.
- **Provider banked** (T0 166): FIRST-PASS 7/7 clean; one round-0
  challenge (the δ-homomorphism star/product clauses unregistered) cured
  by the byte-verbatim `GT-kitaev-def-delta-homomorphism` external.
- **M24 banked** (T0 167): clean re-seed (contract byte-unchanged, budget
  5/2/9), FIRST-PASS 5/5 clean, ZERO challenges — fixed-W binder →
  provider lower bound → M23 refinement upper bound → integer squeeze.
  Bead `aism-twpa` CLOSED.

## Map change 2: M28 banked — the MAIN campaign is COMPLETE (T0 167 → 168)

`lem-maincb-structural-assembly`, the structural-assembly capstone, is
af-validated (20/20 clean): **every MAIN row M01–M28 is now T0**, i.e.
the universal witnesses `C_struct = W.c0_cb*W.K_call` and
`e_struct = W.epsilon_MAIN` (every finite-dimensional extended
ε-C*-algebra with `ε <= e_struct` admits an extended
`C_struct*ε`-isomorphism from `⊕_C M_{|C|}` with matching unit control)
are backed by a fully validated tree of ~119 T0 ancestors.

Two-run history, both cured by protocol, no contract finding:

- **Run 1 ABORTED [BALLOON]** (20 > cap 13; root never challenged). The
  orchestrator classification traced 4 of 6 challenges to MISSING
  WORKSPACE VOCABULARY (`def-delta-projection`, `def-projection-basis`,
  `def-one-dimensional-delta-projection`, `def-compressed-corner`, the
  GT δ-homomorphism external — none in the shard defs list, so
  provisioning never added them) and 2 to glue that must be dedicated
  dependency-explicit nodes (constructing the MAIN partition state before
  citing M25/M27; the `A_J = A` / `u_{A_J} = Co_R(R) = R` identification
  from full-corner identification + `def-compressed-corner`).
- **Response (first-line, no registry mutation):** vocabulary provisioned
  into the workspace; shard BODY guidance sharpened (explicit
  partition-state node, dedicated identification node, no
  pending-sibling citations); **scoped cap amendment 13 → 20 (flagged,
  ceiling 26)**.
- **Run 2 validated:** built 20 nodes, resolved all in-run challenges —
  notably the cross-unit monotonicity pair (an extended-isomorphism tag
  against the CORNER unit R cannot feed unit-monotonicity toward the
  AMBIENT unit merely via `A_J = A`; cured by an amplification-wise
  direct check) — and completed in a resumed `--phase verify` pass after
  hitting max-rounds while converging.

**Operational lesson (now demonstrated twice, M19-S3 and M28): a shard
defs list sized to the CONTRACT's vocabulary is not sized to the PROOF's
vocabulary — provision the proof-plan vocabulary (per-node import lists
from the design skeleton) at seeding time, not after a balloon.**

## Map change 3: tooling gotcha recorded

`af def-add` does NOT reject duplicate def names (it assigns fresh ids) —
re-provisioning into an existing seed silently pollutes the ledger. The
M24 re-seed hit this and was wiped/re-seeded cleanly; check def
uniqueness in any pre-launch pass that re-runs provisioning.

## The open surface after this delta

- **NEXT: the escalated `lem-thmainext-conditional` rewire** (design
  sect-10 step 15) — its design must consume M28's final unit-controlled
  isomorphism WITHOUT reopening the MAINCB package
  (DESIGN-MAINCB-REPAIR-v2 hand-off clause).
- Then the decoupled campaigns (14-row ledger, k-ledger, f0-assembly)
  and the root rewire LAST — unchanged from v41–v43.
- Carried: `aism-9kmt` report sync (P2; unanchored banks now ~120–177 —
  grew by the session-41 banks), the typeset flags, dormant signed-trunk
  defs, the `aism-wazy` duplicate-contract tripwire suggestion.

## Controller note

fr arm FH carried the session (W125–W130: 1 design wave, 1 hostile-audit
wave, 3 elevation dispatches, 4 banked pulls, 1 balloon classified and
cured in-session). The M24 repair validates the escalation protocol
round-trip: a hostile-verifier contract finding became a T0-backed repair
in one session with zero self-judged steps — design, audit, proving, and
verification each by a distinct fresh agent.
