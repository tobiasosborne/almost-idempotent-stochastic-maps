<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 32 (session-25
  phase-2-closure delta). Supersedes v31; everything not restated here is
  unchanged from v31.
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing.
  op-classical remains OPEN. T0 = 62; nothing below is af-validated unless
  said so.
-->

# Top-down proof sketch v32: op-classical (2026-07-25, phase-2 closure delta — the H-CB subtree is L0-rigorous; phase 3 (EXT-CB) is the live runway)

## UNCHANGED from v31

The signed-geometry trunk and its whole surface; Route X as the registered
fallback; the quarantine + GAP interfaces (aism-0163: MAIN-CB assembly rows,
ledger rows, GAP-EA / GAP-S1-POLAR-CONTRACT / GAP-MAIN-STRUCTURE /
GAP-LEDGER-DOMAINS) still blocking phase 4 only; the Stage-1 external
topology provisioning risk; the `lem-routef-k-ledger` DO-NOT-REWIRE-OR-SEED
guard (F2/F3 contracts now exist as registered `proved-mod-audit` lemmas —
the guard lifts only inside the aism-0163 repair).

## Map change: PHASE 2 EXECUTED AND CLOSED (aism-niwk closed; T0 35 → 62)

The entire H-CB subtree is now **af-validated (L0)**: the COMP prerequisites
(11 `lem-compcb-*`), the hcb0–hcb4 tiers (15 `lem-hcb*`, including the
session-25 additions `lem-hcb3-uniform-square-lower`,
`lem-hcb3-diagonal-lower-modulus`, `lem-hcb3-diagonal-inverse`,
`lem-hcb3-offdiagonal-inverse`, `lem-hcb4-canonical-{gram,closeness,inverse}`),
and the **parent `conj-hcb`** (11/11 first-pass; every clause of the
hostile-amended compound contract discharged against a first-class validated
import; banked ▣ with a passing external `fr verify` via oracle
`af-conj-hcb`). The conditional clauses (level-one lower modulus ≥ 1/4;
level-one bijectivity) remain **load-bearing hypotheses in the contracts** —
downstream EXT consumption meets them at `tex:1391` per the recorded
verifier finding (v31, unchanged).

## Process laws distilled this phase (BINDING for phase 3+)

1. **Dep alignment.** The registry `deps:` line and the workspace's
   first-class externals are kept IDENTICAL at seeding; the battery of other
   validated contracts rides along as `-CONTRACT` context. Every stall of the
   phase traced to a mismatch; after alignment, seven consecutive first-pass
   validations.
2. **Default first-class set.** `lem-compcb-corner-algebra` +
   `lem-hcb3-uniform-square-lower` join every H-CB/EXT-adjacent seeding
   (needed 3-for-3 when omitted).
3. **The def kit is cumulative (now 15):** the 7 original displays/axioms +
   `def-ha-map` + `def-hcb-datum` + `operator-space-matrix-norm-axioms`
   (Ruan, kitaev tex:1454-1463) + `def-extended-epsilon-cstar-algebra` +
   `def-canonical-corner-identifications` + `def-compressed-corner` +
   `def-delta-projection` + `def-one-dimensional-delta-projection` — replayed
   byte-identical from the previous workspace's ledger at each seeding.
4. **Tripwire factoring works.** The balloon on lower-modulus factored into
   `lem-hcb3-uniform-square-lower` (stated → af-validated in one small run);
   this is the template for any phase-3 balloon.

## The live campaign frontier: PHASE 3 (EXT-CB, aism-fgr7)

Bottom-up over the independent EXT-6: `lem-extcb-one-dimensional-product` →
`lem-extcb-corner-dimension-additivity` → `lem-extcb-four-corner-merge` →
`lem-extcb-one-dimensional-corner-dimension` →
`lem-extcb1-close-corner-dimension` → `lem-extcb1-cross-corner-dimension` →
parent `conj-extcb` (deps: `conj-hcb`, now validated). Three of the six are
`stated` leaves (fudw safe-subset): their af runs build the proofs from
scratch — the uniform-square-lower precedent says this is fine at this size.
Then phase 4 waits on the aism-0163 repair (unchanged).

## Report surface (session-25 rescope, user mandate)

The `report/` lab-book was rescoped to the live chain: 26 shards reproducing
the 24 af-validated live-route lemmas in prose (statement + byte-verbatim
contract anchor + prose account of each validated tree), the retired
kernel-route surface deprecated (UNWIRED-whitelisted, registry records
intact). The `conj-hcb` parent write-up and the offdiagonal-inverse write-up
are the next report wave. A batched hostile prose-vs-export review is filed
(reviewer ≠ author) and pending.
