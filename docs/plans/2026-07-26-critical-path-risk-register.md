<!--
ROLE: the ranked de-risk ledger for the Route-F critical path. USER MANDATE
  (2026-07-26, session 29): "there is only one priority: de-risk the critical
  path. All energy must be focussed on this, starting with the riskiest and
  working down." This supersedes the W74/XE decider dispatch (voided by the
  user same day); Route X remains the priced FALLBACK, touched only if the
  critical path blocks.
STATUS DISCIPLINE (L0): a planning artifact; promotes nothing; op-classical
  is OPEN.
UPDATE POLICY: strike items only when the de-risk evidence is banked (a
  hostile-audited design, an af validation, or a documented refutation);
  re-rank with a dated note.
-->

# Critical-path risk register — Route F (2026-07-26)

**SESSION-30 CLOSE STATE (2026-07-27): ALL FOUR FRONTS DE-RISKED — designed
and fresh-hostile-audited to a landable state; 19 further codex jobs (11
hostile audits, 7 prescribed repairs, 1 design); ZERO route-level findings
across the whole campaign.** Front 1 (polar): `DESIGN-S1-POLAR-v6.md`, audit
v6 = LAND (7 hostile stages; the v5 stage caught a pre-existing
producer-domain quantifier defect that four earlier audits had accepted —
repaired by coherent finite-dimensional closure, audit §6 option 1).
Front 2 (MAIN): `DESIGN-MAIN-STRUCTURE-v5.md`, audit v5 = REPAIR-CONFIRMED
(6 stages; gated on P0 + G-S1). Front 3 (ledger): unchanged from session 29,
re-audit = LAND-14 with two exact corrections. Front 4 (F0):
`DESIGN-F0-ASSEMBLY.md`, audit = LAND with four corrections (every seam an
exact match; the corrected `lem-routef-k-ledger` honestly reclassified as a
strengthened replacement = new parent proof obligation). Consolidated user
ratification package: `docs/plans/2026-07-27-W78-ratification-package.md`
(4 user decisions D1–D4; merged serial landing/elevation order). The ONE
remaining design gap on the critical path is the three Stage-1 split
producers (G-S1 contents), deliberately sequenced behind the polar landing
(package §5 step 3). NOTHING landed or promoted; T0 = 83 unchanged;
op-classical OPEN.

**SESSION-29 CLOSE STATE (2026-07-26, one full design→audit→repair cycle run
on fronts 1–3; 8 hostile codex jobs; ZERO route-level findings — no dimension
leak, no Kitaev-theorem error, no unclosable gap anywhere).**
Front 1 (polar): `DESIGN-S1-POLAR-v2.md` DESIGNED-CLOSABLE (9 rows + 6
downstream repairs; the C¹→smooth seam closes WITHOUT approximation — the
maps are smooth outright). Front 2 (MAIN): `DESIGN-MAIN-STRUCTURE-v2.md`
acyclic repair, one escalated sequencing gate G-S1 (three Stage-1 split
producers land only after the polar round). Front 3 (ledger):
`DESIGN-LEDGER-DOMAINS-v2.md` all 14 rows close; front DECOUPLED from the
MAIN reset repair (terminal η_K via the landed thmainext interface).
ALL THREE v2 designs await fresh hostile re-audits, then a consolidated user
ratification package, then landing/elevation. Front 4 (F0) remains the last
undesigned risk. Next-session plan: bead **W78**. The three genuine Kitaev
prose gaps found and repaired-by-design: TeX 906 (straight-path right
inverse), TeX 883–888 (printed derivative erratum, unconsumed), TeX 795–807
vs 947–954 (the C¹→smooth jump).

Route F status baseline: T0 = 83; no step rests on an open conjecture
(LEARNINGS 2026-07-26: "requires no conjecture and no new idea" survived
hostile review), but four work fronts contain genuinely unproved mathematics.
Ranked by risk to the route (riskiest first). "Risk" = probability the front
hides a route-killing or route-reshaping defect × how much of the route it
takes down.

**Risk calibration (user challenge, 2026-07-26).** Fronts 1–3 are
re-establishments of Kitaev arXiv:2405.02434 (Thm 12.3 machinery). The
hypothesis being tested there is NOT "Kitaev's theorem is false" (negligible
probability; that outcome would be a bigger result than op-classical itself).
The realistic failure modes, in order of probability: (i) OUR transcription
overclaims — this already happened once (the 15-row retraction; Kitaev
untouched, our domains wrong) and is caught by the hostile pipeline;
(ii) his prose proves less than a closed contract needs in literal form
("suitable constants", unexpanded O(·)) and closing it restructures the plan
or blows budgets — a COST/STRUCTURE risk, not a truth risk; (iii) an
audit-level repairable slip (the realistic ceiling — cf. the corrected
th_almost_idemp, after which the theorem stood). The route's residual TRUTH
risk is concentrated in front 4 (the project-own stochastic bridge), which is
mostly banked. Empirical record: 2,386+ hostile jobs, zero route-level
refutations; every retraction to date was of this repo's own rows.
Interpret every "kill-scenario" below through this lens: it prices the
discovery of restructuring cost BEFORE elevation budget is sunk downstream,
not an expectation that the source theorem fails.

## 1. GAP-S1-POLAR-CONTRACT — RISKIEST  ⟵ de-risk IN FLIGHT (design job dispatched this session)

- **What:** the Stage-1 polar retraction, group-law error, and
  first-derivative error exist ONLY as prose (Kitaev TeX ~690–950 +
  PROOF-W74F-H-STAGE1). No formula-level contract survived hostile review
  (v4.1 R14: "compound and non-closed"; §2.6 reservation, DO NOT SHARD OR SEED).
- **Why riskiest:** it is the only front with NO closed statement at all, and
  it blocks the whole Stage-1 topological engine:
  `lem-stage1-inversion-derivative-control` / `-quotient-manifold-package` /
  `-quotient-left-inversion` → `lem-stage1-extra-fixed-class` (the Lefschetz
  fixed-point argument) → the nontrivial projection → MAIN-CB refinement. If
  uniform dimension-free polar control fails, Route F has no engine.
- **Specific kill-scenarios to test:** a chart radius or derivative constant
  that secretly depends on dim X or on the amplification level; an O(εδ+δ²)
  term that cannot be closed with universal constants; the C¹ (not C²)
  regularity being insufficient for the downstream manifold/H-space rows.
- **De-risk instrument:** fresh codex design job (BRIEF in
  `2026-07-26-S1-POLAR-design/`) producing closed contracts + per-row
  feasibility verdicts + a dimension-freeness audit, then a SEPARATE fresh
  hostile audit (GAP-EA pattern). GAP found = success.

## 2. GAP-MAIN-STRUCTURE — 8 unverified glue rows

- **What:** the eight `stated` MAIN-CB structural targets (strict refinement,
  maximality, corner equivalence, cross-class merging datum, one-class
  extension, binary merge, finite recombination, structural assembly) —
  "new glue, not verified artifact results" (v4.1 R23).
- **Why second:** never hostile-verified even once; the known induction
  hazards are structural (R19 circularity without a strict refinement
  measure; R21 one-class vs cross-class inductions conflated; R22 the zero
  off-diagonal datum omitted). A defect here reshapes the MAIN theorem's
  proof but has a published-source template (Kitaev TeX 1414–1444) to fall
  back on, so it is less likely fatal than #1.
- **De-risk instrument (next after #1 is in flight):** independent codex
  design job producing per-row proof plans + a hostile audit hunting the
  three named hazards, per the §3.3 branch order. IMPROVE-CB's proof plan
  (already ratified-narrowed) belongs to this front too (v34 §3.3 note).

## 3. GAP-LEDGER-DOMAINS — 14 local-radius derivations

- **What:** the fourteen withdrawn K-ledger rows; each needs its own
  dependency-produced local radius (the η_A-overuse defect). Mechanisms were
  verified; domains were not (LEARNINGS 2026-07-26).
- **Why third:** per-row risk is low (derivation, not discovery), but the
  aggregate carries R27 (the finite-minimum threshold DAG must be sound and
  non-circular, importing `lem-maincb-reset-constant-ledger`) and the same
  dimension-freeness alarm as #1 (a radius shrinking with n kills the
  theorem). Volume: 14 rows in the §D serial order, `lem-routef-k-finiteness`
  last.
- **De-risk instrument:** the per-row derivation campaign (HANDOFF next-step
  3), each row a fresh prover + fresh hostile verifier.

## 4. Assembly & F0/root wiring — undesigned plumbing

- **What:** `lem-thmainext-conditional` (design-blocked on #1/#2),
  `lem-routef-k-ledger` (DO-NOT-REWIRE-OR-SEED until #3 closes), and the
  F0/root composition op-classical ⇐ F2/F3 + K-ledger + PRH — for which NO
  row is even designed (v4.1 phase 5 step 4).
- **Why fourth:** the pieces at the seam (F2, F3, PRH-finish) are
  hostile-reviewed with explicitly matching hypothesis/conclusion interfaces
  (K, 3Kη/(1−3Kη), (K+4√(2K))√η), so the residual risk is interface
  mismatch, not new mathematics. Still nonzero until an F0 design exists:
  the single-K discipline (one constant threading all three) must be checked
  end-to-end.
- **De-risk instrument:** a cheap F0-design/feasibility audit once #1–#2
  verdicts are in.

## Explicitly OFF this path (do not spend energy here)

- Route X / XE deciders (`conj-kernel`, `conj-l5-gap-1`, W74): FALLBACK
  ONLY, priced insurance. Voided by user 2026-07-26; touch only if a front
  above returns a route-killing GAP.
- The signed-geometry trunk: PAUSED (standing directive viii).
- Low-risk closers (EA bridge row 2–3-node elevation, `finite-triangulation`
  repair `aism-j5t9`): genuinely safe, therefore LAST under riskiest-first —
  they de-risk nothing.
