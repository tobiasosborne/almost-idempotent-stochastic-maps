# Top-down proof sketch v46: op-classical (2026-08-03, session 42 close — the thmainext blocker RESOLVED by contract re-scope and the row BANKED: T0 168 → 169; LEDGER-DOMAINS front landed, registry 348 → 364)

## UNCHANGED from v45

The global architecture (Route F via positive approximate retract), the
completed MAIN campaign (M01–M28 all T0), the complete Stage-1 record, the
parked arms, the parallel-`af` discipline, and the honest headline
(`op-classical` **OPEN**) all stand.

## Map change 1: the v45 blocker is GONE — by re-scoping the contract, not by proving it

v45 recorded `lem-thmainext-conditional` as BLOCKED: its frozen contract's
method clause ("the assembly **uses** the corrected squared COL-HILB estimate
and the hostile-verified H-CB, EXT-CB, and Stage-1 reset packets") was not
dischargeable from the seven frozen T0 deps.

**The user ratified option B**: the clause is a claim about a *proof*, not about
`A`, `B`, `v`, and it has been **removed from the contract and re-scoped as
documentary provenance** (commit `7b044403`). The mathematical content — the
existential in `C_E, epsilon_E`, the map type, and independence of dimension,
amplification level and block data — is **byte-unchanged**.

The supporting evidence for the re-scope, checked before it was proposed:

- all **17** consumers take exactly one thing from the row — it is a
  *"black-box producer of `C_E, epsilon_E`"*; **not one** consumes the method
  clause;
- the frozen contract text appeared **nowhere live** outside its own shard;
- the workspace was still empty, so no `af` root text needed re-matching;
- the report cites the row by **status only**, never restating the contract;
- the shard body **already** carried the MAIN-CB-assembly, squared-correction
  and conditional-ledger content the clause duplicated.

**The decisive argument, and the transferable lesson.** The `deps:` line *is*
the "uses" statement, expressed in the mechanism this repo actually enforces.
The linker checks dependency edges on every gate run; nothing checks prose
inside a contract string. So the seven deps were **deliberately NOT reduced** —
in particular the edge to `lem-hcb-column-hilbert-squared`, the corrected
squared estimate replacing the paper's unsquared display at
`approximate_algebras.tex:1551-1555`. Moving the sentence out of the contract
*strengthened* the guarantee by leaving it to the enforced mechanism. Both the
shard body and its provenance forbid a later reader "simplifying" those six
edges to the transitive reduction.

## Map change 2: the row is BANKED (T0 168 → 169) — and what that does NOT buy

`lem-thmainext-conditional` is af-VALIDATED: a **four-node tree, root
validated, taint clean, FIRST PASS, zero challenges** (fresh codex prover,
separate fresh codex verifier per node; one verifier correctly *blocked* node
1.3 until its children validated). Oracle registered, `fr verify` PASS, shard
flipped mechanically from the codex ledger.

The tree is: fix M28's ledger datum `W` and define the two constants (1.1);
apply M28 to arbitrary `A, epsilon` and substitute (1.2); unfold
`def-extended-delta-inclusion` for the all-amplification reading and
universality (1.3).

**Honest scope — do not let the counter speak for itself.** This proves **no
mathematics absent from `lem-maincb-structural-assembly`**. The hostile auditor
was asked outright and answered: mathematically the row is *redundant* relative
to M28, a strict weakening with no new analytic information; structurally it
remains a meaningful *interface*, hiding `W`, the block form `⊕_C M_{|C|}` and
the unit estimate while exposing exactly the two constants
`lem-routef-k-ledger` consumes. Its value is **validated interface projection
and DAG decoupling**, not theorem strength.

What genuinely improved: the Route-F chain above MAIN now rests on a **T0**
carrier rather than a `proved-mod-audit` one, and the linker's blocked count
falls 117 → 115.

## Map change 3: the LEDGER-DOMAINS front landed — and is now fully unblocked

Sixteen rows landed (14 reserved + the D2/D3 degree-row reconnections), registry
348 → 364, all `status: stated` / `af: none`, both audit corrections folded in
verbatim, and the transcription independently verified (zero substantive
findings; eight editorial provenance-locus typos **inherited from the ratified
design's own table**, fixed in both the shards and the generating script).

**The status cap that split the queue in v45 is gone.** Rows 1–5 and 14 import
`lem-thmainext-conditional`; with that row now T0, the linker's
status-propagation rule no longer caps them. **The whole 16-row queue is
elevatable**, in the design's serial order.

## Map change 4: the obstruction class recorded in v45 now has a worked resolution

v45 named a new class — *a contract that asserts its own provenance*. v46 adds
the remedy, demonstrated end to end: **check whether any consumer depends on
the narrative clause; if none does, re-scope it to `provenance:` and leave the
`deps:` edges to carry it.** The sweep of remaining un-elevated rows recommended
in v45 still stands, and now has a cheap disposition for anything it finds.

## The open surface after this delta

- **The ledger elevation queue** (bead `aism-3fjg`) — 16 rows, serial, per-row
  budgets in `DESIGN-LEDGER-DOMAINS-v2.md` §2. No status cap remains.
- **The F0-assembly landing** — `DESIGN-F0-ASSEMBLY.md` (audit **LAND**, four
  corrections, W78-ratified D4) is still untranscribed: `lem-routef-f0-assembly`
  plus the **strengthened `lem-routef-k-ledger` replacement contract**. This one
  REPLACES a landed contract and releases the DO-NOT-REWIRE guard (W78 §5 step
  6), and needs its own fresh prover + fresh hostile verifier.
- The **root rewire LAST** — unchanged from v41–v45.
- Carried: `aism-9kmt` report sync (P2; unanchored banks now ~120–177 plus the
  16 ledger ids and thmainext), `aism-wazy`, `aism-xjnc`.

## Controller note

fr arm FH carried the whole session: two design waves, three hostile-audit
waves (one DESIGN-REJECTED, one DESIGN-CONFIRMED, one
TRANSCRIPTION-CONFIRMED), one landing, one elevation, one `banked` pull. **Six
distinct fresh agents; zero self-judged steps.** The session's most valuable
artifact was the *rejection* — without it the campaign would have spent
substantial effort proving a claim about a proof. The `fr` bank gate also
refused a `banked` log for the registry landing (no oracle verdict) and was not
worked around.

`op-classical` remains **OPEN**.
