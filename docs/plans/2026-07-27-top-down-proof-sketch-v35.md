<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 35 (sessions
  28-addendum + 29 + 30 delta: T0 82 -> 85; the de-risk campaign closed all
  four risk-register fronts at design level; D1-D4 ratified and landed; the
  F0 stochastic<->Kitaev seam is T0; F2 is the live workfront). Supersedes
  v34; everything not restated here is unchanged from v34 (and, through it,
  v33/v32/v31).
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing.
  op-classical remains OPEN. T0 = 85; nothing below is af-validated unless
  said so.
-->

# Top-down proof sketch v35: op-classical (2026-07-27, sessions 28-addendum–30 delta — de-risk campaign complete, D1–D4 ratified, F0 seam T0, F2 typing repair in flight)

## UNCHANGED from v34

The signed-geometry trunk (PAUSED); Route X/XE as registered fallback only;
the phase-2/3 process laws; the orchestration-infrastructure laws (v34 map
change 4, plus the session-30 additions below); the LEARNINGS 2026-07-26
partial retraction as the canonical account of the 15 demoted local-domain
rows; the GAP-EA discharge and the Stage-1 topology state except as updated
below.

## Map change 1 (session 28 addendum): T0 82 → 83; both session-28 escalations ratified and executed

IMPROVE-CB (`lem-maincb-error-improvement`) landed with the §2.3 NARROWED
contract (dep = the general correction row `lem-extcb-exact-target-correction`).
`lem-topology-local-index-sign` narrowed to `def-lefschetz-fixed-point-data`'s
smooth scope, root amended in-place, **af-VALIDATED (83rd, 23/23 clean)** —
Stage-1 topology now 6/7 at L0. `lem-topology-finite-triangulation` re-pinned
to the compact-smooth form against the newly promoted Munkres *Elementary
Differential Topology* ref (Thm 10.6; 23/23 sha256), but the re-run ballooned;
parked on `aism-j5t9` with the exact fix (provision the C^r-triangulation
DEFINITION external).

## Map change 2 (sessions 29–30): the de-risk campaign — ALL FOUR risk-register fronts are DESIGNED-LANDABLE; zero route-level findings

Governing mandate (user, 2026-07-26): de-risk the critical path, riskiest
first. Register: `docs/plans/2026-07-26-critical-path-risk-register.md`.
~27 fresh codex jobs over two sessions (design → fresh-hostile-audit →
prescribed-repair chains); **ZERO route-level findings** — no dimension leak,
no Kitaev-theorem error, no unclosable gap; every defect was in OUR contract
factoring and every one was repaired under a binding audit.

- **S1-POLAR → `DESIGN-S1-POLAR-v6.md` = LAND** (seven hostile stages).
  Final form: 12 analytic rows + 7 parameterized transport helpers 13a–g +
  ledger row 13 + 6 downstream contract repairs; coherent finite-dimensional
  closure (audit-v5 §6 option 1) resolving the base-producer quantifier
  defect that four earlier audits had accepted; the C¹→smooth Kitaev prose
  seam closes WITHOUT approximation (the maps are smooth outright).
- **MAIN-STRUCTURE → `DESIGN-MAIN-STRUCTURE-v5.md` = REPAIR-CONFIRMED**
  (six hostile stages; v4.1's 8 MAIN rows definitively dead as written —
  five structural defects confirmed). The v5 repair: nested-corner
  comparison, outer-compression transfer, conditional equivalence/cross-union
  before the reset ledger, recombination on the complete one-class family;
  P0 + M19-S1/S2/S3 + M19-R; ONE sequencing gate **G-S1** (the three
  Stage-1 split producers land after polar — the one remaining
  critical-path design gap).
- **LEDGER-DOMAINS → `DESIGN-LEDGER-DOMAINS-v2.md` = LAND-14** + 2 exact
  corrections (rho_id^corr gains rho_theta exposing Kitaev's eta<1/4 domain;
  'unital' wording). All 14 local-radius rows close;
  eta_K = min{rho_fac,(24K)⁻¹,1} via the landed `lem-thmainext-conditional`
  interface — the ledger front is DECOUPLED from the MAIN reset repair.
- **F0-ASSEMBLY → `DESIGN-F0-ASSEMBLY.md` = LAND** + 4 corrections. Every
  seam K-ledger→F2→F3→PRH→op-classical recomputed EXACT (single K;
  C = K+4√(2K); dimension-free). Honest catches: the corrected k-ledger is a
  STRENGTHENED REPLACEMENT (a NEW parent proof obligation, not a rewire);
  the sharpness parenthetical forced root decision D1.

## Map change 3 (session 30): D1–D4 RATIFIED and LANDED (registry 267, defs 45)

The user ratified all four decisions of
`docs/plans/2026-07-27-W78-ratification-package.md` in-chat 2026-07-27:

- **D1 (option A):** the `op-classical` contract SPLIT to upper-bound-only;
  sharpness carried by `ex-hume`; the future OR-route root wiring is recorded
  in the shard body and applied only at the campaign's LAST step.
- **D2:** the six datum-only definition shards landed
  (def-maincb-{reset-state,raw-call,partition-state},
  def-approximate-unitary-space, def-stage1-polar-witness-data, plus
  def-operator-space CITED — byte-verified against TeX 1453–1464).
- **D3:** `def-ucp-map` provisioned (over a recorded L2 exemption).
- **D4:** the strengthened k-ledger authorized as a NEW proof obligation
  (fresh prover + fresh hostile verifier); the
  `lem-routef-k-ledger` DO-NOT-REWIRE guard releases ONLY at package §5
  step 6.

## Map change 4 (session 30): THE F0 SEAM IS T0 (83 → 85) — the stochastic↔Kitaev interface of Route F is rigorous in-repo

- **`lem-routef-f0-ucp-lift` af-VALIDATED (84th; 9/9, taint clean).** The
  fresh verifier raised a GENUINE major typing challenge mid-run
  (ch-7651a4a59d0519c0): D: M_n → l_inf^n with def-stochastic fixing
  l_inf^n = ℝⁿ while the validated children route through the complex
  diagonal ℂⁿ — a defect that had survived TWO hostile audits of the F0
  design. Repaired by recorded contract amendment on BOTH seam rows
  (D: M_n → ℂⁿ, J: ℂⁿ → M_n, Q_ℂ the canonical complex-linear extension,
  Φ = J Q_ℂ D); 8 validated nodes retained.
- **`lem-routef-f0-defect-identity` af-VALIDATED (85th; 12/12 first-pass,
  ZERO challenges, both directions incl. the zero-row edge case).**

Both banked per the working sequence (export → per-id oracle → fr verify →
mechanical flip → regenerate → gate → fr log T0 → commit).

## Map change 5 (session 30): F2 STUCK-ABORT with a clean classification — the live workfront

`lem-routef-f2-positive-unital-compression` elevation ABORTED [STUCK] at
11/30 validated (workspace retained): (i) the SAME real-vs-complex typing
defect family as F0, in F2's LANDED hostile-endorsed contract
(ch-2163ee19860aa3d7); (ii) MISSING provisionable facts —
fd-commutative-C*-classification (byte anchor TeX 1361–1363; locked
`def-projection-basis`) and UCP complete contractivity; (iii) two
cross-sibling DAG defects + an ε-scoping leak. The surgical repair path is
MANDATORY (a hand-edit of the hostile-endorsed contract was self-caught as
an author-role violation and reverted): fresh design job per
`docs/plans/2026-07-27-F2-TYPING-design/BRIEF-F2-TYPING.md` → fresh hostile
check → land the corrected contract VERBATIM → re-seed → re-elevate. The
correction must keep the Φ end EXACTLY on the T0 F0 seam typing and the
A/M output end verbatim-consumable by F3 and PRH on the real spaces.

## Orchestration-law additions (session 30, BINDING)

fr/bd writes FIRST, commit, af launch as the turn's LAST action (a mid-run
fr-log append aborts a live run as a PROVER-OVERREACH false positive);
commits only in zero-live-run windows; the banking sequence of map change 4
is the one that works; routine tier sufficed for both F0 rows.

## The open surface after this delta (the ratified §5 serial order, W80)

1. **F2 typing repair + re-elevation** (design job in flight), then **F3
   elevation** (real-space contract, no typing issue expected) — package §5
   step 1.
2. **Sketch reconciliation** — THIS FILE (Rule-9 debt discharged).
3. **Polar front** per `DESIGN-S1-POLAR-v6.md` §9: rows 1–12 → helpers
   13a–g → ledger row 13 → 6 downstream (defs already landed).
4. **The Stage-1 split-producer design round (G-S1)** — the ONE remaining
   critical-path design gap.
5. **MAIN** per `DESIGN-MAIN-STRUCTURE-v5.md` §10.
6. **The ledger 14-row campaign** (decoupled; can interleave).
7. **The strengthened k-ledger** (D4: NEW proof obligation; guard releases
   here), then **f0-assembly**, then the **root rewire LAST** (D1 OR-route
   wiring applied at this step only).
8. Carried small closers: `aism-j5t9` (Munkres C^r-triangulation def
   external); the parked finite-triangulation tree.

## Controller note

FH un-stalled via genuine frontier reductions (sessions 29–30); user
breaker-override on record; XE remains the registered fallback decider arm
(fallback only — the 2026-07-26 mandate keeps all work on the Route-F
critical path).
