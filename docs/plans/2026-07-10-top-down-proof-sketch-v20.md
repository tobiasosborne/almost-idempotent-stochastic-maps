<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 20 (W56 SL1a-surface delta).
  Supersedes v19; everything not restated here is unchanged from v19.
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing.
-->

# Top-down proof sketch v20: op-classical (2026-07-10, W56 SL1a-surface delta)

## UNCHANGED from v19

The theorem, trunk, 28 af-validated results, verified W54 huddle-charge assembly, the W55
map (L6.5 exact front end, gauge split, completion wall, starvation gadget), the parked
Tier-2/3 queue, and user decisions remain unchanged. `conj-cotop-web-coupling`, SL1b,
the L5 minimax, the Kernel Conjecture, and `op-classical` remain OPEN.

## W56 map change: SL1a is now three quantified cells on a proved interface

Registry 140 -> 150 (+7 proved L5 lemmas, +3 conjectures; every banked proof passed a
SEPARATE fresh hostile codex verifier; L0 still claimed ONLY for the af-validated 28).

**The wall (certified twice, rounds 1-2):** after the free routine pipeline (selector /
kernel-corner / partition bookkeeping, all provable from standing shards), ANY terminal
hard leaf that retains the full SL1a counterexample class is a RESTATEMENT of SL1a.
"One hard leaf with a strictly smaller configuration space" is DEAD as a decomposition
objective for SL1a (FINDINGS 2026-07-10). Lex-minimal-counterexample stratification is
DEAD (free preprocessing; minimality has no valid consumer; transient-row unstable).

**The harvest (all hostile-verified, banked):**

- `lem-sl1a-score-selector` — every SL1a web admits a supported row f with
  2*z_f/D + h(p_f) <= 12*tau/13 (universal over top support functionals and exposers).
- `lem-sl1a-corner-ledger` — at delta <= 2^-16, every legal vertex kernel puts coupled
  corner mass Gamma_f(C_f) >= 58079731/109051904 > 1/2 (universal over legal kernels).
- `lem-radial-horn-partition` — the corner mass splits into boundary-owned far/near
  horns, one of mass >= 1/4.
- `lem-zero-face-one-sixteenth-capacity-kill` — zero-face shipping >= 1/16 to the
  kappa-high slab forces tau >= 1/64, impossible at delta <= 2^-16.
- `lem-affine-barycenter-identity` (affine integrals ONLY — the nonlinear
  interchangeability overclaim is deleted), `lem-clone-invariant-row-complexity`
  (the (V,R) invariants are well-defined; NOTE: minimality is a dead route, this shard
  is bookkeeping only).
- **`lem-sl1a-three-cell-reduction` (the prize):** IF the three cell exclusions below
  hold THEN SL1a holds with delta_0 = min(delta_D, delta_I, delta_X, 2^-16). The
  routing is existence-plus-universality (every legal tuple killed); the three guarded
  cells are disjoint and exhaustive with explicit boundary ownership.

**The new SL1a surface — three sigma-cells (each a single-contract conjecture,
af-elevation shape, refuter criteria demand delta->0 families):**

1. `conj-sl1a-deep-diagonal-cell` (H-D): no majority horn carries > 1/16 diagonal mass
   on disjoint-hull carriers (K_T(u) cap K_O(u) = empty).
2. `conj-sl1a-intersection-diagonal-cell` (H-I): no majority horn carries >= 1/16
   diagonal mass on intersecting-hull carriers.
3. `conj-sl1a-off-diagonal-cell` (H-X): no majority horn carries > 1/8 off-diagonal
   freight mass.

The cells partition the residual: SL1a is EQUIVALENT to their conjunction (the
reduction lemma gives <=; the converse is the two-line free-preprocessing derivation
the round-2 verifier exhibited). This is the SAME shape as the W54 close: not a
strict-progress decomposition but a SHARPER, MECHANISM-SEPARATED restatement of the
open surface, each cell with its own named refuter target and proved toolkit.

## New dead-route certificates (FINDINGS 2026-07-10; do not re-walk)

- One-hard-leaf after free preprocessing (any retained-class terminal leaf restates SL1a).
- Lex-(V,R) minimal-counterexample stratification (free + unused + transient-unstable).
- Freight-row censoring without a proved ||A||_{inf->inf} < 1 gap.
- Second-generation L-C recursion (hypotheses fail at second-web rows, both centers).
- Max-principle far-side return (sign unconstrained off T(u); = the W55
  carrier-coincidence obstruction).

## Revised Tier-1 order (the v19 order, re-anchored on the new surface)

0. **Attack the three sigma-cells** — H-X first (off-diagonal freight: the same
   completion-obstruction shape as the W55 large-gauge wall, so the exact
   `P=L*B, B*L=I` completion/refutation LP (v19 item 3) now serves BOTH; a stable dual
   infeasibility certificate would be the first genuinely new mathematics on this
   front). H-D couples to v's own coefficients via the far-horn v in F_u geometry;
   H-I owns the intersecting-hull anatomy (lem-intersection-witness-confinement).
1. Codify W55 E1-E5 after a fresh standalone prover/verifier pass (unchanged).
2. Codify the small-gauge SL1a/SL1b bridge (unchanged; SL1a now means the three cells).
3. Attack mixed co-top straddle jointly with the cells; SL1b remains the easiest sibling.
4. L5 minimax (aism-vuc) remains independent and open.
5. Elevation queue: the W56 proved family (7 lemmas) are prime af-elevation shapes.
