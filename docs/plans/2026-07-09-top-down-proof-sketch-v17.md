<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 17 (session 13, W53 bank) — THE
  CANONICAL STRATEGIC MAP. Supersedes v16 (kept for line citations) after W53
  (docs/waves/2026-07-09-W53-binding-constraint-lemmaization.md).
  DELTA redraw: everything not restated here is UNCHANGED from v16.
STEWARDSHIP CONTRACT (binding): as in v10-v16. STATUS DISCIPLINE (L0): a SKETCH — promotes nothing.
-->

# Top-down proof sketch v17: op-classical (2026-07-09, session 13, post-W53)

## UNCHANGED from v16

Theorem; trunk; the endgame stack's lower half; the pivot (ONE face); 28 af-validated;
the closed-by-certificate list; USER DECISIONS aism-nlg, aism-z98; parked Tier-2/3
farm-out queue. Registry now 125 (W53: +4 proved-L5 bricks, +2 conjectures).

## THE W53 COLLAPSE: three constraints -> ONE question

The (B1)-(B3) lemma-ization wave (4 codex provers, 4 fresh hostile verifiers, ALL
VALID-WITH-CORRECTIONS) proved the three W52 binding constraints are one wall seen from
three sides:

- **(B3) top reversion == the huddle, both directions.** `lem-disjointness-huddle-reduction`:
  in-class disjointness at a mass-carrying ball-cluster vertex FORCES the pair (u, w) —
  two geometrically distinct hidden vertices within 4*tau at depths > H-4*tau, > H-8*tau;
  contrapositive: no pair => the intersection horn of `conj-zero-face-elimination` holds.
- **(B2) forced visibility, static content == the third actor.** `lem-top-witness-third-actor`:
  > 13/16 of any small-beta hiddenness witness of a tall top sits rho-FAR at depth
  > H-4*tau — the huddle is never two-body; `lem-bounded-alpha-forced-far-slab`: a huddle
  partner with a bounded-alpha witness forces its OWN far top-slab actor. (The literal W52
  phrasing was dynamical and is subsumed by `lem-ball-cluster-exposure-void` — FINDINGS
  2026-07-09.)
- **(B1) delta inflation, exact form == the top-deficit price.** `lem-top-deficit-price`:
  sum_j a_j^+ (H - phi(p_j)) <= delta*(2+4*delta), universal, first-principles. Its blind
  spot is structural: the rho-ball has top-deficit < 4*tau — the huddle lives exactly
  where the pairing cannot see.

## THE TERMINAL NODE v17: THE HUDDLE CHARGE

**One Tier-1 question.** A tall (H > a*tau) heavy hidden top v with its FORCED anatomy —
the huddle pair (u, w) within 4*tau ([[lem-disjointness-huddle-reduction]]) + the forced
rho-far top-slab web ([[lem-top-witness-third-actor]], and at u via
[[lem-bounded-alpha-forced-far-slab]]) — must contradict exact idempotence at small delta.
Three codified handles, ANY of which closes the (M2) tall-emptiness target (rank-free
except (iii)):

- **(i) `conj-top-deficit-coupling`** (the sharpest closer): Z_v(phi) >= c_def*H on tall
  heavy tops; with `lem-top-deficit-price` gives H <= 4*tau outright.
- **(ii) `conj-tall-bounded-alpha` + web incompatibility** (GAP-B2-2, deliberately
  uncodified until W54 sharpens it; VB2: the two halves compose only JOINTLY): bounded
  alpha activates the forced-far-slab lemma at every huddle member; the resulting
  multi-witness top-slab web must be shown incompatible.
- **(iii) the rank-3 transverse reach floor** (= `conj-rank3-cluster-zero-face-reach`,
  contract now C_4-explicit): closes the node at rank 3 only; general-rank price named.

**Consumption:** huddle charge => tall-emptiness == `conj-near-cluster-absorption`
(skeleton finding: contrapositives) => `conj-zero-face-elimination` horn (via
`lem-disjointness-huddle-reduction`) AND directly toward `conj-low-slab-cap`.

## NAMED Tier-1.5 SURFACE (Opus skeleton, W53 — interface gaps, likely-provable derivations)

- **E1 [PROSE]:** node/absorption => `conj-low-slab-cap` — NO registered conditional lemma
  (both DAG leaves); register + prove as a lemma wave (the largest unscoped non-creative
  surface).
- **E2 [PROSE hop]:** `conj-low-slab-cap` => `conj-min-a-w4` at (4,1/2) (wired only through
  lem-min-a-implies-height afterwards).
- **E3 [scope]:** the height clause needs W-nonemptiness (rank >= 3, parked) + the delta=0
  endpoint (proved) to become `conj-kernel`.
- **E5 [named open sub-lemma]:** loose-delta robustness (retired without proof if aism-nlg
  adopted); Q-not-yet-stochastic honest interface for <2>7.
- Constants discipline (skeleton G8): (delta_0, theta_0, c_def/A_0/reach) must be chosen
  in ONE feasible order across handles.

## Next opening move (session 13, in flight)

**W54 — the huddle-charge DECOMPOSITION wave (user mandate 2026-07-09: the objective
function of Tier-1 attacks is DECOMPOSITION into lower-complexity pieces).** One
Fable-grade decomposition architect (AUTHOR output, hostile-verified before any
codification) produces a case tree over the forced three-body anatomy whose leaves are
minimal independently-attackable lemmas and whose assembly implication is itself a
checkable statement; then parallel codex provers per leaf + fresh hostile verifiers;
E1 registered as a conditional lemma alongside.
