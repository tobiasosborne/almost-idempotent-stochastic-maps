# Aborted row-8 tree (run 1, 2026-08-08) — af status dump

```
=== Proof Status ===

1 [pending/unresolved] After first fixing one global witness package W_RF supplied by lem-routef-raw-factor-setting-formation, for every input (H,Phi,eta) to which that formation result applies, fix one def-routef-raw-factor-setting datum S over that same W_RF supplied by the same result, for every Delta' supplied for (W_RF,S) by lem-routef-delta-prime-closeness, and every Delta supplied from that same Delta' by lem-routef-delta-normalization-closeness, writing the fields of (W_RF,S) as the unqualified symbols below: Upsilon-prime CP closeness: with C_N, C_R, C_L, C_Upsilon' from (1.3) and rho_Upsilon' := min{rho_T, rho_id, rho_Delta, rho_2, rho_3, (2*C_R)^(-1)}, for 0 <= eta <= rho_Upsilon', every Choi multiplicity space used below is nonzero and the componentwise construction produces CP Upsilon' with ||Upsilon' - tilde-Upsilon||_cb <= C_Upsilon'*eta.
  1.1 [validated/unresolved] Ambient and radius consequences: invoke lem-routef-raw-factor-setting-formation to fix the one global W_RF and the datum S, invoke lem-routef-delta-prime-closeness and lem-routef-delta-normalization-closeness for the stipulated DeltaPrime and UCP Delta, and, for 0 <= eta <= rho_UpsilonPrime, derive the amplified lower bound ||Delta_q(X)|| >= (1-C_N*eta)||X||, the bound ||tilde-Upsilon||_cb <= 2, and the exact identity tilde-Phi=tilde-Delta tilde-Upsilon.
    1.1.1 [validated/unresolved] By lem-routef-raw-factor-setting-formation fix W_RF once and then S, and take DeltaPrime and Delta from lem-routef-delta-prime-closeness and lem-routef-delta-normalization-closeness. From def-routef-raw-factor-setting, eta<=rho_UpsilonPrime implies eta<=rho_T,rho_Delta,rho_2,rho_3 and, since rho_T<=rho_theta,rho_AI,epsilon_E/C_A, also eta<=rho_id^corr; hence every named dependency is applicable. In particular lem-routef-raw-factor-identities gives tilde-Phi=tilde-Delta tilde-Upsilon.
    1.1.2 [validated/unresolved] For q>=1 and X in M_q(B), lem-routef-raw-factor-norms and lem-routef-delta-normalization-closeness give ||Delta_q X|| >= ||tilde-Delta_q X||-||Delta-tilde-Delta||_cb||X|| >= (1-(C_V+C_Delta)eta)||X||=(1-C_N eta)||X||. Also lem-routef-raw-factor-norms gives ||tilde-Upsilon||_cb<=1+C_T eta; the two guards eta<=1/[4(1+C_theta)] and eta<=1/[4(1+C_V)] inside rho_T imply C_T eta=(C_theta+3C_V)eta<=1, so ||tilde-Upsilon||_cb<=2.
  1.2 [pending/unresolved] Finite-dimensional Choi and twirl data: write B as the finite direct sum of its nonzero simple blocks B(L_j); obtain finite-dimensional Stinespring families Delta(X)=sum_j W_j^*(X_j tensor I_Ej)W_j and Phi(T)=V^*(T tensor I_F)V; and choose in each B(L_j) an explicit finite Weyl unitary ensemble whose conjugation average sends every operator on L_j tensor E_j into I_Lj tensor B(E_j).
    1.2.1 [validated/unresolved] Because B is a finite-dimensional unital C*-algebra, B is a finite direct sum of full matrix algebras B(L_j) with each L_j nonzero. Complete positivity of the UCP map Delta means each restriction Delta_j is CP; diagonalizing its finite Choi matrix gives finitely many Kraus maps K_jr:H->L_j and hence Delta(X)=sum_j W_j^*(X_j tensor I_Ej)W_j with E_j the Kraus multiplicity space and W_j h=sum_r K_jr h tensor e_r. Unitality gives sum_j W_j^*W_j=I, so every W_j is a contraction (E_j is provisionally allowed to be zero). The identical finite Choi-matrix construction for UCP Phi gives an isometry V:H->H tensor F with Phi(T)=V^*(T tensor I_F)V.
      1.2.1.1 [validated/unresolved] The center of finite-dimensional unital C*-algebra B has finitely many minimal central projections e_j summing to I; each simple ideal e_jB is a full matrix algebra B(L_j) on a nonzero finite-dimensional L_j, giving B=direct-sum_j B(L_j). Restricting a CP map Delta to a direct summand preserves complete positivity.
      1.2.1.2 [validated/unresolved] For a CP map T:B(L)->B(H), its finite Choi matrix Ch(T)=sum_ab E_ab tensor T(E_ab) is positive because it is the image under id tensor T of the positive rank-one matrix sum_ab E_ab tensor E_ab. Diagonalize Ch(T)=sum_r z_r z_r^* and reshape each z_r into K_r:H->L; comparison of matrix entries gives T(X)=sum_r K_r^* X K_r. Applying this to every restriction Delta_j yields W_j and E_j; applying it to Phi yields V and F. The equation T(I)=I is exactly sum_r K_r^*K_r=I, so the assembled V is an isometry and sum_j W_j^*W_j=I.
    1.2.2 [validated/unresolved] For d_j=dim L_j, fix an orthonormal basis and the d_j^2 Weyl unitaries U_jab (cyclic shift times phase), each with weight d_j^(-2). Direct matrix-unit calculation gives d_j^(-2) sum_ab (U_jab^* tensor I_Ej) Z (U_jab tensor I_Ej)=I_Lj tensor C_Z for every Z in B(L_j tensor E_j), where C_Z is the normalized partial trace over L_j. Thus the average lies exactly in the commutant I_Lj tensor B(E_j); it preserves positivity and order because it is a convex average of unitary conjugations.
  1.3 [validated/unresolved] Nonzero-multiplicity repair: for each block construct the positive contraction R_j=average_s (U_js^* tensor I)W_jW_j^*(U_js tensor I)=I_Lj tensor C_j, prove ||C_j|| >= 1-C_R*eta, use eta <= (2*C_R)^(-1) to prove E_j is nonzero, and choose a unit xi_j with 1-<xi_j,C_j^*C_j xi_j> <= 2*C_R*eta.
    1.3.1 [validated/unresolved] Let e_j be the unit of the j-th summand and let hat-U_js be U_js in that summand and zero elsewhere. Twirling W_jW_j^* as in 1.2 gives a positive contraction R_j=I_Lj tensor C_j with 0<=C_j<=I. Moreover W_j^*R_jW_j=average_s Delta(hat-U_js^*)Delta(hat-U_js). Applying lem-routef-degree-two-estimate to each pair (hat-U_js^*,hat-U_js), whose norms are one and whose product is e_j, gives ||Phi(W_j^*R_jW_j)-Delta(e_j)||<=C_2 eta. Contractivity of UCP Phi and of W_j gives ||Phi(W_j^*R_jW_j)||<=||R_j||=||C_j||, while 1.1 gives ||Delta(e_j)||>=1-C_N eta. Hence ||C_j||>=1-(C_N+C_2)eta=1-C_R eta.
    1.3.2 [validated/unresolved] Because eta<=rho_UpsilonPrime<=(2C_R)^(-1), the preceding estimate yields ||C_j||>=1/2. If E_j were zero then B(E_j) would contain only the zero operator and C_j would have norm zero, a contradiction; thus every E_j is nonzero. On finite-dimensional nonzero E_j choose a unit xi_j attaining ||C_j xi_j||=||C_j||. Since 0<=C_j<=I and 0<=C_R eta<=1/2, 1-<xi_j,C_j^*C_j xi_j>=1-||C_j||^2<=1-(1-C_R eta)^2<=2C_R eta.
  1.4 [validated/unresolved] CP component construction: from V,W_j,U_js,xi_j define L_j and UpsilonPrime_j(T)=L_j^*(Phi(T) tensor I_F)L_j; prove ||L_j|| <= 1, each UpsilonPrime_j is CP, and their direct sum UpsilonPrime:B(H)->B is CP with ||UpsilonPrime||_cb <= 1.
    1.4.1 [validated/unresolved] For each j define the isometry iota_js:L_j->L_j tensor E_j by iota_js(z)=U_js z tensor xi_j and set L_j:=average_s (Delta(hat-U_js^*) tensor I_F) V W_j^* iota_js. Every factor in every summand has norm at most one: Delta is UCP and contractive by def-ucp-map, V and iota_js are isometries, and W_j is a contraction by 1.2. The Weyl weights are nonnegative and sum to one, so ||L_j||<=1.
    1.4.2 [validated/unresolved] Define UpsilonPrime_j(T)=L_j^*(Phi(T) tensor I_F)L_j and UpsilonPrime(T)=(UpsilonPrime_j(T))_j. By def-ucp-map, Phi and all its matrix amplifications are positive; tensoring with an identity and compression by L_j preserve positivity, hence each UpsilonPrime_j and the direct sum UpsilonPrime are CP. At every amplification q, UCP contractivity of Phi_q and ||L_j||<=1 give ||(UpsilonPrime_j)_q(X)||<=||X||; the norm on the finite direct sum B=direct-sum_j B(L_j) is the maximum of component norms, so ||UpsilonPrime||_cb<=1 without a block-count factor.
  1.5 [pending/unresolved] Uniform approximate-left-inverse estimate: at every amplification prove ||UpsilonPrime_q Delta_q(Y)-Y|| <= C_L*eta*||Y||, hence ||UpsilonPrime Delta-I_B||_cb <= C_L*eta, using only lem-routef-degree-two-estimate, lem-routef-degree-three-estimate, and the scalar estimate for C_j.
    1.5.1 [validated/unresolved] Because Delta is unital, Delta_q(I)=I. Setting the second input equal to I in lem-routef-degree-two-estimate gives, for every q and Y in M_q(B), ||Phi_q(Delta_q Y)-Delta_q Y||<=C_2 eta||Y||. Since ||L_j||<=1, replacing Phi_q(Delta_q Y) by Delta_q Y inside (UpsilonPrime_j)_q(Delta_q Y) costs at most C_2 eta||Y||.
    1.5.2 [validated/unresolved] Expand the two probability averages defining L_j on both sides of Delta_q Y. The Stinespring identity for Phi converts each (s,t) summand to the compression of Phi_q(Delta_q(hat-U_js) Delta_q(Y) Delta_q(hat-U_jt^*)). By lem-routef-degree-three-estimate this differs, with total probability-averaged error at most C_3 eta||Y||, from the compression of Delta_q(hat-U_js Y hat-U_jt^*). The latter input has only j-th block U_js Y_j U_jt^*. Substituting the exact Stinespring formula for Delta and factoring the two averages yields exactly (I_q tensor J_xi_j)^* (I_q tensor R_j)(Y_j tensor I_Ej)(I_q tensor R_j)(I_q tensor J_xi_j)=Y_j <xi_j,C_j^*C_j xi_j>, where J_xi_j(z)=z tensor xi_j and R_j=I_Lj tensor C_j.
      1.5.2.1 [validated/unresolved] Writing A_js=(Delta(hat-U_js^*) tensor I_F)V W_j^* iota_js, the post-C_2 expression is sum_st p_js p_jt A_js^*(Delta_qY tensor I_F)A_jt. Since A_js^*=iota_js^*W_jV^*(Delta(hat-U_js) tensor I_F), the Stinespring formula for Phi makes this exactly the double average of iota_js^*W_j Phi_q(Delta_q(hat-U_js)Delta_qYDelta_q(hat-U_jt^*))W_j^*iota_jt. Lem-routef-degree-three-estimate replaces the middle term by Delta_q(hat-U_js Y hat-U_jt^*) with error at most C_3 eta||Y|| after averaging, because all outer maps are contractions and the probabilities sum to one.
      1.5.2.2 [validated/unresolved] Now hat-U_js Y hat-U_jt^* has only j-th block U_jsY_jU_jt^*. The Delta Stinespring formula therefore changes the resulting summand exactly into iota_js^*W_jW_j^*(U_jsY_jU_jt^* tensor I_Ej)W_jW_j^*iota_jt. With iota_js=(U_js tensor I)J_xi_j, the independent s- and t-averages factor as J_xi_j^*R_j(Y_j tensor I_Ej)R_jJ_xi_j. Since R_j=I_Lj tensor C_j and C_j is positive, this equals Y_j<xi_j,C_j^*C_jxi_j>; tensoring throughout with I_q proves the same identity at every amplification.
    1.5.3 [pending/unresolved] Combining the C_2 and C_3 replacements with 1-<xi_j,C_j^*C_j xi_j><=2C_R eta from 1.3 gives ||(UpsilonPrime_j)_q(Delta_qY)-Y_j||<=(C_2+C_3+2C_R)eta||Y||=C_L eta||Y||. Taking the maximum over the finitely many direct-sum components and then the supremum over q proves ||UpsilonPrime Delta-I_B||_cb<=C_L eta; probability weights and the maximum norm introduce no multiplicity, block-count, or amplification factor.
      1.5.3.1 [pending/unresolved] Fix q>=1, a block j, and Y in M_q(B), and put alpha_j:=<xi_j,C_j^*C_j xi_j>. By validated node 1.5.1, replacing Phi_q(Delta_q Y) by Delta_q Y in (UpsilonPrime_j)_q(Delta_qY) costs at most C_2 eta||Y||. Validated node 1.5.2.1 identifies the resulting double average and bounds its degree-three replacement error by C_3 eta||Y||, and validated node 1.5.2.2 identifies the replaced average exactly as alpha_j Y_j. Hence the triangle inequality gives ||(UpsilonPrime_j)_q(Delta_qY)-alpha_j Y_j|| <= (C_2+C_3)eta||Y||.
      1.5.3.2 [pending/unresolved] By validated node 1.3.2, 0<=1-alpha_j<=2C_R eta. Since ||Y_j||<=||Y|| for the direct-sum maximum norm, ||alpha_jY_j-Y_j||=(1-alpha_j)||Y_j||<=2C_R eta||Y||. Combining this with the preceding child gives ||(UpsilonPrime_j)_q(Delta_qY)-Y_j|| <= (C_2+C_3+2C_R)eta||Y||=C_L eta||Y||, where the equality is equation (1.3) in def-routef-raw-factor-setting.
      1.5.3.3 [pending/unresolved] For fixed q and Y, the j-th component of (UpsilonPrime Delta-I_B)_q(Y) is (UpsilonPrime_j)_q(Delta_qY)-Y_j. The C*-norm on M_q(B)=direct-sum_j M_q(B(L_j)) is the maximum of the finitely many component norms, so the preceding component estimate gives ||(UpsilonPrime Delta-I_B)_q(Y)||<=C_L eta||Y|| with no sum or block-count factor. Taking the supremum over nonzero Y and then q>=1 yields ||UpsilonPrime Delta-I_B||_cb<=C_L eta; the constants are independent of q.
  1.6 [pending/unresolved] Final telescope: prove ||UpsilonPrime Phi-UpsilonPrime||_cb <= eta and telescope through lem-routef-functional-calculus-closeness, lem-routef-raw-factor-identities, lem-routef-delta-normalization-closeness, and the approximate-left-inverse estimate to obtain ||UpsilonPrime-tilde-Upsilon||_cb <= (1+C_theta+2*C_Delta+2*C_L)*eta=C_UpsilonPrime*eta.
    1.6.1 [validated/unresolved] For every q and T in M_q(B(H)), the component formula gives (UpsilonPrime_j)_q(Phi_q(T))-(UpsilonPrime_j)_q(T)=L_{j,q}^* ((Phi_q^2(T)-Phi_q(T)) tensor I_F)L_{j,q}. Since ||L_j||<=1 and ||Phi^2-Phi||_cb<=eta is part of the input bound in lem-routef-raw-factor-setting-formation, taking the component maximum and amplification supremum yields ||UpsilonPrime Phi-UpsilonPrime||_cb<=eta.
    1.6.2 [pending/unresolved] Use the cb triangle inequality along UpsilonPrime -> UpsilonPrime Phi -> UpsilonPrime tilde-Phi = UpsilonPrime tilde-Delta tilde-Upsilon -> UpsilonPrime Delta tilde-Upsilon -> tilde-Upsilon. The first cost is eta by the preceding step; the second is at most C_theta eta by lem-routef-functional-calculus-closeness and ||UpsilonPrime||_cb<=1; the equality is lem-routef-raw-factor-identities; the fourth cost is at most 2C_Delta eta by lem-routef-delta-normalization-closeness and ||tilde-Upsilon||_cb<=2 from 1.1; and the last is at most 2C_L eta by 1.5 and the same raw norm bound. Therefore ||UpsilonPrime-tilde-Upsilon||_cb<=(1+C_theta+2C_Delta+2C_L)eta=C_UpsilonPrime eta by equation (1.3) of def-routef-raw-factor-setting.
      1.6.2.1 [archived/clean] At every amplification q, validated nodes 1.5.1 and 1.5.2 give, componentwise, an error at most (C_2+C_3)*eta*||Y|| between (UpsilonPrime_j)_q(Delta_q Y) and alpha_j Y_j, where alpha_j=<xi_j,C_j^*C_j xi_j>; validated node 1.3.2 gives |1-alpha_j|<=2*C_R*eta. Hence ||(UpsilonPrime_j)_q(Delta_q Y)-Y_j||<=(C_2+C_3+2*C_R)*eta*||Y||=C_L*eta*||Y||. Taking the direct-sum maximum and then sup_q yields ||UpsilonPrime Delta-I_B||_cb<=C_L*eta. Now for every q and T, the triangle inequality along UpsilonPrime_q(T), UpsilonPrime_q(Phi_q(T)), UpsilonPrime_q(tilde-Phi_q(T))=UpsilonPrime_q(tilde-Delta_q(tilde-Upsilon_q(T))), UpsilonPrime_q(Delta_q(tilde-Upsilon_q(T))), and tilde-Upsilon_q(T) costs respectively eta, C_theta*eta, at most ||UpsilonPrime||_cb*C_Delta*eta*||tilde-Upsilon||_cb<=2*C_Delta*eta, and at most C_L*eta*||tilde-Upsilon||_cb<=2*C_L*eta, using validated nodes 1.6.1, 1.4.2, 1.1.1, and 1.1.2 together with the allowed functional-calculus and Delta-normalization lemmas. Thus ||UpsilonPrime-tilde-Upsilon||_cb<=(1+C_theta+2*C_Delta+2*C_L)*eta=C_UpsilonPrime*eta by (1.3).
      1.6.2.2 [pending/unresolved] At every amplification q, validated node 1.5.1 bounds by C_2*eta*||Y|| the replacement of Phi_q(Delta_qY) by Delta_qY inside each UpsilonPrime component; validated node 1.5.2.1 bounds the subsequent degree-three replacement by C_3*eta*||Y||; and validated node 1.5.2.2 identifies the resulting double average exactly as alpha_j Y_j, where alpha_j=<xi_j,C_j^*C_j xi_j>. Validated node 1.3.2 gives |1-alpha_j|=1-alpha_j<=2*C_R*eta. Hence ||(UpsilonPrime_j)_q(Delta_qY)-Y_j||<=(C_2+C_3+2*C_R)*eta*||Y||=C_L*eta*||Y||. Taking the direct-sum maximum and then sup_q yields ||UpsilonPrime Delta-I_B||_cb<=C_L*eta. For every q and T, telescope through UpsilonPrime_q(T), UpsilonPrime_q(Phi_q(T)), UpsilonPrime_q(tilde-Phi_q(T))=UpsilonPrime_q(tilde-Delta_q(tilde-Upsilon_q(T))), UpsilonPrime_q(Delta_q(tilde-Upsilon_q(T))), and tilde-Upsilon_q(T). Validated nodes 1.6.1, 1.4.2, 1.1.1, and 1.1.2, together with the allowed functional-calculus and Delta-normalization lemmas, bound the four nonzero costs by eta*||T||, C_theta*eta*||T||, ||UpsilonPrime||_cb*C_Delta*eta*||tilde-Upsilon||_cb*||T||<=2*C_Delta*eta*||T||, and C_L*eta*||tilde-Upsilon||_cb*||T||<=2*C_L*eta*||T||. Taking suprema gives ||UpsilonPrime-tilde-Upsilon||_cb<=(1+C_theta+2*C_Delta+2*C_L)*eta=C_UpsilonPrime*eta by (1.3).

--- Statistics ---
Nodes: 29 total
  Epistemic: 0 draft, 10 pending, 18 validated, 0 admitted, 0 refuted, 1 archived, 0 needs_refinement
  Taint: 1 clean, 0 self_admitted, 0 tainted, 28 unresolved

--- Jobs ---
  Prover: 10 nodes awaiting refinement
  Verifier: 0 nodes ready for review

--- Legend ---
Epistemic States:
  draft      - Work in progress (not submitted)
  pending    - Awaiting proof/verification
  validated  - Verified by adversarial verifier
  admitted   - Accepted without full verification
  refuted    - Proven false
  archived   - Superseded or abandoned
  needs_refinement - Reopened for further refinement

Taint States:
  clean         - No epistemic uncertainty
  self_admitted - Contains admitted node
  tainted       - Depends on tainted/refuted node
  unresolved    - Taint status not yet computed
```
