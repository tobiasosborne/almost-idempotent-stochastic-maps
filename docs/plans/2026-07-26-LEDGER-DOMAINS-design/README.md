# GAP-LEDGER-DOMAINS design wave (2026-07-26, session 29 — de-risk campaign, front #3)

Working directory for the LEDGER-DOMAINS design job (fresh codex gpt-5.6-sol
xhigh). De-risk front #3 of `../2026-07-26-critical-path-risk-register.md`:
the fourteen withdrawn K-ledger rows, each needing a dependency-produced
local radius (the η_A-overuse defect, v3 verdict BLOCKER 2.3). Deliverable:
the closed local-domain DAG — fourteen replacement contracts with derived
radii — per `BRIEF-LEDGER-DOMAINS.md`. Runs in parallel with the front-#1
(polar) and front-#2 (MAIN-structure) design jobs; separate directories.

A SEPARATE fresh codex hostile audit follows before anything lands (GAP-EA
pattern).

Pre-created by the orchestrator so the design job's writes collapse into one
untracked-dir porcelain line and do not trip concurrent-run overreach guards.
