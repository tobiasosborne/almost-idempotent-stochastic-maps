<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 15 (session 12) — THE
  CANONICAL STRATEGIC MAP. Supersedes v14 (kept for line citations) after the Fable deep
  proofs W49F (9b0dade + 52b41e3), executing the pre-committed theatre-guard pivot.
  DELTA redraw: everything not restated here is UNCHANGED from v14.
STEWARDSHIP CONTRACT (binding): as in v10-v14. STATUS DISCIPLINE (L0): a SKETCH — promotes nothing.
-->

# Top-down proof sketch v15: op-classical (2026-07-07, session 12, round 5)

## UNCHANGED from v14

Theorem; trunk; the endgame stack's lower half; Kernel(i) rank >= 3; 28 af-validated;
elevation queue PARKED; USER DECISIONS aism-nlg, aism-z98; <2>7 factoring aism-ik6.

## THE PIVOT EXECUTED (W49F): ONE FACE, TWO ATTACK MODES

[[prop-f2-t1-equivalence]] (VFP2-verified) proves the two v14 faces were ONE: (F2) is the
(T1) in-class intersection horn in contrapositive form; the exposure horn is VOID in tall
regimes ([[lem-ball-cluster-exposure-void]]). The single remaining unscoped mathematics is
the IN-CLASS INTERSECTION HORN of [[conj-zero-face-elimination]], attackable two ways:

- **(M1) THE RANK-3 REACH INEQUALITY** ([[conj-rank3-cluster-zero-face-reach]], width-4
  form): via the VFP1-verified anatomy chain ([[lem-rank3-zero-face-anatomy]] — disjointness
  has ONE rigid shape; [[lem-rank3-zero-face-min-mass]] — A_min = gap_2/reach EXACT,
  reproducing the banked 100; [[lem-rank3-supporting-functional-pinning]] — co-top forcing;
  [[lem-rank3-downhill-dichotomy]]), this ONE inequality closes the node at rank 3.
  Rank-3-scoped; the general-rank step remains the separate named price (v14).
- **(M2) TALL-EMPTINESS**: NO in-class instance has ever been realized (FINDINGS W49F —
  all census H^2/delta < 16; every refuter family dies at the antecedent; certified
  absorption kills tall attempts). Proving the class empty — i.e. excluding the HUDDLE
  ([[lem-zero-face-vertex-support]]: two mutually-exempt deep hidden vertices within
  4*tau) — IS the Kernel height clause directly, rank-free. The huddle anatomy + the
  exchange identity ([[lem-zero-face-exchange-identity]]) are the handles.

**Decisive data in flight:** W52-BH, the pre-registered tall-entry experiment (can ANY
exact perturbation enter the class; binding-constraint anatomy either way).

## CLOSED this round (by certificate, cumulative with v14)

Budget-class attacks (obs-gamma-two-level-class-count-wall, obs-gamma-capacity-scale-blind);
ledger-only (F2) proofs (obs-thin-zero-face-blocker-graft); the exposure horn; the (F2)
constant-hunt (c0 was a certificate of emptiness); the B4 bypass; spectral imports (W50).

## THE OPEN LEDGER v15

1. The in-class intersection horn — via (M1) the reach inequality or (M2) tall-emptiness.
   THE only unscoped mathematics.
2. The rank-3 -> general-rank step (named price if (M1) is the route; VOID if (M2) lands,
   since tall-emptiness is rank-free).
3. Kernel(i) rank >= 3; trunk <2>7 factoring; elevation queue (all PARKED, Tier 2/3 —
   farm-out targets per the user's directive once Tier 1 falls).
4. USER DECISIONS: aism-nlg, aism-z98.
