<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 38 (session 33
  final delta: the Stage-1 binder sweep — four more retractions, T0 101;
  fourteen Stage-1 trees re-certified sound; the W97 campaign is now the
  single explicit-binder rebuild of the whole defective set). Supersedes
  v37; everything not restated here is unchanged from v37.
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing.
  op-classical remains OPEN. T0 = 101; nothing below is af-validated unless
  said so.
-->

# Top-down proof sketch v38: op-classical (2026-07-28, session 33 final delta — the binder sweep: T0 101; six total retractions, fourteen Stage-1 trees re-certified; W97 = the explicit-binder rebuild)

## UNCHANGED from v37

Everything except the items below; in particular the Route-F chain
through F3 and all non-Stage-1 T0 results are untouched by the sweep.

## Map change 1: the comprehensive Stage-1 binder sweep (T0 105 → 101)

A fresh sweep adjudication over ALL 18 remaining Stage-1 polar T0 exports
(`docs/plans/2026-07-28-13E-BINDER-design/SWEEP-ADJUDICATION-STAGE1.md`)
settled the untyped-map defect class in one pass:

- **Four more RETRACTED** (statuses demoted, LEARNINGS second 2026-07-28
  entry, report shards 47/49/50/51 corrected): `approximate-group-laws`
  (was 95th; node 1.1.2), `smooth-unitary-operations` (was 100th; nodes
  1.2.1–1.2.2/1.3.1.2/1.3.2), `maurer-cartan-transport` (was 104th; node
  1.3.3 — a sound bypass exists in-tree, pruning + revalidation
  suffices), `polar-path-transport` (was 106th; node 1.3.1).
- **Fourteen CERTIFIED SOUND** — the entire typed backbone survives:
  rectified-cstar-control, unitary-graph-control,
  maurer-cartan-trivialization, polar-retraction,
  polar-coherence-naturality, group-domain-membership, group-closeness,
  polar-path-admissibility, smooth-unitary-atlas, smooth-polar-inverse,
  polar-scalar-arithmetic, and transports 13a/13b/13d.
- **Cascade CLOSED**: with the two v37 retractions independently
  re-confirmed, the six demoted results have no further validated
  descendants. Total Stage-1 damage: 6 of 20 trees; all six CONTRACTS
  undisputed.
- **Root cause (recorded):** the elevating cohorts treated repeated
  notation / definite descriptions as binder unification across opaque
  theorem boundaries. Ordinary uniqueness remains valid in
  single-construction chains — exactly why fourteen trees survive.

## Map change 2: the W97 campaign is now the single rebuild of the defective set

Scope for design v3 (supersedes the v37 statement), all on the
explicit-binder / typed-witness law:

1. The two quantitative bridge rows (unchallenged through two audits).
2. The explicit smooth-operations bridge (audit-v2 finding 1).
3. Direct explicit-binder re-derivations: group-laws parent (bypass, per
   the sweep), control, 13e (sans coherence-naturality), 13g — with the
   audit-v2 finding-2 repair (fixed base witness tuple W_0, monotonicity
   transport; never cite an unbounded receiving coefficient through an
   opaque contract).
4. Cheap repairs where the sweep licensed them: maurer-cartan-transport
   (prune node 1.3.3, revalidate on the in-tree bypass);
   polar-path-transport (typed-witness re-derivation against the SOUND
   13d import, mirroring the sound half of the old 13g tree).
5. The rows-14+ deps cleanups of design v2 §1.7 plus the
   quotient-manifold-package decision forced by the smooth-operations
   retraction (audit-v2 finding 1's prescribed repair).

## The open surface after this delta

1. Design v3 (fresh codex, the full sweep + both audits in its brief) →
   fresh hostile audit → land → the serial elevation queue (now ~8
   workspaces) → row 13 → the v36 queue.
2. The user has been given the honest interpretation: formalization-layer
   synchronization gaps plus one genuine quantitative catch (the
   unbounded receiving coefficient); no contract and no estimate is
   mathematically disputed; the re-derivability claim is itself untested
   until the queue validates.
