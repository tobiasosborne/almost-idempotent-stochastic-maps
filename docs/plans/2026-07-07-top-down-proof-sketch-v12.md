<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 12 (session 12) — THE
  CANONICAL STRATEGIC MAP. Supersedes docs/plans/2026-07-07-top-down-proof-sketch-v11.md
  (kept intact for line citations) after W44 (db8ee54) + W45/W46 (same commit) + elevation
  #26 (e0478c6). v12 is a DELTA redraw: everything not restated here is UNCHANGED from v11.
STEWARDSHIP CONTRACT (binding): as in v10/v11. STATUS DISCIPLINE (L0): a SKETCH — promotes nothing.
-->

# Top-down proof sketch v12: op-classical (2026-07-07, session 12)

## UNCHANGED from v11

Theorem; the trunk BELOW <2>7; the endgame stack's LOWER half (conj-low-slab-cap ⇒
[pincer #21 RIGOROUS] ⇒ [collapse #17 RIGOROUS] ⇒ height clause ⇒ reviewed chain ⇒
op-classical); Kernel(i) rank >= 3. Rigour ladder NOW: **26 af-validated** (#26
lem-depth-d-halo-collapse — the collapse engine now L0 at EVERY row; elevation #27
lem-row-zero-capacity in flight); registry 92.

## THE TERMINAL NODE, EXACT FORM (W44; supersedes v11's (T1)/(T2) prose)

The (T1) intersection question is now a THEOREM-SHAPED conic statement,
[[conj-zero-face-elimination]]: at every mass-carrying cluster vertex u of a tall heavy
near-cluster top, either the always-tight hulls meet (all-a_z = 0 display) or the zero-face
conic mass is universally bounded (sum a_z <= A_0). Four reviewed bricks (W44, L5) pin it:

- [[lem-always-tight-dual-support]] — witnesses live ONLY on the always-tight T/O/Z;
  the alpha-carriers ARE the zero-face rows Z. (Two independent provers, one proof.)
- [[lem-optimal-face-conic-reduction]] — optimal witnesses = conic displays
  sum_T lambda d_f + sum_Z a_z d_z = t*·sum_O gamma d_i; (T1) == kill/bound the a_z.
- [[lem-tight-far-geography]] — the LOCATED families: witness far-mass concentrates on
  rho-far TOP-SLAB rows (unconditional at tops; K = 1/2+delta+4(1+A_0) at bounded-alpha
  cluster vertices); O-displacements are (1/2+delta)tau-small after t*-scaling.
- [[lem-separator-zero-face-obstruction]] — if the hulls DON'T meet, the failure is a
  P-harmonic affine direction blocked ONLY by a nonclone zero-face row with negative value.
  (T2) == charge that blocker (ship mass out of the cluster or expose a cluster vertex).

Hypotheses are load-bearing in exactly the right direction: empty intersections are
REALIZED only outside the tall-heavy class (W44-AU exact rank-3 census, pending VAU
recomputation; obs-realized-alpha-blowup needs alpha = 100 at H/tau = 1/505), and hold with
exact convex certificates on every banked in-class instance.

**Attack routes (priced):** (a) located-hulls interlacing (geography + a Helly/planar step;
rank-3 first — the named lem-rank3-cluster-uniform-optimal-face-interlacing candidate);
(b) charge the separator blocker via harmonicity + the cluster coefficient ledger (the
hybrid-circuit object); (c) the Fable ideation portfolio (in flight) for orthogonal
mechanisms.

## RESOLVED-TO-DECISION in this redraw (new; cumulative retired list in v8-v11)

28. **loose-delta (W46): RETIRABLE, not open math.** Every consumer runs pinned at
    d = delta(P); fix = op-exposed-hull contract re-wording — **USER DECISION aism-nlg**.
    If declined, it returns to the ledger as genuinely open.
29. **The <2>6 Q-not-stochastic interface fear (W45): does NOT bind** — <2>7 needs only
    row-distance-to-hull. Residual <2>7 risk is priced: G2/G3 factoring (aism-ik6, routine
    per auditor), G4 JB restatement (inside aism-nlg).

## THE OPEN LEDGER v12

1. [[conj-zero-face-elimination]] — THE terminal node, the ONLY unscoped mathematics.
2. Kernel(i) rank >= 3 (shares the mechanism; rides route (a)'s rank-3 case).
3. Trunk <2>7 factoring (aism-ik6; priced routine) — after USER DECISION aism-nlg.
4. Elevation queue: #27 in flight; then lem-always-tight-dual-support,
   lem-optimal-face-conic-reduction, lem-optimal-face-alpha-free-characterization,
   lem-separator-zero-face-obstruction, lem-tight-far-geography (dep chain note: geography
   needs bounded-alpha-top-slab-reduction elevated first).
5. USER DECISIONS pending: aism-nlg (contract re-wordings), aism-z98 (standing).
