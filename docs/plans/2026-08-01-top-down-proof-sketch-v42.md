# Top-down proof sketch v42: op-classical (2026-08-01, session 39 — the aism-jl4g gate DISSOLVED; the MAIN campaign advanced to 26 of 31+4 rows T0 (T0 144 → 156 net); three latent certificate gaps caught and retracted same-day)

## UNCHANGED from v41

The global architecture (Route F via positive approximate retract), the
complete Stage-1 record, the parked arms, the parallel-af discipline
(worktrees, serial banking, scoped cap amendments), and the honest headline
(`op-classical` OPEN) all stand.

## Map change 1: the two-defect repair (aism-jl4g) designed, audited, ratified, and PROVEN THROUGH

Five user ratifications this session (all in-session, dated 2026-08-01):
1. **The unit-clause thread + witness ledger** (`DESIGN-MAINCB-REPAIR-v2.md`,
   audit F1–F3 applied verbatim): NEW `def-maincb-witness-ledger` (locked,
   data-only, 12 fields), 3 new result rows, 17 amended contracts.
2. **The recorded-field ENV repair** (`DESIGN-RECFIELD-REPAIR.md`): a
   validated M_2 countermodel refuted the pre-ENV M19-S2 wording (the
   recorded epsilon_U of a supplied state is a free datum); 6 contracts
   repaired with explicit ENV hypotheses + M04-certificate selection.
3. **The M17 typed-quantification + bijectivity bridge**
   (`DESIGN-M17-TYPING-v3.md`): NEW `lem-maincb-cross-datum-bijectivity`
   (corner-typed explicit maps) + typed M17 + the M26 bridge import.
4. **The monotonicity micro-row** (`DESIGN-M18-MONOTONE.md`): NEW
   `lem-maincb-extended-inclusion-monotone` (the L4-factored fact M18's
   balloon re-derived per stage map).
5. **The consumer-chain repair** (`DESIGN-CONSUMER-REPAIR.md` + audit):
   M26/M27 one-dimensional-images hypothesis RESTORED; NEW
   `lem-maincb-reset-output-typing` (the typed reset provider — M19-R and
   M18 byte-unchanged); THREE latent-gap demotions (map change 3).

## Map change 2: TWELVE net new T0 rows (152nd–168th bank events; T0 144 → 156)

Banked through the full external-oracle sequence:
`lem-maincb-isomorphism-unit-control` (trivialized by audit F1:
C_iso_unit=1), `lem-maincb-witness-arithmetic`, M19-R
`lem-maincb-reset-invariant-preservation` (the RI+UI reset row, 12/12),
M16 `lem-maincb-stage2-raw-extension`,
`lem-maincb-compressed-corner-unit-comparison` (the bridge the parked M12
cohort demanded), M19-S1 `lem-maincb-stage1-call-envelope` (stalled x3
pre-repair; first-pass under the typed ledger), M12
`lem-maincb-cross-class-merging-datum` (the ORIGINAL aism-jl4g blocker,
closed by the ratified unit thread), M19-S2
`lem-maincb-stage2-call-envelope` (ENV form; re-validated after the F5
demotion with the explicit monotonicity import), M17
`lem-maincb-stage3-raw-merge` (typed form, 6/6 on the exact design
budget), M18 `lem-maincb-reset-constant-ledger` and M20
`lem-maincb-structural-domain-ledger` (banked, currently SUSPENDED — map
change 3), `lem-maincb-cross-datum-bijectivity`,
`lem-maincb-extended-inclusion-monotone`,
`lem-maincb-reset-output-typing`.
Registry 341 → 347 (6 new rows, all user-ratified).

## Map change 3: THREE latent certificate gaps caught, RETRACTED, one already cured (docs/LEARNINGS.md 2026-08-01)

The layered machinery (design codex allegation → independent hostile-audit
confirmation by reading the exports → linker propagation) established that
three same-day banked certificates used inferences underivable from their
REGISTERED premises: M25 (bijective=>isomorphism without the
extended-inclusion typing), M19-S2 and M19-S3 (unimported monotonicity).
All three were demoted proved->stated with a LEARNINGS retraction entry;
the linker's L0 propagation then honestly SUSPENDED M18/M20 (their own
certificates are intact; workspaces preserved; they re-flip mechanically
once S2/S3 re-bank). M19-S2 was RE-VALIDATED same-day (10/10 first-pass
with the explicit import). No contract was refuted — the statements stand;
the certificates were the defect. Nothing downstream was ever banked on
the flawed certificates.

## The open surface after this delta

- **PENDING RE-VALIDATIONS (the only path work):** M19-S3 (parked ~15-19/24;
  corner-envelope export-scope + monotonicity-typing applications need a
  re-architected tree) and M25 (parked; single-typed-provider witness
  discipline + explicit induction deps). Their re-banks re-flip M18/M20
  mechanically (T0 156 → 159 without new mathematics).
- **THEN the consumer chain:** M21, M23 (re-seed with the typed-reset
  provider per AUDIT-CONSUMER-REPAIR F6 wiring — deps already landed),
  M22, M24, M26, M27, M28 (the structural assembly). All contracts are
  ratified and audit-stable; no design round is known to be pending.
- **THEN** the escalated `lem-thmainext-conditional` rewire, the decoupled
  campaigns (14-row ledger, k-ledger, f0-assembly), and the root rewire
  LAST — unchanged from v41.

## Controller note

fr arm FH carried the whole session (~30 pulls/dispatches, W99–W112).
The interface-defect catch rate (5 contract-level findings + 3 certificate
retractions in one session, all verifier- or audit-established, zero
self-judged) is the designed behavior of the adversarial pipeline; the
exact-input standard is now demonstrably enforced by the newer verifier
cohorts, which is why re-validations run stricter than the originals.
