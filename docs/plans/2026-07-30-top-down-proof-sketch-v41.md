# Top-down proof sketch v41: op-classical (2026-07-30, session 38 part 2 — the MAIN campaign OPENED under parallel-af: 18 of 31 rows T0 (T0 130 → 144); two systemic interface defects isolated and escalated)

## UNCHANGED from v40

The global architecture (Route F via positive approximate retract), the
complete Stage-1 record (S1-ENDGAME 13/13, G-S1 discharged), the parked
arms, and the honest headline (`op-classical` OPEN) all stand.

## Map change 1: the full MAIN row package LANDED and the campaign ran under the user-ratified parallel-af architecture

- **P0 was already discharged** (the four defs were ratified+locked
  2026-07-27, W79 D2; the aism-dm8n HARD STOP was stale). The user
  re-ratified the full package 2026-07-30; all 30 result rows (M01,
  M02, M04–M18, M19-S1/S2/S3/R, M20–M28) landed VERBATIM (registry
  311→341), M03's deps rewired per design sect-10.
- **Parallel-af ROLLED OUT** (user-ratified): detached-worktree
  orchestrations (up to 5 concurrent), serial banking in the main
  checkout (fr's absolute-path oracle contract respected — the
  aism-2kyc migration is unnecessary). The pipeline banked its first
  result (M14) end-to-end and then carried the whole campaign.

## Map change 2: EIGHTEEN MAIN rows now T0 (137th–151st rigorous results; T0 130 → 144)

Banked this session, every one through the full external-oracle
sequence: **M14, M01, M06, M10, M02 (twice: the weaker form, then the
user-ratified STRENGTHENED full-inclusion form), M07 (the load-bearing
telescope — both directions hostile-verified, the design's declared
gap-stop CLEARED), M05, M08, M15, M11, M09, M03 (IMPROVE-CB, the
consumer gate — closed on run 4 after the M02 strengthening), M04,
M13 (twice: a VACUOUS validation honestly flagged, then the
NON-VACUOUS form under the user-ratified def amendment).**

Process instruments that carried it: verifier-driven vocabulary
completion (the systematic missing def-extended-epsilon-cstar-algebra
import, 26 shards); deps-only widenings (13e precedent, contracts
byte-unchanged); clean re-seeds for tangled trees (M03, M09, M12, M13,
M19-S1 — patched trees thrash, fresh builds close); scoped cap
amendments classified transparent repair growth (flagged per
bc3ca739); the new GT external `GT-kitaev-standard-diagonal`
(tex:1245-1254, locus-trap PASS).

## Map change 3: THREE contract-level interface defects caught by the adversarial pipeline (two resolved by user ratification, one open)

1. **M02 under-export** (RESOLVED): 'has extended defect at most
   K_floor*epsilon' does not supply full-inclusion norm bounds; three
   verifier cohorts + a 26-node internalization rebuild forced it; the
   user-ratified one-line strengthening closed M03 as the designed
   consumer.
2. **Partition-state semantics** (RESOLVED): the locked 'one current
   union U of classes' field made the M13/M19-S2/M25 partial-class
   hypotheses UNSATISFIABLE — M13 validated VACUOUSLY (banked with a
   loud flag, then superseded). User-ratified amendment: 'one current
   nonempty subset U of J'. M13 re-elevated non-vacuously (151st).
3. **OPEN — the aism-jl4g design-repair package (P0, next session's
   first work):** (a) the diagonal-unit clause of
   def-four-corner-merging-datum is underivable from extended
   isomorphisms (def-extended-delta-inclusion has NO unit clause);
   the unit estimate must thread M12→M19-S3→M26/M25/M19-R. (b) The
   W93 anaphoric-constant pattern: M19-S1/M20–M28 use c_0^cb, L, e_1,
   e_s2, K_1..3, K_call, epsilon_MAIN as unquantified anaphors — the
   exact defect class Stage-1 solved with def-stage1-polar-witness-data;
   MAIN needs its own typed witness-ledger def + rebound contracts.
   M12 parked at 9/10 (unit clause only); M19-S1 parked at 15/17
   (ledger nodes only); both trees preserved in main.

## The open surface after this delta

1. **aism-jl4g (P0):** fresh-codex design + hostile audit for the
   two-defect repair package (unit-clause thread + maincb witness
   ledger), then user ratification, then: M12/M19-S1 completions,
   M16, M17, M19-S2/S3/R, M20–M28. Everything left funnels through it.
2. On M28 + M19-R: the escalated `lem-thmainext-conditional` rewire
   (design sect-10 step 15) — the Kitaev main-theorem bridge.
3. Then the decoupled campaigns (14-row ledger, k-ledger, f0-assembly,
   root rewire LAST) toward `op-classical`.
4. Report sync `aism-9kmt` (now banks 120–151, ~31 results).

## Controller note

Session 38 total: **22 af-validated banks** (13 Stage-1 close-out was
session 37; this session: 3 S1-ENDGAME C-rows + 18 MAIN + the
strengthened/non-vacuous re-elevations), 3 design-level defects
surfaced, zero unsound claims banked (the one vacuous validation was
flagged at bank time and superseded). The parallel-af architecture is
production-quality: worktree isolation held across ~20 concurrent
codex workers; the single failure (an orphaned launch via an inner
'&') was caught pre-ledger-write. The binding next step is ONE design
round (aism-jl4g), not elevation capacity.
