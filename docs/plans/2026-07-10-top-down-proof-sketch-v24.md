<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 24 (af-elevation delta).
  Supersedes v23; everything not restated here is unchanged from v23.
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing.
-->

# Top-down proof sketch v24: op-classical (2026-07-10, af-elevation delta)

## UNCHANGED from v23

Everything: registry 151, the three-cell SL1a surface, the W55-W59 gadget arc, all
dead routes, the Tier-1 order (generalization wave toward the H-X tableau first).

## Map change: lem-starvation-completion-obstruction is af-VALIDATED (T0)

**Rigorous (af-validated) count: 28 -> 29.** The af orchestration validated the root
in 3 rounds (7 nodes, all validated, taint clean); `fr verify` PASS against the af
oracle (▣). The K-free completion obstruction is now at the SAME rigour rung as the
trunk results — the first T0 result of the H-X / large-gauge front, and the terminal
artifact of the W55-W59 arc: threat named -> exact kills -> paper proof -> af oracle.

Tier-1 order unchanged: the generalization wave (slab confinement / rank / the H-X
tableau constants) is the live item; its target is now anchored to a T0 lemma.
