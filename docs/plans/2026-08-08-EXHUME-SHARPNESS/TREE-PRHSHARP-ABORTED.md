=== Proof Status ===

1 [[33mpending[0m/[33munresolved[0m] PRH square-root sharpness: for every 0 < lambda < 1/2 there are positive unital maps A:l-infinity(2)->l-infinity(4) and M:l-infinity(4)->l-infinity(2) with epsilon_lambda=||MA-I_2||_{infinity->infinity}=2*lambda^2 tending to 0 such that every stochastic idempotent F on l-infinity(4) satisfies ||AM-F||_{infinity->infinity} >= lambda=sqrt(epsilon_lambda/2); hence the sqrt(epsilon) order in PRH is intrinsically sharp.
  1.1 [[32mvalidated[0m/[33munresolved[0m] Fix an arbitrary real lambda with 0 < lambda < 1/2. Define A (rows indexed 1,...,4) by A_1=(1,0), A_2=(0,1), A_3=(1-lambda,lambda), A_4=(lambda,1-lambda), and define M (rows indexed 1,2) by M_1=(1-lambda,0,lambda,0), M_2=(0,1-lambda,0,lambda). All entries are nonnegative and every row sums to 1, so A:l-infinity(2)->l-infinity(4) and M:l-infinity(4)->l-infinity(2) are positive unital maps in the sense of def-positive-approximate-retract.
    1.1.1 [[32mvalidated[0m/[33munresolved[0m] From 0<lambda<1/2, lambda and 1-lambda are strictly positive, so every displayed entry of A and M is nonnegative. The A-row sums are 1, 1, (1-lambda)+lambda=1, and lambda+(1-lambda)=1, and each M-row sum is (1-lambda)+lambda=1. Thus all rows are probability vectors, which by def-positive-approximate-retract means that these matrices represent positive unital maps between the stated finite l-infinity spaces.
    1.1.2 [[90marchived[0m/[32mclean[0m] The four row sums of A are 1, 1, (1-lambda)+lambda=1, and lambda+(1-lambda)=1; the two row sums of M are likewise (1-lambda)+lambda=1.
    1.1.3 [[90marchived[0m/[32mclean[0m] By def-positive-approximate-retract, matrices with probability-vector rows represent positive unital maps between the stated finite l-infinity spaces; therefore these A and M have the required types and positivity/unitality.
  1.2 [[32mvalidated[0m/[33munresolved[0m] For every real m-by-n matrix B acting from l-infinity(n) to l-infinity(m), ||B||_{infinity->infinity}=max_i sum_j |B_ij|.
    1.2.1 [[32mvalidated[0m/[33munresolved[0m] For x in R^n, |(Bx)_i| <= (sum_j |B_ij|)*||x||_infinity for every i; hence ||Bx||_infinity <= (max_i sum_j |B_ij|)*||x||_infinity and ||B||_{infinity->infinity} is at most the maximum absolute row sum.
    1.2.2 [[32mvalidated[0m/[33munresolved[0m] Choose a row i_0 attaining the finite maximum and choose x_j=1 when B_{i_0j}>=0 and x_j=-1 when B_{i_0j}<0. Then ||x||_infinity=1 and (Bx)_{i_0}=sum_j |B_{i_0j}|, so ||B||_{infinity->infinity} is at least the maximum absolute row sum.
  1.3 [[33mpending[0m/[33munresolved[0m] For the A and M constructed in 1.1, MA has rows (1-lambda^2,lambda^2) and (lambda^2,1-lambda^2); consequently epsilon_lambda=||MA-I_2||_{infinity->infinity}=2*lambda^2.
    1.3.1 [[33mpending[0m/[33munresolved[0m] Direct multiplication using 1.1 gives (MA)_1=(1-lambda)A_1+lambda A_3=(1-lambda^2,lambda^2) and (MA)_2=(1-lambda)A_2+lambda A_4=(lambda^2,1-lambda^2).
      1.3.1.1 [[33mpending[0m/[33munresolved[0m] Using the row definitions in node 1.1, the first row of M has nonzero coefficients M_11=1-lambda and M_13=lambda, while the second has M_22=1-lambda and M_24=lambda. Hence matrix multiplication gives (MA)_1=(1-lambda)A_1+lambda A_3 and (MA)_2=(1-lambda)A_2+lambda A_4. Substituting A_1=(1,0), A_2=(0,1), A_3=(1-lambda,lambda), and A_4=(lambda,1-lambda), one obtains (MA)_1=((1-lambda)+lambda(1-lambda),lambda^2)=(1-lambda^2,lambda^2) and (MA)_2=(lambda^2,(1-lambda)+lambda(1-lambda))=(lambda^2,1-lambda^2).
    1.3.2 [[33mpending[0m/[33munresolved[0m] Thus MA-I_2 has rows (-lambda^2,lambda^2) and (lambda^2,-lambda^2); by the norm identity 1.2 both absolute row sums are 2*lambda^2, so ||MA-I_2||_{infinity->infinity}=2*lambda^2.
  1.4 [[32mvalidated[0m/[33munresolved[0m] Writing P=AM for the A and M from 1.1, its first and third rows are p_1=(1-lambda,0,lambda,0) and p_3=((1-lambda)^2,lambda*(1-lambda),lambda*(1-lambda),lambda^2), and ||p_1-p_3||_1=2*lambda.
    1.4.1 [[32mvalidated[0m/[33munresolved[0m] Because A_1=(1,0) and A_3=(1-lambda,lambda), direct multiplication yields p_1=A_1M=M_1=(1-lambda,0,lambda,0) and p_3=A_3M=(1-lambda)M_1+lambda M_2=((1-lambda)^2,lambda(1-lambda),lambda(1-lambda),lambda^2).
    1.4.2 [[32mvalidated[0m/[33munresolved[0m] Coordinate subtraction gives p_1-p_3=(lambda(1-lambda),-lambda(1-lambda),lambda^2,-lambda^2); since lambda>0 and 1-lambda>0, its l1 norm is 2*lambda*(1-lambda)+2*lambda^2=2*lambda.
  1.5 [[32mvalidated[0m/[33munresolved[0m] Row-coincidence lemma: if F is a finite row-stochastic idempotent matrix and F_ii>0 and F_ij>0, then row_i(F)=row_j(F).
    1.5.1 [[32mvalidated[0m/[33munresolved[0m] For a row-stochastic idempotent F, induction from F^2=F gives F^r=F for every integer r>=1. Hence, in the directed support graph (edge a->b iff F_ab>0), every positive directed path a=i_0->i_1->...->i_r=b forces F_ab=(F^r)_ab >= product_{s=0}^{r-1} F_{i_s i_{s+1}}>0.
    1.5.2 [[32mvalidated[0m/[33munresolved[0m] If F_aa>0, every b with F_ab>0 can reach a in the support graph. Indeed, otherwise let T be all vertices reachable from a that cannot reach a and put q=sum_{t in T}F_at; then q>0, T is forward closed, and row-stochasticity gives sum_{t in T}(F^2)_{at} >= q+F_aa*q>q=sum_{t in T}F_at, contradicting F^2=F.
    1.5.3 [[32mvalidated[0m/[33munresolved[0m] Under F_ii>0 and F_ij>0, vertices i and j lie in one communicating class C. Every a in C has F_aa>0 (use a positive cycle and 1.5.1), every positive transition from a returns to a by 1.5.2, so C is closed; moreover 1.5.1 shows F_ab>0 for every a,b in C.
    1.5.4 [[32mvalidated[0m/[33munresolved[0m] Finite maximum principle: if C is a closed support class with F_ab>0 for all a,b in C, then all rows indexed by C are equal. For each fixed column k, h_a=F_ak satisfies h_a=sum_{b in C}F_ab*h_b by F^2=F and closedness. At a maximizer a_0 of h on finite C, this is a convex average with every weight F_{a_0b}>0, so every h_b equals h_{a_0}; doing this for each k proves row_a(F)=row_b(F) for all a,b in C.
    1.5.5 [[90marchived[0m/[32mclean[0m] Applying 1.5.3 and 1.5.4 to the class C containing i and j yields row_i(F)=row_j(F), proving the row-coincidence lemma.
  1.6 [[33mpending[0m/[33munresolved[0m] For every stochastic idempotent F on l-infinity(4), the constructed P=AM satisfies ||P-F||_{infinity->infinity} >= lambda.
    1.6.1 [[32mvalidated[0m/[33munresolved[0m] Assume for contradiction that d=||P-F||_{infinity->infinity}<lambda. By 1.2, each row has l1 error at most d; using p_1=(1-lambda,0,lambda,0) from 1.4 gives F_11 >= 1-lambda-d > 1-2*lambda > 0 and F_13 >= lambda-d > 0.
    1.6.2 [[33mpending[0m/[33munresolved[0m] Since F is stochastic idempotent and F_11>0 and F_13>0, the row-coincidence lemma 1.5 (with i=1,j=3) gives row_1(F)=row_3(F).
    1.6.3 [[32mvalidated[0m/[33munresolved[0m] Using row_1(F)=row_3(F), the l1 triangle inequality and the row error bounds give ||p_1-p_3||_1 <= ||p_1-row_1(F)||_1+||row_3(F)-p_3||_1 <= 2*d < 2*lambda, contradicting the exact equality ||p_1-p_3||_1=2*lambda from 1.4.
    1.6.4 [[32mvalidated[0m/[33munresolved[0m] Thus ||P-F||_{infinity->infinity}<lambda is impossible for every stochastic idempotent F, so ||P-F||_{infinity->infinity}>=lambda.
  1.7 [[33mpending[0m/[33munresolved[0m] Because lambda was arbitrary, the construction exists for every 0<lambda<1/2; epsilon_lambda=2*lambda^2 tends to 0 as lambda tends to 0, lambda=sqrt(epsilon_lambda/2), and the lower bound in 1.6 therefore proves that no PRH approximation estimate can have order o(sqrt(epsilon)); in particular the sqrt(epsilon) order is intrinsically sharp.
    1.7.1 [[32mvalidated[0m/[33munresolved[0m] The construction and estimates were obtained after fixing an arbitrary lambda in (0,1/2), so universal generalization supplies the asserted family for every such lambda.
    1.7.2 [[32mvalidated[0m/[33munresolved[0m] From epsilon_lambda=2*lambda^2 with lambda>0, epsilon_lambda tends to 0 as lambda tends to 0 and sqrt(epsilon_lambda/2)=sqrt(lambda^2)=lambda.
    1.7.3 [[33mpending[0m/[33munresolved[0m] If a universal approximation rate r(epsilon)=o(sqrt(epsilon)) held for all positive approximate retract data, then r(epsilon_lambda)/lambda = sqrt(2)*r(epsilon_lambda)/sqrt(epsilon_lambda) would tend to 0, so for all sufficiently small lambda one would have r(epsilon_lambda)<lambda; this contradicts 1.6. Hence no order smaller than sqrt(epsilon) is possible, proving intrinsic square-root sharpness.
      1.7.3.1 [[33mpending[0m/[33munresolved[0m] Let epsilon_lambda=2*lambda^2. Under the asserted universal rate hypothesis, for each sufficiently small lambda>0 there must exist a stochastic idempotent F_lambda with ||AM-F_lambda||_{infinity->infinity} <= r(epsilon_lambda). By 1.7.2, r(epsilon_lambda)/lambda=sqrt(2)*r(epsilon_lambda)/sqrt(epsilon_lambda) tends to 0, so r(epsilon_lambda)<lambda for all sufficiently small lambda. But applying the lower bound 1.6 to F_lambda gives ||AM-F_lambda||_{infinity->infinity}>=lambda, contradicting the preceding upper bound. Thus such a universal o(sqrt(epsilon)) rate cannot exist.

--- Statistics ---
Nodes: 31 total
  Epistemic: 0 [34mdraft[0m, 10 [33mpending[0m, 18 [32mvalidated[0m, 0 [36madmitted[0m, 0 [31mrefuted[0m, 3 [90marchived[0m, 0 [35mneeds_refinement[0m
  Taint: 3 [32mclean[0m, 0 [36mself_admitted[0m, 0 [31mtainted[0m, 28 [33munresolved[0m

--- Jobs ---
--- run 2 aborted tree (28 live > cap 26; 18 validated) captured for the factoring designer ---
