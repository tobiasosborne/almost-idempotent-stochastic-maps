<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 14 (session 12) — THE
  CANONICAL STRATEGIC MAP. Supersedes v13 (kept for line citations) after W48/W49/W50 +
  the FP3 bypass audit (a4677ba) + elevation #28 (9b1dde7). DELTA redraw: everything not
  restated here is UNCHANGED from v13.
STEWARDSHIP CONTRACT (binding): as in v10-v13. STATUS DISCIPLINE (L0): a SKETCH — promotes nothing.
-->

# Top-down proof sketch v14: op-classical (2026-07-07, session 12, round 4)

## UNCHANGED from v13

Theorem; trunk; the endgame stack's lower half; Kernel(i) rank >= 3; elevation ladder
(NOW: 28 af-validated — #28 lem-always-tight-dual-support, the W44 core; queue PARKED per
the Tier-1 directive); USER DECISIONS aism-nlg, aism-z98; <2>7 factoring aism-ik6.

## THE ONE WALL UNDER BOTH FACES (W49): TIGHTNESS PROMOTION

Both faces of [[conj-zero-face-elimination]] reduce to the SAME open difficulty — forcing
specific rows to be tight on the WHOLE optimal face under tall/heavy hypotheses:

- **(F1) corrected scope (FP3 audit flag; supersedes v13's "either face closes the node"):**
  the rank-3 interval interlacing closes the node AT RANK 3 ONLY; the rank-3 -> general-rank
  step (a general-D_u Helly/interlacing argument) is a separate, now-NAMED price. (F1)'s
  residual after BF: promote row-positive far balance-witnesses into always-tight T
  (HEIGHT+A certifies promotion fails out-of-class).
- **(F2), still rank-free:** the bridge fires from ANY blocker kappa-high mass > 4*tau (BD;
  constant c0 overkill). Ledger-only proofs are DEAD (the thin-blocker graft gadget, pends
  VBD). The residual: tall anti-thin-blocker SELECTION.
- **The empirical convergence (BE exact frontier):** in-class disjointness has NEVER been
  realized; every construction dies at G_4 = C_4 = empty, H < 4*tau, and restoring topness
  restores the intersection. The faces are two shots at one truth; (F2) may be vacuous
  in-class because (F1)-type overlap always holds.

## The verified mechanism toolkit (W48, all L5)

[[lem-signed-carre-du-champ]] (the tau^2-native budget; one fixed vector budgets ALL
harmonic observables), [[lem-cluster-return-flow]] (sharp 1/2 uniformity; lower-bound-type
output), [[lem-wedderburn-deflation]], [[lem-censoring-exactness]].

## External state (W50 + FP3, promotes nothing)

Not known mathematics (A:0 B:0, fourth pass; Kitaev's named open problem — we are its
commutative case). NO-CHEAPER-BYPASS across six axes. Spectral/NCD imports for (F2) are
dimension-dependent (Hartfiel-Meyer, FINDINGS) — the mass floor must be native. Importable
shapes: SBD reset-trick; Kitaev incremental merge-and-extend.

## THE OPEN LEDGER v14

1. TIGHTNESS PROMOTION — the one wall, attackable as (F2)-selection (rank-free, softer
   proof target) or (F1)-interlacing (rank-3-scoped, softer decision target) + the named
   general-rank step. In flight: FP1/FP2 (Fable deep proofs), VBD (BD/BF verification),
   next: the dual-use TWO-LEVEL SPLITTING decider (Gamma + heaviness => <= 2 approximate
   harmonic levels; its output IS (F2)'s mass floor; class-count failure = the lumping
   bypass dies too — one wave, two verdicts).
2. Kernel(i) rank >= 3 (rides (F1) machinery). 3. Trunk <2>7 factoring (parked). 4.
   Elevation queue (parked). 5. USER DECISIONS: aism-nlg, aism-z98.
