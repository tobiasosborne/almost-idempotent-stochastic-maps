# Top-down proof sketch v53: op-classical (2026-08-09, session 46 close — THE SHARPNESS CAMPAIGN CLOSES AT T0: both halves of op-classical now af-validated; Stage D landed)

## The theorem, both halves at the af-validated rung

**Upper bound** (discharged 2026-08-08, unchanged): `op-classical` is
`proved` / `af: validated` — universal, dimension-free eta_0 = eta_K and
C = K + 4*sqrt(2K) with the C*sqrt(eta) conclusion, via Route F.

**Sharpness** (NEW, banked 2026-08-09): `cor-classical-sharpness` is
`proved` / `af: validated` — for every C > 0, eta_0 > 0, beta > 1/2 there
is a row-stochastic 4x4 witness Q with defect eta < min{eta_0, 1/4} whose
distance to EVERY stochastic idempotent exceeds C*eta^beta; no uniform
exponent beta > 1/2 can replace 1/2. Carrier chain (all T0, all fresh
xhigh prover + separate fresh xhigh verifiers + external `fr verify`):
`lem-prh-sharpness-family-arithmetic` (24/24) →
`lem-prh-sharpness-row-coincidence` (19/19) →
`lem-prh-sharpness` (12/12 first-pass) →
`cor-classical-sharpness` (5/5 first-pass under the audited remedy-(b)
skeleton, after a 26-node run-1 balloon; ADDENDUM/AUDIT-CORSHARP-SKELETON).
T0 = **200** of 374.

## The honest boundary (unchanged in kind)

- af-validation is L0 rung (b): **no Lean/mathlib proof exists** (top rung
  only on user elevation).
- `ex-hume` remains `disproved` (the project's first retraction);
  signed-parameter (delta) sharpness has NO rigorous carrier at any rung.
- The 2026-07-25 off-live-route signed-geometry results stay registry-only
  by design.

## Stage D landed (this session)

The deferred halves of the binding 50-locus census are applied: the
`op-classical` pointer block (loci 12-16, body/provenance only), the
report sweep + the typeset four-row sharpness subsection in
`report/sections/02_prh.tex` (loci 21-46), the paper sect-5 switch to the
4x4 witness with the af-validated/no-Lean footnote (locus 47), and the
root-doc carrier texts (loci 1-9, 48-50). PROVENANCE +12 sharpness rows;
UNWIRED -4; PDF clean; check-all OK.

## What remains (ranked)

1. Paper polish (`aism-aywn`, user feedback pending) — content now
   consistent with both banked halves.
2. Lean/mathlib: only on user elevation.
3. Optional portfolio decision: the legacy signed chain
   (`conj-kernel`/`op-hlc`/`op-exposed-hull`) via the recurrent-to-exposed
   identification bridge over the T0 root + `lem-classical-equiv` (real
   content; exposedness-window / cloning-obstruction terrain; (EX) is
   linear-in-delta and out of reach of that route).
4. Report meta-shards (39-41) refresh on the next natural pass; the
   statistics layer is already the post-discharge census.
