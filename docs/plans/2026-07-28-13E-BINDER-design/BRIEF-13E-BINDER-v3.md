# BRIEF v3 — the explicit-binder rebuild of the Stage-1 defective set

You are a fresh, independent design mathematician (round 3; unrelated to
all prior workers). Design ONLY; no registry mutation; your output is
escalated for verbatim landing after a fresh hostile check.

## Required reading (in order)

1. `BRIEF-13E-BINDER.md`, `DESIGN-13E-BINDER.md`, `AUDIT-13E-BINDER.md`
   (round 1: the two bridge contracts — unchallenged through BOTH audits —
   and finding 1's smooth-bridge requirement).
2. `ADJUDICATION-T0-ALLEGATION.md` (the defect class and its typed-witness
   uniqueness principle).
3. `BRIEF-13E-BINDER-v2.md`, `DESIGN-13E-BINDER-v2.md`,
   `AUDIT-13E-BINDER-v2.md` (round 2: the explicit smooth bridge §1.3 and
   the deps-only amendment pattern survive; finding 2's fixed-W_0 law).
4. **`SWEEP-ADJUDICATION-STAGE1.md`** — the settled per-target ground
   truth. SIX results now stand retracted (statuses `stated`/`af: seeded`,
   contracts byte-frozen and undisputed): `lem-stage1-approximate-group-laws`,
   `lem-stage1-smooth-unitary-operations`,
   `lem-stage1-inversion-derivative-control`,
   `lem-stage1-maurer-cartan-transport`,
   `lem-stage1-polar-path-transport`,
   `lem-stage1-inversion-derivative-transport`; 13e remains `stated` and
   never validated. FOURTEEN trees are certified SOUND — treat their
   contracts and certificates as fixed T0 inputs.
5. `docs/LEARNINGS.md`, both 2026-07-28 entries (the two BINDING process
   laws: typed-witness providers for every definite description a root
   binds; fixed-W_0 for any parent applied inside a parameterized proof).

## Your task — write `DESIGN-13E-BINDER-v3.md` in this directory

ONE coherent rebuild plan for the entire defective set, with exact
verbatim-landable registry texts (one-line `contract:` / `defs:` /
`deps:`; NEW / AMENDED-deps-only / BYTE-UNCHANGED per shard), covering:

1. The two quantitative bridges (round-1 §§1.1–1.2, presumptively
   verbatim) and the explicit smooth-operations bridge (round-2 §1.3,
   presumptively verbatim) — re-examine each against the sweep; justify
   any change.
2. Direct explicit-binder re-derivation plans for: the group-laws parent
   (a `u_pol` bypass per the sweep — decide whether its byte-frozen
   anaphoric contract re-elevates against the bridges or whether it
   should instead be left retired-in-place with 13e as the sole live
   consumer path; if retired, say exactly what happens to its shard,
   deps, and report anchor), control (round-2 §1.4 pattern),
   13e (round-2 §1.5), and 13g (round-2 §1.6 CORRECTED per audit-v2
   finding 2: fixed base witness tuple W_0 + monotonicity, or
   explicit-closeness in deps with its fixed witness in the maxima —
   pick one and write the actual threshold construction).
3. The two cheap repairs the sweep licensed: `maurer-cartan-transport`
   (prune node 1.3.3, revalidate on the in-tree bypass nodes 1.3.4–1.3.7
   — specify the exact workspace operation: re-seed vs. archive-node +
   fresh verification round) and `polar-path-transport` (typed
   re-derivation: its root already displays Pi_delta and the SOUND 13d
   transport supplies the typed inverse for the SAME displayed map at the
   receiving tuple — design the deps/externals so the identification is
   ordinary inverse uniqueness, and handle the parent path-admissibility
   conclusion, whose export is SOUND but whose contract exports only the
   anaphoric path formula: re-derive the path content or find the typed
   route).
4. The rows-14+ deps cleanups (round-2 §1.7) plus the
   quotient-manifold-package decision forced by the smooth-operations
   retraction (audit-v2 finding 1's prescribed repair: local scalar-action
   proof vs. depending on the new smooth bridge — pick and justify).
5. **Elevation order** (strictly sequential, caps < 26, observed 5–22
   range), per-workspace typed-witness external lists obeying BOTH
   process laws, and a full cost table (codex jobs, shards, elevations).
6. **Consumer re-check**: row 13 (A_5)–(A_7) verbatim; rows 14+ after the
   cleanups; the witness-tuple field types unchanged.

Do NOT edit anything under `argument/`, `definitions/`, `proofs/`, or
`report/`. Write ONLY the design file named above.
