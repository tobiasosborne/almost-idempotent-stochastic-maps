# W49-FP3 — Bypass strategist verdict on conj-zero-face-elimination

NO-CHEAPER-BYPASS — every alternative path to op-classical either needs >= 2 unknowns each individually comparable to or harder than the two faces, or collapses to exactly 1 unknown that is an unscoped restatement of the whole theorem; the two faces remain the unique scoped single-statement formulations, and (F2) is the softer proof target while (F1) is the softer decision target.

*(Fable bypass-strategist wave, 2026-07-07, session 12, W49. Status discipline: IDEATION/STRATEGY — promotes nothing; repo untouched. Grounded in: PRD, sketch v9-v13, argument/INDEX (98 results, 28 af-validated per the live fr trail), FINDINGS + LEARNINGS, DC4 + W17 audits, D1/E1/E2 wave docs, the ideation mechanism portfolio, W47/W48 harvest, portfolio.json arms A-G/R.)*

## The baseline to beat

The current critical path is ONE unscoped unknown: `conj-zero-face-elimination`, closable by either single-statement face —
**(F1)** rank-3 interval interlacing (force overlap of the two always-tight intervals on the exposer level line from tall/heavy/near-cluster), or
**(F2)** `conj-downhill-zero-face-lower-mass` (every disjoint-hull separator blocker carries universal c0 mass in the kappa-high slab; then c0·kappa <= nu_z <= delta is a contradiction for delta < (c0/4)^2 — any universal c0 wins, rank-free).
Below the node the chain is rigorous or reviewed: pincer #21 => collapse #17/#26 => height clause => `lem-kernel-implies-hlc` [L5] => `op-hlc` => `lem-hlc-implies-exposed-hull` [L5] => `op-exposed-hull` => trunk <2>7 (priced routine-per-auditor, W45) => `op-classical`. Remaining non-Tier-1 debt: Kernel(i) rank >= 3, <2>7 factoring, elevation queue, 2 user decisions. A bypass is "cheaper" only if its total NEW unknown mathematics is strictly less than ONE scoped face.

## Bypass B1 — the (EX) route completed differently

**(a) Chains.** (1a) `conj-ex` -> `lem-factorization` [L0: S*_s <= 2Phi_s+6delta] -> NEW transport edge -> `conj-kernel` -> existing trunk. (1b, Kernel-free) `conj-ex` -> `lem-factorization` -> NEW per-row coordinate cleanup -> `prop-approx-simplex` [mod-audit] -> `op-classical`, skipping Kernel/HLC/exposed-hull entirely.

**(b) New unknowns (brutal form).**
- U1 `(EX) itself`: "every rank>=3 exact signed idempotent with delta<=1/4 has a theta-1/2 chart with max_s Phi_s <= C0·delta" — OPEN, with its own internal conjecture stack (`conj-sc`, `conj-rh`, `conj-b-restricted`) and a kill swamp (G5: aggregate orphan financing FALSE for every finite constant; DC3: the K<1>6 master formula RED as written; plateau-2 unproved).
- U2 (for 1a) `weight/quantifier transport`: "row-v demand P_vj^+ is chargeable by pivot weights P_{u_s j}^+ uniformly over hidden tops" — W17's D5 wall, priced "Kernel-sized"; DC4: NO proved implication EX=>Kernel in either direction; three named mismatches (chart-vs-vertex, P_vj vs P_{u_s j}, maximal-pivot drift). D2 (W nonempty) is shared with the baseline.
- U3 (for 1b) `per-row coordinate control`: "at the (EX) chart every row has mu(j) = O(delta)" — already refuted-adjacent: H-M T_c (zero-column) rows have beta_s(j)=0 for EVERY pivot (invisible to every Phi/S* functional) yet can carry coordinate negative mass; the balanced staircase has mu(e0)/delta ~ 1.26 with rowneg/delta ~ 0.004 (D1). Relaxing into the gamma-error of `prop-approx-simplex` needs "every row within O(tau) of the chart simplex" — the exposed-hull/Kernel content again. DC4 row: "<1>7 output => prop-approx-simplex: genuine interface gap".

**(c) Dead routes.** Not a re-walk per se, but U2 is the certified D5 wall and U3's pointwise form carries the D1 death-certificate row. **Is there a DIRECT (EX)=>exposed-hull argument? No structural room:** exposed-hull needs a per-row conclusion (every row near conv W); (EX)'s functional is pivot-row-weighted and provably blind to zero-column rows — any per-row conclusion needs a new transport lemma, which IS U2.

**(d) One-wave decider (kill, not proof).** W17's named door-ratio hunt: an exact rank-3 instance with hidden top sigma_g > tau and max_s S*_s(U)/(sigma_g·H) -> 0 over ALL theta-1/2 argmins retires every (EX)->Kernel door permanently.

**Cost: U1 + (U2 or U3) = 2 unknowns, one Kernel-sized, one swamp-recorded. Strictly worse than one face.**

## Bypass B2 — dynamic operations: censoring/deflation induction on rank or states

**(a) Chain.** W48 bricks (BB deflation: D^2=D, D1=0, exact rank drop, trace=rank clone-invariant, exposer vectors D-fixed — harvested pending joint verification; BC censoring exactness S^2=S, S1=1 — in flight) -> NEW visibility-transfer + NEW non-accumulating error scheme -> base cases [rank<=2 banked L5] -> `conj-kernel` -> trunk.

**(b) New unknowns.**
- U4 `visibility/height transfer`: "for censored S = D + C'(I-A)^{-1}B with ||A|| <= 1-c: exposed vertices of S are exposed in P at universal(c)-comparable (rho,kappa), and H(P) <= C(c)·(H(S)+tau)" — unscoped; this IS the cluster geometry in disguise (censoring moves kept-row positions; the ideation pricing itself names W(S)-vs-W(P) as the likely failure point).
- U5 `error scheme`: censoring multiplies delta by (1 + C/(1-||A||)) per step and deflation leaves the row-sum-1 category (P'1 = 0, tau/rho/kappa conventions do not transport); an unbounded-depth induction accumulates C^k over k ~ number of geometric classes — re-importing the c10 class-count wall in step-count clothing. The ideation record already concedes: "a reformulation lever, not an induction."

**(c) Dead routes.** Clone-safe and resolvent-based (dodges raw path products), but the induction SHAPE hits the class-count wall unless steps are O(1) — and an O(1)-step middleman censoring lands on the SAME terminal statement posed on a smaller object (its legitimate use: an (F2) reformulation, which is how W48 aims it).

**(d) One-wave decider.** After BC banks: compute W(S) vs W(P) exactly on the banked W35/W36 absorption-transition family with the middle censored — if visibility is not transferable at universal constants there, the induction axis dies (the reformulation use for (F2) survives independently).

**Cost: 2 unknowns, one of which is the cluster geometry renamed. Not a bypass; at best a supply line for (F2).**

## Bypass B3 — the delta=0 tangent cone (per-rank rigidity + uniformity)

**(a) Chain.** Ideation candidate 5: fixed rank r, counterexample sequence with H/tau -> infinity forces tau -> 0, delta -> 0; limit = stochastic idempotent E + Peirce-tangent X; NEW first-order rigidity gives B(r); NEW uniformity gives sup_r B(r) < infinity -> Kernel height clause -> trunk.

**(b) New unknowns.**
- U6 `first-order hiddenness rigidity (fixed rank)`: "for stochastic idempotent E and tangent X (EX+XE=X, X1=0), no curve P_t = E+tX+O(t^2) of signed idempotents creates a hidden vertex of height >> sqrt(created negativity) carrying a heavy near-cluster" — unscoped, comparable to a face; plus the candidate's own named risk (rank semicontinuity: the limit's rank can drop, changing which E is approached).
- U7 `rank uniformity`: sup_r B(r) < infinity — unscoped and IS the dimension-free difficulty. Checked against the banked toolkit: trace(P)=rank(P) is an exact identity, not a cap, and the pincer is per-row — NOTHING banked converts compactness constants (non-effective) into a uniform B. Compactness/blow-up gives existence of B(r) with zero r-control.

**(c) Dead routes.** Adjacent to `finite-corner-as-asymptotic` (recorded refuted upstream): the guarded failure is exactly "per-rank result presented as the theorem". Scoped honestly it dodges the certificate — and thereby concedes it is not a bypass.

**(d) One-wave decider.** The candidate-5 tangent-space lemma is near-certain in one wave, but it decides the LANGUAGE, not the bypass; U7 has no one-wave decider. Terrain-mapping value only (the mechanism at the delta=0 stratum where ex-hume must appear extremal).

**Cost: 2 unknowns, one being dimension-freeness itself. Worthless as a bypass.**

## Bypass B4 — Gamma-calculus straight to approximate lumping (the only genuine architecture change)

**(a) Chain.** `lem-harmonic-affine-bridge` [L0] -> AZ variance-annihilation package (PGamma = 0, nu-uniform tau^2 budgets; in flight, near-certain algebra) -> NEW approximate-lumping theorem -> `lem-classical-equiv` [L0] -> `op-classical`. On paper the shortest chain in the repo: it skips Kernel, HLC, exposed-hull, thm-cluster, prop-approx-simplex, Kernel(i), loose-delta AND trunk <2>7 — the only axis that discharges the whole mod-audit trunk debt for free.

**(b) New unknown (exactly one — and that is the problem).**
- U8 `approximate lumping`: "there exist universal delta_0, C: every exact signed idempotent P with delta(P) <= delta_0 admits a stochastic idempotent E with ||P-E||_{inf->inf} <= C·sqrt(delta)" — this is op-classical's signed form VERBATIM (modulo the af-validated equivalence). One unknown, but unscoped and program-sized. What the Gamma budgets actually give (nu·Gamma(g)=0 for every harmonic g, one nu for all cluster vertices) is level-straddle control per mass-step at the native tau scale; converting budgets into a PARTITION with dimension-free class separation is the anti-splitting/class-count wall (c10) that killed `conj-no-free-frontier`'s composition, and the literature is a checked blank ("quantitative Baake-Sumner web stability does not exist", 2026-07-04 sweep). The ideation portfolio's own honest failure mode: the quadratic channel "may constrain both sides the same way and fail to separate witness measure from row measure."

**(c) Dead routes.** Genuinely differentiated from the canonical-g energy / Jensen kills (exact linear identity on the quadratic field, every harmonic g, Markov localization, no minimization) — the differentiation is argued in the portfolio and must be restated in any wave brief.

**(d) One-wave decider (best value of all bypass deciders — dual-use).** After AZ banks: "two-level splitting at rank 3" — does nu·Gamma = 0 + heaviness force the cluster+witness territory into <= 2 approximate harmonic levels with universal separation on a banked tall family (W29 frontier / W35 transition)? If splitting needs a class count even at rank 3, the lumping bypass dies at the recorded wall; if it works, its output is precisely a lower bound on level-straddling mass — i.e. the (F2) blocker's kappa-high mass — so the wave feeds the face attack either way.

**Cost: 1 unknown = the whole theorem, unscoped. Not cheaper than one scoped face; correct role = mechanism supply line for (F2) + the designated fallback architecture if BOTH faces die.**

*(Arm E, error-bound route, is the same verdict by the same logic: "dist(Q, S_n) <= C·sqrt(eta) clone-invariant dimension-free" IS op-classical; E1/E2 kills — Assumption 4.1 fails, quadratic residuals provably blind at every n, fixed-n exponent can be 1/4 — leave only the degree->=3 residual crack. E-int-1 is fixed-n terrain-mapping, not a bypass.)*

## Bypass B5 — the H-M Thm 1.12 route (arm D residue)

**(a) Chain.** H-M 1.12 [cited, refs/] -> `conj-degenerate-transport` (the TT tax) -> repaired (EX)-engine assembly -> `conj-ex` -> then B1's gaps ANYWAY.
**(b) New unknowns.** U9 the TT step-4 source inequality (D1: GAP; pointwise forms carry death certificates — balanced-staircase e0, no-center k=6 class pressure); U10 the nesting-aware financing assembly ((SC)+(RH); G5/G6/DC3 record); then STILL U2 (D5 transport). **>= 3 unknowns deep — strictly the worst axis.**
**(c)/(d)** The arm's own wave record (D1-D6, G1-G13, DC1-4) is the death-price certificate; fund nothing.

## Bypass B6 — literature import (what WOULD change this ranking)

In order of impact if the parallel search finds it:
1. **A quantitative lumpability / approximate-lumping theorem for near-idempotent stochastic matrices** (Buchholz-style near-lumpability, Rubino-Sericola weak lumpability, KMS aggregation error bounds) with dimension-free constants — would collapse B4-U8 to an import and flip the verdict to BYPASS-FOUND(B4). Prior sweep negative, and known results are spectral/fixed-n; expect n-dependence — demand the clone-lift test before any excitement.
2. **A dimension-free Lojasiewicz/error bound on polytopes for degree->=3 residual systems** — would revive arm E as a true bypass (the E2 degree-3 hatch F_2=(a-b)^2((1-a)+b) is the exact shape to search for at general n). Near-hopeless per E1/E2 but the only crack left there.
3. **Parametric-LP / strict-complementarity interval-structure theorems (Helly-on-a-line for optimal faces)** — NOT a bypass; would soften (F1) and price its general-rank step.
4. **Meyer stochastic-complement structural theory extended to signed idempotents** — softens B2-U4 (visibility transfer); does not remove it.

## VERDICT

**NO-CHEAPER-BYPASS.** Axis by axis: B1 = 2 unknowns (one Kernel-sized, one a swamp); B2 = 2 (one is the cluster geometry renamed); B3 = 2 (one is dimension-freeness itself); B4 = 1 but it is the whole theorem unscoped; B5 >= 3; B6 = contingent on finds the prior sweep says don't exist. The two faces are the unique scoped single-statement formulations, produced by a genuine 5-route convergence (W26-W38) + the W44 conic reduction — that convergence, with every route bottoming out on the SAME node, is itself the strongest structural evidence that no cheaper pass exists at the current knowledge state. The direct agents are aimed at the right place.

## Ranking the two faces (sharpening the aim)

**(F2) is the softer PROOF target; (F1) is the softer DECISION target.**

- **(F2) first for proof effort.** (i) Scale advantage: any universal c0 wins (c0·kappa <= nu_z <= delta needs only delta < (c0/4)^2) — no constant fight anywhere. (ii) Rank-free: the capacity bridge closes the terminal node at ALL ranks in one statement. (iii) Rigorous base: the support localization (#28) and the charging primitive `lem-row-zero-capacity` (#27) are L0. (iv) Mechanism supply exists: the W48 BA return-flow package is the FIRST banked tool whose conclusion is a lower bound on specific coefficient masses — exactly (F2)'s needed output type, which the dual-direction wall (W37) says nothing else in the toolkit provides; the AZ Gamma budget (level-straddle mass control) is the second such tool in flight. Honest risk: (F2) rhymes with the dead anti-splitting/sigma-cap walls — the pre-committed theatre guard (proof / starving-blocker counterexample / "needs new mechanism" = PIVOT) is the right discipline, and BE's exact refuter hunt is cheap insurance.
- **(F1) second.** Bounded, planar, exact-fixture-rich (disjoint endpoints realized ONLY at out-of-class HEIGHT+A) — the most DECIDABLE open object in the repo, and its machinery co-pays Kernel(i) rank >= 3. But the payout asymmetry is real and v13 understates it: `lem-rank3-optimal-face-interval-reduction` is rank-3-scoped (dim D_u = 2), so an (F1) win closes the terminal node AT RANK 3 and leaves a general-rank Helly/interlacing step that is not yet a named single statement. **Flag for the sketch steward: v13's "either face closes conj-zero-face-elimination" is exact for (F2) but for (F1) silently includes that unpriced generalization — name it or rescope (F1)'s payout in the next redraw.** Also (F1)'s tall/heavy hypotheses enter only through geography constants (K = 1/2+delta+4(1+A_0)), and forcing betweenness from them is adjacent to the sign/rebalancing content that killed the thickness route (W42 value-normalization stop).
- **Portfolio note.** The faces are mutually informative (an (F1) interlacing mechanism is a mass lower bound on the level line; an (F2) blocker bound is an interval-overlap force), BE's refuter feeds both, and the two cheap bypass deciders worth holding in reserve are dual-use: the B4 two-level-splitting wave (feeds (F2)) and the B1 door-ratio kill (retires the (EX) door permanently for one wave's cost).
