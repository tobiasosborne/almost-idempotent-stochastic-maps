<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 31 (session-24 fudw
  decomposition delta). Supersedes v30; everything not restated here is unchanged
  from v30.
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing. Route F is
  proved-mod-audit COMPLETE — NOT rigorous by this repo's L0. T0 = 35; nothing
  below is af-validated unless said so.
-->

# Top-down proof sketch v31: op-classical (2026-07-24, fudw decomposition delta — the Route F chain is FACTORED; the af-elevation runway for phases 2–3 is laid)

## UNCHANGED from v30

The signed-geometry trunk and its whole surface, Route X as the registered
fallback, the Route F chain itself (`proved-mod-audit` complete, sketch v30),
**T0 = 35** (`lem-prh` af-validated, campaign phase 0). The four W74F defs are
LOCKED (ratified 2026-07-24).

## Map change 1: the decomposition pass EXECUTED (aism-fudw closed)

Three-round adversarial design loop (6 fresh codex workers: architect → hostile
reviewer → repair → reviewer → repair → reviewer; all artifacts + verdicts banked
in `docs/plans/2026-07-24-fudw-decomposition-artifacts/`). All three reviews
returned INVALID on the whole design, converging from architecture-level defects
(v1: missing COMP-CB subtree, semantic cycles) through row-level defects (v2) to
an endorsed **77-row honest inventory with a named safe seed-first subset** (v3).
The underlying Route F mathematics was never faulted; every defect was in the
factoring/transcription layer (contract drift, unproduced constants, compound
contracts).

**Codified (registry 215 → 248, commit d05be5b):** 33 safe-subset shards —
COMP (8) `lem-compcb-*`, H-CB (14) `lem-hcb*`, independent EXT (6)
`lem-extcb*`, Stage-1 (2), detached finish leaves (3) — 28 `proved-mod-audit`,
5 `stated`, all `af: none`; + 12 `draft` definitions (**RATIFICATION NEEDED**).
Parent contracts (`conj-hcb`, `conj-extcb`, `lem-thmainext-conditional`,
`lem-routef-k-ledger`) unchanged.

## Map change 2: the quarantine + named GAP interfaces (aism-0163)

NOT codified, design-doc-only until a focused repair + fresh review: the MAIN-CB
assembly rows, the ledger rows (14 domain-defective targets per the v3 verdict),
and five named GAP interfaces — **GAP-EA** (exact-target approximation),
**GAP-S1-POLAR-CONTRACT**, **GAP-MAIN-STRUCTURE**, **GAP-LEDGER-DOMAINS**, and
the **uncontracted F2/F3 bridge** (positive-unital compression / retract-defect).
`lem-routef-k-ledger` carries a DO-NOT-REWIRE-OR-SEED guard until reviewed F2/F3
contracts exist. aism-0163 blocks phase 4 (aism-5byv) only; phases 2–3
(aism-niwk H-CB, aism-fgr7 EXT-CB) are UNBLOCKED.

## The live campaign frontier (unchanged goal, sharper runway)

L0 closure of Route F, bottom-up: **phase 2 next** — Ha/COL-HILB def-layer
provisioning (byte-matched `af def-add` where the source definitions are sound)
+ seed the COMP/H-CB workspaces from the new registry contracts, strictly serial
orchestrations. Blockers on user input: ratification of the 12 draft defs;
decision on the F2/F3 bridge contracts (aism-0163). The 7 Stage-1 external
topology inputs remain a provisioning risk (acquisition into `refs/` before
phase 4).
