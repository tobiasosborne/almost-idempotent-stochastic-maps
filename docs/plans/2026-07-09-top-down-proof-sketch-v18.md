<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 18 (session 13 close) — THE
  CANONICAL STRATEGIC MAP. Supersedes v17 (kept for line citations) after W54
  (docs/waves/2026-07-09-W54-huddle-charge-decomposition.md + the verified tree
  docs/plans/2026-07-09-w54-huddle-charge-decomposition-tree.md).
  DELTA redraw: everything not restated here is UNCHANGED from v17.
STEWARDSHIP CONTRACT (binding): as in v10-v17. STATUS DISCIPLINE (L0): a SKETCH — promotes nothing.
-->

# Top-down proof sketch v18: op-classical (2026-07-09, session 13 close)

## UNCHANGED from v17

Theorem; trunk; the endgame stack; 28 af-validated; the closed-by-certificate list; USER
DECISIONS aism-nlg, aism-z98; parked Tier-2/3. Registry now 140 (session 13: +21 — every
banked item through a SEPARATE fresh hostile codex verifier; af-validated count unchanged).

## THE W54 RESULT: the huddle charge is a VERIFIED four-leaf system

The decomposition wave (Fable architect -> hostile assembly verification -> repairs ->
re-verification -> leaf provers -> per-leaf verifiers; user mandate: decomposition IS the
objective) produced a HOSTILE-VERIFIED derivation (the tree doc, read PART A through
PART B): THE HUDDLE CHARGE (v17's terminal node) follows from four sharply-scoped open
conjectures — everything else is proved (L5-tier):

- **Branch II (intersecting hulls at the top), COLLAPSED** (`lem-l2-core-collapse`: the
  plateau/averaging clauses are automatic; averaging is degenerate — Y_v convex):
  - `conj-straddling-web-exclusion` (SL1a): no rho-spanning co-top probability web with
    barycenter within 2.2*tau of the top and universal exposer-average <= (16/13)*kappa.
  - `conj-shallow-counterweight-exclusion` (SL1b): no tau/D-mass of rho-far SHALLOW rows
    universally shadowed below kappa. **Graded most attackable — W55's first target.**
  - Alternative route (Theorem C, recorded in the wave doc): `conj-summit-cylinder-
    exclusion` + narrow dual face closes Branch II when omega_v is small; the joint
    residual is the wide-face summit.
- **Branch I (disjoint hulls), COLLAPSED onto one coupling + one minimax:**
  - `conj-cotop-web-coupling` (L6.5): the dual-forced co-top far web must receive >= c_*
    of the top's own positive mass — THE lambda-vs-P+ wall in minimal localized form.
  - the L5 dual-face mass minimax (L5-GAP-1, aism-vuc): sup over y in Y_v of the
    mass-normalized far-deep deficit >= gamma*tau. (L7 chain cap foldable, aism-2ii.)

**The proved scaffolding banked this session (all L5, hostile-verified):**
lem-top-deficit-price, lem-disjointness-huddle-reduction, lem-top-witness-third-actor,
lem-bounded-alpha-forced-far-slab (W53); lem-absorption-implies-low-slab-cap (+
conj-far-low-slab-cap) wiring absorption => conj-low-slab-cap; lem-averaged-deficit-charge,
lem-zero-face-capacity-kill, lem-top-support-dual-face (legal tilts == the dual face Y_v;
the summit cylinder/axis objects), lem-positive-exposedness-margin (t* > 0 always — the
t*=0 boundary vacuous), lem-cotop-witness-pinning (the exact display pairing; witness AND
zero-face mass co-top), lem-downhill-cotop-conic-mass, lem-psi-corner-trap (the t*-FREE
harmonic toolkit), lem-l2-core-collapse, lem-intersection-witness-confinement (the
identity-level averaging cap).

**Dead by certificate this session (FINDINGS 2026-07-09; do not re-walk):** the
witness-averaging mechanism for Branch II (identity cap); the averaging axis (degenerate
via Y_v); pointwise-to-simultaneous by averaging (sharp simplex witness); pure co-top
mutual-exposure rigidity (the shallow-counterweight escape — the honest surface is the
SL1a+SL1b pair); any starvation constant dividing by t*.

## THE OPEN LEDGER v18

1. **The four huddle-charge leaves** (Tier 1, THE only unscoped math): SL1b (small,
   first), SL1a, conj-cotop-web-coupling, L5-minimax — four windows on ONE object (the
   confined co-top web of lem-intersection-witness-confinement /
   lem-cotop-witness-pinning); cylinder-exclusion as the Theorem-C alternative.
2. conj-far-low-slab-cap (the far sibling; consumed by the absorption bridge).
3. Kernel(i) W-nonemptiness rank >= 3, trunk <2>7, loose-delta (aism-nlg), elevation
   queue — parked Tier-2/3 (farm out once Tier 1 falls).
4. USER DECISIONS: aism-nlg, aism-z98.

## Next session opening move (W55)

Attack SL1b first (codex-grade candidate: shallow rows live near the visible hull where
exposers have room — universal shadowing should contradict lem-row-far-dual-certificate /
the visible-side machinery); then SL1a + conj-cotop-web-coupling as ONE joint
Fable-grade wave on the confined co-top web (they are two windows on it), consuming the
t*-free toolkit + the pinning/confinement anatomy; L5-minimax (aism-vuc) in parallel as
a codex geometry problem on Y_v. Wave discipline unchanged: hostile fresh-codex
verification before any codification.
