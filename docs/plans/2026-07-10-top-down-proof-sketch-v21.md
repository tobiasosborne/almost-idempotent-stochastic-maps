<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 21 (W57 completion-LP delta).
  Supersedes v20; everything not restated here is unchanged from v20.
STATUS DISCIPLINE (L0): a SKETCH / STRATEGY artifact; promotes nothing. The W57 result
  is L3 (exact-rational computation) — evidence and mechanism-seed, NEVER proof.
-->

# Top-down proof sketch v21: op-classical (2026-07-10, W57 completion-LP delta)

## UNCHANGED from v20

Everything: the theorem, trunk, 28 af-validated, registry 150, the three-cell SL1a
surface on the proved reduction interface, SL1b, L6.5 + the W55 map, the L5 minimax,
all dead routes. No registry change this wave (the W57 output is a runs/ bundle, L3).

## W57 map change: the starvation gadget's minimal completions are dead (L3, exact)

`runs/2026-07-10-w57-starvation-completion-lp`: the W55 gadget (A0 = 5, g = 5*tau) has
NO minimal rank-3 actor-hull completion — three cases (literal + both H-X horn
relaxations) decided INFEASIBLE in exact rational arithmetic with Farkas certificates
that are STABLE over A0 in [4,6], 0 < tau <= 1/256 (independent check OVERALL PASS,
orchestrator-reproduced). Honest scope: the first EXTRA-VERTEX completion family is
UNDECIDED — that family is now the gadget's only remaining refuter route.

Consequences for the map (evidence-level, not proof):

- The W55 warning "the gadget survives every scalar ledger" is now sharpened: it
  survives ledgers but NOT exact minimal completion — the global P = L*B, B*L = I
  resource does bite, exactly as the large-gauge wall analysis predicted.
- A CANDIDATE MECHANISM for `conj-sl1a-off-diagonal-cell` (H-X) and the large-gauge
  completion theorem now exists: the stable multipliers' candidate lemma (bundle
  CERTIFICATE.md) — "minimal actor-hull starvation completion obstruction". It needs a
  dimension-free paper proof before it is anything (bd aism-cq2).

## Revised Tier-1 order (v20 order, item 0 split by the result)

0a. Decide the extra-vertex completion family (bd aism-hjm) — same harness; this
    keeps the refuter pressure honest while the mechanism is developed.
0b. Paper-proof wave on the completion-obstruction candidate lemma (bd aism-cq2) —
    the multipliers are the guide; fresh hostile verification; codify then af-elevate.
1-5. Unchanged from v20 (E1-E5 codification; small-gauge bridge; H-D/H-I; SL1b; L5
    minimax; elevation queue).
