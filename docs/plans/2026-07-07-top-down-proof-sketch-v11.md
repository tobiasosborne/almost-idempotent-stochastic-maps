<!--
ROLE: the top-down FULL proof sketch of op-classical, VERSION 11 (session 11, round 8) — THE
  CANONICAL STRATEGIC MAP. Supersedes docs/plans/2026-07-07-top-down-proof-sketch-v10.md
  (kept intact for line citations) after W42 (f137d19) + elevation #22 (harmonic-affine
  bridge). v11 is a DELTA redraw: everything not restated here is UNCHANGED from v10.
STEWARDSHIP CONTRACT (binding): as in v10. STATUS DISCIPLINE (L0): a SKETCH — promotes nothing.
-->

# Top-down proof sketch v11: op-classical (2026-07-07, session 11 round 8)

## UNCHANGED from v10

Theorem; trunk (<2>7 + loose-delta = the only non-reviewed links below <2>4); the endgame
stack's LOWER half (conj-near-cluster-absorption ⇒ conj-low-slab-cap ⇒ [pincer #21 RIGOROUS]
⇒ [collapse #17 RIGOROUS] ⇒ height clause ⇒ reviewed chain ⇒ op-classical); Kernel(i)
rank >= 3; the rigour ladder queue (NOW: 22 af-validated — #22 harmonic-affine-bridge;
elevation #7 row-far-dual-certificate in flight).

## THE TERMINAL NODE, REFORMULATED (W42; supersedes v10's questions 1-2 as stated)

The mechanism in every certificate is **RESIDUAL CANCELLATION**: topness acts by making the
exposedness LP's optimal face contain a datum with R = 0 (alpha-free) — NOT by measurable
zero-face hull reach (the thickness route hard-stops at value normalization: "h* small on the
cluster" never becomes "h* zero"; the missing LP step is OPTIMAL-FACE SELECTION). And any
closure must be **CLUSTER-UNIFORM** (v's own datum is insufficient — witnesses of every
mass-carrying cluster vertex need control). The two equivalent-ish terminal formulations:

- **(T1) Cluster-uniform optimal-face cancellation:** in the tall heavy near-cluster regime,
  every mass-carrying cluster vertex u admits an optimal hiddenness datum with R = 0 (or
  bounded conic gauge) — i.e. [[conj-tall-zero-face-radial-thickness]] STRENGTHENED to the
  cluster and re-aimed at the R = 0 / optimal-face-selection form.
- **(T2) The row-to-circuit absorption bridge (single statement):** if v carries >= 1 - theta
  on its rho-near top-slab cluster C and every mass-carrying u in C has a cheap bounded-alpha
  witness, then v ships >= c positive mass outside C or some u in C is (rho, kappa)-exposed.

Supporting bricks banked: [[lem-bounded-alpha-top-slab-reduction]] (cheap bounded-alpha
witnesses of cluster vertices are top-slab-confined — the graph half);
[[lem-radial-alpha-bound]] + [[lem-zero-face-alpha-gauge]] (the gauge calculus);
[[obs-realized-alpha-blowup]] (why unconditional alpha bounds are impossible).

## RETIRED in this redraw (new; cumulative list in v8-v10)

26. **The thickness route in raw form** (W42): value normalization is a hard stop; only the
    optimal-face-selection / R = 0 form remains live.
27. **v-only radial thickness as the sole open node** (W42, AP): cluster-uniformity is
    required; do not cite the v-only conjecture as sufficient.

## THE OPEN LEDGER v11

1. (T1)/(T2) above — THE terminal node. 2. Kernel(i) rank >= 3. 3. Trunk <2>7. 4. loose-delta.
5. Elevation queue (#23+: row-far-dual-certificate in flight; then depth-d-halo-collapse,
top-slab-companion, depth-Markov). 6. Held/fallback routes unchanged.

## Unscoped surface

The (T1)/(T2) mechanism — the ONLY unscoped mathematics. All else is priced bookkeeping.
